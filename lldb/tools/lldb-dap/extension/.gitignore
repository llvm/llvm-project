out
bin
node_modules
.vscode-test
*.vsix
!.vscode
# Generated by `npm run sync-readme` from the parent README.md so `vsce
# package`/`vsce publish` have a README.md to show in the Marketplace
# "Details" tab. Not tracked, to avoid two copies drifting out of sync.
/README.md
