---
orphan: true
---

% This is a sub page of the Python API docs and linked from the main API page.
% The page isn't in any toctree, so silence the sphinx warnings by marking it as orphan.

% The page contents are generated at build time from lldb-enumerations.h and
% lldb-defines.h by lldb/scripts/gen-python-api-enums.py. Don't edit it by hand;
% update those headers instead.

```{build-include} generated-python-api-enums.md
:parser: myst_parser.sphinx_
```
