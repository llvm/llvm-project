SWIFT_SOURCES := main.swift
# -enable-bare-slash-regex enables the /pattern/ literal syntax.
# Do not hardcode a macOS deployment target here — the test runner
# supplies the correct target triple via TARGET_SWIFTFLAGS for both
# local macOS and remote Darwin builds.
SWIFTFLAGS_EXTRAS := -enable-bare-slash-regex

include Makefile.rules
