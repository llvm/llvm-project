# The flatten directive's depth clause requires OpenMP 6.1.
for i, (pattern, replacement) in enumerate(config.substitutions):
    if pattern == "%openmp_flags":
        config.substitutions[i] = (pattern, replacement + " -fopenmp-version=61")
        break
