// RUN: cir-tool %s

func.func @s0() {
  %1 = cir.cst(2 : i32) : i32
  cir.switch (%1 : i32) [
    case (default) {
      cir.return
    },
    case (equal, 3) {
      cir.return
    }
  ]
  cir.return
}
