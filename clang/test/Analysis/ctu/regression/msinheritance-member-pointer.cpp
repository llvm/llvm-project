// RUN: rm -rf %t
// RUN: mkdir -p %t
// RUN: split-file %s %t

// Pathological case: under the Microsoft C++ ABI a class's inheritance model is
// assigned lazily. Sema::assignInheritanceModel() attaches an implicit
// MSInheritanceAttr to whichever declaration of the class is most recent at the
// moment a pointer-to-member type first has to be complete, and
// CXXRecordDecl::getMSInheritanceModel() later reads it back off
// getMostRecentCXXRecordDecl(). Importing a declaration of that class appends it
// to the redeclaration chain, so it becomes the most recent one, but the
// attribute stays behind on the declaration Sema picked -- it is not inherited
// along the chain. Any later query then reaches a declaration without the
// attribute and asserts; a release build dereferences null instead.

// RUN: %clang_cc1 -std=c++17 -triple x86_64-pc-windows \
// RUN:   -fms-compatibility -fms-extensions -fms-compatibility-version=19.40 \
// RUN:   -fdelayed-template-parsing \
// RUN:   -emit-pch -o %t/MavLinkConnectionImpl.cpp.ast %t/MavLinkConnectionImpl.cpp

// RUN: %clang_extdef_map %t/MavLinkConnectionImpl.cpp -- -std=c++17 \
// RUN:   --target=x86_64-pc-windows -fms-compatibility -fms-extensions \
// RUN:   -fms-compatibility-version=19.40 -fdelayed-template-parsing \
// RUN:   > %t/externalDefMap.tmp.txt
// On windows, absolute paths generated by extdef_map are not recognized,
// so CSA prepends the workdir path to them. Force relative paths to work
// around this issue. The match is anchored and excludes spaces because the USR
// of Holder::Holder contains one (its member-pointer parameter) -- which is what
// the "<length>:" prefix of every defmap entry exists to tolerate.
// RUN: sed -e 's| [^ ]*/MavLinkConnectionImpl\.cpp$| MavLinkConnectionImpl.cpp.ast|' \
// RUN:   %t/externalDefMap.tmp.txt > %t/externalDefMap.txt

// RUN: %clang_cc1 -std=c++17 -triple x86_64-pc-windows \
// RUN:   -fms-compatibility -fms-extensions -fms-compatibility-version=19.40 \
// RUN:   -fdelayed-template-parsing -analyze \
// RUN:   -analyzer-checker=core \
// RUN:   -analyzer-config experimental-enable-naive-ctu-analysis=true \
// RUN:   -analyzer-config ctu-dir=%t \
// RUN:   -verify %t/main.cpp

//--- main.cpp

// expected-no-diagnostics

namespace impl {
class Inner;
}

class Outer {
  void entry();

public:
  impl::Inner *pInner;
};

// This definition is the one Sema hands the MSInheritanceAttr to, because it is
// the most recent declaration of impl::Inner while this TU is parsed.
namespace impl {
class Inner {
public:
  void entry();
  void target();
};
} // namespace impl

void Outer::entry() {
  // Importing impl::Inner::entry brings the other TU's forward declaration of
  // impl::Inner along, appending it after the definition above and making it the
  // most recent declaration -- without the attribute. Inlining the imported body
  // then lays out Holder, which asks for the inheritance model again.
  pInner->entry(); // no-crash
}

//--- MavLinkConnectionImpl.cpp

namespace impl {
// This forward declaration is what ends up appended to the main TU's chain.
class Inner;
} // namespace impl

class Outer {
public:
  impl::Inner *pInner;
};

namespace impl {
class Inner {
  void entry();
  void target();
};
} // namespace impl

// Holds a pointer-to-member by value, so laying this record out needs the MS
// inheritance model of impl::Inner. It must not be a template: an implicit
// instantiation of a template member never reaches the external definition map,
// so naive CTU could not import it, and an uninlined constructor never triggers
// the layout.
class Holder {
  void (impl::Inner::*pm)();

public:
  Holder(void (impl::Inner::*p)()) : pm(p) {}
};

Outer *g;

void impl::Inner::target() {
  // A field access through a pointer, so the analyzer builds the region whose
  // getAsOffset() forces the record layout. An empty body does not reproduce.
  g->pInner;
}

void impl::Inner::entry() {
  Holder h(&impl::Inner::target);
}
