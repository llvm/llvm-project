import lit.util

if any(target in config.target_triple for target in ("aix", "zos")):
    config.unsupported = True

if lit.util.which("lld", config.llvm_tools_dir):
    config.available_features.add("lld")
