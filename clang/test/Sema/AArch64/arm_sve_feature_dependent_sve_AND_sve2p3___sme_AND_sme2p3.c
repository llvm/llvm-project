// NOTE: File has been autogenerated by utils/aarch64_builtins_test_generator.py
// RUN: %clang_cc1 %s -fsyntax-only -triple aarch64-none-linux-gnu -target-feature +sme -target-feature +sve -target-feature +sve2p3 -verify=guard
// RUN: %clang_cc1 %s -fsyntax-only -triple aarch64-none-linux-gnu -target-feature +sme -target-feature +sme2p3 -target-feature +sve -verify=streaming-guard
// RUN: %clang_cc1 %s -fsyntax-only -triple aarch64-none-linux-gnu -target-feature +sme -target-feature +sme2p3 -target-feature +sve -target-feature +sve2p3 -verify
// expected-no-diagnostics

// REQUIRES: aarch64-registered-target

#include <arm_sve.h>

// Properties: guard="sve,sve2p3" streaming_guard="sme,sme2p3" flags="feature-dependent"

void test(void) {
  svint16x2_t svint16x2_t_val;
  svint32x2_t svint32x2_t_val;
  svuint16x2_t svuint16x2_t_val;
  svuint32x2_t svuint32x2_t_val;

  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqrshrn_n_s8_s16_x2(svint16x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqrshrn_n_u8_u16_x2(svuint16x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqrshrn_s8(svint16x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqrshrn_u8(svuint16x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqrshrun_n_u8_s16_x2(svint16x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqrshrun_u8(svint16x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_n_s8_s16_x2(svint16x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_n_s16_s32_x2(svint32x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_n_u8_u16_x2(svuint16x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_n_u16_u32_x2(svuint32x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_s8(svint16x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_s16(svint32x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_u8(svuint16x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_u16(svuint32x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrun_n_u8_s16_x2(svint16x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrun_n_u16_s32_x2(svint32x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrun_u8(svint16x2_t_val, 2);
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrun_u16(svint32x2_t_val, 2);
}

void test_streaming(void) __arm_streaming{
  svint16x2_t svint16x2_t_val;
  svint32x2_t svint32x2_t_val;
  svuint16x2_t svuint16x2_t_val;
  svuint32x2_t svuint32x2_t_val;

  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqrshrn_n_s8_s16_x2(svint16x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqrshrn_n_u8_u16_x2(svuint16x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqrshrn_s8(svint16x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqrshrn_u8(svuint16x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqrshrun_n_u8_s16_x2(svint16x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqrshrun_u8(svint16x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqshrn_n_s8_s16_x2(svint16x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqshrn_n_s16_s32_x2(svint32x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqshrn_n_u8_u16_x2(svuint16x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqshrn_n_u16_u32_x2(svuint32x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqshrn_s8(svint16x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqshrn_s16(svint32x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqshrn_u8(svuint16x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqshrn_u16(svuint32x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqshrun_n_u8_s16_x2(svint16x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqshrun_n_u16_s32_x2(svint32x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqshrun_u8(svint16x2_t_val, 2);
  // guard-error@+1 {{builtin can only be called from a non-streaming function}}
  svqshrun_u16(svint32x2_t_val, 2);
}

void test_streaming_compatible(void) __arm_streaming_compatible{
  svint16x2_t svint16x2_t_val;
  svint32x2_t svint32x2_t_val;
  svuint16x2_t svuint16x2_t_val;
  svuint32x2_t svuint32x2_t_val;

  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqrshrn_n_s8_s16_x2(svint16x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqrshrn_n_u8_u16_x2(svuint16x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqrshrn_s8(svint16x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqrshrn_u8(svuint16x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqrshrun_n_u8_s16_x2(svint16x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqrshrun_u8(svint16x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_n_s8_s16_x2(svint16x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_n_s16_s32_x2(svint32x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_n_u8_u16_x2(svuint16x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_n_u16_u32_x2(svuint32x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_s8(svint16x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_s16(svint32x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_u8(svuint16x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrn_u16(svuint32x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrun_n_u8_s16_x2(svint16x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrun_n_u16_s32_x2(svint32x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrun_u8(svint16x2_t_val, 2);
  // guard-error@+2 {{builtin can only be called from a non-streaming function}}
  // streaming-guard-error@+1 {{builtin can only be called from a streaming function}}
  svqshrun_u16(svint32x2_t_val, 2);
}
