#ifndef HIP_UTILS_H
#define HIP_UTILS_H

#include <string>

namespace hipcc {
namespace utils {
// gets the path of the executable name
std::string getSelfPath();

} // namespace utils
} // namespace hipcc

#endif
