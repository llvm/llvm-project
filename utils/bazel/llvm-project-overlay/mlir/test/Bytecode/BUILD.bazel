load("//llvm:lit_test.bzl", "lit_test")

licenses(["notice"])

package(default_visibility = ["//visibility:public"])

[
    lit_test(
        name = "%s.test" % src,
        srcs = [src],
        data = [
            "//llvm:llvm-strings",
            "//mlir:mlir-opt",
            "//mlir/test:lit_data",
        ] + glob(["**/*.mlirbc"]),
    )
    for src in glob(["**/*.mlir"])
]
