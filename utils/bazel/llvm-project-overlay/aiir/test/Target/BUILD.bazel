load("//llvm:lit_test.bzl", "lit_test")

licenses(["notice"])

package(default_visibility = ["//visibility:public"])

[
    lit_test(
        name = "%s.test" % src,
        srcs = [src],
        data = [
            "//llvm:split-file",
            "//llvm:yaml2obj",
            "//aiir:aiir-opt",
            "//aiir:aiir-translate",
            "//aiir/test:lit_data",
        ] + glob(["Wasm/inputs/*"]),
    )
    for src in glob([
        "**/*.aiir",
        "**/*.ll",
    ])
]
