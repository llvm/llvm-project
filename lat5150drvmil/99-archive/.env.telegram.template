# Telegram API Configuration
#
# Get your API credentials from https://my.telegram.org/apps
#
# 1. Log in with your phone number
# 2. Go to "API development tools"
# 3. Create a new application
# 4. Copy your api_id and api_hash
#

# Telegram API credentials (REQUIRED)
TELEGRAM_API_ID=your_api_id_here
TELEGRAM_API_HASH=your_api_hash_here

# Security channels to monitor (comma-separated, no @ symbol)
# Default includes cveNotify (CVEs) and Pwn3rzs (security research/exploits)
SECURITY_CHANNELS=cveNotify,Pwn3rzs

# Auto-update settings
AUTO_UPDATE_EMBEDDINGS=true          # Automatically update RAG embeddings
UPDATE_BATCH_SIZE=10                 # Update RAG after N new documents
UPDATE_INTERVAL_SECONDS=300          # Periodic update interval (5 minutes)

# Additional channels (examples - uncomment and add as needed)
# SECURITY_CHANNELS=cveNotify,Pwn3rzs,malware_traffic,exploit_db,zerodaytoday
