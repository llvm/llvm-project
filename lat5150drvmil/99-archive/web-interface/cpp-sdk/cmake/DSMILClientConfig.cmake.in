@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find required dependencies
find_dependency(OpenSSL REQUIRED)
find_dependency(CURL REQUIRED)
find_dependency(Threads REQUIRED)

# Optional dependencies
find_dependency(Boost COMPONENTS system thread QUIET)
if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
    find_path(URING_INCLUDE_DIR liburing.h QUIET)
    find_library(URING_LIBRARY uring QUIET)
endif()

# Include the exported targets
include("${CMAKE_CURRENT_LIST_DIR}/DSMILClientTargets.cmake")

check_required_components(DSMILClient)