# Device 0x802B: PacketFilterDevice

## Device Information

| Property | Value |
|----------|-------|
| **Device ID** | `0x802B` (32811) |
| **Name** | PacketFilterDevice |
| **Group** | Group 3: Data Processing (0x8024-0x802F) |
| **Risk Level** | 🟢 SAFE |
| **Total Operations** | 6 |
| **Total Registers** | 4 |
| **Implementation File** | `device_0x802B_packet_filter.py` |

## Description

Hardware Packet Filter (0x802B)

## Operations (6)

### Core Operations

#### `initialize()`

Initialize Packet Filter device

**Returns:** `OperationResult`

#### `get_capabilities()`

Get device capabilities

#### `get_status()`

Get current device status

### Advanced Operations

#### `read_register(register)`

Read a device register

#### `get_filter_rules()`

Get all filter rules

#### `get_statistics()`

Get packet filter statistics

## Hardware Registers (4)

| Register Name | Constant | Offset |
|---------------|----------|--------|
| FILTER_STATUS | `REG_FILTER_STATUS` | 0x00 |
| PACKETS_BLOCKED | `REG_PACKETS_BLOCKED` | 0x0C |
| PACKETS_FILTERED | `REG_PACKETS_FILTERED` | 0x08 |
| RULE_COUNT | `REG_RULE_COUNT` | 0x04 |

## Device Constants

### Status Bits

- `STATUS_FILTER_ACTIVE`
- `STATUS_DPI_ENABLED`
- `STATUS_IPS_MODE`

## Usage Example

```python
from dsmil_auto_discover import discover_all_devices

# Auto-discover all devices
registry = discover_all_devices()

# Get device 0x802B
device = registry.get_device(32811)

# Initialize device
result = device.initialize()
if result.success:
    print(f'Device initialized: {device.name}')

# Get device status
status = device.get_status()
print(f'Device status: {status}')
```

---

**Document Generated:** 2025-11-08  
**Framework Version:** 2.0.0 (Auto-Discovery)  
**Classification:** UNCLASSIFIED // FOR OFFICIAL USE ONLY
