# Device 0x8007: PowerStateDevice

## Device Information

| Property | Value |
|----------|-------|
| **Device ID** | `0x8007` (32775) |
| **Name** | PowerStateDevice |
| **Group** | Group 0: Core Security (0x8000-0x800B) |
| **Risk Level** | 🟡 MONITORED (85% safe for READ) |
| **Total Operations** | 9 |
| **Total Registers** | 8 |
| **Implementation File** | `device_0x8007_power_state.py` |

## Description

Power State / Power Management Controller (0x8007)

## Operations (9)

### Core Operations

#### `initialize()`

Initialize Power State device

**Returns:** `OperationResult`

#### `get_capabilities()`

Get device capabilities

#### `get_status()`

Get current device status

### Advanced Operations

#### `read_register(register)`

Read a device register

#### `get_power_summary()`

Get comprehensive power status summary

#### `get_battery_info()`

Get detailed battery information

#### `get_state_transitions(limit)`

Get recent power state transitions

#### `get_performance_info()`

Get CPU performance state information

#### `get_statistics()`

Get power management statistics

## Hardware Registers (8)

| Register Name | Constant | Offset |
|---------------|----------|--------|
| BATTERY_STATUS | `REG_BATTERY_STATUS` | 0x14 |
| PERFORMANCE_STATE | `REG_PERFORMANCE_STATE` | 0x0C |
| POLICY | `REG_POLICY` | 0x18 |
| POWER_CONSUMPTION | `REG_POWER_CONSUMPTION` | 0x10 |
| POWER_STATUS | `REG_POWER_STATUS` | 0x00 |
| PROCESSOR_STATE | `REG_PROCESSOR_STATE` | 0x08 |
| SYSTEM_STATE | `REG_SYSTEM_STATE` | 0x04 |
| WAKE_EVENTS | `REG_WAKE_EVENTS` | 0x1C |

## Device Constants

### Status Bits

- `STATUS_PM_ACTIVE`
- `STATUS_BATTERY_PRESENT`
- `STATUS_AC_POWER`
- `STATUS_LOW_POWER_MODE`
- `STATUS_THERMAL_THROTTLE`
- `STATUS_WAKE_ENABLED`

## Usage Example

```python
from dsmil_auto_discover import discover_all_devices

# Auto-discover all devices
registry = discover_all_devices()

# Get device 0x8007
device = registry.get_device(32775)

# Initialize device
result = device.initialize()
if result.success:
    print(f'Device initialized: {device.name}')

# Get device status
status = device.get_status()
print(f'Device status: {status}')
```

## ⚠️ Safety Notes

This device is classified as MONITORED:
- READ operations are generally safe
- WRITE operations require careful review
- Always test in safe environment first
- Monitor for unexpected behavior

---

**Document Generated:** 2025-11-08  
**Framework Version:** 2.0.0 (Auto-Discovery)  
**Classification:** UNCLASSIFIED // FOR OFFICIAL USE ONLY
