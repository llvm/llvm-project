# Device 0x8018: PreIsolationDevice

## Device Information

| Property | Value |
|----------|-------|
| **Device ID** | `0x8018` (32792) |
| **Name** | PreIsolationDevice |
| **Group** | Group 2: Network/Communications (0x8018-0x8023) |
| **Risk Level** | 🟢 SAFE |
| **Total Operations** | 11 |
| **Total Registers** | 8 |
| **Implementation File** | `device_0x8018_pre_isolation.py` |

## Description

Pre-Isolation (0x8018)

## Operations (11)

### Core Operations

#### `initialize()`

Initialize Pre-Isolation device

**Returns:** `OperationResult`

#### `get_capabilities()`

Get device capabilities

#### `get_status()`

Get current device status

### Advanced Operations

#### `read_register(register)`

Read a device register

#### `get_isolation_status()`

Get comprehensive isolation status

#### `list_isolated_systems()`

List all isolated systems

#### `get_quarantine_history(limit)`

Get quarantine action history

#### `get_threat_assessment()`

Get current threat assessment

#### `get_network_zones()`

Get network zone configuration

#### `get_isolation_summary()`

Get isolation system summary

#### `get_statistics()`

Get pre-isolation statistics

## Hardware Registers (8)

| Register Name | Constant | Offset |
|---------------|----------|--------|
| AUTO_ISOLATION | `REG_AUTO_ISOLATION` | 0x1C |
| ISOLATED_COUNT | `REG_ISOLATED_COUNT` | 0x10 |
| ISOLATION_LEVEL | `REG_ISOLATION_LEVEL` | 0x04 |
| ISOLATION_STATUS | `REG_ISOLATION_STATUS` | 0x00 |
| LAST_ISOLATION | `REG_LAST_ISOLATION` | 0x18 |
| NETWORK_ZONE | `REG_NETWORK_ZONE` | 0x0C |
| QUARANTINE_COUNT | `REG_QUARANTINE_COUNT` | 0x14 |
| THREAT_LEVEL | `REG_THREAT_LEVEL` | 0x08 |

## Device Constants

### Status Bits

- `STATUS_ISOLATION_ACTIVE`
- `STATUS_MONITORING_ENABLED`
- `STATUS_AUTO_ISOLATION`
- `STATUS_THREAT_DETECTED`
- `STATUS_QUARANTINE_ACTIVE`
- `STATUS_ROLLBACK_AVAILABLE`

## Usage Example

```python
from dsmil_auto_discover import discover_all_devices

# Auto-discover all devices
registry = discover_all_devices()

# Get device 0x8018
device = registry.get_device(32792)

# Initialize device
result = device.initialize()
if result.success:
    print(f'Device initialized: {device.name}')

# Get device status
status = device.get_status()
print(f'Device status: {status}')
```

---

**Document Generated:** 2025-11-08  
**Framework Version:** 2.0.0 (Auto-Discovery)  
**Classification:** UNCLASSIFIED // FOR OFFICIAL USE ONLY
