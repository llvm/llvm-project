# Device 0x801E: TacticalDisplayDevice

## Device Information

| Property | Value |
|----------|-------|
| **Device ID** | `0x801E` (32798) |
| **Name** | TacticalDisplayDevice |
| **Group** | Group 2: Network/Communications (0x8018-0x8023) |
| **Risk Level** | 🟢 SAFE |
| **Total Operations** | 12 |
| **Total Registers** | 8 |
| **Implementation File** | `device_0x801E_tactical_display.py` |

## Description

Tactical Display Control (0x801E)

## Operations (12)

### Core Operations

#### `initialize()`

Initialize Tactical Display Control device

**Returns:** `OperationResult`

#### `get_capabilities()`

Get device capabilities

#### `get_status()`

Get current device status

### Configuration Operations

#### `get_security_config()`

Get security configuration

### Advanced Operations

#### `read_register(register)`

Read a device register

#### `list_displays()`

List all connected displays

#### `get_display_info(display_id)`

Get detailed display information

#### `get_display_modes()`

Get available display modes

#### `get_tempest_status()`

Get TEMPEST compliance status

#### `get_overlay_status()`

Get tactical overlay status

#### `get_protection_features()`

Get content protection features

#### `get_statistics()`

Get display statistics

## Hardware Registers (8)

| Register Name | Constant | Offset |
|---------------|----------|--------|
| ACTIVE_DISPLAYS | `REG_ACTIVE_DISPLAYS` | 0x18 |
| BRIGHTNESS | `REG_BRIGHTNESS` | 0x10 |
| DISPLAY_MODE | `REG_DISPLAY_MODE` | 0x08 |
| DISPLAY_STATUS | `REG_DISPLAY_STATUS` | 0x00 |
| OVERLAY_STATUS | `REG_OVERLAY_STATUS` | 0x1C |
| PROTECTION_LEVEL | `REG_PROTECTION_LEVEL` | 0x0C |
| SECURITY_ZONE | `REG_SECURITY_ZONE` | 0x04 |
| TEMPEST_STATUS | `REG_TEMPEST_STATUS` | 0x14 |

## Device Constants

### Status Bits

- `STATUS_DISPLAY_ACTIVE`
- `STATUS_SECURE_MODE`
- `STATUS_PROTECTION_ENABLED`
- `STATUS_TEMPEST_COMPLIANT`
- `STATUS_OVERLAY_ACTIVE`
- `STATUS_NIGHT_MODE`
- `STATUS_WATERMARK_ACTIVE`
- `STATUS_CAPTURE_BLOCKED`

## Usage Example

```python
from dsmil_auto_discover import discover_all_devices

# Auto-discover all devices
registry = discover_all_devices()

# Get device 0x801E
device = registry.get_device(32798)

# Initialize device
result = device.initialize()
if result.success:
    print(f'Device initialized: {device.name}')

# Get device status
status = device.get_status()
print(f'Device status: {status}')
```

---

**Document Generated:** 2025-11-08  
**Framework Version:** 2.0.0 (Auto-Discovery)  
**Classification:** UNCLASSIFIED // FOR OFFICIAL USE ONLY
