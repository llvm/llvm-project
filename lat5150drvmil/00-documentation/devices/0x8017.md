# Device 0x8017: RemoteAccessDevice

## Device Information

| Property | Value |
|----------|-------|
| **Device ID** | `0x8017` (32791) |
| **Name** | RemoteAccessDevice |
| **Group** | Group 1: Extended Security (0x800C-0x8017) |
| **Risk Level** | 🟢 SAFE |
| **Total Operations** | 12 |
| **Total Registers** | 8 |
| **Implementation File** | `device_0x8017_remote_access.py` |

## Description

Remote Access (0x8017)

## Operations (12)

### Core Operations

#### `initialize()`

Initialize Remote Access device

**Returns:** `OperationResult`

#### `get_capabilities()`

Get device capabilities

#### `get_status()`

Get current device status

### Configuration Operations

#### `get_security_config()`

Get remote access security configuration

### Advanced Operations

#### `read_register(register)`

Read a device register

#### `list_sessions(status_filter)`

List all sessions or filter by status

#### `get_active_sessions()`

Get all active sessions

#### `get_session_info(session_id)`

Get detailed information about a specific session

#### `get_access_history(limit)`

Get access history

#### `get_failed_attempts()`

Get failed access attempts summary

#### `get_session_summary()`

Get comprehensive session summary

#### `get_statistics()`

Get remote access statistics

## Hardware Registers (8)

| Register Name | Constant | Offset |
|---------------|----------|--------|
| ACCESS_POLICY | `REG_ACCESS_POLICY` | 0x18 |
| ACCESS_STATUS | `REG_ACCESS_STATUS` | 0x00 |
| ACTIVE_SESSIONS | `REG_ACTIVE_SESSIONS` | 0x08 |
| FAILED_ATTEMPTS | `REG_FAILED_ATTEMPTS` | 0x0C |
| LAST_ACCESS | `REG_LAST_ACCESS` | 0x1C |
| LOCKOUT_COUNT | `REG_LOCKOUT_COUNT` | 0x10 |
| MFA_ENABLED | `REG_MFA_ENABLED` | 0x14 |
| SESSION_COUNT | `REG_SESSION_COUNT` | 0x04 |

## Device Constants

### Status Bits

- `STATUS_ACCESS_ENABLED`
- `STATUS_MFA_REQUIRED`
- `STATUS_CERT_REQUIRED`
- `STATUS_LOCKOUT_ACTIVE`
- `STATUS_AUDIT_ENABLED`
- `STATUS_RATE_LIMITED`

## Usage Example

```python
from dsmil_auto_discover import discover_all_devices

# Auto-discover all devices
registry = discover_all_devices()

# Get device 0x8017
device = registry.get_device(32791)

# Initialize device
result = device.initialize()
if result.success:
    print(f'Device initialized: {device.name}')

# Get device status
status = device.get_status()
print(f'Device status: {status}')
```

---

**Document Generated:** 2025-11-08  
**Framework Version:** 2.0.0 (Auto-Discovery)  
**Classification:** UNCLASSIFIED // FOR OFFICIAL USE ONLY
