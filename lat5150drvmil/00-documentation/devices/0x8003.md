# Device 0x8003: AuditLogDevice

## Device Information

| Property | Value |
|----------|-------|
| **Device ID** | `0x8003` (32771) |
| **Name** | AuditLogDevice |
| **Group** | Group 0: Core Security (0x8000-0x800B) |
| **Risk Level** | 🟢 SAFE |
| **Total Operations** | 9 |
| **Total Registers** | 6 |
| **Implementation File** | `device_0x8003_audit_log.py` |

## Description

Audit Log (0x8003)

## Operations (9)

### Core Operations

#### `initialize()`

Initialize Audit Log device

**Returns:** `OperationResult`

#### `get_capabilities()`

Get device capabilities

#### `get_status()`

Get current device status

### Advanced Operations

#### `read_register(register)`

Read a device register

#### `get_recent_entries(limit)`

Get recent audit entries

#### `get_entries_by_severity(severity)`

Get audit entries by severity level

#### `get_entries_by_category(category)`

Get audit entries by category

#### `get_summary()`

Get audit log summary

#### `get_statistics()`

Get audit log statistics

## Hardware Registers (6)

| Register Name | Constant | Offset |
|---------------|----------|--------|
| LAST_SEVERITY | `REG_LAST_SEVERITY` | 0x10 |
| LOG_CAPACITY | `REG_LOG_CAPACITY` | 0x08 |
| LOG_COUNT | `REG_LOG_COUNT` | 0x04 |
| LOG_POSITION | `REG_LOG_POSITION` | 0x0C |
| LOG_STATUS | `REG_LOG_STATUS` | 0x00 |
| TAMPER_STATUS | `REG_TAMPER_STATUS` | 0x14 |

## Device Constants

### Status Bits

- `STATUS_LOG_ACTIVE`
- `STATUS_LOG_FULL`
- `STATUS_TAMPER_DETECTED`
- `STATUS_SEALED`
- `STATUS_ENCRYPTED`
- `STATUS_REMOTE_SYNC`

## Usage Example

```python
from dsmil_auto_discover import discover_all_devices

# Auto-discover all devices
registry = discover_all_devices()

# Get device 0x8003
device = registry.get_device(32771)

# Initialize device
result = device.initialize()
if result.success:
    print(f'Device initialized: {device.name}')

# Get device status
status = device.get_status()
print(f'Device status: {status}')
```

---

**Document Generated:** 2025-11-08  
**Framework Version:** 2.0.0 (Auto-Discovery)  
**Classification:** UNCLASSIFIED // FOR OFFICIAL USE ONLY
