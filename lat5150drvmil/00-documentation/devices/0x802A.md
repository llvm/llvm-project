# Device 0x802A: NetworkMonitorDevice

## Device Information

| Property | Value |
|----------|-------|
| **Device ID** | `0x802A` (32810) |
| **Name** | NetworkMonitorDevice |
| **Group** | Group 3: Data Processing (0x8024-0x802F) |
| **Risk Level** | 🟢 SAFE |
| **Total Operations** | 13 |
| **Total Registers** | 8 |
| **Implementation File** | `device_0x802A_network_monitor.py` |

## Description

Network Monitor (0x802A)

## Operations (13)

### Core Operations

#### `initialize()`

Initialize Network Monitor device

**Returns:** `OperationResult`

#### `get_capabilities()`

Get device capabilities

#### `get_status()`

Get current device status

### Configuration Operations

#### `get_monitoring_config()`

Get network monitoring configuration

### Advanced Operations

#### `read_register(register)`

Read a device register

#### `list_interfaces()`

List all monitored network interfaces

#### `get_traffic_stats()`

Get overall traffic statistics

#### `get_protocol_breakdown()`

Get traffic breakdown by protocol

#### `get_alerts(severity_filter, limit)`

Get network alerts

#### `get_bandwidth_usage()`

Get current bandwidth usage

#### `get_anomalies(limit)`

Get detected network anomalies

#### `get_summary()`

Get comprehensive network monitoring summary

#### `get_statistics()`

Get network monitor statistics

## Hardware Registers (8)

| Register Name | Constant | Offset |
|---------------|----------|--------|
| ALERT_COUNT | `REG_ALERT_COUNT` | 0x10 |
| ANOMALY_COUNT | `REG_ANOMALY_COUNT` | 0x14 |
| BANDWIDTH_USAGE | `REG_BANDWIDTH_USAGE` | 0x0C |
| BYTE_COUNT | `REG_BYTE_COUNT` | 0x08 |
| INTERFACE_COUNT | `REG_INTERFACE_COUNT` | 0x1C |
| MONITORING_MODE | `REG_MONITORING_MODE` | 0x18 |
| MONITOR_STATUS | `REG_MONITOR_STATUS` | 0x00 |
| PACKET_COUNT | `REG_PACKET_COUNT` | 0x04 |

## Device Constants

### Status Bits

- `STATUS_MONITORING_ACTIVE`
- `STATUS_IDS_ENABLED`
- `STATUS_ANOMALY_DETECTION`
- `STATUS_ALERTS_ACTIVE`
- `STATUS_DEEP_INSPECTION`
- `STATUS_PCAP_ENABLED`

## Usage Example

```python
from dsmil_auto_discover import discover_all_devices

# Auto-discover all devices
registry = discover_all_devices()

# Get device 0x802A
device = registry.get_device(32810)

# Initialize device
result = device.initialize()
if result.success:
    print(f'Device initialized: {device.name}')

# Get device status
status = device.get_status()
print(f'Device status: {status}')
```

---

**Document Generated:** 2025-11-08  
**Framework Version:** 2.0.0 (Auto-Discovery)  
**Classification:** UNCLASSIFIED // FOR OFFICIAL USE ONLY
