# Device 0x8004: EventLoggerDevice

## Device Information

| Property | Value |
|----------|-------|
| **Device ID** | `0x8004` (32772) |
| **Name** | EventLoggerDevice |
| **Group** | Group 0: Core Security (0x8000-0x800B) |
| **Risk Level** | 🟢 SAFE |
| **Total Operations** | 10 |
| **Total Registers** | 7 |
| **Implementation File** | `device_0x8004_event_logger.py` |

## Description

Event Logger (0x8004)

## Operations (10)

### Core Operations

#### `initialize()`

Initialize Event Logger device

**Returns:** `OperationResult`

#### `get_capabilities()`

Get device capabilities

#### `get_status()`

Get current device status

### Advanced Operations

#### `read_register(register)`

Read a device register

#### `get_recent_events(limit)`

Get recent events

#### `get_events_by_level(level)`

Get events by severity level

#### `get_events_by_type(event_type)`

Get events by type

#### `get_error_events()`

Get all error and critical events

#### `get_summary()`

Get event log summary

#### `get_statistics()`

Get event logger statistics

## Hardware Registers (7)

| Register Name | Constant | Offset |
|---------------|----------|--------|
| CRITICAL_COUNT | `REG_CRITICAL_COUNT` | 0x18 |
| ERROR_COUNT | `REG_ERROR_COUNT` | 0x14 |
| EVENT_CAPACITY | `REG_EVENT_CAPACITY` | 0x08 |
| EVENT_COUNT | `REG_EVENT_COUNT` | 0x04 |
| EVENT_POSITION | `REG_EVENT_POSITION` | 0x0C |
| LAST_LEVEL | `REG_LAST_LEVEL` | 0x10 |
| LOGGER_STATUS | `REG_LOGGER_STATUS` | 0x00 |

## Device Constants

### Status Bits

- `STATUS_LOGGER_ACTIVE`
- `STATUS_LOGGER_FULL`
- `STATUS_ERRORS_PRESENT`
- `STATUS_CRITICAL_EVENTS`
- `STATUS_REAL_TIME`
- `STATUS_REMOTE_LOGGING`

## Usage Example

```python
from dsmil_auto_discover import discover_all_devices

# Auto-discover all devices
registry = discover_all_devices()

# Get device 0x8004
device = registry.get_device(32772)

# Initialize device
result = device.initialize()
if result.success:
    print(f'Device initialized: {device.name}')

# Get device status
status = device.get_status()
print(f'Device status: {status}')
```

---

**Document Generated:** 2025-11-08  
**Framework Version:** 2.0.0 (Auto-Discovery)  
**Classification:** UNCLASSIFIED // FOR OFFICIAL USE ONLY
