# Device 0x8013: KeyManagementDevice

## Device Information

| Property | Value |
|----------|-------|
| **Device ID** | `0x8013` (32787) |
| **Name** | KeyManagementDevice |
| **Group** | Group 1: Extended Security (0x800C-0x8017) |
| **Risk Level** | 🟢 SAFE |
| **Total Operations** | 12 |
| **Total Registers** | 8 |
| **Implementation File** | `device_0x8013_key_management.py` |

## Description

Key Management (0x8013)

## Operations (12)

### Core Operations

#### `initialize()`

Initialize Key Management device

**Returns:** `OperationResult`

#### `get_capabilities()`

Get device capabilities

#### `get_status()`

Get current device status

### Advanced Operations

#### `read_register(register)`

Read a device register

#### `list_keys(status_filter)`

List all keys or filter by status

#### `get_key_info(key_id)`

Get detailed information about a specific key

#### `get_keys_by_type(key_type)`

Get all keys of a specific type

#### `get_rotation_history(limit)`

Get key rotation history

#### `get_storage_summary()`

Get summary of key storage locations

#### `get_key_summary()`

Get comprehensive key management summary

#### `check_pqc_compliance()`

Check Post-Quantum Cryptography compliance for all keys

#### `get_statistics()`

Get key management statistics

## Hardware Registers (8)

| Register Name | Constant | Offset |
|---------------|----------|--------|
| ACTIVE_KEYS | `REG_ACTIVE_KEYS` | 0x0C |
| EXPIRED_KEYS | `REG_EXPIRED_KEYS` | 0x10 |
| FIPS_MODE | `REG_FIPS_MODE` | 0x1C |
| KEY_CAPACITY | `REG_KEY_CAPACITY` | 0x08 |
| KEY_COUNT | `REG_KEY_COUNT` | 0x04 |
| KM_STATUS | `REG_KM_STATUS` | 0x00 |
| LAST_ROTATION | `REG_LAST_ROTATION` | 0x18 |
| ROTATION_COUNT | `REG_ROTATION_COUNT` | 0x14 |

## Device Constants

### Status Bits

- `STATUS_KM_ACTIVE`
- `STATUS_TPM_AVAILABLE`
- `STATUS_HSM_AVAILABLE`
- `STATUS_FIPS_MODE`
- `STATUS_AUTO_ROTATION`
- `STATUS_BACKUP_ENABLED`

## Usage Example

```python
from dsmil_auto_discover import discover_all_devices

# Auto-discover all devices
registry = discover_all_devices()

# Get device 0x8013
device = registry.get_device(32787)

# Initialize device
result = device.initialize()
if result.success:
    print(f'Device initialized: {device.name}')

# Get device status
status = device.get_status()
print(f'Device status: {status}')
```

---

**Document Generated:** 2025-11-08  
**Framework Version:** 2.0.0 (Auto-Discovery)  
**Classification:** UNCLASSIFIED // FOR OFFICIAL USE ONLY
