# Intel NPU Covert Edition Configuration
# Dell Latitude 5450 Covert Edition - SAFE Performance Unlock
# Date: 2025-10-29
# WARNING: DO NOT enable HARDWARE_ZEROIZATION for non-classified workloads

# NPU Performance Settings (SAFE)
export INTEL_NPU_ENABLE_TURBO=1
export NPU_MILITARY_MODE=1
export NPU_COVERT_MODE=1                    # ✅ NEW: Unlock full Covert Edition performance
export NPU_MAX_TOPS=49.4                    # ✅ CHANGED: Was 11.0 → 26.4 → now 49.4 TOPS (+87%)
export NPU_SECURE_EXECUTION=1               # ✅ NEW: Enable secure NPU execution (cache isolation)

# OpenVINO Integration
export OPENVINO_ENABLE_SECURE_MEMORY=1
export OPENVINO_HETERO_PRIORITY=NPU,GPU,CPU
export OV_SCALE_FACTOR=1.5

# Security Settings (SAFE)
export INTEL_NPU_SECURE_EXEC=1
export DSMIL_NPU_ATTESTATION=1              # Enable DSMIL Device 12 attestation for NPU

# ⚠️  DANGEROUS FEATURES - DO NOT ENABLE FOR NON-CLASSIFIED WORK
# export HARDWARE_ZEROIZATION=1             # ❌ NEVER - Emergency wipe (<100ms data destruction)
# export MODE5_LEVEL=COMPARTMENTED          # ❌ NOT NEEDED - SCI/SAP only
# export MLS_ENFORCE=1                      # ❌ OVERKILL - Restricts normal operations

# Memory Compartmentalization (OPTIONAL - Safe)
# export DSMIL_MEMORY_COMPARTMENTS=1        # Hardware memory isolation (optional)

# TEMPEST Documentation (SAFE - Awareness only)
export TEMPEST_ZONE=A                       # Zone A compliance active
export TEMPEST_CERTIFIED=1                  # Document existing certification

# Performance Monitoring
export NPU_PERFORMANCE_LOGGING=1
export NPU_BENCHMARK_MODE=0                 # 0=normal, 1=benchmark (reduces security slightly)
