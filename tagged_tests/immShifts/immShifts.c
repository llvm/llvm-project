int shl(int x) { return x << 3; }
int asr(int x) { return x >> 3; }           // arithmetic shift expected in C for signed
unsigned lsr(unsigned x) { return x >> 3; } // logical shift expected for unsigned