declare_args() {
  # Iterate unordered llvm containers in reverse.
  llvm_enable_reverse_iteration = false

  # Enable OnDiskCAS.
  llvm_enable_ondisk_cas = current_cpu == "x64" || current_cpu == "arm64"
}
