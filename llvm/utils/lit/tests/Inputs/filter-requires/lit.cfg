import lit.formats

config.name = "filter-requires"
config.suffixes = [".txt"]
config.test_format = lit.formats.ShTest()
config.test_source_root = None
config.test_exec_root = None
config.available_features = {"host"}
if lit_config.params.get("limit"):
    config.limit_to_features = ["Half"]
