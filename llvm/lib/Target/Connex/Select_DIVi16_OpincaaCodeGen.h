//===-- Select_DIVi16_OpincaaCodeGen.h --------------------------*- C++ -*-===//
//
// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
// See https://llvm.org/LICENSE.txt for license information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
//===----------------------------------------------------------------------===//
/// \file
// Code auto-generated by method Kernel::genLLVMISelManualCode()
//   from the OPINCAA lib, from kernel div.i16.
// You should put this code in the Select() method of the SelectionDAGISel
//   class of your back end.
// Number of instructions generated: 471.
//
//===----------------------------------------------------------------------===//


// From /home/asusu/LLVM/Tests/opincaa_standalone_apps/Emulate_i16/DIV_i16_manual/DumpISel_OpincaaCodeGen_old190_330.cpp


SDValue ct0 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R31 = 0;// Instr #0
SDNode *vload0 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct0,
                                    // glue (or chain) input edge
                                    SDValue(nodeOpSrcCast2, 1)
                                    );

SDValue ct1 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R30 = 1;// Instr #1
SDNode *vload1 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct1,
                                    // glue (or chain) input edge
                                    SDValue(vload0, 1)
                                    );

SDValue ct2 = CrtDAG->getSignedConstant(2, DL, MVT::i16, true, false);
// R29 = 2;// Instr #2
SDNode *vload2 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct2,
                                    // glue (or chain) input edge
                                    SDValue(vload1, 1)
                                    );

SDValue ct3 = CrtDAG->getSignedConstant(16, DL, MVT::i16, true, false);
// R09 = 16;// Instr #3
SDNode *vload3 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct3,
                                    // glue (or chain) input edge
                                    SDValue(vload2, 1)
                                    );

SDValue ct4 = CrtDAG->getSignedConstant(-32768, DL, MVT::i16, true, false);
// R28 = -32768;// Instr #4
SDNode *vload4 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct4,
                                    // glue (or chain) input edge
                                    SDValue(vload3, 1)
                                    );

SDValue ct5 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R24 = 0;// Instr #5
SDNode *vload5 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct5,
                                    // glue (or chain) input edge
                                    SDValue(vload4, 1)
                                    );

SDValue ct6 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R25 = 0;// Instr #6
SDNode *vload6 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct6,
                                    // glue (or chain) input edge
                                    SDValue(vload5, 1)
                                    );

SDValue ct7 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R15 = 0;// Instr #7
SDNode *vload7 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct7,
                                    // glue (or chain) input edge
                                    SDValue(vload6, 1)
                                    );

// R11 = R22 == R28;// Instr #8
SDNode *eq0 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast2, 0),
                                    SDValue(vload4, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload7, 1)
                                    );

SDValue ct8 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #9
SDNode *nop0 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct8,
                                    // glue (or chain) input edge
                                    SDValue(eq0, 1)
                                    );

// WHERE_EQ;// Instr #10
SDNode *whereeq0 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop0, 0)
                                    );

// R24 = R21 | R21;// Instr #11
SDNode *or0 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(nodeOpSrcCast1, 0),
                                    SDValue(nodeOpSrcCast1, 0),
                                    SDValue(vload5, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq0, 0)
                                    }
                                    );

// END_WHERE;// Instr #12
SDNode *endwhere0 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or0, 1)
                                    );

// R14 = R21 == R28;// Instr #13
SDNode *eq1 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast1, 0),
                                    SDValue(vload4, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere0, 0)
                                    );

// R14 = R14 & R11;// Instr #14
SDNode *and0 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq0, 0),
                                    SDValue(eq1, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq1, 1)
                                    );

// R14 = R14 == R30;// Instr #15
SDNode *eq2 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and0, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and0, 1)
                                    );

SDValue ct9 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #16
SDNode *nop1 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct9,
                                    // glue (or chain) input edge
                                    SDValue(eq2, 1)
                                    );

// WHERE_EQ;// Instr #17
SDNode *whereeq1 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop1, 0)
                                    );

SDValue ct10 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R25 = 1;// Instr #18
SDNode *vload8 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct10,
                                    SDValue(vload6, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq1, 0)
                                    );

SDValue ct11 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R24 = 0;// Instr #19
SDNode *vload9 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct11,
                                    SDValue(or0, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload8, 1)
                                    );

// END_WHERE;// Instr #20
SDNode *endwhere1 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload9, 1)
                                    );

// R11 = R30 - R11;// Instr #21
SDNode *sub0 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq0, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere1, 0)
                                    );

SDValue ct12 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R23 = 0;// Instr #22
SDNode *vload10 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct12,
                                    // glue (or chain) input edge
                                    SDValue(sub0, 1)
                                    );

// R14 = R21 == R28;// Instr #23
SDNode *eq3 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast1, 0),
                                    SDValue(vload4, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload10, 1)
                                    );

// R14 = R14 & R11;// Instr #24
SDNode *and1 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub0, 0),
                                    SDValue(eq3, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq3, 1)
                                    );

// R14 = R14 == R30;// Instr #25
SDNode *eq4 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and1, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and1, 1)
                                    );

SDValue ct13 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #26
SDNode *nop2 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct13,
                                    // glue (or chain) input edge
                                    SDValue(eq4, 1)
                                    );

// WHERE_EQ;// Instr #27
SDNode *whereeq2 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop2, 0)
                                    );

// R21 = R30 + R21;// Instr #28
SDNode *add0 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(nodeOpSrcCast1, 0),
                                    SDValue(vload1, 0),
                                    SDValue(nodeOpSrcCast1, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq2, 0)
                                    }
                                    );

SDValue ct14 = CrtDAG->getSignedConstant(15, DL, MVT::i16, true, false);
// R23 = R22 >> 15;// Instr #29
SDNode *ishr0 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(nodeOpSrcCast2, 0),
                                    ct14,
                                    SDValue(vload10, 0),
                                    // glue (or chain) input edge
                                    SDValue(add0, 1)
                                    }
                                    );

SDValue ct15 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R23 = R23 << 1;// Instr #30
SDNode *ishl0 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishr0, 0),
                                    ct15,
                                    SDValue(ishr0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr0, 1)
                                    }
                                    );

// R23 = R23 - R30;// Instr #31
SDNode *sub1 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl0, 0),
                                    SDValue(vload1, 0),
                                    SDValue(ishl0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl0, 1)
                                    }
                                    );

// END_WHERE;// Instr #32
SDNode *endwhere2 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub1, 1)
                                    );

// R14 = R21 < R31;// Instr #33
SDNode *lt0 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(add0, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere2, 0)
                                    );

SDValue ct16 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #34
SDNode *nop3 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct16,
                                    // glue (or chain) input edge
                                    SDValue(lt0, 1)
                                    );

// WHERE_LT;// Instr #35
SDNode *wherelt0 = CrtDAG->getMachineNode(
                                    Connex::WHERELT,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop3, 0)
                                    );

// R21 = R31 - R21;// Instr #36
SDNode *sub2 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload0, 0),
                                    SDValue(add0, 0),
                                    SDValue(add0, 0),
                                    // glue (or chain) input edge
                                    SDValue(wherelt0, 0)
                                    }
                                    );

// R15 = R30 | R30;// Instr #37
SDNode *or1 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(vload1, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub2, 1)
                                    }
                                    );

// END_WHERE;// Instr #38
SDNode *endwhere3 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or1, 1)
                                    );

// R14 = R22 < R31;// Instr #39
SDNode *lt1 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast2, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere3, 0)
                                    );

SDValue ct17 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #40
SDNode *nop4 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct17,
                                    // glue (or chain) input edge
                                    SDValue(lt1, 1)
                                    );

// WHERE_LT;// Instr #41
SDNode *wherelt1 = CrtDAG->getMachineNode(
                                    Connex::WHERELT,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop4, 0)
                                    );

// R22 = R31 - R22;// Instr #42
SDNode *sub3 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload0, 0),
                                    SDValue(nodeOpSrcCast2, 0),
                                    SDValue(nodeOpSrcCast2, 0),
                                    // glue (or chain) input edge
                                    SDValue(wherelt1, 0)
                                    }
                                    );

// R15 = R15 | R29;// Instr #43
SDNode *or2 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(or1, 0),
                                    SDValue(or1, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub3, 1)
                                    }
                                    );

// END_WHERE;// Instr #44
SDNode *endwhere4 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or2, 1)
                                    );

SDValue ct18 = CrtDAG->getSignedConstant(16384, DL, MVT::i16, true, false);
// R26 = 16384;// Instr #45
SDNode *vload11 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct18,
                                    // glue (or chain) input edge
                                    SDValue(endwhere4, 0)
                                    );

// R18 = R21 | R21;// Instr #46
SDNode *or3 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub2, 0),
                                    SDValue(sub2, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload11, 1)
                                    );

SDValue ct19 = CrtDAG->getSignedConstant(14, DL, MVT::i16, true, false);
// R20 = R22 << 14;// Instr #47
SDNode *ishl1 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct19,
                                    // glue (or chain) input edge
                                    SDValue(or3, 1)
                                    );

SDValue ct20 = CrtDAG->getSignedConstant(2, DL, MVT::i16, true, false);
// R19 = R22 >> 2;// Instr #48
SDNode *ishr1 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct20,
                                    // glue (or chain) input edge
                                    SDValue(ishl1, 1)
                                    );

// R14 = R20 == R18;// Instr #49
SDNode *eq5 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl1, 0),
                                    SDValue(or3, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr1, 1)
                                    );

// R13 = R19 == R31;// Instr #50
SDNode *eq6 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr1, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq5, 1)
                                    );

// R14 = R14 & R13;// Instr #51
SDNode *and2 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq6, 0),
                                    SDValue(eq5, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq6, 1)
                                    );

// R14 = R14 & R11;// Instr #52
SDNode *and3 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub0, 0),
                                    SDValue(and2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and2, 1)
                                    );

// R14 = R14 == R30;// Instr #53
SDNode *eq7 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and3, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and3, 1)
                                    );

SDValue ct21 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #54
SDNode *nop5 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct21,
                                    // glue (or chain) input edge
                                    SDValue(eq7, 1)
                                    );

// WHERE_EQ;// Instr #55
SDNode *whereeq3 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop5, 0)
                                    );

SDValue ct22 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #56
SDNode *vload12 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct22,
                                    SDValue(or3, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq3, 0)
                                    );

// R25 = R25 | R26;// Instr #57
SDNode *or4 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload11, 0),
                                    SDValue(vload8, 0),
                                    SDValue(vload8, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload12, 1)
                                    }
                                    );

// END_WHERE;// Instr #58
SDNode *endwhere5 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or4, 1)
                                    );

// R14 = R30 - R14;// Instr #59
SDNode *sub4 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq7, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere5, 0)
                                    );

// R11 = R11 & R14;// Instr #60
SDNode *and4 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub4, 0),
                                    SDValue(sub0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub4, 1)
                                    );

// R14 = ULT(R18, R20);// Instr #61
SDNode *ult0 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload12, 0),
                                    SDValue(ishl1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and4, 1)
                                    );

// R14 = R30 - R14;// Instr #62
SDNode *sub5 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ult0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult0, 1)
                                    );

// R13 = R19 == R31;// Instr #63
SDNode *eq8 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr1, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub5, 1)
                                    );

// R14 = R14 & R13;// Instr #64
SDNode *and5 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq8, 0),
                                    SDValue(sub5, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq8, 1)
                                    );

// R14 = R14 & R11;// Instr #65
SDNode *and6 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and4, 0),
                                    SDValue(and5, 0),
                                    // glue (or chain) input edge
                                    SDValue(and5, 1)
                                    );

// R14 = R14 == R30;// Instr #66
SDNode *eq9 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and6, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and6, 1)
                                    );

SDValue ct23 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #67
SDNode *nop6 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct23,
                                    // glue (or chain) input edge
                                    SDValue(eq9, 1)
                                    );

// WHERE_EQ;// Instr #68
SDNode *whereeq4 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop6, 0)
                                    );

// R18 = R18 - R20;// Instr #69
SDNode *sub6 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload12, 0),
                                    SDValue(ishl1, 0),
                                    SDValue(vload12, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq4, 0)
                                    }
                                    );

// R25 = R25 | R26;// Instr #70
SDNode *or5 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload11, 0),
                                    SDValue(or4, 0),
                                    SDValue(or4, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub6, 1)
                                    }
                                    );

// END_WHERE;// Instr #71
SDNode *endwhere6 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or5, 1)
                                    );

// R26 = R26 >> R30;// Instr #72
SDNode *shr0 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload11, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere6, 0)
                                    );

SDValue ct24 = CrtDAG->getSignedConstant(13, DL, MVT::i16, true, false);
// R20 = R22 << 13;// Instr #73
SDNode *ishl2 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct24,
                                    // glue (or chain) input edge
                                    SDValue(shr0, 1)
                                    );

SDValue ct25 = CrtDAG->getSignedConstant(3, DL, MVT::i16, true, false);
// R19 = R22 >> 3;// Instr #74
SDNode *ishr2 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct25,
                                    // glue (or chain) input edge
                                    SDValue(ishl2, 1)
                                    );

// R14 = R20 == R18;// Instr #75
SDNode *eq10 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl2, 0),
                                    SDValue(sub6, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr2, 1)
                                    );

// R13 = R19 == R31;// Instr #76
SDNode *eq11 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr2, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq10, 1)
                                    );

// R14 = R14 & R13;// Instr #77
SDNode *and7 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq11, 0),
                                    SDValue(eq10, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq11, 1)
                                    );

// R14 = R14 & R11;// Instr #78
SDNode *and8 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and4, 0),
                                    SDValue(and7, 0),
                                    // glue (or chain) input edge
                                    SDValue(and7, 1)
                                    );

// R14 = R14 == R30;// Instr #79
SDNode *eq12 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and8, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and8, 1)
                                    );

SDValue ct26 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #80
SDNode *nop7 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct26,
                                    // glue (or chain) input edge
                                    SDValue(eq12, 1)
                                    );

// WHERE_EQ;// Instr #81
SDNode *whereeq5 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop7, 0)
                                    );

SDValue ct27 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #82
SDNode *vload13 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct27,
                                    SDValue(sub6, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq5, 0)
                                    );

// R25 = R25 | R26;// Instr #83
SDNode *or6 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr0, 0),
                                    SDValue(or5, 0),
                                    SDValue(or5, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload13, 1)
                                    }
                                    );

// END_WHERE;// Instr #84
SDNode *endwhere7 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or6, 1)
                                    );

// R14 = R30 - R14;// Instr #85
SDNode *sub7 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq12, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere7, 0)
                                    );

// R11 = R11 & R14;// Instr #86
SDNode *and9 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub7, 0),
                                    SDValue(and4, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub7, 1)
                                    );

// R14 = ULT(R18, R20);// Instr #87
SDNode *ult1 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload13, 0),
                                    SDValue(ishl2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and9, 1)
                                    );

// R14 = R30 - R14;// Instr #88
SDNode *sub8 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ult1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult1, 1)
                                    );

// R13 = R19 == R31;// Instr #89
SDNode *eq13 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr2, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub8, 1)
                                    );

// R14 = R14 & R13;// Instr #90
SDNode *and10 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq13, 0),
                                    SDValue(sub8, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq13, 1)
                                    );

// R14 = R14 & R11;// Instr #91
SDNode *and11 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and9, 0),
                                    SDValue(and10, 0),
                                    // glue (or chain) input edge
                                    SDValue(and10, 1)
                                    );

// R14 = R14 == R30;// Instr #92
SDNode *eq14 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and11, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and11, 1)
                                    );

SDValue ct28 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #93
SDNode *nop8 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct28,
                                    // glue (or chain) input edge
                                    SDValue(eq14, 1)
                                    );

// WHERE_EQ;// Instr #94
SDNode *whereeq6 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop8, 0)
                                    );

// R18 = R18 - R20;// Instr #95
SDNode *sub9 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload13, 0),
                                    SDValue(ishl2, 0),
                                    SDValue(vload13, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq6, 0)
                                    }
                                    );

// R25 = R25 | R26;// Instr #96
SDNode *or7 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr0, 0),
                                    SDValue(or6, 0),
                                    SDValue(or6, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub9, 1)
                                    }
                                    );

// END_WHERE;// Instr #97
SDNode *endwhere8 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or7, 1)
                                    );

// R26 = R26 >> R30;// Instr #98
SDNode *shr1 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr0, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere8, 0)
                                    );

SDValue ct29 = CrtDAG->getSignedConstant(12, DL, MVT::i16, true, false);
// R20 = R22 << 12;// Instr #99
SDNode *ishl3 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct29,
                                    // glue (or chain) input edge
                                    SDValue(shr1, 1)
                                    );

SDValue ct30 = CrtDAG->getSignedConstant(4, DL, MVT::i16, true, false);
// R19 = R22 >> 4;// Instr #100
SDNode *ishr3 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct30,
                                    // glue (or chain) input edge
                                    SDValue(ishl3, 1)
                                    );

// R14 = R20 == R18;// Instr #101
SDNode *eq15 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl3, 0),
                                    SDValue(sub9, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr3, 1)
                                    );

// R13 = R19 == R31;// Instr #102
SDNode *eq16 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr3, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq15, 1)
                                    );

// R14 = R14 & R13;// Instr #103
SDNode *and12 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq16, 0),
                                    SDValue(eq15, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq16, 1)
                                    );

// R14 = R14 & R11;// Instr #104
SDNode *and13 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and9, 0),
                                    SDValue(and12, 0),
                                    // glue (or chain) input edge
                                    SDValue(and12, 1)
                                    );

// R14 = R14 == R30;// Instr #105
SDNode *eq17 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and13, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and13, 1)
                                    );

SDValue ct31 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #106
SDNode *nop9 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct31,
                                    // glue (or chain) input edge
                                    SDValue(eq17, 1)
                                    );

// WHERE_EQ;// Instr #107
SDNode *whereeq7 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop9, 0)
                                    );

SDValue ct32 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #108
SDNode *vload14 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct32,
                                    SDValue(sub9, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq7, 0)
                                    );

// R25 = R25 | R26;// Instr #109
SDNode *or8 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr1, 0),
                                    SDValue(or7, 0),
                                    SDValue(or7, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload14, 1)
                                    }
                                    );

// END_WHERE;// Instr #110
SDNode *endwhere9 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or8, 1)
                                    );

// R14 = R30 - R14;// Instr #111
SDNode *sub10 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq17, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere9, 0)
                                    );

// R11 = R11 & R14;// Instr #112
SDNode *and14 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub10, 0),
                                    SDValue(and9, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub10, 1)
                                    );

// R14 = ULT(R18, R20);// Instr #113
SDNode *ult2 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload14, 0),
                                    SDValue(ishl3, 0),
                                    // glue (or chain) input edge
                                    SDValue(and14, 1)
                                    );

// R14 = R30 - R14;// Instr #114
SDNode *sub11 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ult2, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult2, 1)
                                    );

// R13 = R19 == R31;// Instr #115
SDNode *eq18 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr3, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub11, 1)
                                    );

// R14 = R14 & R13;// Instr #116
SDNode *and15 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq18, 0),
                                    SDValue(sub11, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq18, 1)
                                    );

// R14 = R14 & R11;// Instr #117
SDNode *and16 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and14, 0),
                                    SDValue(and15, 0),
                                    // glue (or chain) input edge
                                    SDValue(and15, 1)
                                    );

// R14 = R14 == R30;// Instr #118
SDNode *eq19 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and16, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and16, 1)
                                    );

SDValue ct33 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #119
SDNode *nop10 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct33,
                                    // glue (or chain) input edge
                                    SDValue(eq19, 1)
                                    );

// WHERE_EQ;// Instr #120
SDNode *whereeq8 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop10, 0)
                                    );

// R18 = R18 - R20;// Instr #121
SDNode *sub12 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload14, 0),
                                    SDValue(ishl3, 0),
                                    SDValue(vload14, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq8, 0)
                                    }
                                    );

// R25 = R25 | R26;// Instr #122
SDNode *or9 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr1, 0),
                                    SDValue(or8, 0),
                                    SDValue(or8, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub12, 1)
                                    }
                                    );

// END_WHERE;// Instr #123
SDNode *endwhere10 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or9, 1)
                                    );

// R26 = R26 >> R30;// Instr #124
SDNode *shr2 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr1, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere10, 0)
                                    );

SDValue ct34 = CrtDAG->getSignedConstant(11, DL, MVT::i16, true, false);
// R20 = R22 << 11;// Instr #125
SDNode *ishl4 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct34,
                                    // glue (or chain) input edge
                                    SDValue(shr2, 1)
                                    );

SDValue ct35 = CrtDAG->getSignedConstant(5, DL, MVT::i16, true, false);
// R19 = R22 >> 5;// Instr #126
SDNode *ishr4 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct35,
                                    // glue (or chain) input edge
                                    SDValue(ishl4, 1)
                                    );

// R14 = R20 == R18;// Instr #127
SDNode *eq20 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl4, 0),
                                    SDValue(sub12, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr4, 1)
                                    );

// R13 = R19 == R31;// Instr #128
SDNode *eq21 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr4, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq20, 1)
                                    );

// R14 = R14 & R13;// Instr #129
SDNode *and17 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq21, 0),
                                    SDValue(eq20, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq21, 1)
                                    );

// R14 = R14 & R11;// Instr #130
SDNode *and18 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and14, 0),
                                    SDValue(and17, 0),
                                    // glue (or chain) input edge
                                    SDValue(and17, 1)
                                    );

// R14 = R14 == R30;// Instr #131
SDNode *eq22 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and18, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and18, 1)
                                    );

SDValue ct36 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #132
SDNode *nop11 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct36,
                                    // glue (or chain) input edge
                                    SDValue(eq22, 1)
                                    );

// WHERE_EQ;// Instr #133
SDNode *whereeq9 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop11, 0)
                                    );

SDValue ct37 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #134
SDNode *vload15 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct37,
                                    SDValue(sub12, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq9, 0)
                                    );

// R25 = R25 | R26;// Instr #135
SDNode *or10 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr2, 0),
                                    SDValue(or9, 0),
                                    SDValue(or9, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload15, 1)
                                    }
                                    );

// END_WHERE;// Instr #136
SDNode *endwhere11 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or10, 1)
                                    );

// R14 = R30 - R14;// Instr #137
SDNode *sub13 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq22, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere11, 0)
                                    );

// R11 = R11 & R14;// Instr #138
SDNode *and19 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub13, 0),
                                    SDValue(and14, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub13, 1)
                                    );

// R14 = ULT(R18, R20);// Instr #139
SDNode *ult3 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload15, 0),
                                    SDValue(ishl4, 0),
                                    // glue (or chain) input edge
                                    SDValue(and19, 1)
                                    );

// R14 = R30 - R14;// Instr #140
SDNode *sub14 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ult3, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult3, 1)
                                    );

// R13 = R19 == R31;// Instr #141
SDNode *eq23 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr4, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub14, 1)
                                    );

// R14 = R14 & R13;// Instr #142
SDNode *and20 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq23, 0),
                                    SDValue(sub14, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq23, 1)
                                    );

// R14 = R14 & R11;// Instr #143
SDNode *and21 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and19, 0),
                                    SDValue(and20, 0),
                                    // glue (or chain) input edge
                                    SDValue(and20, 1)
                                    );

// R14 = R14 == R30;// Instr #144
SDNode *eq24 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and21, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and21, 1)
                                    );

SDValue ct38 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #145
SDNode *nop12 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct38,
                                    // glue (or chain) input edge
                                    SDValue(eq24, 1)
                                    );

// WHERE_EQ;// Instr #146
SDNode *whereeq10 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop12, 0)
                                    );

// R18 = R18 - R20;// Instr #147
SDNode *sub15 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload15, 0),
                                    SDValue(ishl4, 0),
                                    SDValue(vload15, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq10, 0)
                                    }
                                    );

// R25 = R25 | R26;// Instr #148
SDNode *or11 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr2, 0),
                                    SDValue(or10, 0),
                                    SDValue(or10, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub15, 1)
                                    }
                                    );

// END_WHERE;// Instr #149
SDNode *endwhere12 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or11, 1)
                                    );

// R26 = R26 >> R30;// Instr #150
SDNode *shr3 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr2, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere12, 0)
                                    );

SDValue ct39 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R20 = R22 << 10;// Instr #151
SDNode *ishl5 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct39,
                                    // glue (or chain) input edge
                                    SDValue(shr3, 1)
                                    );

SDValue ct40 = CrtDAG->getSignedConstant(6, DL, MVT::i16, true, false);
// R19 = R22 >> 6;// Instr #152
SDNode *ishr5 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct40,
                                    // glue (or chain) input edge
                                    SDValue(ishl5, 1)
                                    );

// R14 = R20 == R18;// Instr #153
SDNode *eq25 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl5, 0),
                                    SDValue(sub15, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr5, 1)
                                    );

// R13 = R19 == R31;// Instr #154
SDNode *eq26 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr5, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq25, 1)
                                    );

// R14 = R14 & R13;// Instr #155
SDNode *and22 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq26, 0),
                                    SDValue(eq25, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq26, 1)
                                    );

// R14 = R14 & R11;// Instr #156
SDNode *and23 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and19, 0),
                                    SDValue(and22, 0),
                                    // glue (or chain) input edge
                                    SDValue(and22, 1)
                                    );

// R14 = R14 == R30;// Instr #157
SDNode *eq27 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and23, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and23, 1)
                                    );

SDValue ct41 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #158
SDNode *nop13 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct41,
                                    // glue (or chain) input edge
                                    SDValue(eq27, 1)
                                    );

// WHERE_EQ;// Instr #159
SDNode *whereeq11 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop13, 0)
                                    );

SDValue ct42 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #160
SDNode *vload16 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct42,
                                    SDValue(sub15, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq11, 0)
                                    );

// R25 = R25 | R26;// Instr #161
SDNode *or12 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr3, 0),
                                    SDValue(or11, 0),
                                    SDValue(or11, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload16, 1)
                                    }
                                    );

// END_WHERE;// Instr #162
SDNode *endwhere13 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or12, 1)
                                    );

// R14 = R30 - R14;// Instr #163
SDNode *sub16 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq27, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere13, 0)
                                    );

// R11 = R11 & R14;// Instr #164
SDNode *and24 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub16, 0),
                                    SDValue(and19, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub16, 1)
                                    );

// R14 = ULT(R18, R20);// Instr #165
SDNode *ult4 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload16, 0),
                                    SDValue(ishl5, 0),
                                    // glue (or chain) input edge
                                    SDValue(and24, 1)
                                    );

// R14 = R30 - R14;// Instr #166
SDNode *sub17 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ult4, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult4, 1)
                                    );

// R13 = R19 == R31;// Instr #167
SDNode *eq28 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr5, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub17, 1)
                                    );

// R14 = R14 & R13;// Instr #168
SDNode *and25 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq28, 0),
                                    SDValue(sub17, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq28, 1)
                                    );

// R14 = R14 & R11;// Instr #169
SDNode *and26 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and24, 0),
                                    SDValue(and25, 0),
                                    // glue (or chain) input edge
                                    SDValue(and25, 1)
                                    );

// R14 = R14 == R30;// Instr #170
SDNode *eq29 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and26, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and26, 1)
                                    );

SDValue ct43 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #171
SDNode *nop14 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct43,
                                    // glue (or chain) input edge
                                    SDValue(eq29, 1)
                                    );

// WHERE_EQ;// Instr #172
SDNode *whereeq12 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop14, 0)
                                    );

// R18 = R18 - R20;// Instr #173
SDNode *sub18 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload16, 0),
                                    SDValue(ishl5, 0),
                                    SDValue(vload16, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq12, 0)
                                    }
                                    );

// R25 = R25 | R26;// Instr #174
SDNode *or13 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr3, 0),
                                    SDValue(or12, 0),
                                    SDValue(or12, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub18, 1)
                                    }
                                    );

// END_WHERE;// Instr #175
SDNode *endwhere14 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or13, 1)
                                    );

// R26 = R26 >> R30;// Instr #176
SDNode *shr4 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr3, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere14, 0)
                                    );

SDValue ct44 = CrtDAG->getSignedConstant(9, DL, MVT::i16, true, false);
// R20 = R22 << 9;// Instr #177
SDNode *ishl6 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct44,
                                    // glue (or chain) input edge
                                    SDValue(shr4, 1)
                                    );

SDValue ct45 = CrtDAG->getSignedConstant(7, DL, MVT::i16, true, false);
// R19 = R22 >> 7;// Instr #178
SDNode *ishr6 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct45,
                                    // glue (or chain) input edge
                                    SDValue(ishl6, 1)
                                    );

// R14 = R20 == R18;// Instr #179
SDNode *eq30 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl6, 0),
                                    SDValue(sub18, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr6, 1)
                                    );

// R13 = R19 == R31;// Instr #180
SDNode *eq31 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr6, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq30, 1)
                                    );

// R14 = R14 & R13;// Instr #181
SDNode *and27 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq31, 0),
                                    SDValue(eq30, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq31, 1)
                                    );

// R14 = R14 & R11;// Instr #182
SDNode *and28 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and24, 0),
                                    SDValue(and27, 0),
                                    // glue (or chain) input edge
                                    SDValue(and27, 1)
                                    );

// R14 = R14 == R30;// Instr #183
SDNode *eq32 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and28, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and28, 1)
                                    );

SDValue ct46 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #184
SDNode *nop15 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct46,
                                    // glue (or chain) input edge
                                    SDValue(eq32, 1)
                                    );

// WHERE_EQ;// Instr #185
SDNode *whereeq13 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop15, 0)
                                    );

SDValue ct47 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #186
SDNode *vload17 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct47,
                                    SDValue(sub18, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq13, 0)
                                    );

// R25 = R25 | R26;// Instr #187
SDNode *or14 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr4, 0),
                                    SDValue(or13, 0),
                                    SDValue(or13, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload17, 1)
                                    }
                                    );

// END_WHERE;// Instr #188
SDNode *endwhere15 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or14, 1)
                                    );

// R14 = R30 - R14;// Instr #189
SDNode *sub19 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq32, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere15, 0)
                                    );

// R11 = R11 & R14;// Instr #190
SDNode *and29 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub19, 0),
                                    SDValue(and24, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub19, 1)
                                    );

// R14 = ULT(R18, R20);// Instr #191
SDNode *ult5 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload17, 0),
                                    SDValue(ishl6, 0),
                                    // glue (or chain) input edge
                                    SDValue(and29, 1)
                                    );

// R14 = R30 - R14;// Instr #192
SDNode *sub20 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ult5, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult5, 1)
                                    );

// R13 = R19 == R31;// Instr #193
SDNode *eq33 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr6, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub20, 1)
                                    );

// R14 = R14 & R13;// Instr #194
SDNode *and30 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq33, 0),
                                    SDValue(sub20, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq33, 1)
                                    );

// R14 = R14 & R11;// Instr #195
SDNode *and31 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and29, 0),
                                    SDValue(and30, 0),
                                    // glue (or chain) input edge
                                    SDValue(and30, 1)
                                    );

// R14 = R14 == R30;// Instr #196
SDNode *eq34 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and31, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and31, 1)
                                    );

SDValue ct48 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #197
SDNode *nop16 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct48,
                                    // glue (or chain) input edge
                                    SDValue(eq34, 1)
                                    );

// WHERE_EQ;// Instr #198
SDNode *whereeq14 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop16, 0)
                                    );

// R18 = R18 - R20;// Instr #199
SDNode *sub21 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload17, 0),
                                    SDValue(ishl6, 0),
                                    SDValue(vload17, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq14, 0)
                                    }
                                    );

// R25 = R25 | R26;// Instr #200
SDNode *or15 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr4, 0),
                                    SDValue(or14, 0),
                                    SDValue(or14, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub21, 1)
                                    }
                                    );

// END_WHERE;// Instr #201
SDNode *endwhere16 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or15, 1)
                                    );

// R26 = R26 >> R30;// Instr #202
SDNode *shr5 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr4, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere16, 0)
                                    );

SDValue ct49 = CrtDAG->getSignedConstant(8, DL, MVT::i16, true, false);
// R20 = R22 << 8;// Instr #203
SDNode *ishl7 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct49,
                                    // glue (or chain) input edge
                                    SDValue(shr5, 1)
                                    );

SDValue ct50 = CrtDAG->getSignedConstant(8, DL, MVT::i16, true, false);
// R19 = R22 >> 8;// Instr #204
SDNode *ishr7 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct50,
                                    // glue (or chain) input edge
                                    SDValue(ishl7, 1)
                                    );

// R14 = R20 == R18;// Instr #205
SDNode *eq35 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl7, 0),
                                    SDValue(sub21, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr7, 1)
                                    );

// R13 = R19 == R31;// Instr #206
SDNode *eq36 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr7, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq35, 1)
                                    );

// R14 = R14 & R13;// Instr #207
SDNode *and32 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq36, 0),
                                    SDValue(eq35, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq36, 1)
                                    );

// R14 = R14 & R11;// Instr #208
SDNode *and33 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and29, 0),
                                    SDValue(and32, 0),
                                    // glue (or chain) input edge
                                    SDValue(and32, 1)
                                    );

// R14 = R14 == R30;// Instr #209
SDNode *eq37 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and33, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and33, 1)
                                    );

SDValue ct51 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #210
SDNode *nop17 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct51,
                                    // glue (or chain) input edge
                                    SDValue(eq37, 1)
                                    );

// WHERE_EQ;// Instr #211
SDNode *whereeq15 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop17, 0)
                                    );

SDValue ct52 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #212
SDNode *vload18 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct52,
                                    SDValue(sub21, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq15, 0)
                                    );

// R25 = R25 | R26;// Instr #213
SDNode *or16 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr5, 0),
                                    SDValue(or15, 0),
                                    SDValue(or15, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload18, 1)
                                    }
                                    );

// END_WHERE;// Instr #214
SDNode *endwhere17 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or16, 1)
                                    );

// R14 = R30 - R14;// Instr #215
SDNode *sub22 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq37, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere17, 0)
                                    );

// R11 = R11 & R14;// Instr #216
SDNode *and34 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub22, 0),
                                    SDValue(and29, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub22, 1)
                                    );

// R14 = ULT(R18, R20);// Instr #217
SDNode *ult6 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload18, 0),
                                    SDValue(ishl7, 0),
                                    // glue (or chain) input edge
                                    SDValue(and34, 1)
                                    );

// R14 = R30 - R14;// Instr #218
SDNode *sub23 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ult6, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult6, 1)
                                    );

// R13 = R19 == R31;// Instr #219
SDNode *eq38 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr7, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub23, 1)
                                    );

// R14 = R14 & R13;// Instr #220
SDNode *and35 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq38, 0),
                                    SDValue(sub23, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq38, 1)
                                    );

// R14 = R14 & R11;// Instr #221
SDNode *and36 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and34, 0),
                                    SDValue(and35, 0),
                                    // glue (or chain) input edge
                                    SDValue(and35, 1)
                                    );

// R14 = R14 == R30;// Instr #222
SDNode *eq39 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and36, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and36, 1)
                                    );

SDValue ct53 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #223
SDNode *nop18 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct53,
                                    // glue (or chain) input edge
                                    SDValue(eq39, 1)
                                    );

// WHERE_EQ;// Instr #224
SDNode *whereeq16 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop18, 0)
                                    );

// R18 = R18 - R20;// Instr #225
SDNode *sub24 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload18, 0),
                                    SDValue(ishl7, 0),
                                    SDValue(vload18, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq16, 0)
                                    }
                                    );

// R25 = R25 | R26;// Instr #226
SDNode *or17 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr5, 0),
                                    SDValue(or16, 0),
                                    SDValue(or16, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub24, 1)
                                    }
                                    );

// END_WHERE;// Instr #227
SDNode *endwhere18 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or17, 1)
                                    );

// R26 = R26 >> R30;// Instr #228
SDNode *shr6 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr5, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere18, 0)
                                    );

SDValue ct54 = CrtDAG->getSignedConstant(7, DL, MVT::i16, true, false);
// R20 = R22 << 7;// Instr #229
SDNode *ishl8 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct54,
                                    // glue (or chain) input edge
                                    SDValue(shr6, 1)
                                    );

SDValue ct55 = CrtDAG->getSignedConstant(9, DL, MVT::i16, true, false);
// R19 = R22 >> 9;// Instr #230
SDNode *ishr8 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct55,
                                    // glue (or chain) input edge
                                    SDValue(ishl8, 1)
                                    );

// R14 = R20 == R18;// Instr #231
SDNode *eq40 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl8, 0),
                                    SDValue(sub24, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr8, 1)
                                    );

// R13 = R19 == R31;// Instr #232
SDNode *eq41 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr8, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq40, 1)
                                    );

// R14 = R14 & R13;// Instr #233
SDNode *and37 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq41, 0),
                                    SDValue(eq40, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq41, 1)
                                    );

// R14 = R14 & R11;// Instr #234
SDNode *and38 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and34, 0),
                                    SDValue(and37, 0),
                                    // glue (or chain) input edge
                                    SDValue(and37, 1)
                                    );

// R14 = R14 == R30;// Instr #235
SDNode *eq42 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and38, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and38, 1)
                                    );

SDValue ct56 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #236
SDNode *nop19 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct56,
                                    // glue (or chain) input edge
                                    SDValue(eq42, 1)
                                    );

// WHERE_EQ;// Instr #237
SDNode *whereeq17 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop19, 0)
                                    );

SDValue ct57 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #238
SDNode *vload19 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct57,
                                    SDValue(sub24, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq17, 0)
                                    );

// R25 = R25 | R26;// Instr #239
SDNode *or18 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr6, 0),
                                    SDValue(or17, 0),
                                    SDValue(or17, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload19, 1)
                                    }
                                    );

// END_WHERE;// Instr #240
SDNode *endwhere19 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or18, 1)
                                    );

// R14 = R30 - R14;// Instr #241
SDNode *sub25 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq42, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere19, 0)
                                    );

// R11 = R11 & R14;// Instr #242
SDNode *and39 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub25, 0),
                                    SDValue(and34, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub25, 1)
                                    );

// R14 = ULT(R18, R20);// Instr #243
SDNode *ult7 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload19, 0),
                                    SDValue(ishl8, 0),
                                    // glue (or chain) input edge
                                    SDValue(and39, 1)
                                    );

// R14 = R30 - R14;// Instr #244
SDNode *sub26 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ult7, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult7, 1)
                                    );

// R13 = R19 == R31;// Instr #245
SDNode *eq43 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr8, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub26, 1)
                                    );

// R14 = R14 & R13;// Instr #246
SDNode *and40 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq43, 0),
                                    SDValue(sub26, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq43, 1)
                                    );

// R14 = R14 & R11;// Instr #247
SDNode *and41 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and39, 0),
                                    SDValue(and40, 0),
                                    // glue (or chain) input edge
                                    SDValue(and40, 1)
                                    );

// R14 = R14 == R30;// Instr #248
SDNode *eq44 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and41, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and41, 1)
                                    );

SDValue ct58 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #249
SDNode *nop20 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct58,
                                    // glue (or chain) input edge
                                    SDValue(eq44, 1)
                                    );

// WHERE_EQ;// Instr #250
SDNode *whereeq18 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop20, 0)
                                    );

// R18 = R18 - R20;// Instr #251
SDNode *sub27 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload19, 0),
                                    SDValue(ishl8, 0),
                                    SDValue(vload19, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq18, 0)
                                    }
                                    );

// R25 = R25 | R26;// Instr #252
SDNode *or19 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr6, 0),
                                    SDValue(or18, 0),
                                    SDValue(or18, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub27, 1)
                                    }
                                    );

// END_WHERE;// Instr #253
SDNode *endwhere20 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or19, 1)
                                    );

// R26 = R26 >> R30;// Instr #254
SDNode *shr7 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr6, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere20, 0)
                                    );

SDValue ct59 = CrtDAG->getSignedConstant(6, DL, MVT::i16, true, false);
// R20 = R22 << 6;// Instr #255
SDNode *ishl9 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct59,
                                    // glue (or chain) input edge
                                    SDValue(shr7, 1)
                                    );

SDValue ct60 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R19 = R22 >> 10;// Instr #256
SDNode *ishr9 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct60,
                                    // glue (or chain) input edge
                                    SDValue(ishl9, 1)
                                    );

// R14 = R20 == R18;// Instr #257
SDNode *eq45 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl9, 0),
                                    SDValue(sub27, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr9, 1)
                                    );

// R13 = R19 == R31;// Instr #258
SDNode *eq46 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr9, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq45, 1)
                                    );

// R14 = R14 & R13;// Instr #259
SDNode *and42 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq46, 0),
                                    SDValue(eq45, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq46, 1)
                                    );

// R14 = R14 & R11;// Instr #260
SDNode *and43 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and39, 0),
                                    SDValue(and42, 0),
                                    // glue (or chain) input edge
                                    SDValue(and42, 1)
                                    );

// R14 = R14 == R30;// Instr #261
SDNode *eq47 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and43, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and43, 1)
                                    );

SDValue ct61 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #262
SDNode *nop21 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct61,
                                    // glue (or chain) input edge
                                    SDValue(eq47, 1)
                                    );

// WHERE_EQ;// Instr #263
SDNode *whereeq19 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop21, 0)
                                    );

SDValue ct62 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #264
SDNode *vload20 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct62,
                                    SDValue(sub27, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq19, 0)
                                    );

// R25 = R25 | R26;// Instr #265
SDNode *or20 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr7, 0),
                                    SDValue(or19, 0),
                                    SDValue(or19, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload20, 1)
                                    }
                                    );

// END_WHERE;// Instr #266
SDNode *endwhere21 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or20, 1)
                                    );

// R14 = R30 - R14;// Instr #267
SDNode *sub28 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq47, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere21, 0)
                                    );

// R11 = R11 & R14;// Instr #268
SDNode *and44 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub28, 0),
                                    SDValue(and39, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub28, 1)
                                    );

// R14 = ULT(R18, R20);// Instr #269
SDNode *ult8 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload20, 0),
                                    SDValue(ishl9, 0),
                                    // glue (or chain) input edge
                                    SDValue(and44, 1)
                                    );

// R14 = R30 - R14;// Instr #270
SDNode *sub29 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ult8, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult8, 1)
                                    );

// R13 = R19 == R31;// Instr #271
SDNode *eq48 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr9, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub29, 1)
                                    );

// R14 = R14 & R13;// Instr #272
SDNode *and45 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq48, 0),
                                    SDValue(sub29, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq48, 1)
                                    );

// R14 = R14 & R11;// Instr #273
SDNode *and46 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and44, 0),
                                    SDValue(and45, 0),
                                    // glue (or chain) input edge
                                    SDValue(and45, 1)
                                    );

// R14 = R14 == R30;// Instr #274
SDNode *eq49 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and46, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and46, 1)
                                    );

SDValue ct63 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #275
SDNode *nop22 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct63,
                                    // glue (or chain) input edge
                                    SDValue(eq49, 1)
                                    );

// WHERE_EQ;// Instr #276
SDNode *whereeq20 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop22, 0)
                                    );

// R18 = R18 - R20;// Instr #277
SDNode *sub30 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload20, 0),
                                    SDValue(ishl9, 0),
                                    SDValue(vload20, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq20, 0)
                                    }
                                    );

// R25 = R25 | R26;// Instr #278
SDNode *or21 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr7, 0),
                                    SDValue(or20, 0),
                                    SDValue(or20, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub30, 1)
                                    }
                                    );

// END_WHERE;// Instr #279
SDNode *endwhere22 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or21, 1)
                                    );

// R26 = R26 >> R30;// Instr #280
SDNode *shr8 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr7, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere22, 0)
                                    );

SDValue ct64 = CrtDAG->getSignedConstant(5, DL, MVT::i16, true, false);
// R20 = R22 << 5;// Instr #281
SDNode *ishl10 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct64,
                                    // glue (or chain) input edge
                                    SDValue(shr8, 1)
                                    );

SDValue ct65 = CrtDAG->getSignedConstant(11, DL, MVT::i16, true, false);
// R19 = R22 >> 11;// Instr #282
SDNode *ishr10 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct65,
                                    // glue (or chain) input edge
                                    SDValue(ishl10, 1)
                                    );

// R14 = R20 == R18;// Instr #283
SDNode *eq50 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl10, 0),
                                    SDValue(sub30, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr10, 1)
                                    );

// R13 = R19 == R31;// Instr #284
SDNode *eq51 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr10, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq50, 1)
                                    );

// R14 = R14 & R13;// Instr #285
SDNode *and47 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq51, 0),
                                    SDValue(eq50, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq51, 1)
                                    );

// R14 = R14 & R11;// Instr #286
SDNode *and48 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and44, 0),
                                    SDValue(and47, 0),
                                    // glue (or chain) input edge
                                    SDValue(and47, 1)
                                    );

// R14 = R14 == R30;// Instr #287
SDNode *eq52 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and48, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and48, 1)
                                    );

SDValue ct66 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #288
SDNode *nop23 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct66,
                                    // glue (or chain) input edge
                                    SDValue(eq52, 1)
                                    );

// WHERE_EQ;// Instr #289
SDNode *whereeq21 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop23, 0)
                                    );

SDValue ct67 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #290
SDNode *vload21 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct67,
                                    SDValue(sub30, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq21, 0)
                                    );

// R25 = R25 | R26;// Instr #291
SDNode *or22 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr8, 0),
                                    SDValue(or21, 0),
                                    SDValue(or21, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload21, 1)
                                    }
                                    );

// END_WHERE;// Instr #292
SDNode *endwhere23 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or22, 1)
                                    );

// R14 = R30 - R14;// Instr #293
SDNode *sub31 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq52, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere23, 0)
                                    );

// R11 = R11 & R14;// Instr #294
SDNode *and49 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub31, 0),
                                    SDValue(and44, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub31, 1)
                                    );

// R14 = ULT(R18, R20);// Instr #295
SDNode *ult9 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload21, 0),
                                    SDValue(ishl10, 0),
                                    // glue (or chain) input edge
                                    SDValue(and49, 1)
                                    );

// R14 = R30 - R14;// Instr #296
SDNode *sub32 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ult9, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult9, 1)
                                    );

// R13 = R19 == R31;// Instr #297
SDNode *eq53 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr10, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub32, 1)
                                    );

// R14 = R14 & R13;// Instr #298
SDNode *and50 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq53, 0),
                                    SDValue(sub32, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq53, 1)
                                    );

// R14 = R14 & R11;// Instr #299
SDNode *and51 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and49, 0),
                                    SDValue(and50, 0),
                                    // glue (or chain) input edge
                                    SDValue(and50, 1)
                                    );

// R14 = R14 == R30;// Instr #300
SDNode *eq54 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and51, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and51, 1)
                                    );

SDValue ct68 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #301
SDNode *nop24 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct68,
                                    // glue (or chain) input edge
                                    SDValue(eq54, 1)
                                    );

// WHERE_EQ;// Instr #302
SDNode *whereeq22 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop24, 0)
                                    );

// R18 = R18 - R20;// Instr #303
SDNode *sub33 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload21, 0),
                                    SDValue(ishl10, 0),
                                    SDValue(vload21, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq22, 0)
                                    }
                                    );

// R25 = R25 | R26;// Instr #304
SDNode *or23 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr8, 0),
                                    SDValue(or22, 0),
                                    SDValue(or22, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub33, 1)
                                    }
                                    );

// END_WHERE;// Instr #305
SDNode *endwhere24 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or23, 1)
                                    );

// R26 = R26 >> R30;// Instr #306
SDNode *shr9 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr8, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere24, 0)
                                    );

SDValue ct69 = CrtDAG->getSignedConstant(4, DL, MVT::i16, true, false);
// R20 = R22 << 4;// Instr #307
SDNode *ishl11 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct69,
                                    // glue (or chain) input edge
                                    SDValue(shr9, 1)
                                    );

SDValue ct70 = CrtDAG->getSignedConstant(12, DL, MVT::i16, true, false);
// R19 = R22 >> 12;// Instr #308
SDNode *ishr11 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct70,
                                    // glue (or chain) input edge
                                    SDValue(ishl11, 1)
                                    );

// R14 = R20 == R18;// Instr #309
SDNode *eq55 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl11, 0),
                                    SDValue(sub33, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr11, 1)
                                    );

// R13 = R19 == R31;// Instr #310
SDNode *eq56 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr11, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq55, 1)
                                    );

// R14 = R14 & R13;// Instr #311
SDNode *and52 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq56, 0),
                                    SDValue(eq55, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq56, 1)
                                    );

// R14 = R14 & R11;// Instr #312
SDNode *and53 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and49, 0),
                                    SDValue(and52, 0),
                                    // glue (or chain) input edge
                                    SDValue(and52, 1)
                                    );

// R14 = R14 == R30;// Instr #313
SDNode *eq57 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and53, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and53, 1)
                                    );

SDValue ct71 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #314
SDNode *nop25 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct71,
                                    // glue (or chain) input edge
                                    SDValue(eq57, 1)
                                    );

// WHERE_EQ;// Instr #315
SDNode *whereeq23 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop25, 0)
                                    );

SDValue ct72 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #316
SDNode *vload22 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct72,
                                    SDValue(sub33, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq23, 0)
                                    );

// R25 = R25 | R26;// Instr #317
SDNode *or24 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr9, 0),
                                    SDValue(or23, 0),
                                    SDValue(or23, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload22, 1)
                                    }
                                    );

// END_WHERE;// Instr #318
SDNode *endwhere25 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or24, 1)
                                    );

// R14 = R30 - R14;// Instr #319
SDNode *sub34 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq57, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere25, 0)
                                    );

// R11 = R11 & R14;// Instr #320
SDNode *and54 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub34, 0),
                                    SDValue(and49, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub34, 1)
                                    );

// R14 = ULT(R18, R20);// Instr #321
SDNode *ult10 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload22, 0),
                                    SDValue(ishl11, 0),
                                    // glue (or chain) input edge
                                    SDValue(and54, 1)
                                    );

// R14 = R30 - R14;// Instr #322
SDNode *sub35 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ult10, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult10, 1)
                                    );

// R13 = R19 == R31;// Instr #323
SDNode *eq58 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr11, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub35, 1)
                                    );

// R14 = R14 & R13;// Instr #324
SDNode *and55 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq58, 0),
                                    SDValue(sub35, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq58, 1)
                                    );

// R14 = R14 & R11;// Instr #325
SDNode *and56 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and54, 0),
                                    SDValue(and55, 0),
                                    // glue (or chain) input edge
                                    SDValue(and55, 1)
                                    );

// R14 = R14 == R30;// Instr #326
SDNode *eq59 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and56, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and56, 1)
                                    );

SDValue ct73 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #327
SDNode *nop26 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct73,
                                    // glue (or chain) input edge
                                    SDValue(eq59, 1)
                                    );

// WHERE_EQ;// Instr #328
SDNode *whereeq24 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop26, 0)
                                    );

// R18 = R18 - R20;// Instr #329
SDNode *sub36 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload22, 0),
                                    SDValue(ishl11, 0),
                                    SDValue(vload22, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq24, 0)
                                    }
                                    );

// R25 = R25 | R26;// Instr #330
SDNode *or25 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr9, 0),
                                    SDValue(or24, 0),
                                    SDValue(or24, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub36, 1)
                                    }
                                    );

// END_WHERE;// Instr #331
SDNode *endwhere26 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or25, 1)
                                    );

// R26 = R26 >> R30;// Instr #332
SDNode *shr10 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr9, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere26, 0)
                                    );

SDValue ct74 = CrtDAG->getSignedConstant(3, DL, MVT::i16, true, false);
// R20 = R22 << 3;// Instr #333
SDNode *ishl12 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct74,
                                    // glue (or chain) input edge
                                    SDValue(shr10, 1)
                                    );

SDValue ct75 = CrtDAG->getSignedConstant(13, DL, MVT::i16, true, false);
// R19 = R22 >> 13;// Instr #334
SDNode *ishr12 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct75,
                                    // glue (or chain) input edge
                                    SDValue(ishl12, 1)
                                    );

// R14 = R20 == R18;// Instr #335
SDNode *eq60 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl12, 0),
                                    SDValue(sub36, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr12, 1)
                                    );

// R13 = R19 == R31;// Instr #336
SDNode *eq61 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr12, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq60, 1)
                                    );

// R14 = R14 & R13;// Instr #337
SDNode *and57 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq61, 0),
                                    SDValue(eq60, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq61, 1)
                                    );

// R14 = R14 & R11;// Instr #338
SDNode *and58 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and54, 0),
                                    SDValue(and57, 0),
                                    // glue (or chain) input edge
                                    SDValue(and57, 1)
                                    );

// R14 = R14 == R30;// Instr #339
SDNode *eq62 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and58, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and58, 1)
                                    );

SDValue ct76 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #340
SDNode *nop27 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct76,
                                    // glue (or chain) input edge
                                    SDValue(eq62, 1)
                                    );

// WHERE_EQ;// Instr #341
SDNode *whereeq25 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop27, 0)
                                    );

SDValue ct77 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #342
SDNode *vload23 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct77,
                                    SDValue(sub36, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq25, 0)
                                    );

// R25 = R25 | R26;// Instr #343
SDNode *or26 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr10, 0),
                                    SDValue(or25, 0),
                                    SDValue(or25, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload23, 1)
                                    }
                                    );

// END_WHERE;// Instr #344
SDNode *endwhere27 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or26, 1)
                                    );

// R14 = R30 - R14;// Instr #345
SDNode *sub37 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq62, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere27, 0)
                                    );

// R11 = R11 & R14;// Instr #346
SDNode *and59 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub37, 0),
                                    SDValue(and54, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub37, 1)
                                    );

// R14 = ULT(R18, R20);// Instr #347
SDNode *ult11 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(ishl12, 0),
                                    // glue (or chain) input edge
                                    SDValue(and59, 1)
                                    );

// R14 = R30 - R14;// Instr #348
SDNode *sub38 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ult11, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult11, 1)
                                    );

// R13 = R19 == R31;// Instr #349
SDNode *eq63 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr12, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub38, 1)
                                    );

// R14 = R14 & R13;// Instr #350
SDNode *and60 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq63, 0),
                                    SDValue(sub38, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq63, 1)
                                    );

// R14 = R14 & R11;// Instr #351
SDNode *and61 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and59, 0),
                                    SDValue(and60, 0),
                                    // glue (or chain) input edge
                                    SDValue(and60, 1)
                                    );

// R14 = R14 == R30;// Instr #352
SDNode *eq64 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and61, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and61, 1)
                                    );

SDValue ct78 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #353
SDNode *nop28 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct78,
                                    // glue (or chain) input edge
                                    SDValue(eq64, 1)
                                    );

// WHERE_EQ;// Instr #354
SDNode *whereeq26 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop28, 0)
                                    );

// R18 = R18 - R20;// Instr #355
SDNode *sub39 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload23, 0),
                                    SDValue(ishl12, 0),
                                    SDValue(vload23, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq26, 0)
                                    }
                                    );

// R25 = R25 | R26;// Instr #356
SDNode *or27 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr10, 0),
                                    SDValue(or26, 0),
                                    SDValue(or26, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub39, 1)
                                    }
                                    );

// END_WHERE;// Instr #357
SDNode *endwhere28 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or27, 1)
                                    );

// R26 = R26 >> R30;// Instr #358
SDNode *shr11 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr10, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere28, 0)
                                    );

SDValue ct79 = CrtDAG->getSignedConstant(2, DL, MVT::i16, true, false);
// R20 = R22 << 2;// Instr #359
SDNode *ishl13 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct79,
                                    // glue (or chain) input edge
                                    SDValue(shr11, 1)
                                    );

SDValue ct80 = CrtDAG->getSignedConstant(14, DL, MVT::i16, true, false);
// R19 = R22 >> 14;// Instr #360
SDNode *ishr13 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct80,
                                    // glue (or chain) input edge
                                    SDValue(ishl13, 1)
                                    );

// R14 = R20 == R18;// Instr #361
SDNode *eq65 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl13, 0),
                                    SDValue(sub39, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr13, 1)
                                    );

// R13 = R19 == R31;// Instr #362
SDNode *eq66 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr13, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq65, 1)
                                    );

// R14 = R14 & R13;// Instr #363
SDNode *and62 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq66, 0),
                                    SDValue(eq65, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq66, 1)
                                    );

// R14 = R14 & R11;// Instr #364
SDNode *and63 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and59, 0),
                                    SDValue(and62, 0),
                                    // glue (or chain) input edge
                                    SDValue(and62, 1)
                                    );

// R14 = R14 == R30;// Instr #365
SDNode *eq67 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and63, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and63, 1)
                                    );

SDValue ct81 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #366
SDNode *nop29 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct81,
                                    // glue (or chain) input edge
                                    SDValue(eq67, 1)
                                    );

// WHERE_EQ;// Instr #367
SDNode *whereeq27 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop29, 0)
                                    );

SDValue ct82 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #368
SDNode *vload24 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct82,
                                    SDValue(sub39, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq27, 0)
                                    );

// R25 = R25 | R26;// Instr #369
SDNode *or28 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr11, 0),
                                    SDValue(or27, 0),
                                    SDValue(or27, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload24, 1)
                                    }
                                    );

// END_WHERE;// Instr #370
SDNode *endwhere29 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or28, 1)
                                    );

// R14 = R30 - R14;// Instr #371
SDNode *sub40 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq67, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere29, 0)
                                    );

// R11 = R11 & R14;// Instr #372
SDNode *and64 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub40, 0),
                                    SDValue(and59, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub40, 1)
                                    );

// R14 = ULT(R18, R20);// Instr #373
SDNode *ult12 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(ishl13, 0),
                                    // glue (or chain) input edge
                                    SDValue(and64, 1)
                                    );

// R14 = R30 - R14;// Instr #374
SDNode *sub41 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ult12, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult12, 1)
                                    );

// R13 = R19 == R31;// Instr #375
SDNode *eq68 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr13, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub41, 1)
                                    );

// R14 = R14 & R13;// Instr #376
SDNode *and65 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq68, 0),
                                    SDValue(sub41, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq68, 1)
                                    );

// R14 = R14 & R11;// Instr #377
SDNode *and66 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and64, 0),
                                    SDValue(and65, 0),
                                    // glue (or chain) input edge
                                    SDValue(and65, 1)
                                    );

// R14 = R14 == R30;// Instr #378
SDNode *eq69 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and66, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and66, 1)
                                    );

SDValue ct83 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #379
SDNode *nop30 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct83,
                                    // glue (or chain) input edge
                                    SDValue(eq69, 1)
                                    );

// WHERE_EQ;// Instr #380
SDNode *whereeq28 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop30, 0)
                                    );

// R18 = R18 - R20;// Instr #381
SDNode *sub42 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload24, 0),
                                    SDValue(ishl13, 0),
                                    SDValue(vload24, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq28, 0)
                                    }
                                    );

// R25 = R25 | R26;// Instr #382
SDNode *or29 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr11, 0),
                                    SDValue(or28, 0),
                                    SDValue(or28, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub42, 1)
                                    }
                                    );

// END_WHERE;// Instr #383
SDNode *endwhere30 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or29, 1)
                                    );

// R26 = R26 >> R30;// Instr #384
SDNode *shr12 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr11, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere30, 0)
                                    );

SDValue ct84 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R20 = R22 << 1;// Instr #385
SDNode *ishl14 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct84,
                                    // glue (or chain) input edge
                                    SDValue(shr12, 1)
                                    );

SDValue ct85 = CrtDAG->getSignedConstant(15, DL, MVT::i16, true, false);
// R19 = R22 >> 15;// Instr #386
SDNode *ishr14 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct85,
                                    // glue (or chain) input edge
                                    SDValue(ishl14, 1)
                                    );

// R14 = R20 == R18;// Instr #387
SDNode *eq70 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl14, 0),
                                    SDValue(sub42, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr14, 1)
                                    );

// R13 = R19 == R31;// Instr #388
SDNode *eq71 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr14, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq70, 1)
                                    );

// R14 = R14 & R13;// Instr #389
SDNode *and67 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq71, 0),
                                    SDValue(eq70, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq71, 1)
                                    );

// R14 = R14 & R11;// Instr #390
SDNode *and68 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and64, 0),
                                    SDValue(and67, 0),
                                    // glue (or chain) input edge
                                    SDValue(and67, 1)
                                    );

// R14 = R14 == R30;// Instr #391
SDNode *eq72 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and68, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and68, 1)
                                    );

SDValue ct86 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #392
SDNode *nop31 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct86,
                                    // glue (or chain) input edge
                                    SDValue(eq72, 1)
                                    );

// WHERE_EQ;// Instr #393
SDNode *whereeq29 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop31, 0)
                                    );

SDValue ct87 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #394
SDNode *vload25 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct87,
                                    SDValue(sub42, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq29, 0)
                                    );

// R25 = R25 | R26;// Instr #395
SDNode *or30 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr12, 0),
                                    SDValue(or29, 0),
                                    SDValue(or29, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload25, 1)
                                    }
                                    );

// END_WHERE;// Instr #396
SDNode *endwhere31 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or30, 1)
                                    );

// R14 = R30 - R14;// Instr #397
SDNode *sub43 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq72, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere31, 0)
                                    );

// R11 = R11 & R14;// Instr #398
SDNode *and69 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub43, 0),
                                    SDValue(and64, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub43, 1)
                                    );

// R14 = ULT(R18, R20);// Instr #399
SDNode *ult13 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload25, 0),
                                    SDValue(ishl14, 0),
                                    // glue (or chain) input edge
                                    SDValue(and69, 1)
                                    );

// R14 = R30 - R14;// Instr #400
SDNode *sub44 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ult13, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult13, 1)
                                    );

// R13 = R19 == R31;// Instr #401
SDNode *eq73 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr14, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub44, 1)
                                    );

// R14 = R14 & R13;// Instr #402
SDNode *and70 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq73, 0),
                                    SDValue(sub44, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq73, 1)
                                    );

// R14 = R14 & R11;// Instr #403
SDNode *and71 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and69, 0),
                                    SDValue(and70, 0),
                                    // glue (or chain) input edge
                                    SDValue(and70, 1)
                                    );

// R14 = R14 == R30;// Instr #404
SDNode *eq74 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and71, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and71, 1)
                                    );

SDValue ct88 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #405
SDNode *nop32 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct88,
                                    // glue (or chain) input edge
                                    SDValue(eq74, 1)
                                    );

// WHERE_EQ;// Instr #406
SDNode *whereeq30 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop32, 0)
                                    );

// R18 = R18 - R20;// Instr #407
SDNode *sub45 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload25, 0),
                                    SDValue(ishl14, 0),
                                    SDValue(vload25, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq30, 0)
                                    }
                                    );

// R25 = R25 | R26;// Instr #408
SDNode *or31 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr12, 0),
                                    SDValue(or30, 0),
                                    SDValue(or30, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub45, 1)
                                    }
                                    );

// END_WHERE;// Instr #409
SDNode *endwhere32 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or31, 1)
                                    );

// R26 = R26 >> R30;// Instr #410
SDNode *shr13 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr12, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere32, 0)
                                    );

SDValue ct89 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R20 = R22 << 0;// Instr #411
SDNode *ishl15 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct89,
                                    // glue (or chain) input edge
                                    SDValue(shr13, 1)
                                    );

SDValue ct90 = CrtDAG->getSignedConstant(16, DL, MVT::i16, true, false);
// R19 = R22 >> 16;// Instr #412
SDNode *ishr15 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    ct90,
                                    // glue (or chain) input edge
                                    SDValue(ishl15, 1)
                                    );

// R14 = R20 == R18;// Instr #413
SDNode *eq75 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl15, 0),
                                    SDValue(sub45, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr15, 1)
                                    );

// R13 = R19 == R31;// Instr #414
SDNode *eq76 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr15, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq75, 1)
                                    );

// R14 = R14 & R13;// Instr #415
SDNode *and72 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq76, 0),
                                    SDValue(eq75, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq76, 1)
                                    );

// R14 = R14 & R11;// Instr #416
SDNode *and73 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and69, 0),
                                    SDValue(and72, 0),
                                    // glue (or chain) input edge
                                    SDValue(and72, 1)
                                    );

// R14 = R14 == R30;// Instr #417
SDNode *eq77 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and73, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and73, 1)
                                    );

SDValue ct91 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #418
SDNode *nop33 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct91,
                                    // glue (or chain) input edge
                                    SDValue(eq77, 1)
                                    );

// WHERE_EQ;// Instr #419
SDNode *whereeq31 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop33, 0)
                                    );

SDValue ct92 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #420
SDNode *vload26 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct92,
                                    SDValue(sub45, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq31, 0)
                                    );

// R25 = R25 | R26;// Instr #421
SDNode *or32 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr13, 0),
                                    SDValue(or31, 0),
                                    SDValue(or31, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload26, 1)
                                    }
                                    );

// END_WHERE;// Instr #422
SDNode *endwhere33 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or32, 1)
                                    );

// R14 = R30 - R14;// Instr #423
SDNode *sub46 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq77, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere33, 0)
                                    );

// R11 = R11 & R14;// Instr #424
SDNode *and74 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub46, 0),
                                    SDValue(and69, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub46, 1)
                                    );

// R14 = ULT(R18, R20);// Instr #425
SDNode *ult14 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload26, 0),
                                    SDValue(ishl15, 0),
                                    // glue (or chain) input edge
                                    SDValue(and74, 1)
                                    );

// R14 = R30 - R14;// Instr #426
SDNode *sub47 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ult14, 0),
                                    // glue (or chain) input edge
                                    SDValue(ult14, 1)
                                    );

// R13 = R19 == R31;// Instr #427
SDNode *eq78 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr15, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub47, 1)
                                    );

// R14 = R14 & R13;// Instr #428
SDNode *and75 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq78, 0),
                                    SDValue(sub47, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq78, 1)
                                    );

// R14 = R14 & R11;// Instr #429
SDNode *and76 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and74, 0),
                                    SDValue(and75, 0),
                                    // glue (or chain) input edge
                                    SDValue(and75, 1)
                                    );

// R14 = R14 == R30;// Instr #430
SDNode *eq79 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and76, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and76, 1)
                                    );

SDValue ct93 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #431
SDNode *nop34 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct93,
                                    // glue (or chain) input edge
                                    SDValue(eq79, 1)
                                    );

// WHERE_EQ;// Instr #432
SDNode *whereeq32 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop34, 0)
                                    );

// R18 = R18 - R20;// Instr #433
SDNode *sub48 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload26, 0),
                                    SDValue(ishl15, 0),
                                    SDValue(vload26, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq32, 0)
                                    }
                                    );

// R25 = R25 | R26;// Instr #434
SDNode *or33 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr13, 0),
                                    SDValue(or32, 0),
                                    SDValue(or32, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub48, 1)
                                    }
                                    );

// END_WHERE;// Instr #435
SDNode *endwhere34 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or33, 1)
                                    );

// R26 = R26 >> R30;// Instr #436
SDNode *shr14 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr13, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere34, 0)
                                    );

// R14 = R11 == R30;// Instr #437
SDNode *eq80 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and74, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr14, 1)
                                    );

SDValue ct94 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #438
SDNode *nop35 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct94,
                                    // glue (or chain) input edge
                                    SDValue(eq80, 1)
                                    );

// WHERE_EQ;// Instr #439
SDNode *whereeq33 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop35, 0)
                                    );

// R24 = R18 | R18;// Instr #440
SDNode *or34 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub48, 0),
                                    SDValue(sub48, 0),
                                    SDValue(vload9, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq33, 0)
                                    }
                                    );

// END_WHERE;// Instr #441
SDNode *endwhere35 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or34, 1)
                                    );

// R14 = POPCNT(R15);// Instr #442
SDNode *popcnt0 = CrtDAG->getMachineNode(
                                    Connex::POPCNT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or2, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere35, 0)
                                    );

// R14 = R14 & R11;// Instr #443
SDNode *and77 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and74, 0),
                                    SDValue(popcnt0, 0),
                                    // glue (or chain) input edge
                                    SDValue(popcnt0, 1)
                                    );

// R14 = R14 == R30;// Instr #444
SDNode *eq81 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and77, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and77, 1)
                                    );

SDValue ct95 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #445
SDNode *nop36 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct95,
                                    // glue (or chain) input edge
                                    SDValue(eq81, 1)
                                    );

// WHERE_EQ;// Instr #446
SDNode *whereeq34 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop36, 0)
                                    );

// R25 = R31 - R25;// Instr #447
SDNode *sub49 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload0, 0),
                                    SDValue(or33, 0),
                                    SDValue(or33, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq34, 0)
                                    }
                                    );

// END_WHERE;// Instr #448
SDNode *endwhere36 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub49, 1)
                                    );

// R14 = R15 & R30;// Instr #449
SDNode *and78 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(or2, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere36, 0)
                                    );

// R14 = R14 & R11;// Instr #450
SDNode *and79 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and74, 0),
                                    SDValue(and78, 0),
                                    // glue (or chain) input edge
                                    SDValue(and78, 1)
                                    );

// R14 = R14 == R30;// Instr #451
SDNode *eq82 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and79, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and79, 1)
                                    );

SDValue ct96 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #452
SDNode *nop37 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct96,
                                    // glue (or chain) input edge
                                    SDValue(eq82, 1)
                                    );

// WHERE_EQ;// Instr #453
SDNode *whereeq35 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop37, 0)
                                    );

// R24 = R31 - R24;// Instr #454
SDNode *sub50 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload0, 0),
                                    SDValue(or34, 0),
                                    SDValue(or34, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq35, 0)
                                    }
                                    );

// END_WHERE;// Instr #455
SDNode *endwhere37 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub50, 1)
                                    );

// R14 = R23 == R31;// Instr #456
SDNode *eq83 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub1, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere37, 0)
                                    );

// R14 = R14 == R31;// Instr #457
SDNode *eq84 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq83, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq83, 1)
                                    );

SDValue ct97 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #458
SDNode *nop38 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct97,
                                    // glue (or chain) input edge
                                    SDValue(eq84, 1)
                                    );

// WHERE_EQ;// Instr #459
SDNode *whereeq36 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop38, 0)
                                    );

// R24 = R24 - R30;// Instr #460
SDNode *sub51 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub50, 0),
                                    SDValue(vload1, 0),
                                    SDValue(sub50, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq36, 0)
                                    }
                                    );

// R22 = R31 - R22;// Instr #461
SDNode *sub52 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload0, 0),
                                    SDValue(sub3, 0),
                                    SDValue(sub3, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub51, 1)
                                    }
                                    );

// END_WHERE;// Instr #462
SDNode *endwhere38 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub52, 1)
                                    );

// R13 = R24 == R22;// Instr #463
SDNode *eq85 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub51, 0),
                                    SDValue(sub52, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere38, 0)
                                    );

// R14 = R14 & R13;// Instr #464
SDNode *and80 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq85, 0),
                                    SDValue(eq84, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq85, 1)
                                    );

// R14 = R14 == R30;// Instr #465
SDNode *eq86 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and80, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and80, 1)
                                    );

SDValue ct98 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #466
SDNode *nop39 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct98,
                                    // glue (or chain) input edge
                                    SDValue(eq86, 1)
                                    );

// WHERE_EQ;// Instr #467
SDNode *whereeq37 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop39, 0)
                                    );

SDValue ct99 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R24 = 0;// Instr #468
SDNode *vload27 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct99,
                                    SDValue(sub51, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq37, 0)
                                    );

// R25 = R23 + R25;// Instr #469
SDNode *resH /*add1*/ = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub49, 0),
                                    SDValue(sub1, 0),
                                    SDValue(sub49, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload27, 1)
                                    }
                                    );

// END_WHERE;// Instr #470
SDNode *lastNode /*endwhere39*/ = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
// Alex:                                    MVT::Glue,
                                    MVT::Other,
                                    // glue (or chain) input edge
                                    SDValue(resH /*add1*/, 1)
                                    );
