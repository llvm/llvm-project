//===-- Select_DIVf16_OpincaaCodeGen.h --------------------------*- C++ -*-===//
//
// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
// See https://llvm.org/LICENSE.txt for license information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
//===----------------------------------------------------------------------===//
/// \file
// Code auto-generated by method Kernel::genLLVMISelManualCode()
//   from the OPINCAA lib, from kernel div.f16.
// You should put this code in the Select() method of the SelectionDAGISel
//   class of your back end.
// Number of instructions generated: 232.
//
//===----------------------------------------------------------------------===//


// From /home/asusu/LLVM/Tests/opincaa_standalone_apps/Emulate_f16/DIVf16_manual/DumpISel_OpincaaCodeGen_080_191.cpp



SDValue ct0 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R04 = 0;// Instr #0
SDNode *vload0 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct0,
                                    // glue (or chain) input edge
                                    SDValue(nodeOpSrcCast1, 1)
                                    );

SDValue ct1 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R31 = 0;// Instr #1
SDNode *vload1 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct1,
                                    // glue (or chain) input edge
                                    SDValue(vload0, 1)
                                    );

SDValue ct2 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R30 = 1;// Instr #2
SDNode *vload2 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct2,
                                    // glue (or chain) input edge
                                    SDValue(vload1, 1)
                                    );

SDValue ct3 = CrtDAG->getSignedConstant(2, DL, MVT::i16, true, false);
// R29 = 2;// Instr #3
SDNode *vload3 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct3,
                                    // glue (or chain) input edge
                                    SDValue(vload2, 1)
                                    );

SDValue ct4 = CrtDAG->getSignedConstant(15, DL, MVT::i16, true, false);
// R27 = 15;// Instr #4
SDNode *vload4 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct4,
                                    // glue (or chain) input edge
                                    SDValue(vload3, 1)
                                    );

SDValue ct5 = CrtDAG->getSignedConstant(31, DL, MVT::i16, true, false);
// R28 = 31;// Instr #5
SDNode *vload5 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct5,
                                    // glue (or chain) input edge
                                    SDValue(vload4, 1)
                                    );

SDValue ct6 = CrtDAG->getSignedConstant(1023, DL, MVT::i16, true, false);
// R11 = 1023;// Instr #6
SDNode *vload6 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct6,
                                    // glue (or chain) input edge
                                    SDValue(vload5, 1)
                                    );

SDValue ct7 = CrtDAG->getSignedConstant(31744, DL, MVT::i16, true, false);
// R10 = 31744;// Instr #7
SDNode *vload7 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct7,
                                    // glue (or chain) input edge
                                    SDValue(vload6, 1)
                                    );

SDValue ct8 = CrtDAG->getSignedConstant(-32768, DL, MVT::i16, true, false);
// R09 = -32768;// Instr #8
SDNode *vload8 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct8,
                                    // glue (or chain) input edge
                                    SDValue(vload7, 1)
                                    );

SDValue ct9 = CrtDAG->getSignedConstant(1024, DL, MVT::i16, true, false);
// R08 = 1024;// Instr #9
SDNode *vload9 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct9,
                                    // glue (or chain) input edge
                                    SDValue(vload8, 1)
                                    );

SDValue ct10 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R23 = 0;// Instr #10
SDNode *vload10 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct10,
                                    // glue (or chain) input edge
                                    SDValue(vload9, 1)
                                    );

// R23 = R26 & R09;// Instr #11
SDNode *and0 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload8, 0),
                                    SDValue(nodeOpSrcCast1, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload10, 1)
                                    );

// R24 = R26 & R10;// Instr #12
SDNode *and1 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(nodeOpSrcCast1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and0, 1)
                                    );

SDValue ct11 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R24 = R24 >> 10;// Instr #13
SDNode *ishr0 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and1, 0),
                                    ct11,
                                    // glue (or chain) input edge
                                    SDValue(and1, 1)
                                    );

// R25 = R26 & R11;// Instr #14
SDNode *and2 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload6, 0),
                                    SDValue(nodeOpSrcCast1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr0, 1)
                                    );

// R14 = R31 < R25;// Instr #15
SDNode *lt0 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(and2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and2, 1)
                                    );

// R13 = R24 == R31;// Instr #16
SDNode *eq0 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr0, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt0, 1)
                                    );

// R12 = R13 & R14;// Instr #17
SDNode *and3 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt0, 0),
                                    SDValue(eq0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq0, 1)
                                    );

// R12 = R12 == R30;// Instr #18
SDNode *eq1 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and3, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and3, 1)
                                    );

SDValue ct12 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #19
SDNode *nop0 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct12,
                                    // glue (or chain) input edge
                                    SDValue(eq1, 1)
                                    );

// WHERE_EQ;// Instr #20
SDNode *whereeq0 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop0, 0)
                                    );

SDValue ct13 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R24 = 1;// Instr #21
SDNode *vload11 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct13,
                                    SDValue(ishr0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq0, 0)
                                    );

SDValue ct14 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R14 = R25 << 0;// Instr #22
SDNode *ishl0 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and2, 0),
                                    ct14,
                                    SDValue(lt0, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload11, 1)
                                    }
                                    );

SDValue ct15 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R04 = R14 >> 1;// Instr #23
SDNode *ishr1 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl0, 0),
                                    ct15,
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl0, 1)
                                    }
                                    );

// R14 = R14 | R04;// Instr #24
SDNode *or0 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishr1, 0),
                                    SDValue(ishl0, 0),
                                    SDValue(ishl0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr1, 1)
                                    }
                                    );

SDValue ct16 = CrtDAG->getSignedConstant(2, DL, MVT::i16, true, false);
// R04 = R14 >> 2;// Instr #25
SDNode *ishr2 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or0, 0),
                                    ct16,
                                    SDValue(ishr1, 0),
                                    // glue (or chain) input edge
                                    SDValue(or0, 1)
                                    }
                                    );

// R14 = R14 | R04;// Instr #26
SDNode *or1 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishr2, 0),
                                    SDValue(or0, 0),
                                    SDValue(or0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr2, 1)
                                    }
                                    );

SDValue ct17 = CrtDAG->getSignedConstant(4, DL, MVT::i16, true, false);
// R04 = R14 >> 4;// Instr #27
SDNode *ishr3 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or1, 0),
                                    ct17,
                                    SDValue(ishr2, 0),
                                    // glue (or chain) input edge
                                    SDValue(or1, 1)
                                    }
                                    );

// R14 = R14 | R04;// Instr #28
SDNode *or2 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishr3, 0),
                                    SDValue(or1, 0),
                                    SDValue(or1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr3, 1)
                                    }
                                    );

SDValue ct18 = CrtDAG->getSignedConstant(8, DL, MVT::i16, true, false);
// R04 = R14 >> 8;// Instr #29
SDNode *ishr4 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or2, 0),
                                    ct18,
                                    SDValue(ishr3, 0),
                                    // glue (or chain) input edge
                                    SDValue(or2, 1)
                                    }
                                    );

// R14 = R14 | R04;// Instr #30
SDNode *or3 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishr4, 0),
                                    SDValue(or2, 0),
                                    SDValue(or2, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr4, 1)
                                    }
                                    );

// R14 = ~R14;// Instr #31
SDNode *not0 = CrtDAG->getMachineNode(
                                    Connex::NOT_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or3, 0),
                                    SDValue(or3, 0),
                                    // glue (or chain) input edge
                                    SDValue(or3, 1)
                                    );

// R14 = POPCNT(R14);// Instr #32
SDNode *popcnt0 = CrtDAG->getMachineNode(
                                    Connex::POPCNT_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(not0, 0),
                                    SDValue(not0, 0),
                                    // glue (or chain) input edge
                                    SDValue(not0, 1)
                                    );

SDValue ct19 = CrtDAG->getSignedConstant(5, DL, MVT::i16, true, false);
// R04 = 5;// Instr #33
SDNode *vload12 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct19,
                                    SDValue(ishr4, 0),
                                    // glue (or chain) input edge
                                    SDValue(popcnt0, 1)
                                    );

// R04 = R14 - R04;// Instr #34
SDNode *sub0 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(popcnt0, 0),
                                    SDValue(vload12, 0),
                                    SDValue(vload12, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload12, 1)
                                    }
                                    );

// R24 = R24 - R04;// Instr #35
SDNode *sub1 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload11, 0),
                                    SDValue(sub0, 0),
                                    SDValue(vload11, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub0, 1)
                                    }
                                    );

// R25 = R25 << R04;// Instr #36
SDNode *shl0 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and2, 0),
                                    SDValue(sub0, 0),
                                    SDValue(and2, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub1, 1)
                                    }
                                    );

// END_WHERE;// Instr #37
SDNode *endwhere0 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(shl0, 1)
                                    );

// R14 = R24 == R28;// Instr #38
SDNode *eq2 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub1, 0),
                                    SDValue(vload5, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere0, 0)
                                    );

// R14 = R14 | R13;// Instr #39
SDNode *or4 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq0, 0),
                                    SDValue(eq2, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq2, 1)
                                    );

// R14 = R14 == R31;// Instr #40
SDNode *eq3 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or4, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(or4, 1)
                                    );

SDValue ct20 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #41
SDNode *nop1 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct20,
                                    // glue (or chain) input edge
                                    SDValue(eq3, 1)
                                    );

// WHERE_EQ;// Instr #42
SDNode *whereeq1 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop1, 0)
                                    );

// R25 = R25 | R08;// Instr #43
SDNode *or5 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload9, 0),
                                    SDValue(shl0, 0),
                                    SDValue(shl0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq1, 0)
                                    }
                                    );

// END_WHERE;// Instr #44
SDNode *endwhere1 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or5, 1)
                                    );

// R19 = R22 & R09;// Instr #45
SDNode *and4 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload8, 0),
                                    SDValue(nodeOpSrcCast2, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere1, 0)
                                    );

// R20 = R22 & R10;// Instr #46
SDNode *and5 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(nodeOpSrcCast2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and4, 1)
                                    );

SDValue ct21 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R20 = R20 >> 10;// Instr #47
SDNode *ishr5 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and5, 0),
                                    ct21,
                                    // glue (or chain) input edge
                                    SDValue(and5, 1)
                                    );

// R21 = R22 & R11;// Instr #48
SDNode *and6 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload6, 0),
                                    SDValue(nodeOpSrcCast2, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr5, 1)
                                    );

// R14 = R31 < R21;// Instr #49
SDNode *lt1 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(and6, 0),
                                    // glue (or chain) input edge
                                    SDValue(and6, 1)
                                    );

// R13 = R20 == R31;// Instr #50
SDNode *eq4 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr5, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt1, 1)
                                    );

// R12 = R13 & R14;// Instr #51
SDNode *and7 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt1, 0),
                                    SDValue(eq4, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq4, 1)
                                    );

// R12 = R12 == R30;// Instr #52
SDNode *eq5 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and7, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and7, 1)
                                    );

SDValue ct22 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #53
SDNode *nop2 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct22,
                                    // glue (or chain) input edge
                                    SDValue(eq5, 1)
                                    );

// WHERE_EQ;// Instr #54
SDNode *whereeq2 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop2, 0)
                                    );

SDValue ct23 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R20 = 1;// Instr #55
SDNode *vload13 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct23,
                                    SDValue(ishr5, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq2, 0)
                                    );

SDValue ct24 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R14 = R21 << 0;// Instr #56
SDNode *ishl1 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and6, 0),
                                    ct24,
                                    SDValue(lt1, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload13, 1)
                                    }
                                    );

SDValue ct25 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R04 = R14 >> 1;// Instr #57
SDNode *ishr6 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl1, 0),
                                    ct25,
                                    SDValue(sub0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl1, 1)
                                    }
                                    );

// R14 = R14 | R04;// Instr #58
SDNode *or6 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishr6, 0),
                                    SDValue(ishl1, 0),
                                    SDValue(ishl1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr6, 1)
                                    }
                                    );

SDValue ct26 = CrtDAG->getSignedConstant(2, DL, MVT::i16, true, false);
// R04 = R14 >> 2;// Instr #59
SDNode *ishr7 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or6, 0),
                                    ct26,
                                    SDValue(ishr6, 0),
                                    // glue (or chain) input edge
                                    SDValue(or6, 1)
                                    }
                                    );

// R14 = R14 | R04;// Instr #60
SDNode *or7 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishr7, 0),
                                    SDValue(or6, 0),
                                    SDValue(or6, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr7, 1)
                                    }
                                    );

SDValue ct27 = CrtDAG->getSignedConstant(4, DL, MVT::i16, true, false);
// R04 = R14 >> 4;// Instr #61
SDNode *ishr8 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or7, 0),
                                    ct27,
                                    SDValue(ishr7, 0),
                                    // glue (or chain) input edge
                                    SDValue(or7, 1)
                                    }
                                    );

// R14 = R14 | R04;// Instr #62
SDNode *or8 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishr8, 0),
                                    SDValue(or7, 0),
                                    SDValue(or7, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr8, 1)
                                    }
                                    );

SDValue ct28 = CrtDAG->getSignedConstant(8, DL, MVT::i16, true, false);
// R04 = R14 >> 8;// Instr #63
SDNode *ishr9 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or8, 0),
                                    ct28,
                                    SDValue(ishr8, 0),
                                    // glue (or chain) input edge
                                    SDValue(or8, 1)
                                    }
                                    );

// R14 = R14 | R04;// Instr #64
SDNode *or9 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishr9, 0),
                                    SDValue(or8, 0),
                                    SDValue(or8, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr9, 1)
                                    }
                                    );

// R14 = ~R14;// Instr #65
SDNode *not1 = CrtDAG->getMachineNode(
                                    Connex::NOT_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or9, 0),
                                    SDValue(or9, 0),
                                    // glue (or chain) input edge
                                    SDValue(or9, 1)
                                    );

// R14 = POPCNT(R14);// Instr #66
SDNode *popcnt1 = CrtDAG->getMachineNode(
                                    Connex::POPCNT_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(not1, 0),
                                    SDValue(not1, 0),
                                    // glue (or chain) input edge
                                    SDValue(not1, 1)
                                    );

SDValue ct29 = CrtDAG->getSignedConstant(5, DL, MVT::i16, true, false);
// R04 = 5;// Instr #67
SDNode *vload14 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct29,
                                    SDValue(ishr9, 0),
                                    // glue (or chain) input edge
                                    SDValue(popcnt1, 1)
                                    );

// R04 = R14 - R04;// Instr #68
SDNode *sub2 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(popcnt1, 0),
                                    SDValue(vload14, 0),
                                    SDValue(vload14, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload14, 1)
                                    }
                                    );

// R20 = R20 - R04;// Instr #69
SDNode *sub3 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload13, 0),
                                    SDValue(sub2, 0),
                                    SDValue(vload13, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub2, 1)
                                    }
                                    );

// R21 = R21 << R04;// Instr #70
SDNode *shl1 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and6, 0),
                                    SDValue(sub2, 0),
                                    SDValue(and6, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub3, 1)
                                    }
                                    );

// END_WHERE;// Instr #71
SDNode *endwhere2 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(shl1, 1)
                                    );

// R14 = R20 == R28;// Instr #72
SDNode *eq6 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    SDValue(vload5, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere2, 0)
                                    );

// R14 = R14 | R13;// Instr #73
SDNode *or10 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq4, 0),
                                    SDValue(eq6, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq6, 1)
                                    );

// R14 = R14 == R31;// Instr #74
SDNode *eq7 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or10, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(or10, 1)
                                    );

SDValue ct30 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #75
SDNode *nop3 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct30,
                                    // glue (or chain) input edge
                                    SDValue(eq7, 1)
                                    );

// WHERE_EQ;// Instr #76
SDNode *whereeq3 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop3, 0)
                                    );

// R21 = R21 | R08;// Instr #77
SDNode *or11 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload9, 0),
                                    SDValue(shl1, 0),
                                    SDValue(shl1, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq3, 0)
                                    }
                                    );

// END_WHERE;// Instr #78
SDNode *endwhere3 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or11, 1)
                                    );

// R16 = R24 - R20;// Instr #79
SDNode *sub4 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub1, 0),
                                    SDValue(sub3, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere3, 0)
                                    );

SDValue ct31 = CrtDAG->getSignedConstant(14, DL, MVT::i16, true, false);
// R04 = 14;// Instr #80
SDNode *vload15 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct31,
                                    // glue (or chain) input edge
                                    SDValue(sub4, 1)
                                    );

// R16 = R04 + R16;// Instr #81
SDNode *add0 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub4, 0),
                                    SDValue(vload15, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload15, 1)
                                    );

// R14 = ULT(R25, R21);// Instr #82
SDNode *ult0 = CrtDAG->getMachineNode(
                                    Connex::ULT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or5, 0),
                                    SDValue(or11, 0),
                                    // glue (or chain) input edge
                                    SDValue(add0, 1)
                                    );

SDValue ct32 = CrtDAG->getSignedConstant(4, DL, MVT::i16, true, false);
// R25 = R25 << 4;// Instr #83
SDNode *ishl2 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or5, 0),
                                    ct32,
                                    // glue (or chain) input edge
                                    SDValue(ult0, 1)
                                    );

// R14 = R14 == R30;// Instr #84
SDNode *eq8 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ult0, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl2, 1)
                                    );

SDValue ct33 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #85
SDNode *nop4 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct33,
                                    // glue (or chain) input edge
                                    SDValue(eq8, 1)
                                    );

// WHERE_EQ;// Instr #86
SDNode *whereeq4 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop4, 0)
                                    );

// R16 = R16 - R30;// Instr #87
SDNode *sub5 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(add0, 0),
                                    SDValue(vload2, 0),
                                    SDValue(add0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq4, 0)
                                    }
                                    );

SDValue ct34 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R25 = R25 << 1;// Instr #88
SDNode *ishl3 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl2, 0),
                                    ct34,
                                    SDValue(ishl2, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub5, 1)
                                    }
                                    );

// END_WHERE;// Instr #89
SDNode *endwhere4 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(ishl3, 1)
                                    );

SDValue ct35 = CrtDAG->getSignedConstant(6, DL, MVT::i16, true, false);
// R07 = R21 >> 6;// Instr #90
SDNode *ishr10 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or11, 0),
                                    ct35,
                                    // glue (or chain) input edge
                                    SDValue(endwhere4, 0)
                                    );

// R07 = R07 & R27;// Instr #91
SDNode *and8 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload4, 0),
                                    SDValue(ishr10, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr10, 1)
                                    );

SDValue ct36 = CrtDAG->getSignedConstant(1208, DL, MVT::i16, true, false);
// R04 = 1208;// Instr #92
SDNode *vload16 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct36,
                                    // glue (or chain) input edge
                                    SDValue(and8, 1)
                                    );

// R04 = R07 + R04;// Instr #93
SDNode *add1 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload16, 0),
                                    SDValue(and8, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload16, 1)
                                    );

SDValue ct37 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #94
SDNode *nop5 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct37,
                                    // glue (or chain) input edge
                                    SDValue(add1, 1)
                                    );

// R05 = LS[R04];// Instr #95
SDNode *read0 = CrtDAG->getMachineNode(
                                    Connex::LD_INDIRECT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(add1, 0),
                                    // glue (or chain) input edge
                                    SDValue(nop5, 0)
                                    );

SDValue ct38 = CrtDAG->getSignedConstant(1192, DL, MVT::i16, true, false);
// R03 = 1192;// Instr #96
SDNode *vload17 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct38,
                                    // glue (or chain) input edge
                                    SDValue(read0, 1)
                                    );

// R03 = R07 + R03;// Instr #97
SDNode *add2 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload17, 0),
                                    SDValue(and8, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload17, 1)
                                    );

SDValue ct39 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #98
SDNode *nop6 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct39,
                                    // glue (or chain) input edge
                                    SDValue(add2, 1)
                                    );

// R06 = LS[R03];// Instr #99
SDNode *read1 = CrtDAG->getMachineNode(
                                    Connex::LD_INDIRECT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(add2, 0),
                                    // glue (or chain) input edge
                                    SDValue(nop6, 0)
                                    );

SDValue ct40 = CrtDAG->getSignedConstant(63, DL, MVT::i16, true, false);
// R04 = 63;// Instr #100
SDNode *vload18 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct40,
                                    // glue (or chain) input edge
                                    SDValue(read1, 1)
                                    );

// R04 = R21 & R04;// Instr #101
SDNode *and9 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload18, 0),
                                    SDValue(or11, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload18, 1)
                                    );

// MULT_U(R05, R04);// Instr #102
SDNode *mult_u0 = CrtDAG->getMachineNode(
                                    Connex::MULT_U_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(read0, 0),
                                    SDValue(and9, 0),
                                    // glue (or chain) input edge
                                    SDValue(and9, 1)
                                    );

// R04 = MULT_LOW();// Instr #103
SDNode *multlo0 = CrtDAG->getMachineNode(
                                    Connex::MULTLO_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(mult_u0, 0)
                                    );

// R03 = MULT_HIGH();// Instr #104
SDNode *multhi0 = CrtDAG->getMachineNode(
                                    Connex::MULTHI_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(multlo0, 1)
                                    );

SDValue ct41 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R04 = R04 >> 10;// Instr #105
SDNode *ishr11 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(multlo0, 0),
                                    ct41,
                                    // glue (or chain) input edge
                                    SDValue(multhi0, 1)
                                    );

SDValue ct42 = CrtDAG->getSignedConstant(6, DL, MVT::i16, true, false);
// R03 = R03 << 6;// Instr #106
SDNode *ishl4 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(multhi0, 0),
                                    ct42,
                                    // glue (or chain) input edge
                                    SDValue(ishr11, 1)
                                    );

// R04 = R04 | R03;// Instr #107
SDNode *or12 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl4, 0),
                                    SDValue(ishr11, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl4, 1)
                                    );

// R02 = R06 - R04;// Instr #108
SDNode *sub6 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(read1, 0),
                                    SDValue(or12, 0),
                                    // glue (or chain) input edge
                                    SDValue(or12, 1)
                                    );

// MULT_U(R25, R02);// Instr #109
SDNode *mult_u1 = CrtDAG->getMachineNode(
                                    Connex::MULT_U_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(ishl3, 0),
                                    SDValue(sub6, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub6, 1)
                                    );

// R17 = MULT_HIGH();// Instr #110
SDNode *multhi1 = CrtDAG->getMachineNode(
                                    Connex::MULTHI_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(mult_u1, 0)
                                    );

SDValue ct43 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R04 = R25 << 10;// Instr #111
SDNode *ishl5 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl3, 0),
                                    ct43,
                                    // glue (or chain) input edge
                                    SDValue(multhi1, 1)
                                    );

// MULT_U(R17, R21);// Instr #112
SDNode *mult_u2 = CrtDAG->getMachineNode(
                                    Connex::MULT_U_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(multhi1, 0),
                                    SDValue(or11, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl5, 1)
                                    );

// R03 = MULT_LOW();// Instr #113
SDNode *multlo1 = CrtDAG->getMachineNode(
                                    Connex::MULTLO_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(mult_u2, 0)
                                    );

// R01 = R04 - R03;// Instr #114
SDNode *sub7 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl5, 0),
                                    SDValue(multlo1, 0),
                                    // glue (or chain) input edge
                                    SDValue(multlo1, 1)
                                    );

// MULT_U(R01, R02);// Instr #115
SDNode *mult_u3 = CrtDAG->getMachineNode(
                                    Connex::MULT_U_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(sub7, 0),
                                    SDValue(sub6, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub7, 1)
                                    );

// R04 = MULT_HIGH();// Instr #116
SDNode *multhi2 = CrtDAG->getMachineNode(
                                    Connex::MULTHI_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(mult_u3, 0)
                                    );

SDValue ct44 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R04 = R04 >> 10;// Instr #117
SDNode *ishr12 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(multhi2, 0),
                                    ct44,
                                    // glue (or chain) input edge
                                    SDValue(multhi2, 1)
                                    );

// R17 = R04 + R17;// Instr #118
SDNode *add3 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(multhi1, 0),
                                    SDValue(ishr12, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr12, 1)
                                    );

// R17 = R30 + R17;// Instr #119
SDNode *add4 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(add3, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(add3, 1)
                                    );

SDValue ct45 = CrtDAG->getSignedConstant(7, DL, MVT::i16, true, false);
// R04 = 7;// Instr #120
SDNode *vload19 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct45,
                                    // glue (or chain) input edge
                                    SDValue(add4, 1)
                                    );

// R03 = R17 & R04;// Instr #121
SDNode *and10 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload19, 0),
                                    SDValue(add4, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload19, 1)
                                    );

// R14 = R03 == R31;// Instr #122
SDNode *eq9 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and10, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and10, 1)
                                    );

SDValue ct46 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #123
SDNode *nop7 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct46,
                                    // glue (or chain) input edge
                                    SDValue(eq9, 1)
                                    );

// WHERE_EQ;// Instr #124
SDNode *whereeq5 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop7, 0)
                                    );

SDValue ct47 = CrtDAG->getSignedConstant(-2, DL, MVT::i16, true, false);
// R04 = -2;// Instr #125
SDNode *vload20 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct47,
                                    SDValue(vload19, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq5, 0)
                                    );

// R17 = R17 & R04;// Instr #126
SDNode *and11 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload20, 0),
                                    SDValue(add4, 0),
                                    SDValue(add4, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload20, 1)
                                    }
                                    );

// END_WHERE;// Instr #127
SDNode *endwhere5 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(and11, 1)
                                    );

SDValue ct48 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R04 = R25 << 10;// Instr #128
SDNode *ishl6 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl3, 0),
                                    ct48,
                                    // glue (or chain) input edge
                                    SDValue(endwhere5, 0)
                                    );

// MULT_U(R17, R21);// Instr #129
SDNode *mult_u4 = CrtDAG->getMachineNode(
                                    Connex::MULT_U_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(and11, 0),
                                    SDValue(or11, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl6, 1)
                                    );

// R03 = MULT_LOW();// Instr #130
SDNode *multlo2 = CrtDAG->getMachineNode(
                                    Connex::MULTLO_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(mult_u4, 0)
                                    );

// R01 = R01 - R03;// Instr #131
SDNode *sub8 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub7, 0),
                                    SDValue(multlo2, 0),
                                    // glue (or chain) input edge
                                    SDValue(multlo2, 1)
                                    );

// R04 = R01 & R09;// Instr #132
SDNode *and12 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload8, 0),
                                    SDValue(sub8, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub8, 1)
                                    );

// R14 = R04 == R31;// Instr #133
SDNode *eq10 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and12, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and12, 1)
                                    );

// R14 = R30 - R14;// Instr #134
SDNode *sub9 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload2, 0),
                                    SDValue(eq10, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq10, 1)
                                    );

// R14 = R14 == R30;// Instr #135
SDNode *eq11 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub9, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub9, 1)
                                    );

SDValue ct49 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #136
SDNode *nop8 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct49,
                                    // glue (or chain) input edge
                                    SDValue(eq11, 1)
                                    );

// WHERE_EQ;// Instr #137
SDNode *whereeq6 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop8, 0)
                                    );

// R17 = R17 - R29;// Instr #138
SDNode *sub10 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and11, 0),
                                    SDValue(vload3, 0),
                                    SDValue(and11, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq6, 0)
                                    }
                                    );

// END_WHERE;// Instr #139
SDNode *endwhere6 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub10, 1)
                                    );

// R04 = R01 == R31;// Instr #140
SDNode *eq12 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub8, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere6, 0)
                                    );

// R04 = R30 - R04;// Instr #141
SDNode *sub11 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload2, 0),
                                    SDValue(eq12, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq12, 1)
                                    );

// R14 = R30 - R14;// Instr #142
SDNode *sub12 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload2, 0),
                                    SDValue(eq11, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub11, 1)
                                    );

// R14 = R14 & R04;// Instr #143
SDNode *and13 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub11, 0),
                                    SDValue(sub12, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub12, 1)
                                    );

// R14 = R14 == R30;// Instr #144
SDNode *eq13 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and13, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and13, 1)
                                    );

SDValue ct50 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #145
SDNode *nop9 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct50,
                                    // glue (or chain) input edge
                                    SDValue(eq13, 1)
                                    );

// WHERE_EQ;// Instr #146
SDNode *whereeq7 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop9, 0)
                                    );

// R17 = R17 | R30;// Instr #147
SDNode *or13 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(sub10, 0),
                                    SDValue(sub10, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq7, 0)
                                    }
                                    );

// END_WHERE;// Instr #148
SDNode *endwhere7 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or13, 1)
                                    );

SDValue ct51 = CrtDAG->getSignedConstant(16, DL, MVT::i16, true, false);
// R29 = 16;// Instr #149
SDNode *vload21 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct51,
                                    // glue (or chain) input edge
                                    SDValue(endwhere7, 0)
                                    );

SDValue ct52 = CrtDAG->getSignedConstant(8, DL, MVT::i16, true, false);
// R00 = 8;// Instr #150
SDNode *vload22 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct52,
                                    // glue (or chain) input edge
                                    SDValue(vload21, 1)
                                    );

// R15 = R17 & R27;// Instr #151
SDNode *and14 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload4, 0),
                                    SDValue(or13, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload22, 1)
                                    );

SDValue ct53 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R04 = 0;// Instr #152
SDNode *vload23 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct53,
                                    // glue (or chain) input edge
                                    SDValue(and14, 1)
                                    );

SDValue ct54 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R03 = 0;// Instr #153
SDNode *vload24 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct54,
                                    // glue (or chain) input edge
                                    SDValue(vload23, 1)
                                    );

// R12 = R16 < R31;// Instr #154
SDNode *lt2 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub5, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload24, 1)
                                    );

SDValue ct55 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #155
SDNode *nop10 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct55,
                                    // glue (or chain) input edge
                                    SDValue(lt2, 1)
                                    );

// WHERE_LT;// Instr #156
SDNode *wherelt0 = CrtDAG->getMachineNode(
                                    Connex::WHERELT,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop10, 0)
                                    );

// R04 = R31 - R16;// Instr #157
SDNode *sub13 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(sub5, 0),
                                    SDValue(vload23, 0),
                                    // glue (or chain) input edge
                                    SDValue(wherelt0, 0)
                                    }
                                    );

// R03 = R16 & R28;// Instr #158
SDNode *and15 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload5, 0),
                                    SDValue(sub5, 0),
                                    SDValue(vload24, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub13, 1)
                                    }
                                    );

// R03 = R03 - R29;// Instr #159
SDNode *sub14 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and15, 0),
                                    SDValue(vload21, 0),
                                    SDValue(and15, 0),
                                    // glue (or chain) input edge
                                    SDValue(and15, 1)
                                    }
                                    );

// R03 = R17 << R03;// Instr #160
SDNode *shl2 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or13, 0),
                                    SDValue(sub14, 0),
                                    SDValue(sub14, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub14, 1)
                                    }
                                    );

// R03 = R03 == R31;// Instr #161
SDNode *eq14 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl2, 0),
                                    SDValue(vload1, 0),
                                    SDValue(shl2, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl2, 1)
                                    }
                                    );

// R03 = R30 - R03;// Instr #162
SDNode *sub15 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(eq14, 0),
                                    SDValue(eq14, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq14, 1)
                                    }
                                    );

SDValue ct56 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R16 = 0;// Instr #163
SDNode *vload25 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct56,
                                    SDValue(sub5, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub15, 1)
                                    );

// R15 = R17 & R27;// Instr #164
SDNode *and16 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload4, 0),
                                    SDValue(or13, 0),
                                    SDValue(and14, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload25, 1)
                                    }
                                    );

// END_WHERE;// Instr #165
SDNode *endwhere8 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(and16, 1)
                                    );

// R14 = R27 < R04;// Instr #166
SDNode *lt3 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload4, 0),
                                    SDValue(sub13, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere8, 0)
                                    );

SDValue ct57 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #167
SDNode *nop11 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct57,
                                    // glue (or chain) input edge
                                    SDValue(lt3, 1)
                                    );

// WHERE_LT;// Instr #168
SDNode *wherelt1 = CrtDAG->getMachineNode(
                                    Connex::WHERELT,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop11, 0)
                                    );

// R04 = R04 - R29;// Instr #169
SDNode *sub16 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub13, 0),
                                    SDValue(vload21, 0),
                                    SDValue(sub13, 0),
                                    // glue (or chain) input edge
                                    SDValue(wherelt1, 0)
                                    }
                                    );

SDValue ct58 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R17 = 0;// Instr #170
SDNode *vload26 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct58,
                                    SDValue(or13, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub16, 1)
                                    );

// END_WHERE;// Instr #171
SDNode *endwhere9 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload26, 1)
                                    );

// R17 = R17 >> R04;// Instr #172
SDNode *shr0 = CrtDAG->getMachineNode(
                                    Connex::SHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload26, 0),
                                    SDValue(sub16, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere9, 0)
                                    );

// R17 = R17 | R03;// Instr #173
SDNode *or14 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub15, 0),
                                    SDValue(shr0, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr0, 1)
                                    );

// R17 = R00 + R17;// Instr #174
SDNode *add5 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or14, 0),
                                    SDValue(vload22, 0),
                                    // glue (or chain) input edge
                                    SDValue(or14, 1)
                                    );

SDValue ct59 = CrtDAG->getSignedConstant(4, DL, MVT::i16, true, false);
// R17 = R17 >> 4;// Instr #175
SDNode *ishr13 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(add5, 0),
                                    ct59,
                                    // glue (or chain) input edge
                                    SDValue(add5, 1)
                                    );

SDValue ct60 = CrtDAG->getSignedConstant(8, DL, MVT::i16, true, false);
// R04 = 8;// Instr #176
SDNode *vload27 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct60,
                                    // glue (or chain) input edge
                                    SDValue(ishr13, 1)
                                    );

// R04 = R04 ^ R15;// Instr #177
SDNode *xor0 = CrtDAG->getMachineNode(
                                    Connex::XORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and16, 0),
                                    SDValue(vload27, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload27, 1)
                                    );

// R04 = R04 == R31;// Instr #178
SDNode *eq15 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(xor0, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(xor0, 1)
                                    );

// R04 = ~R04;// Instr #179
SDNode *not2 = CrtDAG->getMachineNode(
                                    Connex::NOT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq15, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq15, 1)
                                    );

// R17 = R17 & R04;// Instr #180
SDNode *and17 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(not2, 0),
                                    SDValue(ishr13, 0),
                                    // glue (or chain) input edge
                                    SDValue(not2, 1)
                                    );

// R12 = R17 == R31;// Instr #181
SDNode *eq16 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and17, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and17, 1)
                                    );

SDValue ct61 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #182
SDNode *nop12 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct61,
                                    // glue (or chain) input edge
                                    SDValue(eq16, 1)
                                    );

// WHERE_EQ;// Instr #183
SDNode *whereeq8 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop12, 0)
                                    );

SDValue ct62 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R16 = 0;// Instr #184
SDNode *vload28 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct62,
                                    SDValue(vload25, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq8, 0)
                                    );

// END_WHERE;// Instr #185
SDNode *endwhere10 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload28, 1)
                                    );

SDValue ct63 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R18 = R16 << 10;// Instr #186
SDNode *ishl7 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload28, 0),
                                    ct63,
                                    // glue (or chain) input edge
                                    SDValue(endwhere10, 0)
                                    );

// R18 = R17 + R18;// Instr #187
SDNode *add6 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl7, 0),
                                    SDValue(and17, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl7, 1)
                                    );

// R16 = R30 + R16;// Instr #188
SDNode *add7 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload28, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(add6, 1)
                                    );

// R16 = R30 + R16;// Instr #189
SDNode *add8 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(add7, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(add7, 1)
                                    );

// R14 = R28 < R16;// Instr #190
SDNode *lt4 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload5, 0),
                                    SDValue(add8, 0),
                                    // glue (or chain) input edge
                                    SDValue(add8, 1)
                                    );

SDValue ct64 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #191
SDNode *nop13 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct64,
                                    // glue (or chain) input edge
                                    SDValue(lt4, 1)
                                    );

// WHERE_LT;// Instr #192
SDNode *wherelt2 = CrtDAG->getMachineNode(
                                    Connex::WHERELT,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop13, 0)
                                    );

SDValue ct65 = CrtDAG->getSignedConstant(31744, DL, MVT::i16, true, false);
// R18 = 31744;// Instr #193
SDNode *vload29 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct65,
                                    SDValue(add6, 0),
                                    // glue (or chain) input edge
                                    SDValue(wherelt2, 0)
                                    );

// END_WHERE;// Instr #194
SDNode *endwhere11 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload29, 1)
                                    );

// R07 = R26 == R23;// Instr #195
SDNode *eq17 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast1, 0),
                                    SDValue(and0, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere11, 0)
                                    );

// R06 = R22 == R19;// Instr #196
SDNode *eq18 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast2, 0),
                                    SDValue(and4, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq17, 1)
                                    );

// R01 = R24 == R28;// Instr #197
SDNode *eq19 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub1, 0),
                                    SDValue(vload5, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq18, 1)
                                    );

// R08 = R31 < R25;// Instr #198
SDNode *lt5 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ishl3, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq19, 1)
                                    );

// R00 = R20 == R28;// Instr #199
SDNode *eq20 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub3, 0),
                                    SDValue(vload5, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt5, 1)
                                    );

// R02 = R31 < R21;// Instr #200
SDNode *lt6 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(or11, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq20, 1)
                                    );

// R11 = R01 & R08;// Instr #201
SDNode *and18 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt5, 0),
                                    SDValue(eq19, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt6, 1)
                                    );

// R05 = R00 & R02;// Instr #202
SDNode *and19 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt6, 0),
                                    SDValue(eq20, 0),
                                    // glue (or chain) input edge
                                    SDValue(and18, 1)
                                    );

// R10 = R11 | R05;// Instr #203
SDNode *or15 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and19, 0),
                                    SDValue(and18, 0),
                                    // glue (or chain) input edge
                                    SDValue(and19, 1)
                                    );

// R11 = R07 & R06;// Instr #204
SDNode *and20 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq18, 0),
                                    SDValue(eq17, 0),
                                    // glue (or chain) input edge
                                    SDValue(or15, 1)
                                    );

// R05 = R01 & R00;// Instr #205
SDNode *and21 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq20, 0),
                                    SDValue(eq19, 0),
                                    // glue (or chain) input edge
                                    SDValue(and20, 1)
                                    );

// R10 = R10 | R11;// Instr #206
SDNode *or16 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and20, 0),
                                    SDValue(or15, 0),
                                    // glue (or chain) input edge
                                    SDValue(and21, 1)
                                    );

// R10 = R10 | R05;// Instr #207
SDNode *or17 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and21, 0),
                                    SDValue(or16, 0),
                                    // glue (or chain) input edge
                                    SDValue(or16, 1)
                                    );

// R03 = R10 == R30;// Instr #208
SDNode *eq21 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or17, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(or17, 1)
                                    );

SDValue ct66 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #209
SDNode *nop14 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct66,
                                    // glue (or chain) input edge
                                    SDValue(eq21, 1)
                                    );

// WHERE_EQ;// Instr #210
SDNode *whereeq9 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop14, 0)
                                    );

SDValue ct67 = CrtDAG->getSignedConstant(31745, DL, MVT::i16, true, false);
// R18 = 31745;// Instr #211
SDNode *vload30 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct67,
                                    SDValue(vload29, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq9, 0)
                                    );

// END_WHERE;// Instr #212
SDNode *endwhere12 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload30, 1)
                                    );

// R04 = R10 == R31;// Instr #213
SDNode *eq22 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or17, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere12, 0)
                                    );

// R03 = R01 == R30;// Instr #214
SDNode *eq23 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq19, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq22, 1)
                                    );

// R03 = R03 & R04;// Instr #215
SDNode *and22 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq22, 0),
                                    SDValue(eq23, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq23, 1)
                                    );

// R03 = R03 == R30;// Instr #216
SDNode *eq24 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and22, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and22, 1)
                                    );

SDValue ct68 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #217
SDNode *nop15 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct68,
                                    // glue (or chain) input edge
                                    SDValue(eq24, 1)
                                    );

// WHERE_EQ;// Instr #218
SDNode *whereeq10 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop15, 0)
                                    );

SDValue ct69 = CrtDAG->getSignedConstant(31744, DL, MVT::i16, true, false);
// R18 = 31744;// Instr #219
SDNode *vload31 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct69,
                                    SDValue(vload30, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq10, 0)
                                    );

// END_WHERE;// Instr #220
SDNode *endwhere13 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload31, 1)
                                    );

// R03 = R01 == R31;// Instr #221
SDNode *eq25 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq19, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere13, 0)
                                    );

// R03 = R03 & R04;// Instr #222
SDNode *and23 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq22, 0),
                                    SDValue(eq25, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq25, 1)
                                    );

// R04 = R00 == R30;// Instr #223
SDNode *eq26 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq20, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and23, 1)
                                    );

// R03 = R03 & R04;// Instr #224
SDNode *and24 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq26, 0),
                                    SDValue(and23, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq26, 1)
                                    );

// R03 = R03 == R30;// Instr #225
SDNode *eq27 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and24, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and24, 1)
                                    );

SDValue ct70 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #226
SDNode *nop16 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct70,
                                    // glue (or chain) input edge
                                    SDValue(eq27, 1)
                                    );

// WHERE_EQ;// Instr #227
SDNode *whereeq11 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop16, 0)
                                    );

SDValue ct71 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #228
SDNode *vload32 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct71,
                                    SDValue(vload31, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq11, 0)
                                    );

// END_WHERE;// Instr #229
SDNode *endwhere14 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload32, 1)
                                    );

// R18 = R18 ^ R23;// Instr #230
SDNode *xor1 = CrtDAG->getMachineNode(
                                    Connex::XORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and0, 0),
                                    SDValue(vload32, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere14, 0)
                                    );

// R18 = R18 ^ R19;// Instr #231
SDNode *resF16 /*xor2*/ = CurDAG->getMachineNode(
                                    Connex::XORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and4, 0),
                                    SDValue(xor1, 0),
                                    // glue (or chain) input edge
                                    SDValue(xor1, 1)
                                    );


SDNode *lastNode = resF16;
