//===-- Select_MULTf16_OpincaaCodeGen.h --------------------------*- C++ -*-===//
//
// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
// See https://llvm.org/LICENSE.txt for license information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
//===----------------------------------------------------------------------===//
/// \file
// Code auto-generated by method Kernel::genLLVMISelManualCode()
//   from the OPINCAA lib, from kernel mul.f16.
// You should include this code in the Select() method of the [Target]SelectionDAGISel
//   class of your back end (or MAYBE in the ISelLowering pass).
// Number of instructions generated: 249.
//
//===----------------------------------------------------------------------===//


// From /home/asusu/LLVM/Tests/opincaa_standalone_apps/Emulate_f16/MULTf16_manual/DumpISel_OpincaaCodeGen_old46_B11c.cpp


SDValue ct0 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R31 = 0;// Instr #0
SDNode *vload0 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct0,
                                    // glue (or chain) input edge
                                    SDValue(nodeOpSrcCast1, 1)
                                    );

SDValue ct1 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R30 = 1;// Instr #1
SDNode *vload1 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct1,
                                    // glue (or chain) input edge
                                    SDValue(vload0, 1)
                                    );

SDValue ct2 = CrtDAG->getSignedConstant(16, DL, MVT::i16, true, false);
// R29 = 16;// Instr #2
SDNode *vload2 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct2,
                                    // glue (or chain) input edge
                                    SDValue(vload1, 1)
                                    );

SDValue ct3 = CrtDAG->getSignedConstant(31, DL, MVT::i16, true, false);
// R28 = 31;// Instr #3
SDNode *vload3 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct3,
                                    // glue (or chain) input edge
                                    SDValue(vload2, 1)
                                    );

SDValue ct4 = CrtDAG->getSignedConstant(1023, DL, MVT::i16, true, false);
// R10 = 1023;// Instr #4
SDNode *vload4 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct4,
                                    // glue (or chain) input edge
                                    SDValue(vload3, 1)
                                    );

SDValue ct5 = CrtDAG->getSignedConstant(31744, DL, MVT::i16, true, false);
// R09 = 31744;// Instr #5
SDNode *vload5 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct5,
                                    // glue (or chain) input edge
                                    SDValue(vload4, 1)
                                    );

SDValue ct6 = CrtDAG->getSignedConstant(-32768, DL, MVT::i16, true, false);
// R08 = -32768;// Instr #6
SDNode *vload6 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct6,
                                    // glue (or chain) input edge
                                    SDValue(vload5, 1)
                                    );

SDValue ct7 = CrtDAG->getSignedConstant(1024, DL, MVT::i16, true, false);
// R07 = 1024;// Instr #7
SDNode *vload7 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct7,
                                    // glue (or chain) input edge
                                    SDValue(vload6, 1)
                                    );

SDValue ct8 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R55 = 0;// Instr #8
SDNode *vload8 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct8,
                                    // glue (or chain) input edge
                                    SDValue(vload7, 1)
                                    );

SDValue ct9 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R00 = 1;// Instr #9
SDNode *vload9 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct9,
                                    // glue (or chain) input edge
                                    SDValue(vload8, 1)
                                    );

// R24 = R27 & R08;// Instr #10
SDNode *and0 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload6, 0),
                                    SDValue(nodeOpSrcCast1, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload9, 1)
                                    );

// R25 = R27 & R09;// Instr #11
SDNode *and1 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload5, 0),
                                    SDValue(nodeOpSrcCast1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and0, 1)
                                    );

SDValue ct10 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R25 = R25 >> 10;// Instr #12
SDNode *ishr0 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and1, 0),
                                    ct10,
                                    // glue (or chain) input edge
                                    SDValue(and1, 1)
                                    );

// R26 = R27 & R10;// Instr #13
SDNode *and2 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload4, 0),
                                    SDValue(nodeOpSrcCast1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr0, 1)
                                    );

// R14 = R31 < R26;// Instr #14
SDNode *lt0 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload0, 0),
                                    SDValue(and2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and2, 1)
                                    );

// R13 = R25 == R31;// Instr #15
SDNode *eq0 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr0, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt0, 1)
                                    );

// R12 = R13 & R14;// Instr #16
SDNode *and3 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt0, 0),
                                    SDValue(eq0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq0, 1)
                                    );

// R12 = R12 == R30;// Instr #17
SDNode *eq1 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and3, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and3, 1)
                                    );

SDValue ct11 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #18
SDNode *nop0 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct11,
                                    // glue (or chain) input edge
                                    SDValue(eq1, 1)
                                    );

// WHERE_EQ;// Instr #19
SDNode *whereeq0 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop0, 0)
                                    );

SDValue ct12 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R25 = 1;// Instr #20
SDNode *vload10 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct12,
                                    SDValue(ishr0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq0, 0)
                                    );

// END_WHERE;// Instr #21
SDNode *endwhere0 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload10, 1)
                                    );

// R14 = R25 == R28;// Instr #22
SDNode *eq2 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload10, 0),
                                    SDValue(vload3, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere0, 0)
                                    );

// R14 = R14 | R13;// Instr #23
SDNode *or0 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq0, 0),
                                    SDValue(eq2, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq2, 1)
                                    );

// R14 = R14 == R31;// Instr #24
SDNode *eq3 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or0, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(or0, 1)
                                    );

SDValue ct13 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #25
SDNode *nop1 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct13,
                                    // glue (or chain) input edge
                                    SDValue(eq3, 1)
                                    );

// WHERE_EQ;// Instr #26
SDNode *whereeq1 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop1, 0)
                                    );

// R26 = R26 | R07;// Instr #27
SDNode *or1 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload7, 0),
                                    SDValue(and2, 0),
                                    SDValue(and2, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq1, 0)
                                    }
                                    );

// END_WHERE;// Instr #28
SDNode *endwhere1 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or1, 1)
                                    );

// R20 = R23 & R08;// Instr #29
SDNode *and4 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload6, 0),
                                    SDValue(nodeOpSrcCast2, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere1, 0)
                                    );

// R21 = R23 & R09;// Instr #30
SDNode *and5 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload5, 0),
                                    SDValue(nodeOpSrcCast2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and4, 1)
                                    );

SDValue ct14 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R21 = R21 >> 10;// Instr #31
SDNode *ishr1 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and5, 0),
                                    ct14,
                                    // glue (or chain) input edge
                                    SDValue(and5, 1)
                                    );

// R22 = R23 & R10;// Instr #32
SDNode *and6 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload4, 0),
                                    SDValue(nodeOpSrcCast2, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr1, 1)
                                    );

// R14 = R31 < R22;// Instr #33
SDNode *lt1 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload0, 0),
                                    SDValue(and6, 0),
                                    // glue (or chain) input edge
                                    SDValue(and6, 1)
                                    );

// R13 = R21 == R31;// Instr #34
SDNode *eq4 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr1, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt1, 1)
                                    );

// R12 = R13 & R14;// Instr #35
SDNode *and7 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt1, 0),
                                    SDValue(eq4, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq4, 1)
                                    );

// R12 = R12 == R30;// Instr #36
SDNode *eq5 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and7, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and7, 1)
                                    );

SDValue ct15 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #37
SDNode *nop2 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct15,
                                    // glue (or chain) input edge
                                    SDValue(eq5, 1)
                                    );

// WHERE_EQ;// Instr #38
SDNode *whereeq2 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop2, 0)
                                    );

SDValue ct16 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R21 = 1;// Instr #39
SDNode *vload11 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct16,
                                    SDValue(ishr1, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq2, 0)
                                    );

// END_WHERE;// Instr #40
SDNode *endwhere2 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload11, 1)
                                    );

// R14 = R21 == R28;// Instr #41
SDNode *eq6 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload11, 0),
                                    SDValue(vload3, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere2, 0)
                                    );

// R14 = R14 | R13;// Instr #42
SDNode *or2 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq4, 0),
                                    SDValue(eq6, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq6, 1)
                                    );

// R14 = R14 == R31;// Instr #43
SDNode *eq7 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or2, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(or2, 1)
                                    );

SDValue ct17 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #44
SDNode *nop3 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct17,
                                    // glue (or chain) input edge
                                    SDValue(eq7, 1)
                                    );

// WHERE_EQ;// Instr #45
SDNode *whereeq3 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop3, 0)
                                    );

// R22 = R22 | R07;// Instr #46
SDNode *or3 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload7, 0),
                                    SDValue(and6, 0),
                                    SDValue(and6, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq3, 0)
                                    }
                                    );

// END_WHERE;// Instr #47
SDNode *endwhere3 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or3, 1)
                                    );

SDValue ct18 = CrtDAG->getSignedConstant(31744, DL, MVT::i16, true, false);
// R19 = 31744;// Instr #48
SDNode *vload12 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct18,
                                    // glue (or chain) input edge
                                    SDValue(endwhere3, 0)
                                    );

// R15 = R24 ^ R20;// Instr #49
SDNode *xor0 = CrtDAG->getMachineNode(
                                    Connex::XORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and4, 0),
                                    SDValue(and0, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload12, 1)
                                    );

SDValue ct19 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R16 = 0;// Instr #50
SDNode *vload13 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct19,
                                    // glue (or chain) input edge
                                    SDValue(xor0, 1)
                                    );

SDValue ct20 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #51
SDNode *vload14 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct20,
                                    // glue (or chain) input edge
                                    SDValue(vload13, 1)
                                    );

SDValue ct21 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R17 = 0;// Instr #52
SDNode *vload15 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct21,
                                    // glue (or chain) input edge
                                    SDValue(vload14, 1)
                                    );

SDValue ct22 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R54 = 0;// Instr #53
SDNode *vload16 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct22,
                                    // glue (or chain) input edge
                                    SDValue(vload15, 1)
                                    );

SDValue ct23 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R60 = 0;// Instr #54
SDNode *vload17 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct23,
                                    // glue (or chain) input edge
                                    SDValue(vload16, 1)
                                    );

SDValue ct24 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R59 = 0;// Instr #55
SDNode *vload18 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct24,
                                    // glue (or chain) input edge
                                    SDValue(vload17, 1)
                                    );

SDValue ct25 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R58 = 0;// Instr #56
SDNode *vload19 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct25,
                                    // glue (or chain) input edge
                                    SDValue(vload18, 1)
                                    );

SDValue ct26 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R57 = 0;// Instr #57
SDNode *vload20 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct26,
                                    // glue (or chain) input edge
                                    SDValue(vload19, 1)
                                    );

// R02 = R27 == R24;// Instr #58
SDNode *eq8 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast1, 0),
                                    SDValue(and0, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload20, 1)
                                    );

// R01 = R23 == R20;// Instr #59
SDNode *eq9 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast2, 0),
                                    SDValue(and4, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq8, 1)
                                    );

// R36 = R25 == R28;// Instr #60
SDNode *eq10 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload10, 0),
                                    SDValue(vload3, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq9, 1)
                                    );

// R38 = R31 < R26;// Instr #61
SDNode *lt2 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload0, 0),
                                    SDValue(or1, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq10, 1)
                                    );

// R35 = R21 == R28;// Instr #62
SDNode *eq11 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload11, 0),
                                    SDValue(vload3, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt2, 1)
                                    );

// R37 = R31 < R22;// Instr #63
SDNode *lt3 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload0, 0),
                                    SDValue(or3, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq11, 1)
                                    );

// R62 = R36 & R38;// Instr #64
SDNode *and8 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt2, 0),
                                    SDValue(eq10, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt3, 1)
                                    );

// R63 = R35 & R37;// Instr #65
SDNode *and9 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt3, 0),
                                    SDValue(eq11, 0),
                                    // glue (or chain) input edge
                                    SDValue(and8, 1)
                                    );

// R61 = R62 | R63;// Instr #66
SDNode *or4 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and9, 0),
                                    SDValue(and8, 0),
                                    // glue (or chain) input edge
                                    SDValue(and9, 1)
                                    );

// R62 = R36 & R01;// Instr #67
SDNode *and10 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq9, 0),
                                    SDValue(eq10, 0),
                                    // glue (or chain) input edge
                                    SDValue(or4, 1)
                                    );

// R63 = R35 & R02;// Instr #68
SDNode *and11 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq8, 0),
                                    SDValue(eq11, 0),
                                    // glue (or chain) input edge
                                    SDValue(and10, 1)
                                    );

// R61 = R61 | R62;// Instr #69
SDNode *or5 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and10, 0),
                                    SDValue(or4, 0),
                                    // glue (or chain) input edge
                                    SDValue(and11, 1)
                                    );

// R61 = R61 | R63;// Instr #70
SDNode *or6 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and11, 0),
                                    SDValue(or5, 0),
                                    // glue (or chain) input edge
                                    SDValue(or5, 1)
                                    );

// R03 = R61 == R30;// Instr #71
SDNode *eq12 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or6, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(or6, 1)
                                    );

SDValue ct27 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #72
SDNode *nop4 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct27,
                                    // glue (or chain) input edge
                                    SDValue(eq12, 1)
                                    );

// WHERE_EQ;// Instr #73
SDNode *whereeq4 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop4, 0)
                                    );

SDValue ct28 = CrtDAG->getSignedConstant(31745, DL, MVT::i16, true, false);
// R19 = 31745;// Instr #74
SDNode *vload21 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct28,
                                    SDValue(vload12, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq4, 0)
                                    );

SDValue ct29 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R00 = 0;// Instr #75
SDNode *vload22 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct29,
                                    SDValue(vload9, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload21, 1)
                                    );

// END_WHERE;// Instr #76
SDNode *endwhere4 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload22, 1)
                                    );

// R39 = R36 | R35;// Instr #77
SDNode *or7 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq11, 0),
                                    SDValue(eq10, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere4, 0)
                                    );

// R04 = R61 == R31;// Instr #78
SDNode *eq13 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or6, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(or7, 1)
                                    );

// R03 = R39 == R30;// Instr #79
SDNode *eq14 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or7, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq13, 1)
                                    );

// R03 = R03 & R04;// Instr #80
SDNode *and12 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq13, 0),
                                    SDValue(eq14, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq14, 1)
                                    );

// R03 = R03 == R30;// Instr #81
SDNode *eq15 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and12, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and12, 1)
                                    );

SDValue ct30 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #82
SDNode *nop5 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct30,
                                    // glue (or chain) input edge
                                    SDValue(eq15, 1)
                                    );

// WHERE_EQ;// Instr #83
SDNode *whereeq5 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop5, 0)
                                    );

// R19 = R19 | R15;// Instr #84
SDNode *or8 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(xor0, 0),
                                    SDValue(vload21, 0),
                                    SDValue(vload21, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq5, 0)
                                    }
                                    );

SDValue ct31 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R00 = 0;// Instr #85
SDNode *vload23 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct31,
                                    SDValue(vload22, 0),
                                    // glue (or chain) input edge
                                    SDValue(or8, 1)
                                    );

// END_WHERE;// Instr #86
SDNode *endwhere5 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload23, 1)
                                    );

// R52 = R00 == R30;// Instr #87
SDNode *eq16 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere5, 0)
                                    );

SDValue ct32 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #88
SDNode *nop6 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct32,
                                    // glue (or chain) input edge
                                    SDValue(eq16, 1)
                                    );

// WHERE_EQ;// Instr #89
SDNode *whereeq6 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop6, 0)
                                    );

// R16 = R21 + R25;// Instr #90
SDNode *add0 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload10, 0),
                                    SDValue(vload11, 0),
                                    SDValue(vload13, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq6, 0)
                                    }
                                    );

SDValue ct33 = CrtDAG->getSignedConstant(15, DL, MVT::i16, true, false);
// R04 = 15;// Instr #91
SDNode *vload24 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct33,
                                    SDValue(eq13, 0),
                                    // glue (or chain) input edge
                                    SDValue(add0, 1)
                                    );

// R16 = R16 - R04;// Instr #92
SDNode *sub0 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(add0, 0),
                                    SDValue(vload24, 0),
                                    SDValue(add0, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload24, 1)
                                    }
                                    );

// R26 * R22;// Instr #93
SDNode *mult0 = CrtDAG->getMachineNode(
                                    Connex::MULT_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(or1, 0),
                                    SDValue(or3, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub0, 1)
                                    );

// R18 = MULT_LOW();// Instr #94
SDNode *multlo0 = CrtDAG->getMachineNode(
                                    Connex::MULTLO_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload14, 0),
                                    // glue (or chain) input edge
                                    SDValue(mult0, 0)
                                    );

// R17 = MULT_HIGH();// Instr #95
SDNode *multhi0 = CrtDAG->getMachineNode(
                                    Connex::MULTHI_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload15, 0),
                                    // glue (or chain) input edge
                                    SDValue(multlo0, 1)
                                    );

// END_WHERE;// Instr #96
SDNode *endwhere6 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(multhi0, 1)
                                    );

SDValue ct34 = CrtDAG->getSignedConstant(16, DL, MVT::i16, true, false);
// R03 = 16;// Instr #97
SDNode *vload25 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct34,
                                    // glue (or chain) input edge
                                    SDValue(endwhere6, 0)
                                    );

SDValue ct35 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R06 = R18 << 0;// Instr #98
SDNode *ishl0 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(multlo0, 0),
                                    ct35,
                                    // glue (or chain) input edge
                                    SDValue(vload25, 1)
                                    );

// R04 = R31 < R17;// Instr #99
SDNode *lt4 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload0, 0),
                                    SDValue(multhi0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl0, 1)
                                    );

// R52 = R04 & R00;// Instr #100
SDNode *and13 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt4, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt4, 1)
                                    );

// R52 = R52 == R30;// Instr #101
SDNode *eq17 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and13, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and13, 1)
                                    );

SDValue ct36 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #102
SDNode *nop7 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct36,
                                    // glue (or chain) input edge
                                    SDValue(eq17, 1)
                                    );

// WHERE_EQ;// Instr #103
SDNode *whereeq7 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop7, 0)
                                    );

SDValue ct37 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R06 = R17 << 0;// Instr #104
SDNode *ishl1 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(multhi0, 0),
                                    ct37,
                                    SDValue(ishl0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq7, 0)
                                    }
                                    );

SDValue ct38 = CrtDAG->getSignedConstant(32, DL, MVT::i16, true, false);
// R03 = 32;// Instr #105
SDNode *vload26 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct38,
                                    SDValue(vload25, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl1, 1)
                                    );

// END_WHERE;// Instr #106
SDNode *endwhere7 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload26, 1)
                                    );

SDValue ct39 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R04 = R06 >> 1;// Instr #107
SDNode *ishr2 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl1, 0),
                                    ct39,
                                    // glue (or chain) input edge
                                    SDValue(endwhere7, 0)
                                    );

// R06 = R06 | R04;// Instr #108
SDNode *or9 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr2, 0),
                                    SDValue(ishl1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr2, 1)
                                    );

SDValue ct40 = CrtDAG->getSignedConstant(2, DL, MVT::i16, true, false);
// R04 = R06 >> 2;// Instr #109
SDNode *ishr3 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or9, 0),
                                    ct40,
                                    // glue (or chain) input edge
                                    SDValue(or9, 1)
                                    );

// R06 = R06 | R04;// Instr #110
SDNode *or10 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr3, 0),
                                    SDValue(or9, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr3, 1)
                                    );

SDValue ct41 = CrtDAG->getSignedConstant(4, DL, MVT::i16, true, false);
// R04 = R06 >> 4;// Instr #111
SDNode *ishr4 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or10, 0),
                                    ct41,
                                    // glue (or chain) input edge
                                    SDValue(or10, 1)
                                    );

// R06 = R06 | R04;// Instr #112
SDNode *or11 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr4, 0),
                                    SDValue(or10, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr4, 1)
                                    );

SDValue ct42 = CrtDAG->getSignedConstant(8, DL, MVT::i16, true, false);
// R04 = R06 >> 8;// Instr #113
SDNode *ishr5 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or11, 0),
                                    ct42,
                                    // glue (or chain) input edge
                                    SDValue(or11, 1)
                                    );

// R06 = R06 | R04;// Instr #114
SDNode *or12 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr5, 0),
                                    SDValue(or11, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr5, 1)
                                    );

// R06 = ~R06;// Instr #115
SDNode *not0 = CrtDAG->getMachineNode(
                                    Connex::NOT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or12, 0),
                                    // glue (or chain) input edge
                                    SDValue(or12, 1)
                                    );

// R05 = POPCNT(R06);// Instr #116
SDNode *popcnt0 = CrtDAG->getMachineNode(
                                    Connex::POPCNT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(not0, 0),
                                    // glue (or chain) input edge
                                    SDValue(not0, 1)
                                    );

// R05 = R03 - R05;// Instr #117
SDNode *sub1 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload26, 0),
                                    SDValue(popcnt0, 0),
                                    // glue (or chain) input edge
                                    SDValue(popcnt0, 1)
                                    );

SDValue ct43 = CrtDAG->getSignedConstant(11, DL, MVT::i16, true, false);
// R04 = 11;// Instr #118
SDNode *vload27 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct43,
                                    // glue (or chain) input edge
                                    SDValue(sub1, 1)
                                    );

// R04 = R05 - R04;// Instr #119
SDNode *sub2 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub1, 0),
                                    SDValue(vload27, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload27, 1)
                                    );

// R12 = R04 < R31;// Instr #120
SDNode *lt5 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub2, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub2, 1)
                                    );

// R52 = R12 & R00;// Instr #121
SDNode *and14 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt5, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt5, 1)
                                    );

// R52 = R52 == R30;// Instr #122
SDNode *eq18 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and14, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and14, 1)
                                    );

SDValue ct44 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #123
SDNode *nop8 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct44,
                                    // glue (or chain) input edge
                                    SDValue(eq18, 1)
                                    );

// WHERE_EQ;// Instr #124
SDNode *whereeq8 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop8, 0)
                                    );

// R03 = R31 - R04;// Instr #125
SDNode *sub3 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload0, 0),
                                    SDValue(sub2, 0),
                                    SDValue(vload26, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq8, 0)
                                    }
                                    );

// R18 = R18 << R03;// Instr #126
SDNode *shl0 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(multlo0, 0),
                                    SDValue(sub3, 0),
                                    SDValue(multlo0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub3, 1)
                                    }
                                    );

// END_WHERE;// Instr #127
SDNode *endwhere8 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(shl0, 1)
                                    );

// R12 = R31 < R04;// Instr #128
SDNode *lt6 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload0, 0),
                                    SDValue(sub2, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere8, 0)
                                    );

// R52 = R12 & R00;// Instr #129
SDNode *and15 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt6, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt6, 1)
                                    );

// R52 = R52 == R30;// Instr #130
SDNode *eq19 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and15, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and15, 1)
                                    );

SDValue ct45 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #131
SDNode *nop9 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct45,
                                    // glue (or chain) input edge
                                    SDValue(eq19, 1)
                                    );

// WHERE_EQ;// Instr #132
SDNode *whereeq9 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop9, 0)
                                    );

SDValue ct46 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R03 = R17 << 0;// Instr #133
SDNode *ishl2 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(multhi0, 0),
                                    ct46,
                                    SDValue(sub3, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq9, 0)
                                    }
                                    );

// R17 = R17 >> R04;// Instr #134
SDNode *shr0 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(multhi0, 0),
                                    SDValue(sub2, 0),
                                    SDValue(multhi0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl2, 1)
                                    }
                                    );

SDValue ct47 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R12 = R04 << 0;// Instr #135
SDNode *ishl3 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub2, 0),
                                    ct47,
                                    SDValue(lt6, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr0, 1)
                                    }
                                    );

// R04 = R29 - R04;// Instr #136
SDNode *sub4 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(sub2, 0),
                                    SDValue(sub2, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl3, 1)
                                    }
                                    );

SDValue ct48 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R55 = R18 << 0;// Instr #137
SDNode *ishl4 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl0, 0),
                                    ct48,
                                    SDValue(vload8, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub4, 1)
                                    }
                                    );

// R55 = R55 << R04;// Instr #138
SDNode *shl1 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl4, 0),
                                    SDValue(sub4, 0),
                                    SDValue(ishl4, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl4, 1)
                                    }
                                    );

// R55 = R55 >> R04;// Instr #139
SDNode *shr1 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl1, 0),
                                    SDValue(sub4, 0),
                                    SDValue(shl1, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl1, 1)
                                    }
                                    );

SDValue ct49 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R54 = R12 << 0;// Instr #140
SDNode *ishl5 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl3, 0),
                                    ct49,
                                    SDValue(vload16, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr1, 1)
                                    }
                                    );

// R18 = R18 >> R12;// Instr #141
SDNode *shr2 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl0, 0),
                                    SDValue(ishl3, 0),
                                    SDValue(shl0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl5, 1)
                                    }
                                    );

// R03 = R03 << R04;// Instr #142
SDNode *shl2 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl2, 0),
                                    SDValue(sub4, 0),
                                    SDValue(ishl2, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr2, 1)
                                    }
                                    );

// R18 = R18 | R03;// Instr #143
SDNode *or13 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl2, 0),
                                    SDValue(shr2, 0),
                                    SDValue(shr2, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl2, 1)
                                    }
                                    );

// END_WHERE;// Instr #144
SDNode *endwhere9 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or13, 1)
                                    );

// R52 = R00 == R30;// Instr #145
SDNode *eq20 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere9, 0)
                                    );

SDValue ct50 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #146
SDNode *nop10 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct50,
                                    // glue (or chain) input edge
                                    SDValue(eq20, 1)
                                    );

// WHERE_EQ;// Instr #147
SDNode *whereeq10 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop10, 0)
                                    );

SDValue ct51 = CrtDAG->getSignedConstant(21, DL, MVT::i16, true, false);
// R04 = 21;// Instr #148
SDNode *vload28 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct51,
                                    SDValue(sub4, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq10, 0)
                                    );

// R04 = R04 - R05;// Instr #149
SDNode *sub5 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload28, 0),
                                    SDValue(sub1, 0),
                                    SDValue(vload28, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload28, 1)
                                    }
                                    );

// R16 = R16 - R04;// Instr #150
SDNode *sub6 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub0, 0),
                                    SDValue(sub5, 0),
                                    SDValue(sub0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub5, 1)
                                    }
                                    );

// END_WHERE;// Instr #151
SDNode *endwhere10 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub6, 1)
                                    );

SDValue ct52 = CrtDAG->getSignedConstant(15, DL, MVT::i16, true, false);
// R03 = 15;// Instr #152
SDNode *vload29 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct52,
                                    // glue (or chain) input edge
                                    SDValue(endwhere10, 0)
                                    );

// R04 = R30 - R16;// Instr #153
SDNode *sub7 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(sub6, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload29, 1)
                                    );

// R12 = R03 < R04;// Instr #154
SDNode *lt7 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload29, 0),
                                    SDValue(sub7, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub7, 1)
                                    );

// R52 = R12 & R00;// Instr #155
SDNode *and16 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt7, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt7, 1)
                                    );

// R52 = R52 == R30;// Instr #156
SDNode *eq21 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and16, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and16, 1)
                                    );

SDValue ct53 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #157
SDNode *nop11 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct53,
                                    // glue (or chain) input edge
                                    SDValue(eq21, 1)
                                    );

// WHERE_EQ;// Instr #158
SDNode *whereeq11 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop11, 0)
                                    );

// R54 = R55 == R31;// Instr #159
SDNode *eq22 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr1, 0),
                                    SDValue(vload0, 0),
                                    SDValue(ishl5, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq11, 0)
                                    }
                                    );

// R54 = R30 - R54;// Instr #160
SDNode *sub8 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq22, 0),
                                    SDValue(eq22, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq22, 1)
                                    }
                                    );

SDValue ct54 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R55 = R18 << 0;// Instr #161
SDNode *ishl6 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or13, 0),
                                    ct54,
                                    SDValue(shr1, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub8, 1)
                                    }
                                    );

// R55 = R55 | R54;// Instr #162
SDNode *or14 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub8, 0),
                                    SDValue(ishl6, 0),
                                    SDValue(ishl6, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl6, 1)
                                    }
                                    );

SDValue ct55 = CrtDAG->getSignedConstant(16, DL, MVT::i16, true, false);
// R54 = 16;// Instr #163
SDNode *vload30 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct55,
                                    SDValue(sub8, 0),
                                    // glue (or chain) input edge
                                    SDValue(or14, 1)
                                    );

SDValue ct56 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = R17 << 0;// Instr #164
SDNode *ishl7 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr0, 0),
                                    ct56,
                                    SDValue(or13, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload30, 1)
                                    }
                                    );

SDValue ct57 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R17 = 0;// Instr #165
SDNode *vload31 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct57,
                                    SDValue(shr0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl7, 1)
                                    );

// R04 = R04 - R29;// Instr #166
SDNode *sub9 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub7, 0),
                                    SDValue(vload2, 0),
                                    SDValue(sub7, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload31, 1)
                                    }
                                    );

// R16 = R29 + R16;// Instr #167
SDNode *add1 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub6, 0),
                                    SDValue(vload2, 0),
                                    SDValue(sub6, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub9, 1)
                                    }
                                    );

// END_WHERE;// Instr #168
SDNode *endwhere11 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(add1, 1)
                                    );

// R12 = R03 < R04;// Instr #169
SDNode *lt8 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload29, 0),
                                    SDValue(sub9, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere11, 0)
                                    );

// R52 = R12 & R00;// Instr #170
SDNode *and17 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt8, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt8, 1)
                                    );

// R52 = R52 == R30;// Instr #171
SDNode *eq23 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and17, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and17, 1)
                                    );

SDValue ct58 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #172
SDNode *nop12 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct58,
                                    // glue (or chain) input edge
                                    SDValue(eq23, 1)
                                    );

// WHERE_EQ;// Instr #173
SDNode *whereeq12 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop12, 0)
                                    );

// R54 = R55 == R31;// Instr #174
SDNode *eq24 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or14, 0),
                                    SDValue(vload0, 0),
                                    SDValue(vload30, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq12, 0)
                                    }
                                    );

// R54 = R30 - R54;// Instr #175
SDNode *sub10 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq24, 0),
                                    SDValue(eq24, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq24, 1)
                                    }
                                    );

SDValue ct59 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R55 = R18 << 0;// Instr #176
SDNode *ishl8 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl7, 0),
                                    ct59,
                                    SDValue(or14, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub10, 1)
                                    }
                                    );

// R55 = R55 | R54;// Instr #177
SDNode *or15 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub10, 0),
                                    SDValue(ishl8, 0),
                                    SDValue(ishl8, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl8, 1)
                                    }
                                    );

SDValue ct60 = CrtDAG->getSignedConstant(16, DL, MVT::i16, true, false);
// R54 = 16;// Instr #178
SDNode *vload32 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct60,
                                    SDValue(sub10, 0),
                                    // glue (or chain) input edge
                                    SDValue(or15, 1)
                                    );

SDValue ct61 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R18 = 0;// Instr #179
SDNode *vload33 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct61,
                                    SDValue(ishl7, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload32, 1)
                                    );

// R04 = R04 - R29;// Instr #180
SDNode *sub11 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub9, 0),
                                    SDValue(vload2, 0),
                                    SDValue(sub9, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload33, 1)
                                    }
                                    );

// R16 = R29 + R16;// Instr #181
SDNode *add2 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(add1, 0),
                                    SDValue(vload2, 0),
                                    SDValue(add1, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub11, 1)
                                    }
                                    );

// END_WHERE;// Instr #182
SDNode *endwhere12 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(add2, 1)
                                    );

// R12 = R16 < R30;// Instr #183
SDNode *lt9 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(add2, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere12, 0)
                                    );

// R52 = R12 & R00;// Instr #184
SDNode *and18 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt9, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt9, 1)
                                    );

// R52 = R52 == R30;// Instr #185
SDNode *eq25 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and18, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and18, 1)
                                    );

SDValue ct62 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #186
SDNode *nop13 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct62,
                                    // glue (or chain) input edge
                                    SDValue(eq25, 1)
                                    );

// WHERE_EQ;// Instr #187
SDNode *whereeq13 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop13, 0)
                                    );

// R04 = R30 - R16;// Instr #188
SDNode *sub12 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(add2, 0),
                                    SDValue(sub11, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq13, 0)
                                    }
                                    );

// R54 = R55 == R31;// Instr #189
SDNode *eq26 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or15, 0),
                                    SDValue(vload0, 0),
                                    SDValue(vload32, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub12, 1)
                                    }
                                    );

// R54 = R30 - R54;// Instr #190
SDNode *sub13 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq26, 0),
                                    SDValue(eq26, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq26, 1)
                                    }
                                    );

SDValue ct63 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R55 = R18 << 0;// Instr #191
SDNode *ishl9 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload33, 0),
                                    ct63,
                                    SDValue(or15, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub13, 1)
                                    }
                                    );

// R55 = R55 | R54;// Instr #192
SDNode *or16 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub13, 0),
                                    SDValue(ishl9, 0),
                                    SDValue(ishl9, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl9, 1)
                                    }
                                    );

// R03 = R29 - R04;// Instr #193
SDNode *sub14 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(sub12, 0),
                                    SDValue(vload29, 0),
                                    // glue (or chain) input edge
                                    SDValue(or16, 1)
                                    }
                                    );

// R55 = R55 << R03;// Instr #194
SDNode *shl3 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or16, 0),
                                    SDValue(sub14, 0),
                                    SDValue(or16, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub14, 1)
                                    }
                                    );

// R55 = R55 >> R03;// Instr #195
SDNode *shr3 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl3, 0),
                                    SDValue(sub14, 0),
                                    SDValue(shl3, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl3, 1)
                                    }
                                    );

SDValue ct64 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R54 = R04 << 0;// Instr #196
SDNode *ishl10 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub12, 0),
                                    ct64,
                                    SDValue(sub13, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr3, 1)
                                    }
                                    );

// R18 = R18 >> R04;// Instr #197
SDNode *shr4 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload33, 0),
                                    SDValue(sub12, 0),
                                    SDValue(vload33, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl10, 1)
                                    }
                                    );

SDValue ct65 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R03 = R17 << 0;// Instr #198
SDNode *ishl11 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload31, 0),
                                    ct65,
                                    SDValue(sub14, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr4, 1)
                                    }
                                    );

// R17 = R17 >> R04;// Instr #199
SDNode *shr5 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload31, 0),
                                    SDValue(sub12, 0),
                                    SDValue(vload31, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl11, 1)
                                    }
                                    );

// R04 = R29 - R04;// Instr #200
SDNode *sub15 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(sub12, 0),
                                    SDValue(sub12, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr5, 1)
                                    }
                                    );

// R03 = R03 << R04;// Instr #201
SDNode *shl4 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl11, 0),
                                    SDValue(sub15, 0),
                                    SDValue(ishl11, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub15, 1)
                                    }
                                    );

// R18 = R18 | R03;// Instr #202
SDNode *or17 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl4, 0),
                                    SDValue(shr4, 0),
                                    SDValue(shr4, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl4, 1)
                                    }
                                    );

SDValue ct66 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R16 = 1;// Instr #203
SDNode *vload34 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct66,
                                    SDValue(add2, 0),
                                    // glue (or chain) input edge
                                    SDValue(or17, 1)
                                    );

// END_WHERE;// Instr #204
SDNode *endwhere13 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload34, 1)
                                    );

SDValue ct67 = CrtDAG->getSignedConstant(254, DL, MVT::i16, true, false);
// R12 = 254;// Instr #205
SDNode *vload35 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct67,
                                    // glue (or chain) input edge
                                    SDValue(endwhere13, 0)
                                    );

// R12 = R12 < R16;// Instr #206
SDNode *lt10 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload35, 0),
                                    SDValue(vload34, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload35, 1)
                                    );

// R52 = R12 & R00;// Instr #207
SDNode *and19 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt10, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt10, 1)
                                    );

// R52 = R52 == R30;// Instr #208
SDNode *eq27 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and19, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and19, 1)
                                    );

SDValue ct68 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #209
SDNode *nop14 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct68,
                                    // glue (or chain) input edge
                                    SDValue(eq27, 1)
                                    );

// WHERE_EQ;// Instr #210
SDNode *whereeq14 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop14, 0)
                                    );

// R19 = R19 | R15;// Instr #211
SDNode *or18 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(xor0, 0),
                                    SDValue(or8, 0),
                                    SDValue(or8, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq14, 0)
                                    }
                                    );

SDValue ct69 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R00 = 0;// Instr #212
SDNode *vload36 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct69,
                                    SDValue(vload23, 0),
                                    // glue (or chain) input edge
                                    SDValue(or18, 1)
                                    );

// END_WHERE;// Instr #213
SDNode *endwhere14 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload36, 1)
                                    );

SDValue ct70 = CrtDAG->getSignedConstant(1024, DL, MVT::i16, true, false);
// R04 = 1024;// Instr #214
SDNode *vload37 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct70,
                                    // glue (or chain) input edge
                                    SDValue(endwhere14, 0)
                                    );

// R04 = R18 < R04;// Instr #215
SDNode *lt11 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or17, 0),
                                    SDValue(vload37, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload37, 1)
                                    );

// R12 = R16 == R30;// Instr #216
SDNode *eq28 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload34, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt11, 1)
                                    );

// R12 = R12 & R04;// Instr #217
SDNode *and20 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt11, 0),
                                    SDValue(eq28, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq28, 1)
                                    );

// R12 = R12 == R30;// Instr #218
SDNode *eq29 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and20, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and20, 1)
                                    );

// R52 = R12 & R00;// Instr #219
SDNode *and21 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload36, 0),
                                    SDValue(eq29, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq29, 1)
                                    );

// R52 = R52 == R30;// Instr #220
SDNode *eq30 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and21, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and21, 1)
                                    );

SDValue ct71 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #221
SDNode *nop15 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct71,
                                    // glue (or chain) input edge
                                    SDValue(eq30, 1)
                                    );

// WHERE_EQ;// Instr #222
SDNode *whereeq15 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop15, 0)
                                    );

SDValue ct72 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R16 = 0;// Instr #223
SDNode *vload38 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct72,
                                    SDValue(vload34, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq15, 0)
                                    );

// END_WHERE;// Instr #224
SDNode *endwhere15 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload38, 1)
                                    );

// R52 = R00 == R30;// Instr #225
SDNode *eq31 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload36, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere15, 0)
                                    );

SDValue ct73 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #226
SDNode *nop16 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct73,
                                    // glue (or chain) input edge
                                    SDValue(eq31, 1)
                                    );

// WHERE_EQ;// Instr #227
SDNode *whereeq16 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop16, 0)
                                    );

// R60 = R18 & R30;// Instr #228
SDNode *and22 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(or17, 0),
                                    SDValue(vload17, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq16, 0)
                                    }
                                    );

// R54 = R54 - R30;// Instr #229
SDNode *sub16 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl10, 0),
                                    SDValue(vload1, 0),
                                    SDValue(ishl10, 0),
                                    // glue (or chain) input edge
                                    SDValue(and22, 1)
                                    }
                                    );

// R54 = R30 << R54;// Instr #230
SDNode *shl5 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(sub16, 0),
                                    SDValue(sub16, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub16, 1)
                                    }
                                    );

// R59 = R55 & R54;// Instr #231
SDNode *and23 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl5, 0),
                                    SDValue(shr3, 0),
                                    SDValue(vload18, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl5, 1)
                                    }
                                    );

// R55 = R55 ^ R59;// Instr #232
SDNode *xor1 = CrtDAG->getMachineNode(
                                    Connex::XORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and23, 0),
                                    SDValue(shr3, 0),
                                    SDValue(shr3, 0),
                                    // glue (or chain) input edge
                                    SDValue(and23, 1)
                                    }
                                    );

// R59 = R59 == R31;// Instr #233
SDNode *eq32 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and23, 0),
                                    SDValue(vload0, 0),
                                    SDValue(and23, 0),
                                    // glue (or chain) input edge
                                    SDValue(xor1, 1)
                                    }
                                    );

// R59 = R30 - R59;// Instr #234
SDNode *sub17 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq32, 0),
                                    SDValue(eq32, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq32, 1)
                                    }
                                    );

// R58 = R55 == R31;// Instr #235
SDNode *eq33 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(xor1, 0),
                                    SDValue(vload0, 0),
                                    SDValue(vload19, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub17, 1)
                                    }
                                    );

// R58 = R30 - R58;// Instr #236
SDNode *sub18 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq33, 0),
                                    SDValue(eq33, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq33, 1)
                                    }
                                    );

// R57 = R58 | R60;// Instr #237
SDNode *or19 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and22, 0),
                                    SDValue(sub18, 0),
                                    SDValue(vload20, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub18, 1)
                                    }
                                    );

// R57 = R57 & R59;// Instr #238
SDNode *and24 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub17, 0),
                                    SDValue(or19, 0),
                                    SDValue(or19, 0),
                                    // glue (or chain) input edge
                                    SDValue(or19, 1)
                                    }
                                    );

// END_WHERE;// Instr #239
SDNode *endwhere16 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(and24, 1)
                                    );

// R52 = R00 == R30;// Instr #240
SDNode *eq34 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload36, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere16, 0)
                                    );

SDValue ct74 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #241
SDNode *nop17 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct74,
                                    // glue (or chain) input edge
                                    SDValue(eq34, 1)
                                    );

// WHERE_EQ;// Instr #242
SDNode *whereeq17 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop17, 0)
                                    );

SDValue ct75 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R19 = R16 << 10;// Instr #243
SDNode *ishl12 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload38, 0),
                                    ct75,
                                    SDValue(or18, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq17, 0)
                                    }
                                    );

// R18 = R18 & R10;// Instr #244
SDNode *and25 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload4, 0),
                                    SDValue(or17, 0),
                                    SDValue(or17, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl12, 1)
                                    }
                                    );

// R19 = R19 | R18;// Instr #245
SDNode *or20 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and25, 0),
                                    SDValue(ishl12, 0),
                                    SDValue(ishl12, 0),
                                    // glue (or chain) input edge
                                    SDValue(and25, 1)
                                    }
                                    );

// R19 = R57 + R19;// Instr #246
SDNode *add3 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or20, 0),
                                    SDValue(and24, 0),
                                    SDValue(or20, 0),
                                    // glue (or chain) input edge
                                    SDValue(or20, 1)
                                    }
                                    );

// R19 = R19 | R15;// Instr #247
SDNode *resF16 /*or21*/ = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(xor0, 0),
                                    SDValue(add3, 0),
                                    SDValue(add3, 0),
                                    // glue (or chain) input edge
                                    SDValue(add3, 1)
                                    }
                                    );

// END_WHERE;// Instr #248
SDNode *lastNode /*endwhere17*/ = CurDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
// Alex:                                    MVT::Glue,
                                    MVT::Other,
                                    // glue (or chain) input edge
// Alex:                                    SDValue(or21, 1)
                                    SDValue(resF16, 1)
                                    );
