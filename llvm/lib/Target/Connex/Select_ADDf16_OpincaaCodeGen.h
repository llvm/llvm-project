//===-- Select_ADDf16_OpincaaCodeGen.h --------------------------*- C++ -*-===//
//
// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
// See https://llvm.org/LICENSE.txt for license information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
//===----------------------------------------------------------------------===//
/// \file
// Code auto-generated by method Kernel::genLLVMISelManualCode()
//   from the OPINCAA lib, from kernel add_or_sub.f16.
// You should include this code in the Select() method of the [Target]SelectionDAGISel
//   class of your back end (or MAYBE in the ISelLowering pass).
// Number of instructions generated: 279.
//
//===----------------------------------------------------------------------===//


// From /home/asusu/LLVM/Tests/opincaa_standalone_apps/Emulate_f16/ADD_SUB_f16_manual/DumpISel_OpincaaCodeGen_old50_C23_ADDf16.cpp


SDValue ct0 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R14 = 1;// Instr #0
SDNode *vload0 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct0,
                                    // glue (or chain) input edge
                                    SDValue(nodeOpSrcCast1, 1)
                                    );

SDValue ct1 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R30 = 1;// Instr #1
SDNode *vload1 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct1,
                                    // glue (or chain) input edge
                                    SDValue(vload0, 1)
                                    );

SDValue ct2 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R31 = 0;// Instr #2
SDNode *vload2 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct2,
                                    // glue (or chain) input edge
                                    SDValue(vload1, 1)
                                    );

SDValue ct3 = CrtDAG->getSignedConstant(16, DL, MVT::i16, true, false);
// R29 = 16;// Instr #3
SDNode *vload3 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct3,
                                    // glue (or chain) input edge
                                    SDValue(vload2, 1)
                                    );

SDValue ct4 = CrtDAG->getSignedConstant(31, DL, MVT::i16, true, false);
// R28 = 31;// Instr #4
SDNode *vload4 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct4,
                                    // glue (or chain) input edge
                                    SDValue(vload3, 1)
                                    );

SDValue ct5 = CrtDAG->getSignedConstant(1023, DL, MVT::i16, true, false);
// R13 = 1023;// Instr #5
SDNode *vload5 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct5,
                                    // glue (or chain) input edge
                                    SDValue(vload4, 1)
                                    );

SDValue ct6 = CrtDAG->getSignedConstant(31744, DL, MVT::i16, true, false);
// R12 = 31744;// Instr #6
SDNode *vload6 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct6,
                                    // glue (or chain) input edge
                                    SDValue(vload5, 1)
                                    );

SDValue ct7 = CrtDAG->getSignedConstant(-32768, DL, MVT::i16, true, false);
// R11 = -32768;// Instr #7
SDNode *vload7 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct7,
                                    // glue (or chain) input edge
                                    SDValue(vload6, 1)
                                    );

SDValue ct8 = CrtDAG->getSignedConstant(1024, DL, MVT::i16, true, false);
// R10 = 1024;// Instr #8
SDNode *vload8 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct8,
                                    // glue (or chain) input edge
                                    SDValue(vload7, 1)
                                    );

SDValue ct9 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R62 = 0;// Instr #9
SDNode *vload9 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct9,
                                    // glue (or chain) input edge
                                    SDValue(vload8, 1)
                                    );

SDValue ct10 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R19 = 0;// Instr #10
SDNode *vload10 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct10,
                                    // glue (or chain) input edge
                                    SDValue(vload9, 1)
                                    );

SDValue ct11 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R62 = 0;// Instr #11
SDNode *vload11 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct11,
                                    // glue (or chain) input edge
                                    SDValue(vload10, 1)
                                    );

SDValue ct12 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R61 = 0;// Instr #12
SDNode *vload12 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct12,
                                    // glue (or chain) input edge
                                    SDValue(vload11, 1)
                                    );

SDValue ct13 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R07 = 0;// Instr #13
SDNode *vload13 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct13,
                                    // glue (or chain) input edge
                                    SDValue(vload12, 1)
                                    );

SDValue ct14 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R04 = 0;// Instr #14
SDNode *vload14 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct14,
                                    // glue (or chain) input edge
                                    SDValue(vload13, 1)
                                    );

SDValue ct15 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R03 = 0;// Instr #15
SDNode *vload15 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct15,
                                    // glue (or chain) input edge
                                    SDValue(vload14, 1)
                                    );

SDValue ct16 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R02 = 0;// Instr #16
SDNode *vload16 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct16,
                                    // glue (or chain) input edge
                                    SDValue(vload15, 1)
                                    );

SDValue ct17 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R01 = 0;// Instr #17
SDNode *vload17 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct17,
                                    // glue (or chain) input edge
                                    SDValue(vload16, 1)
                                    );

SDValue ct18 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R00 = 0;// Instr #18
SDNode *vload18 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct18,
                                    // glue (or chain) input edge
                                    SDValue(vload17, 1)
                                    );

// R24 = R27 & R11;// Instr #19
SDNode *and0 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(nodeOpSrcCast1, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload18, 1)
                                    );

// R25 = R27 & R12;// Instr #20
SDNode *and1 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload6, 0),
                                    SDValue(nodeOpSrcCast1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and0, 1)
                                    );

SDValue ct19 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R25 = R25 >> 10;// Instr #21
SDNode *ishr0 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and1, 0),
                                    ct19,
                                    // glue (or chain) input edge
                                    SDValue(and1, 1)
                                    );

// R26 = R27 & R13;// Instr #22
SDNode *and2 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload5, 0),
                                    SDValue(nodeOpSrcCast1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr0, 1)
                                    );

// R18 = R31 < R26;// Instr #23
SDNode *lt0 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload2, 0),
                                    SDValue(and2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and2, 1)
                                    );

// R17 = R25 == R31;// Instr #24
SDNode *eq0 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr0, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt0, 1)
                                    );

// R09 = R17 & R18;// Instr #25
SDNode *and3 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt0, 0),
                                    SDValue(eq0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq0, 1)
                                    );

// R09 = R09 == R30;// Instr #26
SDNode *eq1 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and3, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and3, 1)
                                    );

SDValue ct20 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #27
SDNode *nop0 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct20,
                                    // glue (or chain) input edge
                                    SDValue(eq1, 1)
                                    );

// WHERE_EQ;// Instr #28
SDNode *whereeq0 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop0, 0)
                                    );

SDValue ct21 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R25 = 1;// Instr #29
SDNode *vload19 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct21,
                                    SDValue(ishr0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq0, 0)
                                    );

// END_WHERE;// Instr #30
SDNode *endwhere0 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload19, 1)
                                    );

// R18 = R25 == R28;// Instr #31
SDNode *eq2 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload19, 0),
                                    SDValue(vload4, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere0, 0)
                                    );

// R18 = R18 | R17;// Instr #32
SDNode *or0 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq0, 0),
                                    SDValue(eq2, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq2, 1)
                                    );

// R18 = R18 == R31;// Instr #33
SDNode *eq3 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or0, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(or0, 1)
                                    );

SDValue ct22 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #34
SDNode *nop1 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct22,
                                    // glue (or chain) input edge
                                    SDValue(eq3, 1)
                                    );

// WHERE_EQ;// Instr #35
SDNode *whereeq1 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop1, 0)
                                    );

// R26 = R26 | R10;// Instr #36
SDNode *or1 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload8, 0),
                                    SDValue(and2, 0),
                                    SDValue(and2, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq1, 0)
                                    }
                                    );

// END_WHERE;// Instr #37
SDNode *endwhere1 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or1, 1)
                                    );

// R20 = R23 & R11;// Instr #38
SDNode *and4 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(nodeOpSrcCast2, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere1, 0)
                                    );

// R21 = R23 & R12;// Instr #39
SDNode *and5 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload6, 0),
                                    SDValue(nodeOpSrcCast2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and4, 1)
                                    );

SDValue ct23 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R21 = R21 >> 10;// Instr #40
SDNode *ishr1 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and5, 0),
                                    ct23,
                                    // glue (or chain) input edge
                                    SDValue(and5, 1)
                                    );

// R22 = R23 & R13;// Instr #41
SDNode *and6 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload5, 0),
                                    SDValue(nodeOpSrcCast2, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr1, 1)
                                    );

// R16 = R31 < R22;// Instr #42
SDNode *lt1 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload2, 0),
                                    SDValue(and6, 0),
                                    // glue (or chain) input edge
                                    SDValue(and6, 1)
                                    );

// R15 = R21 == R31;// Instr #43
SDNode *eq4 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr1, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt1, 1)
                                    );

// R09 = R15 & R16;// Instr #44
SDNode *and7 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt1, 0),
                                    SDValue(eq4, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq4, 1)
                                    );

// R09 = R09 == R30;// Instr #45
SDNode *eq5 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and7, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and7, 1)
                                    );

SDValue ct24 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #46
SDNode *nop2 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct24,
                                    // glue (or chain) input edge
                                    SDValue(eq5, 1)
                                    );

// WHERE_EQ;// Instr #47
SDNode *whereeq2 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop2, 0)
                                    );

SDValue ct25 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R21 = 1;// Instr #48
SDNode *vload20 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct25,
                                    SDValue(ishr1, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq2, 0)
                                    );

// END_WHERE;// Instr #49
SDNode *endwhere2 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload20, 1)
                                    );

// R16 = R21 == R28;// Instr #50
SDNode *eq6 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload20, 0),
                                    SDValue(vload4, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere2, 0)
                                    );

// R16 = R16 | R15;// Instr #51
SDNode *or2 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq4, 0),
                                    SDValue(eq6, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq6, 1)
                                    );

// R16 = R16 == R31;// Instr #52
SDNode *eq7 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or2, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(or2, 1)
                                    );

SDValue ct26 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #53
SDNode *nop3 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct26,
                                    // glue (or chain) input edge
                                    SDValue(eq7, 1)
                                    );

// WHERE_EQ;// Instr #54
SDNode *whereeq3 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop3, 0)
                                    );

// R22 = R22 | R10;// Instr #55
SDNode *or3 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload8, 0),
                                    SDValue(and6, 0),
                                    SDValue(and6, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq3, 0)
                                    }
                                    );

// END_WHERE;// Instr #56
SDNode *endwhere3 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or3, 1)
                                    );

// R50 = R24 == R11;// Instr #57
SDNode *eq8 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and0, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere3, 0)
                                    );

// R49 = R25 == R28;// Instr #58
SDNode *eq9 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload19, 0),
                                    SDValue(vload4, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq8, 1)
                                    );

// R48 = R26 == R31;// Instr #59
SDNode *eq10 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or1, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq9, 1)
                                    );

// R47 = R20 == R11;// Instr #60
SDNode *eq11 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and4, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq10, 1)
                                    );

// R46 = R21 == R28;// Instr #61
SDNode *eq12 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload20, 0),
                                    SDValue(vload4, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq11, 1)
                                    );

// R45 = R22 == R31;// Instr #62
SDNode *eq13 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or3, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq12, 1)
                                    );

// R07 = R49 & R46;// Instr #63
SDNode *and8 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq12, 0),
                                    SDValue(eq9, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq13, 1)
                                    );

// R08 = R07 & R50;// Instr #64
SDNode *and9 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq8, 0),
                                    SDValue(and8, 0),
                                    // glue (or chain) input edge
                                    SDValue(and8, 1)
                                    );

// R44 = ~R47;// Instr #65
SDNode *not0 = CrtDAG->getMachineNode(
                                    Connex::NOT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq11, 0),
                                    // glue (or chain) input edge
                                    SDValue(and9, 1)
                                    );

// R08 = R08 & R44;// Instr #66
SDNode *and10 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(not0, 0),
                                    SDValue(and9, 0),
                                    // glue (or chain) input edge
                                    SDValue(not0, 1)
                                    );

// R44 = ~R50;// Instr #67
SDNode *not1 = CrtDAG->getMachineNode(
                                    Connex::NOT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq8, 0),
                                    // glue (or chain) input edge
                                    SDValue(and10, 1)
                                    );

// R44 = R44 & R07;// Instr #68
SDNode *and11 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and8, 0),
                                    SDValue(not1, 0),
                                    // glue (or chain) input edge
                                    SDValue(not1, 1)
                                    );

// R44 = R44 & R47;// Instr #69
SDNode *and12 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq11, 0),
                                    SDValue(and11, 0),
                                    // glue (or chain) input edge
                                    SDValue(and11, 1)
                                    );

// R08 = R08 | R44;// Instr #70
SDNode *or4 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and12, 0),
                                    SDValue(and10, 0),
                                    // glue (or chain) input edge
                                    SDValue(and12, 1)
                                    );

// R07 = ~R45;// Instr #71
SDNode *not2 = CrtDAG->getMachineNode(
                                    Connex::NOT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq13, 0),
                                    // glue (or chain) input edge
                                    SDValue(or4, 1)
                                    );

// R07 = R07 & R46;// Instr #72
SDNode *and13 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq12, 0),
                                    SDValue(not2, 0),
                                    // glue (or chain) input edge
                                    SDValue(not2, 1)
                                    );

// R08 = R08 | R07;// Instr #73
SDNode *or5 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and13, 0),
                                    SDValue(or4, 0),
                                    // glue (or chain) input edge
                                    SDValue(and13, 1)
                                    );

// R07 = ~R48;// Instr #74
SDNode *not3 = CrtDAG->getMachineNode(
                                    Connex::NOT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq10, 0),
                                    // glue (or chain) input edge
                                    SDValue(or5, 1)
                                    );

// R07 = R07 & R49;// Instr #75
SDNode *and14 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq9, 0),
                                    SDValue(not3, 0),
                                    // glue (or chain) input edge
                                    SDValue(not3, 1)
                                    );

// R08 = R08 | R07;// Instr #76
SDNode *or6 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and14, 0),
                                    SDValue(or5, 0),
                                    // glue (or chain) input edge
                                    SDValue(and14, 1)
                                    );

// R09 = R08 == R30;// Instr #77
SDNode *eq14 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or6, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(or6, 1)
                                    );

SDValue ct27 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #78
SDNode *nop4 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct27,
                                    // glue (or chain) input edge
                                    SDValue(eq14, 1)
                                    );

// WHERE_EQ;// Instr #79
SDNode *whereeq4 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop4, 0)
                                    );

SDValue ct28 = CrtDAG->getSignedConstant(31745, DL, MVT::i16, true, false);
// R19 = 31745;// Instr #80
SDNode *vload21 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct28,
                                    SDValue(vload10, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq4, 0)
                                    );

SDValue ct29 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R14 = 0;// Instr #81
SDNode *vload22 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct29,
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload21, 1)
                                    );

// END_WHERE;// Instr #82
SDNode *endwhere4 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload22, 1)
                                    );

// R08 = R49 | R46;// Instr #83
SDNode *or7 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq12, 0),
                                    SDValue(eq9, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere4, 0)
                                    );

// R09 = R08 & R14;// Instr #84
SDNode *and15 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload22, 0),
                                    SDValue(or7, 0),
                                    // glue (or chain) input edge
                                    SDValue(or7, 1)
                                    );

// R09 = R09 == R30;// Instr #85
SDNode *eq15 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and15, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and15, 1)
                                    );

SDValue ct30 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #86
SDNode *nop5 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct30,
                                    // glue (or chain) input edge
                                    SDValue(eq15, 1)
                                    );

// WHERE_EQ;// Instr #87
SDNode *whereeq5 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop5, 0)
                                    );

SDValue ct31 = CrtDAG->getSignedConstant(31744, DL, MVT::i16, true, false);
// R19 = 31744;// Instr #88
SDNode *vload23 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct31,
                                    SDValue(vload21, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq5, 0)
                                    );

// R08 = R50 & R49;// Instr #89
SDNode *and16 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(eq9, 0),
                                    SDValue(eq8, 0),
                                    SDValue(or7, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload23, 1)
                                    }
                                    );

// R07 = R47 & R46;// Instr #90
SDNode *and17 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(eq12, 0),
                                    SDValue(eq11, 0),
                                    SDValue(and14, 0),
                                    // glue (or chain) input edge
                                    SDValue(and16, 1)
                                    }
                                    );

// R08 = R08 | R07;// Instr #91
SDNode *or8 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and17, 0),
                                    SDValue(and16, 0),
                                    SDValue(and16, 0),
                                    // glue (or chain) input edge
                                    SDValue(and17, 1)
                                    }
                                    );

SDValue ct32 = CrtDAG->getSignedConstant(15, DL, MVT::i16, true, false);
// R08 = R08 << 15;// Instr #92
SDNode *ishl0 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or8, 0),
                                    ct32,
                                    SDValue(or8, 0),
                                    // glue (or chain) input edge
                                    SDValue(or8, 1)
                                    }
                                    );

// R19 = R19 ^ R08;// Instr #93
SDNode *xor0 = CrtDAG->getMachineNode(
                                    Connex::XORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl0, 0),
                                    SDValue(vload23, 0),
                                    SDValue(vload23, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl0, 1)
                                    }
                                    );

SDValue ct33 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R14 = 0;// Instr #94
SDNode *vload24 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct33,
                                    SDValue(vload22, 0),
                                    // glue (or chain) input edge
                                    SDValue(xor0, 1)
                                    );

// END_WHERE;// Instr #95
SDNode *endwhere5 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload24, 1)
                                    );

// R15 = R25 - R21;// Instr #96
SDNode *sub0 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload19, 0),
                                    SDValue(vload20, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere5, 0)
                                    );

SDValue ct34 = CrtDAG->getSignedConstant(-15, DL, MVT::i16, true, false);
// R08 = -15;// Instr #97
SDNode *vload25 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct34,
                                    // glue (or chain) input edge
                                    SDValue(sub0, 1)
                                    );

// R09 = R15 < R08;// Instr #98
SDNode *lt2 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub0, 0),
                                    SDValue(vload25, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload25, 1)
                                    );

// R09 = R09 & R14;// Instr #99
SDNode *and18 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(lt2, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt2, 1)
                                    );

// R09 = R09 == R30;// Instr #100
SDNode *eq16 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and18, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and18, 1)
                                    );

SDValue ct35 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #101
SDNode *nop6 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct35,
                                    // glue (or chain) input edge
                                    SDValue(eq16, 1)
                                    );

// WHERE_EQ;// Instr #102
SDNode *whereeq6 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop6, 0)
                                    );

// R15 = R31 - R15;// Instr #103
SDNode *sub1 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(sub0, 0),
                                    SDValue(sub0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq6, 0)
                                    }
                                    );

SDValue ct36 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R26 = 0;// Instr #104
SDNode *vload26 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct36,
                                    SDValue(or1, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub1, 1)
                                    );

SDValue ct37 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R25 = R21 << 0;// Instr #105
SDNode *ishl1 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload20, 0),
                                    ct37,
                                    SDValue(vload19, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload26, 1)
                                    }
                                    );

SDValue ct38 = CrtDAG->getSignedConstant(32000, DL, MVT::i16, true, false);
// R15 = 32000;// Instr #106
SDNode *vload27 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct38,
                                    SDValue(sub1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl1, 1)
                                    );

// END_WHERE;// Instr #107
SDNode *endwhere6 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload27, 1)
                                    );

SDValue ct39 = CrtDAG->getSignedConstant(-3, DL, MVT::i16, true, false);
// R08 = -3;// Instr #108
SDNode *vload28 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct39,
                                    // glue (or chain) input edge
                                    SDValue(endwhere6, 0)
                                    );

// R09 = R15 < R08;// Instr #109
SDNode *lt3 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload27, 0),
                                    SDValue(vload28, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload28, 1)
                                    );

// R09 = R09 & R14;// Instr #110
SDNode *and19 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(lt3, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt3, 1)
                                    );

// R09 = R09 == R30;// Instr #111
SDNode *eq17 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and19, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and19, 1)
                                    );

SDValue ct40 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #112
SDNode *nop7 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct40,
                                    // glue (or chain) input edge
                                    SDValue(eq17, 1)
                                    );

// WHERE_EQ;// Instr #113
SDNode *whereeq7 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop7, 0)
                                    );

// R15 = R31 - R15;// Instr #114
SDNode *sub2 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(vload27, 0),
                                    SDValue(vload27, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq7, 0)
                                    }
                                    );

// R26 = R26 >> R15;// Instr #115
SDNode *shr0 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload26, 0),
                                    SDValue(sub2, 0),
                                    SDValue(vload26, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub2, 1)
                                    }
                                    );

SDValue ct41 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R25 = R21 << 0;// Instr #116
SDNode *ishl2 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload20, 0),
                                    ct41,
                                    SDValue(ishl1, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr0, 1)
                                    }
                                    );

SDValue ct42 = CrtDAG->getSignedConstant(32000, DL, MVT::i16, true, false);
// R15 = 32000;// Instr #117
SDNode *vload29 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct42,
                                    SDValue(sub2, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl2, 1)
                                    );

// END_WHERE;// Instr #118
SDNode *endwhere7 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload29, 1)
                                    );

// R09 = R15 < R31;// Instr #119
SDNode *lt4 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload29, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere7, 0)
                                    );

// R09 = R09 & R14;// Instr #120
SDNode *and20 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(lt4, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt4, 1)
                                    );

// R09 = R09 == R30;// Instr #121
SDNode *eq18 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and20, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and20, 1)
                                    );

SDValue ct43 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #122
SDNode *nop8 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct43,
                                    // glue (or chain) input edge
                                    SDValue(eq18, 1)
                                    );

// WHERE_EQ;// Instr #123
SDNode *whereeq8 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop8, 0)
                                    );

// R15 = R31 - R15;// Instr #124
SDNode *sub3 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(vload29, 0),
                                    SDValue(vload29, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq8, 0)
                                    }
                                    );

// R22 = R22 << R15;// Instr #125
SDNode *shl0 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or3, 0),
                                    SDValue(sub3, 0),
                                    SDValue(or3, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub3, 1)
                                    }
                                    );

SDValue ct44 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R21 = R25 << 0;// Instr #126
SDNode *ishl3 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl2, 0),
                                    ct44,
                                    SDValue(vload20, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl0, 1)
                                    }
                                    );

SDValue ct45 = CrtDAG->getSignedConstant(32000, DL, MVT::i16, true, false);
// R15 = 32000;// Instr #127
SDNode *vload30 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct45,
                                    SDValue(sub3, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl3, 1)
                                    );

// END_WHERE;// Instr #128
SDNode *endwhere8 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload30, 1)
                                    );

SDValue ct46 = CrtDAG->getSignedConstant(4, DL, MVT::i16, true, false);
// R08 = 4;// Instr #129
SDNode *vload31 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct46,
                                    // glue (or chain) input edge
                                    SDValue(endwhere8, 0)
                                    );

// R09 = R15 < R08;// Instr #130
SDNode *lt5 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload30, 0),
                                    SDValue(vload31, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload31, 1)
                                    );

// R09 = R09 & R14;// Instr #131
SDNode *and21 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(lt5, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt5, 1)
                                    );

// R09 = R09 == R30;// Instr #132
SDNode *eq19 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and21, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and21, 1)
                                    );

SDValue ct47 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #133
SDNode *nop9 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct47,
                                    // glue (or chain) input edge
                                    SDValue(eq19, 1)
                                    );

// WHERE_EQ;// Instr #134
SDNode *whereeq9 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop9, 0)
                                    );

// R26 = R26 << R15;// Instr #135
SDNode *shl1 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr0, 0),
                                    SDValue(vload30, 0),
                                    SDValue(shr0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq9, 0)
                                    }
                                    );

SDValue ct48 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R25 = R21 << 0;// Instr #136
SDNode *ishl4 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl3, 0),
                                    ct48,
                                    SDValue(ishl2, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl1, 1)
                                    }
                                    );

SDValue ct49 = CrtDAG->getSignedConstant(32000, DL, MVT::i16, true, false);
// R15 = 32000;// Instr #137
SDNode *vload32 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct49,
                                    SDValue(vload30, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl4, 1)
                                    );

// END_WHERE;// Instr #138
SDNode *endwhere9 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload32, 1)
                                    );

// R09 = R15 < R29;// Instr #139
SDNode *lt6 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload32, 0),
                                    SDValue(vload3, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere9, 0)
                                    );

// R09 = R09 & R14;// Instr #140
SDNode *and22 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(lt6, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt6, 1)
                                    );

// R09 = R09 == R30;// Instr #141
SDNode *eq20 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and22, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and22, 1)
                                    );

SDValue ct50 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #142
SDNode *nop10 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct50,
                                    // glue (or chain) input edge
                                    SDValue(eq20, 1)
                                    );

// WHERE_EQ;// Instr #143
SDNode *whereeq10 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop10, 0)
                                    );

// R22 = R22 >> R15;// Instr #144
SDNode *shr1 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl0, 0),
                                    SDValue(vload32, 0),
                                    SDValue(shl0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq10, 0)
                                    }
                                    );

SDValue ct51 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R21 = R25 << 0;// Instr #145
SDNode *ishl5 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl4, 0),
                                    ct51,
                                    SDValue(ishl3, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr1, 1)
                                    }
                                    );

SDValue ct52 = CrtDAG->getSignedConstant(32000, DL, MVT::i16, true, false);
// R15 = 32000;// Instr #146
SDNode *vload33 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct52,
                                    SDValue(vload32, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl5, 1)
                                    );

// END_WHERE;// Instr #147
SDNode *endwhere10 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload33, 1)
                                    );

SDValue ct53 = CrtDAG->getSignedConstant(32, DL, MVT::i16, true, false);
// R08 = 32;// Instr #148
SDNode *vload34 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct53,
                                    // glue (or chain) input edge
                                    SDValue(endwhere10, 0)
                                    );

// R09 = R15 < R08;// Instr #149
SDNode *lt7 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload33, 0),
                                    SDValue(vload34, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload34, 1)
                                    );

// R09 = R09 & R14;// Instr #150
SDNode *and23 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(lt7, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt7, 1)
                                    );

// R09 = R09 == R30;// Instr #151
SDNode *eq21 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and23, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and23, 1)
                                    );

SDValue ct54 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #152
SDNode *nop11 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct54,
                                    // glue (or chain) input edge
                                    SDValue(eq21, 1)
                                    );

// WHERE_EQ;// Instr #153
SDNode *whereeq11 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop11, 0)
                                    );

SDValue ct55 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R22 = 0;// Instr #154
SDNode *vload35 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct55,
                                    SDValue(shr1, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq11, 0)
                                    );

SDValue ct56 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R21 = R25 << 0;// Instr #155
SDNode *ishl6 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl4, 0),
                                    ct56,
                                    SDValue(ishl5, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload35, 1)
                                    }
                                    );

// END_WHERE;// Instr #156
SDNode *endwhere11 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(ishl6, 1)
                                    );

// R09 = R24 == R11;// Instr #157
SDNode *eq22 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and0, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere11, 0)
                                    );

// R09 = R09 & R14;// Instr #158
SDNode *and24 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(eq22, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq22, 1)
                                    );

// R09 = R09 == R30;// Instr #159
SDNode *eq23 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and24, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and24, 1)
                                    );

SDValue ct57 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #160
SDNode *nop12 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct57,
                                    // glue (or chain) input edge
                                    SDValue(eq23, 1)
                                    );

// WHERE_EQ;// Instr #161
SDNode *whereeq12 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop12, 0)
                                    );

// R26 = R31 - R26;// Instr #162
SDNode *sub4 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(shl1, 0),
                                    SDValue(shl1, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq12, 0)
                                    }
                                    );

// END_WHERE;// Instr #163
SDNode *endwhere12 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub4, 1)
                                    );

// R09 = R20 == R11;// Instr #164
SDNode *eq24 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and4, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere12, 0)
                                    );

// R09 = R09 & R14;// Instr #165
SDNode *and25 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(eq24, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq24, 1)
                                    );

// R09 = R09 == R30;// Instr #166
SDNode *eq25 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and25, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and25, 1)
                                    );

SDValue ct58 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #167
SDNode *nop13 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct58,
                                    // glue (or chain) input edge
                                    SDValue(eq25, 1)
                                    );

// WHERE_EQ;// Instr #168
SDNode *whereeq13 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop13, 0)
                                    );

// R22 = R31 - R22;// Instr #169
SDNode *sub5 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(vload35, 0),
                                    SDValue(vload35, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq13, 0)
                                    }
                                    );

// END_WHERE;// Instr #170
SDNode *endwhere13 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub5, 1)
                                    );

// R09 = R14 == R30;// Instr #171
SDNode *eq26 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere13, 0)
                                    );

SDValue ct59 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #172
SDNode *nop14 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct59,
                                    // glue (or chain) input edge
                                    SDValue(eq26, 1)
                                    );

// WHERE_EQ;// Instr #173
SDNode *whereeq14 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop14, 0)
                                    );

// R26 = R22 + R26;// Instr #174
SDNode *add0 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub4, 0),
                                    SDValue(sub5, 0),
                                    SDValue(sub4, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq14, 0)
                                    }
                                    );

// END_WHERE;// Instr #175
SDNode *endwhere14 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(add0, 1)
                                    );

// R24 = R26 & R11;// Instr #176
SDNode *and26 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(add0, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere14, 0)
                                    );

// R09 = R24 == R11;// Instr #177
SDNode *eq27 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and26, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(and26, 1)
                                    );

// R09 = R09 & R14;// Instr #178
SDNode *and27 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(eq27, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq27, 1)
                                    );

// R09 = R09 == R30;// Instr #179
SDNode *eq28 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and27, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and27, 1)
                                    );

SDValue ct60 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #180
SDNode *nop15 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct60,
                                    // glue (or chain) input edge
                                    SDValue(eq28, 1)
                                    );

// WHERE_EQ;// Instr #181
SDNode *whereeq15 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop15, 0)
                                    );

// R26 = R31 - R26;// Instr #182
SDNode *sub6 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(add0, 0),
                                    SDValue(add0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq15, 0)
                                    }
                                    );

// END_WHERE;// Instr #183
SDNode *endwhere15 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub6, 1)
                                    );

SDValue ct61 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R05 = R26 << 0;// Instr #184
SDNode *ishl7 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub6, 0),
                                    ct61,
                                    // glue (or chain) input edge
                                    SDValue(endwhere15, 0)
                                    );

SDValue ct62 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R08 = R05 >> 1;// Instr #185
SDNode *ishr2 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl7, 0),
                                    ct62,
                                    // glue (or chain) input edge
                                    SDValue(ishl7, 1)
                                    );

// R05 = R05 | R08;// Instr #186
SDNode *or9 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr2, 0),
                                    SDValue(ishl7, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr2, 1)
                                    );

SDValue ct63 = CrtDAG->getSignedConstant(2, DL, MVT::i16, true, false);
// R08 = R05 >> 2;// Instr #187
SDNode *ishr3 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or9, 0),
                                    ct63,
                                    // glue (or chain) input edge
                                    SDValue(or9, 1)
                                    );

// R05 = R05 | R08;// Instr #188
SDNode *or10 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr3, 0),
                                    SDValue(or9, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr3, 1)
                                    );

SDValue ct64 = CrtDAG->getSignedConstant(4, DL, MVT::i16, true, false);
// R08 = R05 >> 4;// Instr #189
SDNode *ishr4 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or10, 0),
                                    ct64,
                                    // glue (or chain) input edge
                                    SDValue(or10, 1)
                                    );

// R05 = R05 | R08;// Instr #190
SDNode *or11 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr4, 0),
                                    SDValue(or10, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr4, 1)
                                    );

SDValue ct65 = CrtDAG->getSignedConstant(8, DL, MVT::i16, true, false);
// R08 = R05 >> 8;// Instr #191
SDNode *ishr5 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or11, 0),
                                    ct65,
                                    // glue (or chain) input edge
                                    SDValue(or11, 1)
                                    );

// R05 = R05 | R08;// Instr #192
SDNode *or12 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr5, 0),
                                    SDValue(or11, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr5, 1)
                                    );

// R05 = ~R05;// Instr #193
SDNode *not4 = CrtDAG->getMachineNode(
                                    Connex::NOT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or12, 0),
                                    // glue (or chain) input edge
                                    SDValue(or12, 1)
                                    );

// R06 = POPCNT(R05);// Instr #194
SDNode *popcnt0 = CrtDAG->getMachineNode(
                                    Connex::POPCNT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(not4, 0),
                                    // glue (or chain) input edge
                                    SDValue(not4, 1)
                                    );

// R06 = R29 - R06;// Instr #195
SDNode *sub7 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload3, 0),
                                    SDValue(popcnt0, 0),
                                    // glue (or chain) input edge
                                    SDValue(popcnt0, 1)
                                    );

SDValue ct66 = CrtDAG->getSignedConstant(11, DL, MVT::i16, true, false);
// R08 = 11;// Instr #196
SDNode *vload36 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct66,
                                    // glue (or chain) input edge
                                    SDValue(sub7, 1)
                                    );

// R08 = R06 - R08;// Instr #197
SDNode *sub8 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub7, 0),
                                    SDValue(vload36, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload36, 1)
                                    );

// R09 = R31 < R08;// Instr #198
SDNode *lt8 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload2, 0),
                                    SDValue(sub8, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub8, 1)
                                    );

// R09 = R09 & R14;// Instr #199
SDNode *and28 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(lt8, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt8, 1)
                                    );

// R09 = R09 == R30;// Instr #200
SDNode *eq29 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and28, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and28, 1)
                                    );

SDValue ct67 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #201
SDNode *nop16 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct67,
                                    // glue (or chain) input edge
                                    SDValue(eq29, 1)
                                    );

// WHERE_EQ;// Instr #202
SDNode *whereeq16 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop16, 0)
                                    );

SDValue ct68 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R62 = R26 << 0;// Instr #203
SDNode *ishl8 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub6, 0),
                                    ct68,
                                    SDValue(vload11, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq16, 0)
                                    }
                                    );

// R09 = R29 - R08;// Instr #204
SDNode *sub9 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload3, 0),
                                    SDValue(sub8, 0),
                                    SDValue(eq29, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl8, 1)
                                    }
                                    );

// R62 = R62 << R09;// Instr #205
SDNode *shl2 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl8, 0),
                                    SDValue(sub9, 0),
                                    SDValue(ishl8, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub9, 1)
                                    }
                                    );

// R62 = R62 >> R09;// Instr #206
SDNode *shr2 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl2, 0),
                                    SDValue(sub9, 0),
                                    SDValue(shl2, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl2, 1)
                                    }
                                    );

SDValue ct69 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R61 = R08 << 0;// Instr #207
SDNode *ishl9 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub8, 0),
                                    ct69,
                                    SDValue(vload12, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr2, 1)
                                    }
                                    );

// R26 = R26 >> R08;// Instr #208
SDNode *shr3 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub6, 0),
                                    SDValue(sub8, 0),
                                    SDValue(sub6, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl9, 1)
                                    }
                                    );

// R25 = R08 + R25;// Instr #209
SDNode *add1 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl4, 0),
                                    SDValue(sub8, 0),
                                    SDValue(ishl4, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr3, 1)
                                    }
                                    );

// END_WHERE;// Instr #210
SDNode *endwhere16 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(add1, 1)
                                    );

// R09 = R08 < R31;// Instr #211
SDNode *lt9 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub8, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere16, 0)
                                    );

// R09 = R09 & R14;// Instr #212
SDNode *and29 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(lt9, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt9, 1)
                                    );

// R09 = R09 == R30;// Instr #213
SDNode *eq30 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and29, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and29, 1)
                                    );

SDValue ct70 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #214
SDNode *nop17 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct70,
                                    // glue (or chain) input edge
                                    SDValue(eq30, 1)
                                    );

// WHERE_EQ;// Instr #215
SDNode *whereeq17 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop17, 0)
                                    );

// R08 = R31 - R08;// Instr #216
SDNode *sub10 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(sub8, 0),
                                    SDValue(sub8, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq17, 0)
                                    }
                                    );

// R26 = R26 << R08;// Instr #217
SDNode *shl3 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr3, 0),
                                    SDValue(sub10, 0),
                                    SDValue(shr3, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub10, 1)
                                    }
                                    );

// R25 = R25 - R08;// Instr #218
SDNode *sub11 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(add1, 0),
                                    SDValue(sub10, 0),
                                    SDValue(add1, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl3, 1)
                                    }
                                    );

// END_WHERE;// Instr #219
SDNode *endwhere17 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub11, 1)
                                    );

// R09 = R25 < R30;// Instr #220
SDNode *lt10 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub11, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere17, 0)
                                    );

// R09 = R09 & R14;// Instr #221
SDNode *and30 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(lt10, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt10, 1)
                                    );

// R09 = R09 == R30;// Instr #222
SDNode *eq31 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and30, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and30, 1)
                                    );

SDValue ct71 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #223
SDNode *nop18 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct71,
                                    // glue (or chain) input edge
                                    SDValue(eq31, 1)
                                    );

// WHERE_EQ;// Instr #224
SDNode *whereeq18 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop18, 0)
                                    );

// R61 = R30 - R25;// Instr #225
SDNode *sub12 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(sub11, 0),
                                    SDValue(ishl9, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq18, 0)
                                    }
                                    );

SDValue ct72 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R25 = 1;// Instr #226
SDNode *vload37 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct72,
                                    SDValue(sub11, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub12, 1)
                                    );

SDValue ct73 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R62 = R26 << 0;// Instr #227
SDNode *ishl10 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl3, 0),
                                    ct73,
                                    SDValue(shr2, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload37, 1)
                                    }
                                    );

// R09 = R29 - R61;// Instr #228
SDNode *sub13 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload3, 0),
                                    SDValue(sub12, 0),
                                    SDValue(eq31, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl10, 1)
                                    }
                                    );

// R62 = R62 << R09;// Instr #229
SDNode *shl4 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl10, 0),
                                    SDValue(sub13, 0),
                                    SDValue(ishl10, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub13, 1)
                                    }
                                    );

// R62 = R62 >> R09;// Instr #230
SDNode *shr4 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl4, 0),
                                    SDValue(sub13, 0),
                                    SDValue(shl4, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl4, 1)
                                    }
                                    );

// R26 = R26 >> R61;// Instr #231
SDNode *shr5 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl3, 0),
                                    SDValue(sub12, 0),
                                    SDValue(shl3, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr4, 1)
                                    }
                                    );

// END_WHERE;// Instr #232
SDNode *endwhere18 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(shr5, 1)
                                    );

SDValue ct74 = CrtDAG->getSignedConstant(1024, DL, MVT::i16, true, false);
// R08 = 1024;// Instr #233
SDNode *vload38 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct74,
                                    // glue (or chain) input edge
                                    SDValue(endwhere18, 0)
                                    );

// R08 = R26 < R08;// Instr #234
SDNode *lt11 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr5, 0),
                                    SDValue(vload38, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload38, 1)
                                    );

// R09 = R25 == R30;// Instr #235
SDNode *eq32 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload37, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt11, 1)
                                    );

// R09 = R09 & R14;// Instr #236
SDNode *and31 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(eq32, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq32, 1)
                                    );

// R09 = R09 & R08;// Instr #237
SDNode *and32 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt11, 0),
                                    SDValue(and31, 0),
                                    // glue (or chain) input edge
                                    SDValue(and31, 1)
                                    );

// R09 = R09 == R30;// Instr #238
SDNode *eq33 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and32, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and32, 1)
                                    );

SDValue ct75 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #239
SDNode *nop19 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct75,
                                    // glue (or chain) input edge
                                    SDValue(eq33, 1)
                                    );

// WHERE_EQ;// Instr #240
SDNode *whereeq19 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop19, 0)
                                    );

SDValue ct76 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R25 = 0;// Instr #241
SDNode *vload39 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct76,
                                    SDValue(vload37, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq19, 0)
                                    );

// END_WHERE;// Instr #242
SDNode *endwhere19 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload39, 1)
                                    );

// R26 = R26 & R13;// Instr #243
SDNode *and33 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload5, 0),
                                    SDValue(shr5, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere19, 0)
                                    );

SDValue ct77 = CrtDAG->getSignedConstant(30, DL, MVT::i16, true, false);
// R09 = 30;// Instr #244
SDNode *vload40 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct77,
                                    // glue (or chain) input edge
                                    SDValue(and33, 1)
                                    );

// R09 = R09 < R25;// Instr #245
SDNode *lt12 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload40, 0),
                                    SDValue(vload39, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload40, 1)
                                    );

// R09 = R09 & R14;// Instr #246
SDNode *and34 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload24, 0),
                                    SDValue(lt12, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt12, 1)
                                    );

// R09 = R09 == R30;// Instr #247
SDNode *eq34 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and34, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and34, 1)
                                    );

SDValue ct78 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #248
SDNode *nop20 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct78,
                                    // glue (or chain) input edge
                                    SDValue(eq34, 1)
                                    );

// WHERE_EQ;// Instr #249
SDNode *whereeq20 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop20, 0)
                                    );

SDValue ct79 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R14 = 0;// Instr #250
SDNode *vload41 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct79,
                                    SDValue(vload24, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq20, 0)
                                    );

SDValue ct80 = CrtDAG->getSignedConstant(31744, DL, MVT::i16, true, false);
// R19 = 31744;// Instr #251
SDNode *vload42 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct80,
                                    SDValue(xor0, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload41, 1)
                                    );

// R19 = R19 | R24;// Instr #252
SDNode *or13 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and26, 0),
                                    SDValue(vload42, 0),
                                    SDValue(vload42, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload42, 1)
                                    }
                                    );

// END_WHERE;// Instr #253
SDNode *endwhere20 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or13, 1)
                                    );

// R08 = R14 == R30;// Instr #254
SDNode *eq35 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload41, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere20, 0)
                                    );

SDValue ct81 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #255
SDNode *nop21 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct81,
                                    // glue (or chain) input edge
                                    SDValue(eq35, 1)
                                    );

// WHERE_EQ;// Instr #256
SDNode *whereeq21 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop21, 0)
                                    );

SDValue ct82 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R19 = R25 << 10;// Instr #257
SDNode *ishl11 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload39, 0),
                                    ct82,
                                    SDValue(or13, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq21, 0)
                                    }
                                    );

// R19 = R19 | R26;// Instr #258
SDNode *or14 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and33, 0),
                                    SDValue(ishl11, 0),
                                    SDValue(ishl11, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl11, 1)
                                    }
                                    );

// R04 = R26 & R30;// Instr #259
SDNode *and35 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(and33, 0),
                                    SDValue(vload14, 0),
                                    // glue (or chain) input edge
                                    SDValue(or14, 1)
                                    }
                                    );

// R07 = R61 - R30;// Instr #260
SDNode *sub14 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub12, 0),
                                    SDValue(vload1, 0),
                                    SDValue(and17, 0),
                                    // glue (or chain) input edge
                                    SDValue(and35, 1)
                                    }
                                    );

// R08 = R30 << R08;// Instr #261
SDNode *shl5 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq35, 0),
                                    SDValue(eq35, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub14, 1)
                                    }
                                    );

// R03 = R62 & R08;// Instr #262
SDNode *and36 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl5, 0),
                                    SDValue(shr4, 0),
                                    SDValue(vload15, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl5, 1)
                                    }
                                    );

// R62 = R62 ^ R03;// Instr #263
SDNode *xor1 = CrtDAG->getMachineNode(
                                    Connex::XORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and36, 0),
                                    SDValue(shr4, 0),
                                    SDValue(shr4, 0),
                                    // glue (or chain) input edge
                                    SDValue(and36, 1)
                                    }
                                    );

// R03 = R03 == R31;// Instr #264
SDNode *eq36 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and36, 0),
                                    SDValue(vload2, 0),
                                    SDValue(and36, 0),
                                    // glue (or chain) input edge
                                    SDValue(xor1, 1)
                                    }
                                    );

// R03 = R30 - R03;// Instr #265
SDNode *sub15 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq36, 0),
                                    SDValue(eq36, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq36, 1)
                                    }
                                    );

SDValue ct83 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R08 = R08 >> 1;// Instr #266
SDNode *ishr6 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl5, 0),
                                    ct83,
                                    SDValue(shl5, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub15, 1)
                                    }
                                    );

// R02 = R62 & R08;// Instr #267
SDNode *and37 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishr6, 0),
                                    SDValue(xor1, 0),
                                    SDValue(vload16, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr6, 1)
                                    }
                                    );

// R62 = R62 ^ R02;// Instr #268
SDNode *xor2 = CrtDAG->getMachineNode(
                                    Connex::XORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and37, 0),
                                    SDValue(xor1, 0),
                                    SDValue(xor1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and37, 1)
                                    }
                                    );

// R02 = R02 == R31;// Instr #269
SDNode *eq37 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and37, 0),
                                    SDValue(vload2, 0),
                                    SDValue(and37, 0),
                                    // glue (or chain) input edge
                                    SDValue(xor2, 1)
                                    }
                                    );

// R02 = R30 - R02;// Instr #270
SDNode *sub16 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq37, 0),
                                    SDValue(eq37, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq37, 1)
                                    }
                                    );

// R01 = R62 == R31;// Instr #271
SDNode *eq38 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(xor2, 0),
                                    SDValue(vload2, 0),
                                    SDValue(vload17, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub16, 1)
                                    }
                                    );

// R01 = R30 - R01;// Instr #272
SDNode *sub17 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq38, 0),
                                    SDValue(eq38, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq38, 1)
                                    }
                                    );

// R00 = R04 | R02;// Instr #273
SDNode *or15 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub16, 0),
                                    SDValue(and35, 0),
                                    SDValue(vload18, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub17, 1)
                                    }
                                    );

// R00 = R00 | R01;// Instr #274
SDNode *or16 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub17, 0),
                                    SDValue(or15, 0),
                                    SDValue(or15, 0),
                                    // glue (or chain) input edge
                                    SDValue(or15, 1)
                                    }
                                    );

// R00 = R00 & R03;// Instr #275
SDNode *and38 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub15, 0),
                                    SDValue(or16, 0),
                                    SDValue(or16, 0),
                                    // glue (or chain) input edge
                                    SDValue(or16, 1)
                                    }
                                    );

// R19 = R00 + R19;// Instr #276
SDNode *add2 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or14, 0),
                                    SDValue(and38, 0),
                                    SDValue(or14, 0),
                                    // glue (or chain) input edge
                                    SDValue(and38, 1)
                                    }
                                    );

// R19 = R19 | R24;// Instr #277
SDNode *resF16 /*or17*/ = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and26, 0),
                                    SDValue(add2, 0),
                                    SDValue(add2, 0),
                                    // glue (or chain) input edge
                                    SDValue(add2, 1)
                                    }
                                    );

// END_WHERE;// Instr #278
SDNode *lastNode /*endwhere21*/ = CurDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
// Alex:                                    MVT::Glue,
                                    MVT::Other,
                                    // glue (or chain) input edge
                                    SDValue(resF16 /*or17*/, 1)
                                    );

