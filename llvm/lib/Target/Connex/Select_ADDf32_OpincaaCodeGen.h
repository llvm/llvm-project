// From /home/asusu/LLVM/Tests/opincaa_standalone_apps/Emulate_f32/ADDf32_manual/DumpISel_OpincaaCodeGen.cpp_old03_REG64

// Code auto-generated by method Kernel::genLLVMISelManualCode()
//   from the OPINCAA lib, from kernel add_or_sub.f32.
// You should include this code in the Select() method of the [Target]SelectionDAGISel
//   class of your back end (or MAYBE in the ISelLowering pass).
// Number of instructions generated: 505.




SDValue ct0 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R14 = 1;// Instr #0
SDNode *vload0 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct0,
                                    // glue (or chain) input edge
                                    SDValue(nodeOpSrcCast1, 1)
                                    );

SDValue ct1 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R30 = 1;// Instr #1
SDNode *vload1 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct1,
                                    // glue (or chain) input edge
                                    SDValue(vload0, 1)
                                    );

SDValue ct2 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R31 = 0;// Instr #2
SDNode *vload2 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct2,
                                    // glue (or chain) input edge
                                    SDValue(vload1, 1)
                                    );

SDValue ct3 = CrtDAG->getSignedConstant(16, DL, MVT::i16, true, false);
// R29 = 16;// Instr #3
SDNode *vload3 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct3,
                                    // glue (or chain) input edge
                                    SDValue(vload2, 1)
                                    );

SDValue ct4 = CrtDAG->getSignedConstant(255, DL, MVT::i16, true, false);
// R28 = 255;// Instr #4
SDNode *vload4 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct4,
                                    // glue (or chain) input edge
                                    SDValue(vload3, 1)
                                    );

SDValue ct5 = CrtDAG->getSignedConstant(127, DL, MVT::i16, true, false);
// R13 = 127;// Instr #5
SDNode *vload5 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct5,
                                    // glue (or chain) input edge
                                    SDValue(vload4, 1)
                                    );

SDValue ct6 = CrtDAG->getSignedConstant(32640, DL, MVT::i16, true, false);
// R12 = 32640;// Instr #6
SDNode *vload6 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct6,
                                    // glue (or chain) input edge
                                    SDValue(vload5, 1)
                                    );

SDValue ct7 = CrtDAG->getSignedConstant(-32768, DL, MVT::i16, true, false);
// R11 = -32768;// Instr #7
SDNode *vload7 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct7,
                                    // glue (or chain) input edge
                                    SDValue(vload6, 1)
                                    );

SDValue ct8 = CrtDAG->getSignedConstant(128, DL, MVT::i16, true, false);
// R10 = 128;// Instr #8
SDNode *vload8 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct8,
                                    // glue (or chain) input edge
                                    SDValue(vload7, 1)
                                    );

SDValue ct9 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R19 = 0;// Instr #9
SDNode *vload9 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct9,
                                    // glue (or chain) input edge
                                    SDValue(vload8, 1)
                                    );

SDValue ct10 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R62 = 0;// Instr #10
SDNode *vload10 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct10,
                                    // glue (or chain) input edge
                                    SDValue(vload9, 1)
                                    );

SDValue ct11 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R61 = 0;// Instr #11
SDNode *vload11 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct11,
                                    // glue (or chain) input edge
                                    SDValue(vload10, 1)
                                    );

SDValue ct12 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R07 = 0;// Instr #12
SDNode *vload12 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct12,
                                    // glue (or chain) input edge
                                    SDValue(vload11, 1)
                                    );

SDValue ct13 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R04 = 0;// Instr #13
SDNode *vload13 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct13,
                                    // glue (or chain) input edge
                                    SDValue(vload12, 1)
                                    );

SDValue ct14 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R03 = 0;// Instr #14
SDNode *vload14 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct14,
                                    // glue (or chain) input edge
                                    SDValue(vload13, 1)
                                    );

SDValue ct15 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R02 = 0;// Instr #15
SDNode *vload15 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct15,
                                    // glue (or chain) input edge
                                    SDValue(vload14, 1)
                                    );

SDValue ct16 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R01 = 0;// Instr #16
SDNode *vload16 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct16,
                                    // glue (or chain) input edge
                                    SDValue(vload15, 1)
                                    );

SDValue ct17 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R00 = 0;// Instr #17
SDNode *vload17 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct17,
                                    // glue (or chain) input edge
                                    SDValue(vload16, 1)
                                    );

// R43 = R27 | R27;// Instr #18
SDNode *or0 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast1, 0),
                                    SDValue(nodeOpSrcCast1, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload17, 1)
                                    );

// CELL_SHL(R27, R30);// Instr #19
SDNode *cellshl0 = CrtDAG->getMachineNode(
                                    Connex::CELLSHL_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast1, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(or0, 1)
                                    );

SDValue ct18 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #20
SDNode *nop0 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct18,
                                    // glue (or chain) input edge
                                    SDValue(cellshl0, 0)
                                    );

// R27 = SHIFT_REG;// Instr #21
SDNode *ldsh0 = CrtDAG->getMachineNode(
                                    Connex::LDSH_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop0, 0)
                                    );

// R24 = R27 & R11;// Instr #22
SDNode *and0 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(ldsh0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldsh0, 1)
                                    );

// R25 = R27 & R12;// Instr #23
SDNode *and1 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload6, 0),
                                    SDValue(ldsh0, 0),
                                    // glue (or chain) input edge
                                    SDValue(and0, 1)
                                    );

SDValue ct19 = CrtDAG->getSignedConstant(7, DL, MVT::i16, true, false);
// R25 = R25 >> 7;// Instr #24
SDNode *ishr0 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and1, 0),
                                    ct19,
                                    // glue (or chain) input edge
                                    SDValue(and1, 1)
                                    );

SDValue ct20 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R26 = R27 << 0;// Instr #25
SDNode *ishl0 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ldsh0, 0),
                                    ct20,
                                    // glue (or chain) input edge
                                    SDValue(ishr0, 1)
                                    );

// R09 = INDEX;// Instr #26
SDNode *ldix0 = CrtDAG->getMachineNode(
                                    Connex::LDIX_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(ishl0, 1)
                                    );

// R09 = R09 & R30;// Instr #27
SDNode *and2 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ldix0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldix0, 1)
                                    );

// R16 = R09 == R31;// Instr #28
SDNode *eq0 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and2, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and2, 1)
                                    );

SDValue ct21 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #29
SDNode *nop1 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct21,
                                    // glue (or chain) input edge
                                    SDValue(eq0, 1)
                                    );

// WHERE_EQ;// Instr #30
SDNode *whereeq0 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop1, 0)
                                    );

// R26 = R27 & R13;// Instr #31
SDNode *and3 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload5, 0),
                                    SDValue(ldsh0, 0),
                                    SDValue(ishl0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq0, 0)
                                    }
                                    );

// END_WHERE;// Instr #32
SDNode *endwhere0 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(and3, 1)
                                    );

// R09 = R43 | R26;// Instr #33
SDNode *or1 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and3, 0),
                                    SDValue(or0, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere0, 0)
                                    );

// R16 = R31 == R09;// Instr #34
SDNode *eq1 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload2, 0),
                                    SDValue(or1, 0),
                                    // glue (or chain) input edge
                                    SDValue(or1, 1)
                                    );

// R16 = R30 - R16;// Instr #35
SDNode *sub0 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq1, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq1, 1)
                                    );

// R15 = R25 == R31;// Instr #36
SDNode *eq2 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr0, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub0, 1)
                                    );

// R09 = R15 & R16;// Instr #37
SDNode *and4 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub0, 0),
                                    SDValue(eq2, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq2, 1)
                                    );

// R09 = R09 == R30;// Instr #38
SDNode *eq3 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and4, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and4, 1)
                                    );

SDValue ct22 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #39
SDNode *nop2 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct22,
                                    // glue (or chain) input edge
                                    SDValue(eq3, 1)
                                    );

// WHERE_EQ;// Instr #40
SDNode *whereeq1 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop2, 0)
                                    );

SDValue ct23 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R25 = 1;// Instr #41
SDNode *vload18 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct23,
                                    SDValue(ishr0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq1, 0)
                                    );

// END_WHERE;// Instr #42
SDNode *endwhere1 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload18, 1)
                                    );

// R16 = R25 == R28;// Instr #43
SDNode *eq4 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload18, 0),
                                    SDValue(vload4, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere1, 0)
                                    );

// R16 = R16 | R15;// Instr #44
SDNode *or2 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq2, 0),
                                    SDValue(eq4, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq4, 1)
                                    );

// R16 = R16 == R31;// Instr #45
SDNode *eq5 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or2, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(or2, 1)
                                    );

SDValue ct24 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #46
SDNode *nop3 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct24,
                                    // glue (or chain) input edge
                                    SDValue(eq5, 1)
                                    );

// WHERE_EQ;// Instr #47
SDNode *whereeq2 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop3, 0)
                                    );

// R26 = R26 | R10;// Instr #48
SDNode *or3 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload8, 0),
                                    SDValue(and3, 0),
                                    SDValue(and3, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq2, 0)
                                    }
                                    );

// END_WHERE;// Instr #49
SDNode *endwhere2 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or3, 1)
                                    );

// R42 = R23 | R23;// Instr #50
SDNode *or4 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast2, 0),
                                    SDValue(nodeOpSrcCast2, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere2, 0)
                                    );

// CELL_SHL(R23, R30);// Instr #51
SDNode *cellshl1 = CrtDAG->getMachineNode(
                                    Connex::CELLSHL_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(nodeOpSrcCast2, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(or4, 1)
                                    );

SDValue ct25 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #52
SDNode *nop4 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct25,
                                    // glue (or chain) input edge
                                    SDValue(cellshl1, 0)
                                    );

// R23 = SHIFT_REG;// Instr #53
SDNode *ldsh1 = CrtDAG->getMachineNode(
                                    Connex::LDSH_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop4, 0)
                                    );

// R20 = R23 & R11;// Instr #54
SDNode *and5 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(ldsh1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldsh1, 1)
                                    );

// R21 = R23 & R12;// Instr #55
SDNode *and6 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload6, 0),
                                    SDValue(ldsh1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and5, 1)
                                    );

SDValue ct26 = CrtDAG->getSignedConstant(7, DL, MVT::i16, true, false);
// R21 = R21 >> 7;// Instr #56
SDNode *ishr1 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and6, 0),
                                    ct26,
                                    // glue (or chain) input edge
                                    SDValue(and6, 1)
                                    );

SDValue ct27 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R22 = R23 << 0;// Instr #57
SDNode *ishl1 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ldsh1, 0),
                                    ct27,
                                    // glue (or chain) input edge
                                    SDValue(ishr1, 1)
                                    );

// R09 = INDEX;// Instr #58
SDNode *ldix1 = CrtDAG->getMachineNode(
                                    Connex::LDIX_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(ishl1, 1)
                                    );

// R09 = R09 & R30;// Instr #59
SDNode *and7 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ldix1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldix1, 1)
                                    );

// R16 = R09 == R31;// Instr #60
SDNode *eq6 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and7, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and7, 1)
                                    );

SDValue ct28 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #61
SDNode *nop5 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct28,
                                    // glue (or chain) input edge
                                    SDValue(eq6, 1)
                                    );

// WHERE_EQ;// Instr #62
SDNode *whereeq3 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop5, 0)
                                    );

// R22 = R23 & R13;// Instr #63
SDNode *and8 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload5, 0),
                                    SDValue(ldsh1, 0),
                                    SDValue(ishl1, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq3, 0)
                                    }
                                    );

// END_WHERE;// Instr #64
SDNode *endwhere3 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(and8, 1)
                                    );

// R09 = R42 | R22;// Instr #65
SDNode *or5 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and8, 0),
                                    SDValue(or4, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere3, 0)
                                    );

// R16 = R31 == R09;// Instr #66
SDNode *eq7 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload2, 0),
                                    SDValue(or5, 0),
                                    // glue (or chain) input edge
                                    SDValue(or5, 1)
                                    );

// R16 = R30 - R16;// Instr #67
SDNode *sub1 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq7, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq7, 1)
                                    );

// R15 = R21 == R31;// Instr #68
SDNode *eq8 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr1, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub1, 1)
                                    );

// R09 = R15 & R16;// Instr #69
SDNode *and9 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub1, 0),
                                    SDValue(eq8, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq8, 1)
                                    );

// R09 = R09 == R30;// Instr #70
SDNode *eq9 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and9, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and9, 1)
                                    );

SDValue ct29 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #71
SDNode *nop6 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct29,
                                    // glue (or chain) input edge
                                    SDValue(eq9, 1)
                                    );

// WHERE_EQ;// Instr #72
SDNode *whereeq4 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop6, 0)
                                    );

SDValue ct30 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R21 = 1;// Instr #73
SDNode *vload19 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct30,
                                    SDValue(ishr1, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq4, 0)
                                    );

// END_WHERE;// Instr #74
SDNode *endwhere4 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload19, 1)
                                    );

// R16 = R21 == R28;// Instr #75
SDNode *eq10 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload19, 0),
                                    SDValue(vload4, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere4, 0)
                                    );

// R16 = R16 | R15;// Instr #76
SDNode *or6 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq8, 0),
                                    SDValue(eq10, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq10, 1)
                                    );

// R16 = R16 == R31;// Instr #77
SDNode *eq11 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or6, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(or6, 1)
                                    );

SDValue ct31 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #78
SDNode *nop7 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct31,
                                    // glue (or chain) input edge
                                    SDValue(eq11, 1)
                                    );

// WHERE_EQ;// Instr #79
SDNode *whereeq5 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop7, 0)
                                    );

// R22 = R22 | R10;// Instr #80
SDNode *or7 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload8, 0),
                                    SDValue(and8, 0),
                                    SDValue(and8, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq5, 0)
                                    }
                                    );

// END_WHERE;// Instr #81
SDNode *endwhere5 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or7, 1)
                                    );

// R50 = R24 == R11;// Instr #82
SDNode *eq12 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and0, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere5, 0)
                                    );

// R49 = R25 == R28;// Instr #83
SDNode *eq13 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload18, 0),
                                    SDValue(vload4, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq12, 1)
                                    );

// R48 = R26 == R31;// Instr #84
SDNode *eq14 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or3, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq13, 1)
                                    );

// R08 = R31 == R43;// Instr #85
SDNode *eq15 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload2, 0),
                                    SDValue(or0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq14, 1)
                                    );

// R48 = R48 & R08;// Instr #86
SDNode *and10 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq15, 0),
                                    SDValue(eq14, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq15, 1)
                                    );

// R47 = R20 == R11;// Instr #87
SDNode *eq16 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and5, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(and10, 1)
                                    );

// R46 = R21 == R28;// Instr #88
SDNode *eq17 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload19, 0),
                                    SDValue(vload4, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq16, 1)
                                    );

// R45 = R22 == R31;// Instr #89
SDNode *eq18 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or7, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq17, 1)
                                    );

// R08 = R31 == R42;// Instr #90
SDNode *eq19 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload2, 0),
                                    SDValue(or4, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq18, 1)
                                    );

// R45 = R45 & R08;// Instr #91
SDNode *and11 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq19, 0),
                                    SDValue(eq18, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq19, 1)
                                    );

// R07 = R49 & R46;// Instr #92
SDNode *and12 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq17, 0),
                                    SDValue(eq13, 0),
                                    // glue (or chain) input edge
                                    SDValue(and11, 1)
                                    );

// R08 = R07 & R50;// Instr #93
SDNode *and13 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq12, 0),
                                    SDValue(and12, 0),
                                    // glue (or chain) input edge
                                    SDValue(and12, 1)
                                    );

// R44 = ~R47;// Instr #94
SDNode *not0 = CrtDAG->getMachineNode(
                                    Connex::NOT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq16, 0),
                                    // glue (or chain) input edge
                                    SDValue(and13, 1)
                                    );

// R08 = R08 & R44;// Instr #95
SDNode *and14 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(not0, 0),
                                    SDValue(and13, 0),
                                    // glue (or chain) input edge
                                    SDValue(not0, 1)
                                    );

// R44 = ~R50;// Instr #96
SDNode *not1 = CrtDAG->getMachineNode(
                                    Connex::NOT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq12, 0),
                                    // glue (or chain) input edge
                                    SDValue(and14, 1)
                                    );

// R44 = R44 & R07;// Instr #97
SDNode *and15 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and12, 0),
                                    SDValue(not1, 0),
                                    // glue (or chain) input edge
                                    SDValue(not1, 1)
                                    );

// R44 = R44 & R47;// Instr #98
SDNode *and16 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq16, 0),
                                    SDValue(and15, 0),
                                    // glue (or chain) input edge
                                    SDValue(and15, 1)
                                    );

// R08 = R08 | R44;// Instr #99
SDNode *or8 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and16, 0),
                                    SDValue(and14, 0),
                                    // glue (or chain) input edge
                                    SDValue(and16, 1)
                                    );

// R07 = ~R45;// Instr #100
SDNode *not2 = CrtDAG->getMachineNode(
                                    Connex::NOT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and11, 0),
                                    // glue (or chain) input edge
                                    SDValue(or8, 1)
                                    );

// R07 = R07 & R46;// Instr #101
SDNode *and17 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq17, 0),
                                    SDValue(not2, 0),
                                    // glue (or chain) input edge
                                    SDValue(not2, 1)
                                    );

// R08 = R08 | R07;// Instr #102
SDNode *or9 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and17, 0),
                                    SDValue(or8, 0),
                                    // glue (or chain) input edge
                                    SDValue(and17, 1)
                                    );

// R07 = ~R48;// Instr #103
SDNode *not3 = CrtDAG->getMachineNode(
                                    Connex::NOT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and10, 0),
                                    // glue (or chain) input edge
                                    SDValue(or9, 1)
                                    );

// R07 = R07 & R49;// Instr #104
SDNode *and18 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq13, 0),
                                    SDValue(not3, 0),
                                    // glue (or chain) input edge
                                    SDValue(not3, 1)
                                    );

// R08 = R08 | R07;// Instr #105
SDNode *or10 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and18, 0),
                                    SDValue(or9, 0),
                                    // glue (or chain) input edge
                                    SDValue(and18, 1)
                                    );

// R09 = R08 == R30;// Instr #106
SDNode *eq20 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or10, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(or10, 1)
                                    );

SDValue ct32 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #107
SDNode *nop8 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct32,
                                    // glue (or chain) input edge
                                    SDValue(eq20, 1)
                                    );

// WHERE_EQ;// Instr #108
SDNode *whereeq6 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop8, 0)
                                    );

SDValue ct33 = CrtDAG->getSignedConstant(32640, DL, MVT::i16, true, false);
// R19 = 32640;// Instr #109
SDNode *vload20 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct33,
                                    SDValue(vload9, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq6, 0)
                                    );

SDValue ct34 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R14 = 0;// Instr #110
SDNode *vload21 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct34,
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload20, 1)
                                    );

// END_WHERE;// Instr #111
SDNode *endwhere6 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload21, 1)
                                    );

// R08 = R49 | R46;// Instr #112
SDNode *or11 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq17, 0),
                                    SDValue(eq13, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere6, 0)
                                    );

// R09 = R08 & R14;// Instr #113
SDNode *and19 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload21, 0),
                                    SDValue(or11, 0),
                                    // glue (or chain) input edge
                                    SDValue(or11, 1)
                                    );

// R09 = R09 == R30;// Instr #114
SDNode *eq21 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and19, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and19, 1)
                                    );

SDValue ct35 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #115
SDNode *nop9 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct35,
                                    // glue (or chain) input edge
                                    SDValue(eq21, 1)
                                    );

// WHERE_EQ;// Instr #116
SDNode *whereeq7 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop9, 0)
                                    );

SDValue ct36 = CrtDAG->getSignedConstant(32640, DL, MVT::i16, true, false);
// R19 = 32640;// Instr #117
SDNode *vload22 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct36,
                                    SDValue(vload20, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq7, 0)
                                    );

// R08 = R50 & R49;// Instr #118
SDNode *and20 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(eq13, 0),
                                    SDValue(eq12, 0),
                                    SDValue(or11, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload22, 1)
                                    }
                                    );

// R07 = R47 & R46;// Instr #119
SDNode *and21 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(eq17, 0),
                                    SDValue(eq16, 0),
                                    SDValue(and18, 0),
                                    // glue (or chain) input edge
                                    SDValue(and20, 1)
                                    }
                                    );

// R08 = R08 | R07;// Instr #120
SDNode *or12 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and21, 0),
                                    SDValue(and20, 0),
                                    SDValue(and20, 0),
                                    // glue (or chain) input edge
                                    SDValue(and21, 1)
                                    }
                                    );

SDValue ct37 = CrtDAG->getSignedConstant(15, DL, MVT::i16, true, false);
// R08 = R08 << 15;// Instr #121
SDNode *ishl2 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or12, 0),
                                    ct37,
                                    SDValue(or12, 0),
                                    // glue (or chain) input edge
                                    SDValue(or12, 1)
                                    }
                                    );

// R19 = R19 ^ R08;// Instr #122
SDNode *xor0 = CrtDAG->getMachineNode(
                                    Connex::XORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl2, 0),
                                    SDValue(vload22, 0),
                                    SDValue(vload22, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl2, 1)
                                    }
                                    );

SDValue ct38 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R14 = 0;// Instr #123
SDNode *vload23 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct38,
                                    SDValue(vload21, 0),
                                    // glue (or chain) input edge
                                    SDValue(xor0, 1)
                                    );

// END_WHERE;// Instr #124
SDNode *endwhere7 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload23, 1)
                                    );

// R15 = R25 - R21;// Instr #125
SDNode *sub2 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload18, 0),
                                    SDValue(vload19, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere7, 0)
                                    );

SDValue ct39 = CrtDAG->getSignedConstant(-6, DL, MVT::i16, true, false);
// R08 = -6;// Instr #126
SDNode *vload24 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct39,
                                    // glue (or chain) input edge
                                    SDValue(sub2, 1)
                                    );

// R16 = R15 < R08;// Instr #127
SDNode *lt0 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub2, 0),
                                    SDValue(vload24, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload24, 1)
                                    );

SDValue ct40 = CrtDAG->getSignedConstant(6, DL, MVT::i16, true, false);
// R08 = 6;// Instr #128
SDNode *vload25 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct40,
                                    // glue (or chain) input edge
                                    SDValue(lt0, 1)
                                    );

// R09 = R08 < R15;// Instr #129
SDNode *lt1 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload25, 0),
                                    SDValue(sub2, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload25, 1)
                                    );

// R09 = R09 | R16;// Instr #130
SDNode *or13 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt0, 0),
                                    SDValue(lt1, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt1, 1)
                                    );

// R09 = R09 & R14;// Instr #131
SDNode *and22 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(or13, 0),
                                    // glue (or chain) input edge
                                    SDValue(or13, 1)
                                    );

// R09 = R09 == R30;// Instr #132
SDNode *eq22 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and22, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and22, 1)
                                    );

SDValue ct41 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #133
SDNode *nop10 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct41,
                                    // glue (or chain) input edge
                                    SDValue(eq22, 1)
                                    );

// WHERE_EQ;// Instr #134
SDNode *whereeq8 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop10, 0)
                                    );

SDValue ct42 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R08 = R43 >> 10;// Instr #135
SDNode *ishr2 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or0, 0),
                                    ct42,
                                    SDValue(vload25, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq8, 0)
                                    }
                                    );

SDValue ct43 = CrtDAG->getSignedConstant(6, DL, MVT::i16, true, false);
// R43 = R43 << 6;// Instr #136
SDNode *ishl3 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or0, 0),
                                    ct43,
                                    SDValue(or0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr2, 1)
                                    }
                                    );

SDValue ct44 = CrtDAG->getSignedConstant(6, DL, MVT::i16, true, false);
// R26 = R26 << 6;// Instr #137
SDNode *ishl4 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or3, 0),
                                    ct44,
                                    SDValue(or3, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl3, 1)
                                    }
                                    );

// R26 = R26 | R08;// Instr #138
SDNode *or14 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishr2, 0),
                                    SDValue(ishl4, 0),
                                    SDValue(ishl4, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl4, 1)
                                    }
                                    );

SDValue ct45 = CrtDAG->getSignedConstant(6, DL, MVT::i16, true, false);
// R08 = 6;// Instr #139
SDNode *vload26 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct45,
                                    SDValue(ishr2, 0),
                                    // glue (or chain) input edge
                                    SDValue(or14, 1)
                                    );

// R25 = R25 - R08;// Instr #140
SDNode *sub3 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload18, 0),
                                    SDValue(vload26, 0),
                                    SDValue(vload18, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload26, 1)
                                    }
                                    );

// R21 = R21 - R08;// Instr #141
SDNode *sub4 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload19, 0),
                                    SDValue(vload26, 0),
                                    SDValue(vload19, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub3, 1)
                                    }
                                    );

SDValue ct46 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R08 = R42 >> 10;// Instr #142
SDNode *ishr3 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or4, 0),
                                    ct46,
                                    SDValue(vload26, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub4, 1)
                                    }
                                    );

SDValue ct47 = CrtDAG->getSignedConstant(6, DL, MVT::i16, true, false);
// R42 = R42 << 6;// Instr #143
SDNode *ishl5 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or4, 0),
                                    ct47,
                                    SDValue(or4, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr3, 1)
                                    }
                                    );

SDValue ct48 = CrtDAG->getSignedConstant(6, DL, MVT::i16, true, false);
// R22 = R22 << 6;// Instr #144
SDNode *ishl6 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or7, 0),
                                    ct48,
                                    SDValue(or7, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl5, 1)
                                    }
                                    );

// R22 = R22 | R08;// Instr #145
SDNode *or15 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishr3, 0),
                                    SDValue(ishl6, 0),
                                    SDValue(ishl6, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl6, 1)
                                    }
                                    );

// END_WHERE;// Instr #146
SDNode *endwhere8 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or15, 1)
                                    );

SDValue ct49 = CrtDAG->getSignedConstant(-31, DL, MVT::i16, true, false);
// R08 = -31;// Instr #147
SDNode *vload27 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct49,
                                    // glue (or chain) input edge
                                    SDValue(endwhere8, 0)
                                    );

// R09 = R15 < R08;// Instr #148
SDNode *lt2 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub2, 0),
                                    SDValue(vload27, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload27, 1)
                                    );

// R09 = R09 & R14;// Instr #149
SDNode *and23 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt2, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt2, 1)
                                    );

// R09 = R09 == R30;// Instr #150
SDNode *eq23 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and23, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and23, 1)
                                    );

SDValue ct50 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #151
SDNode *nop11 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct50,
                                    // glue (or chain) input edge
                                    SDValue(eq23, 1)
                                    );

// WHERE_EQ;// Instr #152
SDNode *whereeq9 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop11, 0)
                                    );

// R15 = R31 - R15;// Instr #153
SDNode *sub5 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(sub2, 0),
                                    SDValue(sub2, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq9, 0)
                                    }
                                    );

SDValue ct51 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R43 = 0;// Instr #154
SDNode *vload28 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct51,
                                    SDValue(ishl3, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub5, 1)
                                    );

SDValue ct52 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R26 = 0;// Instr #155
SDNode *vload29 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct52,
                                    SDValue(or14, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload28, 1)
                                    );

SDValue ct53 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R25 = R21 << 0;// Instr #156
SDNode *ishl7 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub4, 0),
                                    ct53,
                                    SDValue(sub3, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload29, 1)
                                    }
                                    );

SDValue ct54 = CrtDAG->getSignedConstant(32000, DL, MVT::i16, true, false);
// R15 = 32000;// Instr #157
SDNode *vload30 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct54,
                                    SDValue(sub5, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl7, 1)
                                    );

// END_WHERE;// Instr #158
SDNode *endwhere9 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload30, 1)
                                    );

SDValue ct55 = CrtDAG->getSignedConstant(-15, DL, MVT::i16, true, false);
// R08 = -15;// Instr #159
SDNode *vload31 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct55,
                                    // glue (or chain) input edge
                                    SDValue(endwhere9, 0)
                                    );

// R09 = R15 < R08;// Instr #160
SDNode *lt3 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload30, 0),
                                    SDValue(vload31, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload31, 1)
                                    );

// R09 = R09 & R14;// Instr #161
SDNode *and24 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt3, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt3, 1)
                                    );

// R09 = R09 == R30;// Instr #162
SDNode *eq24 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and24, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and24, 1)
                                    );

SDValue ct56 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #163
SDNode *nop12 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct56,
                                    // glue (or chain) input edge
                                    SDValue(eq24, 1)
                                    );

// WHERE_EQ;// Instr #164
SDNode *whereeq10 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop12, 0)
                                    );

// R15 = R31 - R15;// Instr #165
SDNode *sub6 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(vload30, 0),
                                    SDValue(vload30, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq10, 0)
                                    }
                                    );

// R15 = R15 - R29;// Instr #166
SDNode *sub7 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub6, 0),
                                    SDValue(vload3, 0),
                                    SDValue(sub6, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub6, 1)
                                    }
                                    );

SDValue ct57 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R62 = R43 << 0;// Instr #167
SDNode *ishl8 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload28, 0),
                                    ct57,
                                    SDValue(vload10, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub7, 1)
                                    }
                                    );

SDValue ct58 = CrtDAG->getSignedConstant(16, DL, MVT::i16, true, false);
// R61 = 16;// Instr #168
SDNode *vload32 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct58,
                                    SDValue(vload11, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl8, 1)
                                    );

SDValue ct59 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R43 = R26 << 0;// Instr #169
SDNode *ishl9 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload29, 0),
                                    ct59,
                                    SDValue(vload28, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload32, 1)
                                    }
                                    );

SDValue ct60 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R26 = 0;// Instr #170
SDNode *vload33 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct60,
                                    SDValue(vload29, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl9, 1)
                                    );

// R43 = R43 >> R15;// Instr #171
SDNode *shr0 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl9, 0),
                                    SDValue(sub7, 0),
                                    SDValue(ishl9, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload33, 1)
                                    }
                                    );

SDValue ct61 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R25 = R21 << 0;// Instr #172
SDNode *ishl10 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub4, 0),
                                    ct61,
                                    SDValue(ishl7, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr0, 1)
                                    }
                                    );

SDValue ct62 = CrtDAG->getSignedConstant(32000, DL, MVT::i16, true, false);
// R15 = 32000;// Instr #173
SDNode *vload34 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct62,
                                    SDValue(sub7, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl10, 1)
                                    );

// END_WHERE;// Instr #174
SDNode *endwhere10 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload34, 1)
                                    );

SDValue ct63 = CrtDAG->getSignedConstant(-6, DL, MVT::i16, true, false);
// R08 = -6;// Instr #175
SDNode *vload35 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct63,
                                    // glue (or chain) input edge
                                    SDValue(endwhere10, 0)
                                    );

// R09 = R15 < R08;// Instr #176
SDNode *lt4 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload34, 0),
                                    SDValue(vload35, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload35, 1)
                                    );

// R09 = R09 & R14;// Instr #177
SDNode *and25 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt4, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt4, 1)
                                    );

// R09 = R09 == R30;// Instr #178
SDNode *eq25 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and25, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and25, 1)
                                    );

SDValue ct64 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #179
SDNode *nop13 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct64,
                                    // glue (or chain) input edge
                                    SDValue(eq25, 1)
                                    );

// WHERE_EQ;// Instr #180
SDNode *whereeq11 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop13, 0)
                                    );

// R15 = R31 - R15;// Instr #181
SDNode *sub8 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(vload34, 0),
                                    SDValue(vload34, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq11, 0)
                                    }
                                    );

// R08 = R29 - R15;// Instr #182
SDNode *sub9 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload3, 0),
                                    SDValue(sub8, 0),
                                    SDValue(vload35, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub8, 1)
                                    }
                                    );

SDValue ct65 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R62 = R43 << 0;// Instr #183
SDNode *ishl11 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr0, 0),
                                    ct65,
                                    SDValue(ishl8, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub9, 1)
                                    }
                                    );

// R62 = R62 << R08;// Instr #184
SDNode *shl0 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl11, 0),
                                    SDValue(sub9, 0),
                                    SDValue(ishl11, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl11, 1)
                                    }
                                    );

// R62 = R62 >> R08;// Instr #185
SDNode *shr1 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl0, 0),
                                    SDValue(sub9, 0),
                                    SDValue(shl0, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl0, 1)
                                    }
                                    );

SDValue ct66 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R61 = R15 << 0;// Instr #186
SDNode *ishl12 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub8, 0),
                                    ct66,
                                    SDValue(vload32, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr1, 1)
                                    }
                                    );

// R43 = R43 >> R15;// Instr #187
SDNode *shr2 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr0, 0),
                                    SDValue(sub8, 0),
                                    SDValue(shr0, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl12, 1)
                                    }
                                    );

// R08 = R26 << R08;// Instr #188
SDNode *shl1 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload33, 0),
                                    SDValue(sub9, 0),
                                    SDValue(sub9, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr2, 1)
                                    }
                                    );

// R43 = R43 | R08;// Instr #189
SDNode *or16 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl1, 0),
                                    SDValue(shr2, 0),
                                    SDValue(shr2, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl1, 1)
                                    }
                                    );

// R26 = R26 >> R15;// Instr #190
SDNode *shr3 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload33, 0),
                                    SDValue(sub8, 0),
                                    SDValue(vload33, 0),
                                    // glue (or chain) input edge
                                    SDValue(or16, 1)
                                    }
                                    );

SDValue ct67 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R25 = R21 << 0;// Instr #191
SDNode *ishl13 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub4, 0),
                                    ct67,
                                    SDValue(ishl10, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr3, 1)
                                    }
                                    );

SDValue ct68 = CrtDAG->getSignedConstant(32000, DL, MVT::i16, true, false);
// R15 = 32000;// Instr #192
SDNode *vload36 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct68,
                                    SDValue(sub8, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl13, 1)
                                    );

// END_WHERE;// Instr #193
SDNode *endwhere11 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload36, 1)
                                    );

// R09 = R24 == R11;// Instr #194
SDNode *eq26 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and0, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere11, 0)
                                    );

SDValue ct69 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #195
SDNode *nop14 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct69,
                                    // glue (or chain) input edge
                                    SDValue(eq26, 1)
                                    );

// WHERE_EQ;// Instr #196
SDNode *whereeq12 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop14, 0)
                                    );

// R62 = R31 - R62;// Instr #197
SDNode *sub10 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(shr1, 0),
                                    SDValue(shr1, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq12, 0)
                                    }
                                    );

// END_WHERE;// Instr #198
SDNode *endwhere12 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub10, 1)
                                    );

// R09 = R15 < R31;// Instr #199
SDNode *lt5 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload36, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere12, 0)
                                    );

// R09 = R09 & R14;// Instr #200
SDNode *and26 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt5, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt5, 1)
                                    );

// R09 = R09 == R30;// Instr #201
SDNode *eq27 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and26, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and26, 1)
                                    );

SDValue ct70 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #202
SDNode *nop15 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct70,
                                    // glue (or chain) input edge
                                    SDValue(eq27, 1)
                                    );

// WHERE_EQ;// Instr #203
SDNode *whereeq13 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop15, 0)
                                    );

// R15 = R31 - R15;// Instr #204
SDNode *sub11 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(vload36, 0),
                                    SDValue(vload36, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq13, 0)
                                    }
                                    );

// R08 = R29 - R15;// Instr #205
SDNode *sub12 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload3, 0),
                                    SDValue(sub11, 0),
                                    SDValue(shl1, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub11, 1)
                                    }
                                    );

// R08 = R42 >> R08;// Instr #206
SDNode *shr4 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl5, 0),
                                    SDValue(sub12, 0),
                                    SDValue(sub12, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub12, 1)
                                    }
                                    );

// R42 = R42 << R15;// Instr #207
SDNode *shl2 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl5, 0),
                                    SDValue(sub11, 0),
                                    SDValue(ishl5, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr4, 1)
                                    }
                                    );

// R22 = R22 << R15;// Instr #208
SDNode *shl3 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or15, 0),
                                    SDValue(sub11, 0),
                                    SDValue(or15, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl2, 1)
                                    }
                                    );

// R22 = R22 | R08;// Instr #209
SDNode *or17 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr4, 0),
                                    SDValue(shl3, 0),
                                    SDValue(shl3, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl3, 1)
                                    }
                                    );

SDValue ct71 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R21 = R25 << 0;// Instr #210
SDNode *ishl14 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl13, 0),
                                    ct71,
                                    SDValue(sub4, 0),
                                    // glue (or chain) input edge
                                    SDValue(or17, 1)
                                    }
                                    );

SDValue ct72 = CrtDAG->getSignedConstant(32000, DL, MVT::i16, true, false);
// R15 = 32000;// Instr #211
SDNode *vload37 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct72,
                                    SDValue(sub11, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl14, 1)
                                    );

// END_WHERE;// Instr #212
SDNode *endwhere13 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload37, 1)
                                    );

SDValue ct73 = CrtDAG->getSignedConstant(7, DL, MVT::i16, true, false);
// R08 = 7;// Instr #213
SDNode *vload38 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct73,
                                    // glue (or chain) input edge
                                    SDValue(endwhere13, 0)
                                    );

// R09 = R15 < R08;// Instr #214
SDNode *lt6 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload37, 0),
                                    SDValue(vload38, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload38, 1)
                                    );

// R09 = R09 & R14;// Instr #215
SDNode *and27 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt6, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt6, 1)
                                    );

// R09 = R09 == R30;// Instr #216
SDNode *eq28 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and27, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and27, 1)
                                    );

SDValue ct74 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #217
SDNode *nop16 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct74,
                                    // glue (or chain) input edge
                                    SDValue(eq28, 1)
                                    );

// WHERE_EQ;// Instr #218
SDNode *whereeq14 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop16, 0)
                                    );

// R08 = R29 - R15;// Instr #219
SDNode *sub13 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload3, 0),
                                    SDValue(vload37, 0),
                                    SDValue(vload38, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq14, 0)
                                    }
                                    );

// R08 = R43 >> R08;// Instr #220
SDNode *shr5 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or16, 0),
                                    SDValue(sub13, 0),
                                    SDValue(sub13, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub13, 1)
                                    }
                                    );

// R43 = R43 << R15;// Instr #221
SDNode *shl4 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or16, 0),
                                    SDValue(vload37, 0),
                                    SDValue(or16, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr5, 1)
                                    }
                                    );

// R26 = R26 << R15;// Instr #222
SDNode *shl5 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr3, 0),
                                    SDValue(vload37, 0),
                                    SDValue(shr3, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl4, 1)
                                    }
                                    );

// R26 = R26 | R08;// Instr #223
SDNode *or18 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr5, 0),
                                    SDValue(shl5, 0),
                                    SDValue(shl5, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl5, 1)
                                    }
                                    );

SDValue ct75 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R25 = R21 << 0;// Instr #224
SDNode *ishl15 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl14, 0),
                                    ct75,
                                    SDValue(ishl13, 0),
                                    // glue (or chain) input edge
                                    SDValue(or18, 1)
                                    }
                                    );

SDValue ct76 = CrtDAG->getSignedConstant(32000, DL, MVT::i16, true, false);
// R15 = 32000;// Instr #225
SDNode *vload39 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct76,
                                    SDValue(vload37, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl15, 1)
                                    );

// END_WHERE;// Instr #226
SDNode *endwhere14 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload39, 1)
                                    );

// R09 = R15 < R29;// Instr #227
SDNode *lt7 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload39, 0),
                                    SDValue(vload3, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere14, 0)
                                    );

// R09 = R09 & R14;// Instr #228
SDNode *and28 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt7, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt7, 1)
                                    );

// R09 = R09 == R30;// Instr #229
SDNode *eq29 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and28, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and28, 1)
                                    );

SDValue ct77 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #230
SDNode *nop17 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct77,
                                    // glue (or chain) input edge
                                    SDValue(eq29, 1)
                                    );

// WHERE_EQ;// Instr #231
SDNode *whereeq15 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop17, 0)
                                    );

// R08 = R29 - R15;// Instr #232
SDNode *sub14 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload3, 0),
                                    SDValue(vload39, 0),
                                    SDValue(shr5, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq15, 0)
                                    }
                                    );

SDValue ct78 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R62 = R42 << 0;// Instr #233
SDNode *ishl16 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl2, 0),
                                    ct78,
                                    SDValue(sub10, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub14, 1)
                                    }
                                    );

// R62 = R62 << R08;// Instr #234
SDNode *shl6 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl16, 0),
                                    SDValue(sub14, 0),
                                    SDValue(ishl16, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl16, 1)
                                    }
                                    );

// R62 = R62 >> R08;// Instr #235
SDNode *shr6 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl6, 0),
                                    SDValue(sub14, 0),
                                    SDValue(shl6, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl6, 1)
                                    }
                                    );

SDValue ct79 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R61 = R15 << 0;// Instr #236
SDNode *ishl17 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload39, 0),
                                    ct79,
                                    SDValue(ishl12, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr6, 1)
                                    }
                                    );

// R42 = R42 >> R15;// Instr #237
SDNode *shr7 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl2, 0),
                                    SDValue(vload39, 0),
                                    SDValue(shl2, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl17, 1)
                                    }
                                    );

// R08 = R22 << R08;// Instr #238
SDNode *shl7 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or17, 0),
                                    SDValue(sub14, 0),
                                    SDValue(sub14, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr7, 1)
                                    }
                                    );

// R42 = R42 | R08;// Instr #239
SDNode *or19 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl7, 0),
                                    SDValue(shr7, 0),
                                    SDValue(shr7, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl7, 1)
                                    }
                                    );

// R22 = R22 >> R15;// Instr #240
SDNode *shr8 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or17, 0),
                                    SDValue(vload39, 0),
                                    SDValue(or17, 0),
                                    // glue (or chain) input edge
                                    SDValue(or19, 1)
                                    }
                                    );

SDValue ct80 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R21 = R25 << 0;// Instr #241
SDNode *ishl18 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl15, 0),
                                    ct80,
                                    SDValue(ishl14, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr8, 1)
                                    }
                                    );

SDValue ct81 = CrtDAG->getSignedConstant(32001, DL, MVT::i16, true, false);
// R15 = 32001;// Instr #242
SDNode *vload40 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct81,
                                    SDValue(vload39, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl18, 1)
                                    );

// END_WHERE;// Instr #243
SDNode *endwhere15 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload40, 1)
                                    );

SDValue ct82 = CrtDAG->getSignedConstant(32001, DL, MVT::i16, true, false);
// R16 = 32001;// Instr #244
SDNode *vload41 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct82,
                                    // glue (or chain) input edge
                                    SDValue(endwhere15, 0)
                                    );

// R16 = R15 == R16;// Instr #245
SDNode *eq30 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload40, 0),
                                    SDValue(vload41, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload41, 1)
                                    );

// R09 = R20 == R11;// Instr #246
SDNode *eq31 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and5, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq30, 1)
                                    );

// R09 = R09 & R16;// Instr #247
SDNode *and29 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq30, 0),
                                    SDValue(eq31, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq31, 1)
                                    );

// R09 = R09 == R30;// Instr #248
SDNode *eq32 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and29, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and29, 1)
                                    );

SDValue ct83 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #249
SDNode *nop18 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct83,
                                    // glue (or chain) input edge
                                    SDValue(eq32, 1)
                                    );

// WHERE_EQ;// Instr #250
SDNode *whereeq16 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop18, 0)
                                    );

// R62 = R31 - R62;// Instr #251
SDNode *sub15 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(shr6, 0),
                                    SDValue(shr6, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq16, 0)
                                    }
                                    );

// END_WHERE;// Instr #252
SDNode *endwhere16 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub15, 1)
                                    );

SDValue ct84 = CrtDAG->getSignedConstant(32, DL, MVT::i16, true, false);
// R08 = 32;// Instr #253
SDNode *vload42 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct84,
                                    // glue (or chain) input edge
                                    SDValue(endwhere16, 0)
                                    );

// R09 = R15 < R08;// Instr #254
SDNode *lt8 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload40, 0),
                                    SDValue(vload42, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload42, 1)
                                    );

// R09 = R09 & R14;// Instr #255
SDNode *and30 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt8, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt8, 1)
                                    );

// R09 = R09 == R30;// Instr #256
SDNode *eq33 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and30, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and30, 1)
                                    );

SDValue ct85 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #257
SDNode *nop19 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct85,
                                    // glue (or chain) input edge
                                    SDValue(eq33, 1)
                                    );

// WHERE_EQ;// Instr #258
SDNode *whereeq17 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop19, 0)
                                    );

// R15 = R15 - R29;// Instr #259
SDNode *sub16 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload40, 0),
                                    SDValue(vload3, 0),
                                    SDValue(vload40, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq17, 0)
                                    }
                                    );

SDValue ct86 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R42 = R22 << 0;// Instr #260
SDNode *ishl19 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr8, 0),
                                    ct86,
                                    SDValue(or19, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub16, 1)
                                    }
                                    );

SDValue ct87 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R22 = 0;// Instr #261
SDNode *vload43 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct87,
                                    SDValue(shr8, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl19, 1)
                                    );

// R42 = R42 >> R15;// Instr #262
SDNode *shr9 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl19, 0),
                                    SDValue(sub16, 0),
                                    SDValue(ishl19, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload43, 1)
                                    }
                                    );

SDValue ct88 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R21 = R25 << 0;// Instr #263
SDNode *ishl20 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl15, 0),
                                    ct88,
                                    SDValue(ishl18, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr9, 1)
                                    }
                                    );

SDValue ct89 = CrtDAG->getSignedConstant(32000, DL, MVT::i16, true, false);
// R15 = 32000;// Instr #264
SDNode *vload44 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct89,
                                    SDValue(sub16, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl20, 1)
                                    );

// END_WHERE;// Instr #265
SDNode *endwhere17 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload44, 1)
                                    );

SDValue ct90 = CrtDAG->getSignedConstant(256, DL, MVT::i16, true, false);
// R08 = 256;// Instr #266
SDNode *vload45 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct90,
                                    // glue (or chain) input edge
                                    SDValue(endwhere17, 0)
                                    );

// R09 = R15 < R08;// Instr #267
SDNode *lt9 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload44, 0),
                                    SDValue(vload45, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload45, 1)
                                    );

// R09 = R09 & R14;// Instr #268
SDNode *and31 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt9, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt9, 1)
                                    );

// R09 = R09 == R30;// Instr #269
SDNode *eq34 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and31, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and31, 1)
                                    );

SDValue ct91 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #270
SDNode *nop20 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct91,
                                    // glue (or chain) input edge
                                    SDValue(eq34, 1)
                                    );

// WHERE_EQ;// Instr #271
SDNode *whereeq18 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop20, 0)
                                    );

SDValue ct92 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R42 = 0;// Instr #272
SDNode *vload46 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct92,
                                    SDValue(shr9, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq18, 0)
                                    );

SDValue ct93 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R22 = 0;// Instr #273
SDNode *vload47 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct93,
                                    SDValue(vload43, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload46, 1)
                                    );

SDValue ct94 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R21 = R25 << 0;// Instr #274
SDNode *ishl21 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl15, 0),
                                    ct94,
                                    SDValue(ishl20, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload47, 1)
                                    }
                                    );

// END_WHERE;// Instr #275
SDNode *endwhere18 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(ishl21, 1)
                                    );

// R09 = R24 == R11;// Instr #276
SDNode *eq35 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and0, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere18, 0)
                                    );

// R09 = R09 & R14;// Instr #277
SDNode *and32 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(eq35, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq35, 1)
                                    );

// R09 = R09 == R30;// Instr #278
SDNode *eq36 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and32, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and32, 1)
                                    );

SDValue ct95 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #279
SDNode *nop21 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct95,
                                    // glue (or chain) input edge
                                    SDValue(eq36, 1)
                                    );

// WHERE_EQ;// Instr #280
SDNode *whereeq19 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop21, 0)
                                    );

// R43 = R31 - R43;// Instr #281
SDNode *sub17 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(shl4, 0),
                                    SDValue(shl4, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq19, 0)
                                    }
                                    );

// R26 = SUBC(R31, R26);// Instr #282
SDNode *subc0 = CrtDAG->getMachineNode(
                                    Connex::SUBCV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(or18, 0),
                                    SDValue(sub17, 0),
                                    SDValue(or18, 0)
                                    // no need for glue or chain input (since it normally consumes the output of the predecessor)
                                    }
                                    );

// END_WHERE;// Instr #283
SDNode *endwhere19 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(subc0, 1)
                                    );

// R09 = R20 == R11;// Instr #284
SDNode *eq37 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and5, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere19, 0)
                                    );

// R09 = R09 & R14;// Instr #285
SDNode *and33 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(eq37, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq37, 1)
                                    );

// R09 = R09 == R30;// Instr #286
SDNode *eq38 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and33, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and33, 1)
                                    );

SDValue ct96 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #287
SDNode *nop22 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct96,
                                    // glue (or chain) input edge
                                    SDValue(eq38, 1)
                                    );

// WHERE_EQ;// Instr #288
SDNode *whereeq20 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop22, 0)
                                    );

// R42 = R31 - R42;// Instr #289
SDNode *sub18 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(vload46, 0),
                                    SDValue(vload46, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq20, 0)
                                    }
                                    );

// R22 = SUBC(R31, R22);// Instr #290
SDNode *subc1 = CrtDAG->getMachineNode(
                                    Connex::SUBCV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(vload47, 0),
                                    SDValue(sub18, 0),
                                    SDValue(vload47, 0)
                                    // no need for glue or chain input (since it normally consumes the output of the predecessor)
                                    }
                                    );

// END_WHERE;// Instr #291
SDNode *endwhere20 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(subc1, 1)
                                    );

// R09 = R14 == R30;// Instr #292
SDNode *eq39 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere20, 0)
                                    );

SDValue ct97 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #293
SDNode *nop23 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct97,
                                    // glue (or chain) input edge
                                    SDValue(eq39, 1)
                                    );

// WHERE_EQ;// Instr #294
SDNode *whereeq21 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop23, 0)
                                    );

// R43 = R42 + R43;// Instr #295
SDNode *add0 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub17, 0),
                                    SDValue(sub18, 0),
                                    SDValue(sub17, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq21, 0)
                                    }
                                    );

// R26 = ADDC(R22, R26);// Instr #296
SDNode *addc0 = CrtDAG->getMachineNode(
                                    Connex::ADDCV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(subc1, 0),
                                    SDValue(subc0, 0),
                                    SDValue(add0, 0),
                                    SDValue(subc0, 0)
                                    // no need for glue or chain input (since it normally consumes the output of the predecessor)
                                    }
                                    );

// END_WHERE;// Instr #297
SDNode *endwhere21 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(addc0, 1)
                                    );

// R24 = R26 & R11;// Instr #298
SDNode *and34 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(addc0, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere21, 0)
                                    );

// R09 = R24 == R11;// Instr #299
SDNode *eq40 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and34, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(and34, 1)
                                    );

// R09 = R09 & R14;// Instr #300
SDNode *and35 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(eq40, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq40, 1)
                                    );

// R09 = R09 == R30;// Instr #301
SDNode *eq41 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and35, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and35, 1)
                                    );

SDValue ct98 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #302
SDNode *nop24 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct98,
                                    // glue (or chain) input edge
                                    SDValue(eq41, 1)
                                    );

// WHERE_EQ;// Instr #303
SDNode *whereeq22 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop24, 0)
                                    );

// R43 = R31 - R43;// Instr #304
SDNode *sub19 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(add0, 0),
                                    SDValue(add0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq22, 0)
                                    }
                                    );

// R26 = SUBC(R31, R26);// Instr #305
SDNode *subc2 = CrtDAG->getMachineNode(
                                    Connex::SUBCV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(addc0, 0),
                                    SDValue(sub19, 0),
                                    SDValue(addc0, 0)
                                    // no need for glue or chain input (since it normally consumes the output of the predecessor)
                                    }
                                    );

// END_WHERE;// Instr #306
SDNode *endwhere22 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(subc2, 1)
                                    );

// R09 = R26 == R31;// Instr #307
SDNode *eq42 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(subc2, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere22, 0)
                                    );

SDValue ct99 = CrtDAG->getSignedConstant(3, DL, MVT::i16, true, false);
// R09 = R09 << 3;// Instr #308
SDNode *ishl22 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq42, 0),
                                    ct99,
                                    // glue (or chain) input edge
                                    SDValue(eq42, 1)
                                    );

SDValue ct100 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R05 = R43 << 0;// Instr #309
SDNode *ishl23 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub19, 0),
                                    ct100,
                                    // glue (or chain) input edge
                                    SDValue(ishl22, 1)
                                    );

// R16 = R09 == R31;// Instr #310
SDNode *eq43 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl22, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl23, 1)
                                    );

SDValue ct101 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #311
SDNode *nop25 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct101,
                                    // glue (or chain) input edge
                                    SDValue(eq43, 1)
                                    );

// WHERE_EQ;// Instr #312
SDNode *whereeq23 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop25, 0)
                                    );

SDValue ct102 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R05 = R26 << 0;// Instr #313
SDNode *ishl24 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(subc2, 0),
                                    ct102,
                                    SDValue(ishl23, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq23, 0)
                                    }
                                    );

SDValue ct103 = CrtDAG->getSignedConstant(-8, DL, MVT::i16, true, false);
// R09 = -8;// Instr #314
SDNode *vload48 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct103,
                                    SDValue(ishl22, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl24, 1)
                                    );

// END_WHERE;// Instr #315
SDNode *endwhere23 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload48, 1)
                                    );

SDValue ct104 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R08 = R05 >> 1;// Instr #316
SDNode *ishr4 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishl24, 0),
                                    ct104,
                                    // glue (or chain) input edge
                                    SDValue(endwhere23, 0)
                                    );

// R05 = R05 | R08;// Instr #317
SDNode *or20 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr4, 0),
                                    SDValue(ishl24, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr4, 1)
                                    );

SDValue ct105 = CrtDAG->getSignedConstant(2, DL, MVT::i16, true, false);
// R08 = R05 >> 2;// Instr #318
SDNode *ishr5 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or20, 0),
                                    ct105,
                                    // glue (or chain) input edge
                                    SDValue(or20, 1)
                                    );

// R05 = R05 | R08;// Instr #319
SDNode *or21 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr5, 0),
                                    SDValue(or20, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr5, 1)
                                    );

SDValue ct106 = CrtDAG->getSignedConstant(4, DL, MVT::i16, true, false);
// R08 = R05 >> 4;// Instr #320
SDNode *ishr6 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or21, 0),
                                    ct106,
                                    // glue (or chain) input edge
                                    SDValue(or21, 1)
                                    );

// R05 = R05 | R08;// Instr #321
SDNode *or22 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr6, 0),
                                    SDValue(or21, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr6, 1)
                                    );

SDValue ct107 = CrtDAG->getSignedConstant(8, DL, MVT::i16, true, false);
// R08 = R05 >> 8;// Instr #322
SDNode *ishr7 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or22, 0),
                                    ct107,
                                    // glue (or chain) input edge
                                    SDValue(or22, 1)
                                    );

// R05 = R05 | R08;// Instr #323
SDNode *or23 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr7, 0),
                                    SDValue(or22, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr7, 1)
                                    );

// R05 = ~R05;// Instr #324
SDNode *not4 = CrtDAG->getMachineNode(
                                    Connex::NOT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or23, 0),
                                    // glue (or chain) input edge
                                    SDValue(or23, 1)
                                    );

// R06 = POPCNT(R05);// Instr #325
SDNode *popcnt0 = CrtDAG->getMachineNode(
                                    Connex::POPCNT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(not4, 0),
                                    // glue (or chain) input edge
                                    SDValue(not4, 1)
                                    );

// R06 = R09 + R06;// Instr #326
SDNode *add1 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(popcnt0, 0),
                                    SDValue(vload48, 0),
                                    // glue (or chain) input edge
                                    SDValue(popcnt0, 1)
                                    );

// R08 = R31 - R06;// Instr #327
SDNode *sub20 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload2, 0),
                                    SDValue(add1, 0),
                                    // glue (or chain) input edge
                                    SDValue(add1, 1)
                                    );

SDValue ct108 = CrtDAG->getSignedConstant(15, DL, MVT::i16, true, false);
// R09 = 15;// Instr #328
SDNode *vload49 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct108,
                                    // glue (or chain) input edge
                                    SDValue(sub20, 1)
                                    );

// R09 = R09 < R08;// Instr #329
SDNode *lt10 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload49, 0),
                                    SDValue(sub20, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload49, 1)
                                    );

// R09 = R09 & R14;// Instr #330
SDNode *and36 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt10, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt10, 1)
                                    );

// R09 = R09 == R30;// Instr #331
SDNode *eq44 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and36, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and36, 1)
                                    );

SDValue ct109 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #332
SDNode *nop26 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct109,
                                    // glue (or chain) input edge
                                    SDValue(eq44, 1)
                                    );

// WHERE_EQ;// Instr #333
SDNode *whereeq24 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop26, 0)
                                    );

// R07 = R62 == R31;// Instr #334
SDNode *eq45 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub15, 0),
                                    SDValue(vload2, 0),
                                    SDValue(and21, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq24, 0)
                                    }
                                    );

// R07 = R30 - R07;// Instr #335
SDNode *sub21 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq45, 0),
                                    SDValue(eq45, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq45, 1)
                                    }
                                    );

SDValue ct110 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R62 = R43 << 0;// Instr #336
SDNode *ishl25 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub19, 0),
                                    ct110,
                                    SDValue(sub15, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub21, 1)
                                    }
                                    );

// R62 = R62 | R07;// Instr #337
SDNode *or24 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub21, 0),
                                    SDValue(ishl25, 0),
                                    SDValue(ishl25, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl25, 1)
                                    }
                                    );

// R07 = R62 == R31;// Instr #338
SDNode *eq46 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or24, 0),
                                    SDValue(vload2, 0),
                                    SDValue(sub21, 0),
                                    // glue (or chain) input edge
                                    SDValue(or24, 1)
                                    }
                                    );

// R07 = R30 - R07;// Instr #339
SDNode *sub22 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq46, 0),
                                    SDValue(eq46, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq46, 1)
                                    }
                                    );

SDValue ct111 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R62 = R26 << 0;// Instr #340
SDNode *ishl26 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(subc2, 0),
                                    ct111,
                                    SDValue(or24, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub22, 1)
                                    }
                                    );

// R62 = R62 | R07;// Instr #341
SDNode *or25 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub22, 0),
                                    SDValue(ishl26, 0),
                                    SDValue(ishl26, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl26, 1)
                                    }
                                    );

// R16 = R29 - R08;// Instr #342
SDNode *sub23 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload3, 0),
                                    SDValue(sub20, 0),
                                    SDValue(eq43, 0),
                                    // glue (or chain) input edge
                                    SDValue(or25, 1)
                                    }
                                    );

// R62 = R62 << R16;// Instr #343
SDNode *shl8 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or25, 0),
                                    SDValue(sub23, 0),
                                    SDValue(or25, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub23, 1)
                                    }
                                    );

// R62 = R62 >> R16;// Instr #344
SDNode *shr10 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl8, 0),
                                    SDValue(sub23, 0),
                                    SDValue(shl8, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl8, 1)
                                    }
                                    );

SDValue ct112 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R61 = R08 << 0;// Instr #345
SDNode *ishl27 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub20, 0),
                                    ct112,
                                    SDValue(ishl17, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr10, 1)
                                    }
                                    );

// R26 = R26 >> R08;// Instr #346
SDNode *shr11 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(subc2, 0),
                                    SDValue(sub20, 0),
                                    SDValue(subc2, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl27, 1)
                                    }
                                    );

SDValue ct113 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R26 = 0;// Instr #347
SDNode *vload50 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct113,
                                    SDValue(shr11, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr11, 1)
                                    );

// R07 = R08 - R29;// Instr #348
SDNode *sub24 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub20, 0),
                                    SDValue(vload3, 0),
                                    SDValue(sub22, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload50, 1)
                                    }
                                    );

// R43 = R43 >> R08;// Instr #349
SDNode *shr12 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub19, 0),
                                    SDValue(sub20, 0),
                                    SDValue(sub19, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub24, 1)
                                    }
                                    );

// R25 = R08 + R25;// Instr #350
SDNode *add2 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl15, 0),
                                    SDValue(sub20, 0),
                                    SDValue(ishl15, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr12, 1)
                                    }
                                    );

// END_WHERE;// Instr #351
SDNode *endwhere24 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(add2, 1)
                                    );

// R09 = R30 - R09;// Instr #352
SDNode *sub25 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(eq44, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere24, 0)
                                    );

// R16 = R31 < R08;// Instr #353
SDNode *lt11 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload2, 0),
                                    SDValue(sub20, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub25, 1)
                                    );

// R09 = R09 & R16;// Instr #354
SDNode *and37 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt11, 0),
                                    SDValue(sub25, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt11, 1)
                                    );

// R09 = R09 & R14;// Instr #355
SDNode *and38 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(and37, 0),
                                    // glue (or chain) input edge
                                    SDValue(and37, 1)
                                    );

// R09 = R09 == R30;// Instr #356
SDNode *eq47 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and38, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and38, 1)
                                    );

SDValue ct114 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #357
SDNode *nop27 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct114,
                                    // glue (or chain) input edge
                                    SDValue(eq47, 1)
                                    );

// WHERE_EQ;// Instr #358
SDNode *whereeq25 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop27, 0)
                                    );

// R07 = R62 == R31;// Instr #359
SDNode *eq48 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr10, 0),
                                    SDValue(vload2, 0),
                                    SDValue(sub24, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq25, 0)
                                    }
                                    );

// R07 = R30 - R07;// Instr #360
SDNode *sub26 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq48, 0),
                                    SDValue(eq48, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq48, 1)
                                    }
                                    );

SDValue ct115 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R62 = R43 << 0;// Instr #361
SDNode *ishl28 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr12, 0),
                                    ct115,
                                    SDValue(shr10, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub26, 1)
                                    }
                                    );

// R62 = R62 | R07;// Instr #362
SDNode *or26 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub26, 0),
                                    SDValue(ishl28, 0),
                                    SDValue(ishl28, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl28, 1)
                                    }
                                    );

// R16 = R29 - R08;// Instr #363
SDNode *sub27 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload3, 0),
                                    SDValue(sub20, 0),
                                    SDValue(lt11, 0),
                                    // glue (or chain) input edge
                                    SDValue(or26, 1)
                                    }
                                    );

// R62 = R62 << R16;// Instr #364
SDNode *shl9 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or26, 0),
                                    SDValue(sub27, 0),
                                    SDValue(or26, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub27, 1)
                                    }
                                    );

// R62 = R62 >> R16;// Instr #365
SDNode *shr13 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl9, 0),
                                    SDValue(sub27, 0),
                                    SDValue(shl9, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl9, 1)
                                    }
                                    );

SDValue ct116 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R61 = R08 << 0;// Instr #366
SDNode *ishl29 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub20, 0),
                                    ct116,
                                    SDValue(ishl27, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr13, 1)
                                    }
                                    );

// R43 = R43 >> R08;// Instr #367
SDNode *shr14 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr12, 0),
                                    SDValue(sub20, 0),
                                    SDValue(shr12, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl29, 1)
                                    }
                                    );

// R07 = R29 - R08;// Instr #368
SDNode *sub28 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload3, 0),
                                    SDValue(sub20, 0),
                                    SDValue(sub26, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr14, 1)
                                    }
                                    );

// R07 = R26 << R07;// Instr #369
SDNode *shl10 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload50, 0),
                                    SDValue(sub28, 0),
                                    SDValue(sub28, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub28, 1)
                                    }
                                    );

// R43 = R43 | R07;// Instr #370
SDNode *or27 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl10, 0),
                                    SDValue(shr14, 0),
                                    SDValue(shr14, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl10, 1)
                                    }
                                    );

// R26 = R26 >> R08;// Instr #371
SDNode *shr15 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload50, 0),
                                    SDValue(sub20, 0),
                                    SDValue(vload50, 0),
                                    // glue (or chain) input edge
                                    SDValue(or27, 1)
                                    }
                                    );

// R25 = R08 + R25;// Instr #372
SDNode *add3 = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(add2, 0),
                                    SDValue(sub20, 0),
                                    SDValue(add2, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr15, 1)
                                    }
                                    );

// END_WHERE;// Instr #373
SDNode *endwhere25 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(add3, 1)
                                    );

SDValue ct117 = CrtDAG->getSignedConstant(-15, DL, MVT::i16, true, false);
// R07 = -15;// Instr #374
SDNode *vload51 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct117,
                                    // glue (or chain) input edge
                                    SDValue(endwhere25, 0)
                                    );

// R09 = R08 < R07;// Instr #375
SDNode *lt12 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub20, 0),
                                    SDValue(vload51, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload51, 1)
                                    );

// R09 = R09 & R14;// Instr #376
SDNode *and39 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt12, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt12, 1)
                                    );

// R09 = R09 == R30;// Instr #377
SDNode *eq49 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and39, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and39, 1)
                                    );

SDValue ct118 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #378
SDNode *nop28 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct118,
                                    // glue (or chain) input edge
                                    SDValue(eq49, 1)
                                    );

// WHERE_EQ;// Instr #379
SDNode *whereeq26 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop28, 0)
                                    );

// R08 = R31 - R08;// Instr #380
SDNode *sub29 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(sub20, 0),
                                    SDValue(sub20, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq26, 0)
                                    }
                                    );

// R07 = R08 - R29;// Instr #381
SDNode *sub30 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub29, 0),
                                    SDValue(vload3, 0),
                                    SDValue(vload51, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub29, 1)
                                    }
                                    );

SDValue ct119 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R26 = R43 << 0;// Instr #382
SDNode *ishl30 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or27, 0),
                                    ct119,
                                    SDValue(shr15, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub30, 1)
                                    }
                                    );

// R43 = R43 << R07;// Instr #383
SDNode *shl11 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or27, 0),
                                    SDValue(sub30, 0),
                                    SDValue(or27, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl30, 1)
                                    }
                                    );

// R25 = R25 - R08;// Instr #384
SDNode *sub31 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(add3, 0),
                                    SDValue(sub29, 0),
                                    SDValue(add3, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl11, 1)
                                    }
                                    );

// END_WHERE;// Instr #385
SDNode *endwhere26 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub31, 1)
                                    );

SDValue ct120 = CrtDAG->getSignedConstant(-16, DL, MVT::i16, true, false);
// R07 = -16;// Instr #386
SDNode *vload52 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct120,
                                    // glue (or chain) input edge
                                    SDValue(endwhere26, 0)
                                    );

// R07 = R07 < R08;// Instr #387
SDNode *lt13 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload52, 0),
                                    SDValue(sub29, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload52, 1)
                                    );

// R09 = R08 < R31;// Instr #388
SDNode *lt14 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub29, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt13, 1)
                                    );

// R09 = R09 & R14;// Instr #389
SDNode *and40 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt14, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt14, 1)
                                    );

// R09 = R09 == R30;// Instr #390
SDNode *eq50 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and40, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and40, 1)
                                    );

SDValue ct121 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #391
SDNode *nop29 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct121,
                                    // glue (or chain) input edge
                                    SDValue(eq50, 1)
                                    );

// WHERE_EQ;// Instr #392
SDNode *whereeq27 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop29, 0)
                                    );

// R08 = R31 - R08;// Instr #393
SDNode *sub32 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload2, 0),
                                    SDValue(sub29, 0),
                                    SDValue(sub29, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq27, 0)
                                    }
                                    );

// R07 = R29 - R08;// Instr #394
SDNode *sub33 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload3, 0),
                                    SDValue(sub32, 0),
                                    SDValue(lt13, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub32, 1)
                                    }
                                    );

// R07 = R43 >> R07;// Instr #395
SDNode *shr16 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl11, 0),
                                    SDValue(sub33, 0),
                                    SDValue(sub33, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub33, 1)
                                    }
                                    );

// R43 = R43 << R08;// Instr #396
SDNode *shl12 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl11, 0),
                                    SDValue(sub32, 0),
                                    SDValue(shl11, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr16, 1)
                                    }
                                    );

// R26 = R26 << R08;// Instr #397
SDNode *shl13 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl30, 0),
                                    SDValue(sub32, 0),
                                    SDValue(ishl30, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl12, 1)
                                    }
                                    );

// R26 = R26 | R07;// Instr #398
SDNode *or28 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shr16, 0),
                                    SDValue(shl13, 0),
                                    SDValue(shl13, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl13, 1)
                                    }
                                    );

// R25 = R25 - R08;// Instr #399
SDNode *sub34 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub31, 0),
                                    SDValue(sub32, 0),
                                    SDValue(sub31, 0),
                                    // glue (or chain) input edge
                                    SDValue(or28, 1)
                                    }
                                    );

// END_WHERE;// Instr #400
SDNode *endwhere27 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub34, 1)
                                    );

// R09 = R25 < R30;// Instr #401
SDNode *lt15 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub34, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere27, 0)
                                    );

// R09 = R09 & R14;// Instr #402
SDNode *and41 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(lt15, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt15, 1)
                                    );

// R09 = R09 == R30;// Instr #403
SDNode *eq51 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and41, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and41, 1)
                                    );

SDValue ct122 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #404
SDNode *nop30 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct122,
                                    // glue (or chain) input edge
                                    SDValue(eq51, 1)
                                    );

// WHERE_EQ;// Instr #405
SDNode *whereeq28 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop30, 0)
                                    );

// R61 = R30 - R25;// Instr #406
SDNode *sub35 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(sub34, 0),
                                    SDValue(ishl29, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq28, 0)
                                    }
                                    );

SDValue ct123 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R25 = 1;// Instr #407
SDNode *vload53 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct123,
                                    SDValue(sub34, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub35, 1)
                                    );

SDValue ct124 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R62 = R43 << 0;// Instr #408
SDNode *ishl31 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl12, 0),
                                    ct124,
                                    SDValue(shr13, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload53, 1)
                                    }
                                    );

// R09 = R29 - R61;// Instr #409
SDNode *sub36 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload3, 0),
                                    SDValue(sub35, 0),
                                    SDValue(eq51, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl31, 1)
                                    }
                                    );

// R62 = R62 << R09;// Instr #410
SDNode *shl14 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl31, 0),
                                    SDValue(sub36, 0),
                                    SDValue(ishl31, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub36, 1)
                                    }
                                    );

// R62 = R62 >> R09;// Instr #411
SDNode *shr17 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl14, 0),
                                    SDValue(sub36, 0),
                                    SDValue(shl14, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl14, 1)
                                    }
                                    );

// R43 = R43 >> R61;// Instr #412
SDNode *shr18 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl12, 0),
                                    SDValue(sub35, 0),
                                    SDValue(shl12, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr17, 1)
                                    }
                                    );

// R08 = R29 - R61;// Instr #413
SDNode *sub37 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload3, 0),
                                    SDValue(sub35, 0),
                                    SDValue(sub32, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr18, 1)
                                    }
                                    );

// R08 = R26 << R08;// Instr #414
SDNode *shl15 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or28, 0),
                                    SDValue(sub37, 0),
                                    SDValue(sub37, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub37, 1)
                                    }
                                    );

// R43 = R43 | R08;// Instr #415
SDNode *or29 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl15, 0),
                                    SDValue(shr18, 0),
                                    SDValue(shr18, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl15, 1)
                                    }
                                    );

// R26 = R26 >> R61;// Instr #416
SDNode *shr19 = CrtDAG->getMachineNode(
                                    Connex::SHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or28, 0),
                                    SDValue(sub35, 0),
                                    SDValue(or28, 0),
                                    // glue (or chain) input edge
                                    SDValue(or29, 1)
                                    }
                                    );

// R08 = R08 == R31;// Instr #417
SDNode *eq52 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl15, 0),
                                    SDValue(vload2, 0),
                                    SDValue(shl15, 0),
                                    // glue (or chain) input edge
                                    SDValue(shr19, 1)
                                    }
                                    );

// R08 = R30 - R08;// Instr #418
SDNode *sub38 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq52, 0),
                                    SDValue(eq52, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq52, 1)
                                    }
                                    );

// R62 = R62 | R08;// Instr #419
SDNode *or30 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub38, 0),
                                    SDValue(shr17, 0),
                                    SDValue(shr17, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub38, 1)
                                    }
                                    );

// END_WHERE;// Instr #420
SDNode *endwhere28 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or30, 1)
                                    );

SDValue ct125 = CrtDAG->getSignedConstant(128, DL, MVT::i16, true, false);
// R08 = 128;// Instr #421
SDNode *vload54 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct125,
                                    // glue (or chain) input edge
                                    SDValue(endwhere28, 0)
                                    );

// R08 = R26 < R08;// Instr #422
SDNode *lt16 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(shr19, 0),
                                    SDValue(vload54, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload54, 1)
                                    );

// R09 = R25 == R30;// Instr #423
SDNode *eq53 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload53, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt16, 1)
                                    );

// R09 = R09 & R14;// Instr #424
SDNode *and42 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(eq53, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq53, 1)
                                    );

// R09 = R09 & R08;// Instr #425
SDNode *and43 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt16, 0),
                                    SDValue(and42, 0),
                                    // glue (or chain) input edge
                                    SDValue(and42, 1)
                                    );

// R09 = R09 == R30;// Instr #426
SDNode *eq54 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and43, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and43, 1)
                                    );

SDValue ct126 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #427
SDNode *nop31 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct126,
                                    // glue (or chain) input edge
                                    SDValue(eq54, 1)
                                    );

// WHERE_EQ;// Instr #428
SDNode *whereeq29 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop31, 0)
                                    );

SDValue ct127 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R25 = 0;// Instr #429
SDNode *vload55 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct127,
                                    SDValue(vload53, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq29, 0)
                                    );

// END_WHERE;// Instr #430
SDNode *endwhere29 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload55, 1)
                                    );

// R26 = R26 & R13;// Instr #431
SDNode *and44 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload5, 0),
                                    SDValue(shr19, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere29, 0)
                                    );

// CELL_SHR(R24, R30);// Instr #432
SDNode *cellshr0 = CrtDAG->getMachineNode(
                                    Connex::CELLSHR_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(and34, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and44, 1)
                                    );

SDValue ct128 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #433
SDNode *nop32 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct128,
                                    // glue (or chain) input edge
                                    SDValue(cellshr0, 0)
                                    );

// R24 = SHIFT_REG;// Instr #434
SDNode *ldsh2 = CrtDAG->getMachineNode(
                                    Connex::LDSH_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop32, 0)
                                    );

// CELL_SHR(R25, R30);// Instr #435
SDNode *cellshr1 = CrtDAG->getMachineNode(
                                    Connex::CELLSHR_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(vload55, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldsh2, 1)
                                    );

SDValue ct129 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #436
SDNode *nop33 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct129,
                                    // glue (or chain) input edge
                                    SDValue(cellshr1, 0)
                                    );

// R25 = SHIFT_REG;// Instr #437
SDNode *ldsh3 = CrtDAG->getMachineNode(
                                    Connex::LDSH_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop33, 0)
                                    );

// CELL_SHR(R26, R30);// Instr #438
SDNode *cellshr2 = CrtDAG->getMachineNode(
                                    Connex::CELLSHR_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(and44, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldsh3, 1)
                                    );

SDValue ct130 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #439
SDNode *nop34 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct130,
                                    // glue (or chain) input edge
                                    SDValue(cellshr2, 0)
                                    );

// R26 = SHIFT_REG;// Instr #440
SDNode *ldsh4 = CrtDAG->getMachineNode(
                                    Connex::LDSH_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop34, 0)
                                    );

// CELL_SHR(R14, R30);// Instr #441
SDNode *cellshr3 = CrtDAG->getMachineNode(
                                    Connex::CELLSHR_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(vload23, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldsh4, 1)
                                    );

SDValue ct131 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #442
SDNode *nop35 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct131,
                                    // glue (or chain) input edge
                                    SDValue(cellshr3, 0)
                                    );

// R16 = SHIFT_REG;// Instr #443
SDNode *ldsh5 = CrtDAG->getMachineNode(
                                    Connex::LDSH_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop35, 0)
                                    );

// R17 = INDEX;// Instr #444
SDNode *ldix2 = CrtDAG->getMachineNode(
                                    Connex::LDIX_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(ldsh5, 1)
                                    );

// R17 = R17 & R30;// Instr #445
SDNode *and45 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(ldix2, 0),
                                    // glue (or chain) input edge
                                    SDValue(ldix2, 1)
                                    );

// R17 = R17 == R30;// Instr #446
SDNode *eq55 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and45, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and45, 1)
                                    );

SDValue ct132 = CrtDAG->getSignedConstant(255, DL, MVT::i16, true, false);
// R09 = 255;// Instr #447
SDNode *vload56 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct132,
                                    // glue (or chain) input edge
                                    SDValue(eq55, 1)
                                    );

// R09 = R09 < R25;// Instr #448
SDNode *lt17 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload56, 0),
                                    SDValue(ldsh3, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload56, 1)
                                    );

// R09 = R09 & R16;// Instr #449
SDNode *and46 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ldsh5, 0),
                                    SDValue(lt17, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt17, 1)
                                    );

// R09 = R09 & R17;// Instr #450
SDNode *and47 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq55, 0),
                                    SDValue(and46, 0),
                                    // glue (or chain) input edge
                                    SDValue(and46, 1)
                                    );

// R09 = R09 == R30;// Instr #451
SDNode *eq56 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and47, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and47, 1)
                                    );

SDValue ct133 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #452
SDNode *nop36 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct133,
                                    // glue (or chain) input edge
                                    SDValue(eq56, 1)
                                    );

// WHERE_EQ;// Instr #453
SDNode *whereeq30 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop36, 0)
                                    );

SDValue ct134 = CrtDAG->getSignedConstant(-1, DL, MVT::i16, true, false);
// R16 = -1;// Instr #454
SDNode *vload57 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct134,
                                    SDValue(ldsh5, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq30, 0)
                                    );

SDValue ct135 = CrtDAG->getSignedConstant(32640, DL, MVT::i16, true, false);
// R19 = 32640;// Instr #455
SDNode *vload58 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct135,
                                    SDValue(xor0, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload57, 1)
                                    );

// R19 = R19 | R24;// Instr #456
SDNode *or31 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ldsh2, 0),
                                    SDValue(vload58, 0),
                                    SDValue(vload58, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload58, 1)
                                    }
                                    );

// END_WHERE;// Instr #457
SDNode *endwhere30 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or31, 1)
                                    );

// CELL_SHL(R16, R30);// Instr #458
SDNode *cellshl2 = CrtDAG->getMachineNode(
                                    Connex::CELLSHL_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(vload57, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere30, 0)
                                    );

SDValue ct136 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #459
SDNode *nop37 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct136,
                                    // glue (or chain) input edge
                                    SDValue(cellshl2, 0)
                                    );

// R15 = SHIFT_REG;// Instr #460
SDNode *ldsh6 = CrtDAG->getMachineNode(
                                    Connex::LDSH_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop37, 0)
                                    );

SDValue ct137 = CrtDAG->getSignedConstant(-1, DL, MVT::i16, true, false);
// R08 = -1;// Instr #461
SDNode *vload59 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct137,
                                    // glue (or chain) input edge
                                    SDValue(ldsh6, 1)
                                    );

// R08 = R15 == R08;// Instr #462
SDNode *eq57 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ldsh6, 0),
                                    SDValue(vload59, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload59, 1)
                                    );

SDValue ct138 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #463
SDNode *nop38 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct138,
                                    // glue (or chain) input edge
                                    SDValue(eq57, 1)
                                    );

// WHERE_EQ;// Instr #464
SDNode *whereeq31 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop38, 0)
                                    );

SDValue ct139 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R19 = 0;// Instr #465
SDNode *vload60 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct139,
                                    SDValue(or31, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq31, 0)
                                    );

SDValue ct140 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R14 = 0;// Instr #466
SDNode *vload61 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct140,
                                    SDValue(vload23, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload60, 1)
                                    );

// END_WHERE;// Instr #467
SDNode *endwhere31 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload61, 1)
                                    );

// R08 = R16 == R30;// Instr #468
SDNode *eq58 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload57, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere31, 0)
                                    );

// R08 = R08 & R17;// Instr #469
SDNode *and48 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq55, 0),
                                    SDValue(eq58, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq58, 1)
                                    );

// R08 = R08 == R30;// Instr #470
SDNode *eq59 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and48, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and48, 1)
                                    );

SDValue ct141 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #471
SDNode *nop39 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct141,
                                    // glue (or chain) input edge
                                    SDValue(eq59, 1)
                                    );

// WHERE_EQ;// Instr #472
SDNode *whereeq32 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop39, 0)
                                    );

SDValue ct142 = CrtDAG->getSignedConstant(7, DL, MVT::i16, true, false);
// R19 = R25 << 7;// Instr #473
SDNode *ishl32 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ldsh3, 0),
                                    ct142,
                                    SDValue(vload60, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq32, 0)
                                    }
                                    );

// R19 = R19 | R26;// Instr #474
SDNode *or32 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ldsh4, 0),
                                    SDValue(ishl32, 0),
                                    SDValue(ishl32, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl32, 1)
                                    }
                                    );

SDValue ct143 = CrtDAG->getSignedConstant(-2, DL, MVT::i16, true, false);
// R16 = -2;// Instr #475
SDNode *vload62 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct143,
                                    SDValue(vload57, 0),
                                    // glue (or chain) input edge
                                    SDValue(or32, 1)
                                    );

// R19 = R19 | R24;// Instr #476
SDNode *or33 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ldsh2, 0),
                                    SDValue(or32, 0),
                                    SDValue(or32, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload62, 1)
                                    }
                                    );

// END_WHERE;// Instr #477
SDNode *endwhere32 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or33, 1)
                                    );

// CELL_SHL(R16, R30);// Instr #478
SDNode *cellshl3 = CrtDAG->getMachineNode(
                                    Connex::CELLSHL_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(vload62, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere32, 0)
                                    );

SDValue ct144 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #479
SDNode *nop40 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct144,
                                    // glue (or chain) input edge
                                    SDValue(cellshl3, 0)
                                    );

// R15 = SHIFT_REG;// Instr #480
SDNode *ldsh7 = CrtDAG->getMachineNode(
                                    Connex::LDSH_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop40, 0)
                                    );

SDValue ct145 = CrtDAG->getSignedConstant(-2, DL, MVT::i16, true, false);
// R08 = -2;// Instr #481
SDNode *vload63 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct145,
                                    // glue (or chain) input edge
                                    SDValue(ldsh7, 1)
                                    );

// R08 = R15 == R08;// Instr #482
SDNode *eq60 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ldsh7, 0),
                                    SDValue(vload63, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload63, 1)
                                    );

SDValue ct146 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #483
SDNode *nop41 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct146,
                                    // glue (or chain) input edge
                                    SDValue(eq60, 1)
                                    );

// WHERE_EQ;// Instr #484
SDNode *whereeq33 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop41, 0)
                                    );

SDValue ct147 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R19 = R43 << 0;// Instr #485
SDNode *ishl33 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(or29, 0),
                                    ct147,
                                    SDValue(or33, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq33, 0)
                                    }
                                    );

// R04 = R43 & R30;// Instr #486
SDNode *and49 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(or29, 0),
                                    SDValue(vload13, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishl33, 1)
                                    }
                                    );

// R07 = R61 - R30;// Instr #487
SDNode *sub39 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub35, 0),
                                    SDValue(vload1, 0),
                                    SDValue(shr16, 0),
                                    // glue (or chain) input edge
                                    SDValue(and49, 1)
                                    }
                                    );

// R08 = R30 << R07;// Instr #488
SDNode *shl16 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(sub39, 0),
                                    SDValue(eq60, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub39, 1)
                                    }
                                    );

// R03 = R62 & R08;// Instr #489
SDNode *and50 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl16, 0),
                                    SDValue(or30, 0),
                                    SDValue(vload14, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl16, 1)
                                    }
                                    );

// R62 = R62 ^ R03;// Instr #490
SDNode *xor1 = CrtDAG->getMachineNode(
                                    Connex::XORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and50, 0),
                                    SDValue(or30, 0),
                                    SDValue(or30, 0),
                                    // glue (or chain) input edge
                                    SDValue(and50, 1)
                                    }
                                    );

// R03 = R03 == R31;// Instr #491
SDNode *eq61 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and50, 0),
                                    SDValue(vload2, 0),
                                    SDValue(and50, 0),
                                    // glue (or chain) input edge
                                    SDValue(xor1, 1)
                                    }
                                    );

// R03 = R30 - R03;// Instr #492
SDNode *sub40 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq61, 0),
                                    SDValue(eq61, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq61, 1)
                                    }
                                    );

SDValue ct148 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R08 = R08 >> 1;// Instr #493
SDNode *ishr8 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl16, 0),
                                    ct148,
                                    SDValue(shl16, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub40, 1)
                                    }
                                    );

// R02 = R62 & R08;// Instr #494
SDNode *and51 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishr8, 0),
                                    SDValue(xor1, 0),
                                    SDValue(vload15, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr8, 1)
                                    }
                                    );

// R62 = R62 ^ R02;// Instr #495
SDNode *xor2 = CrtDAG->getMachineNode(
                                    Connex::XORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and51, 0),
                                    SDValue(xor1, 0),
                                    SDValue(xor1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and51, 1)
                                    }
                                    );

// R02 = R02 == R31;// Instr #496
SDNode *eq62 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(and51, 0),
                                    SDValue(vload2, 0),
                                    SDValue(and51, 0),
                                    // glue (or chain) input edge
                                    SDValue(xor2, 1)
                                    }
                                    );

// R02 = R30 - R02;// Instr #497
SDNode *sub41 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq62, 0),
                                    SDValue(eq62, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq62, 1)
                                    }
                                    );

// R01 = R62 == R31;// Instr #498
SDNode *eq63 = CrtDAG->getMachineNode(
                                    Connex::EQ_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(xor2, 0),
                                    SDValue(vload2, 0),
                                    SDValue(vload16, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub41, 1)
                                    }
                                    );

// R01 = R30 - R01;// Instr #499
SDNode *sub42 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(eq63, 0),
                                    SDValue(eq63, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq63, 1)
                                    }
                                    );

// R00 = R04 | R02;// Instr #500
SDNode *or34 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub41, 0),
                                    SDValue(and49, 0),
                                    SDValue(vload17, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub42, 1)
                                    }
                                    );

// R00 = R00 | R01;// Instr #501
SDNode *or35 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub42, 0),
                                    SDValue(or34, 0),
                                    SDValue(or34, 0),
                                    // glue (or chain) input edge
                                    SDValue(or34, 1)
                                    }
                                    );

// R00 = R00 & R03;// Instr #502
SDNode *and52 = CrtDAG->getMachineNode(
                                    Connex::ANDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub40, 0),
                                    SDValue(or35, 0),
                                    SDValue(or35, 0),
                                    // glue (or chain) input edge
                                    SDValue(or35, 1)
                                    }
                                    );

// R19 = R00 + R19;// Instr #503
SDNode *resF32 /*add4*/ = CrtDAG->getMachineNode(
                                    Connex::ADDV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(ishl33, 0),
                                    SDValue(and52, 0),
                                    SDValue(ishl33, 0),
                                    // glue (or chain) input edge
                                    SDValue(and52, 1)
                                    }
                                    );

// END_WHERE;// Instr #504
SDNode *lastNode /*endwhere33*/ = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(add4, 1)
                                    );

