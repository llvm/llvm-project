//===-- Select_REDf16_OpincaaCodeGen.h --------------------------*- C++ -*-===//
//
// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
// See https://llvm.org/LICENSE.txt for license information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
//===----------------------------------------------------------------------===//
/// \file
// Code auto-generated by method Kernel::genLLVMISelManualCode()
//   from the OPINCAA lib, from kernel red.f16.
// You should include this code in the Select() method of the [Target]SelectionDAGISel
//   class of your back end (or MAYBE in the ISelLowering pass).
// Number of instructions generated: 122.
//
//===----------------------------------------------------------------------===//


// From /home/asusu/LLVM/Tests/opincaa_standalone_apps/Emulate_f16/REDf16_manual/DumpISel_OpincaaCodeGen_old18_STD_853.cpp



SDValue ct0 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R31 = 1;// Instr #0
SDNode *vload0 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct0,
                                    // glue (or chain) input edge
                                    SDValue(nodeOpSrcCast, 1)
                                    );

SDValue ct1 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R30 = 0;// Instr #1
SDNode *vload1 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct1,
                                    // glue (or chain) input edge
                                    SDValue(vload0, 1)
                                    );

SDValue ct2 = CrtDAG->getSignedConstant(31, DL, MVT::i16, true, false);
// R29 = 31;// Instr #2
SDNode *vload2 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct2,
                                    // glue (or chain) input edge
                                    SDValue(vload1, 1)
                                    );

SDValue ct3 = CrtDAG->getSignedConstant(1023, DL, MVT::i16, true, false);
// R13 = 1023;// Instr #3
SDNode *vload3 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct3,
                                    // glue (or chain) input edge
                                    SDValue(vload2, 1)
                                    );

SDValue ct4 = CrtDAG->getSignedConstant(31744, DL, MVT::i16, true, false);
// R12 = 31744;// Instr #4
SDNode *vload4 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct4,
                                    // glue (or chain) input edge
                                    SDValue(vload3, 1)
                                    );

SDValue ct5 = CrtDAG->getSignedConstant(-32768, DL, MVT::i16, true, false);
// R11 = -32768;// Instr #5
SDNode *vload5 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct5,
                                    // glue (or chain) input edge
                                    SDValue(vload4, 1)
                                    );

SDValue ct6 = CrtDAG->getSignedConstant(1024, DL, MVT::i16, true, false);
// R10 = 1024;// Instr #6
SDNode *vload6 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct6,
                                    // glue (or chain) input edge
                                    SDValue(vload5, 1)
                                    );

// R25 = R28 & R11;// Instr #7
SDNode *and0 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload5, 0),
                                    SDValue(nodeOpSrcCast, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload6, 1)
                                    );

// R26 = R28 & R12;// Instr #8
SDNode *and1 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload4, 0),
                                    SDValue(nodeOpSrcCast, 0),
                                    // glue (or chain) input edge
                                    SDValue(and0, 1)
                                    );

SDValue ct7 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R26 = R26 >> 10;// Instr #9
SDNode *ishr0 = CrtDAG->getMachineNode(
                                    Connex::ISHRV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and1, 0),
                                    ct7,
                                    // glue (or chain) input edge
                                    SDValue(and1, 1)
                                    );

// R27 = R28 & R13;// Instr #10
SDNode *and2 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload3, 0),
                                    SDValue(nodeOpSrcCast, 0),
                                    // glue (or chain) input edge
                                    SDValue(ishr0, 1)
                                    );

// R17 = R30 < R27;// Instr #11
SDNode *lt0 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload1, 0),
                                    SDValue(and2, 0),
                                    // glue (or chain) input edge
                                    SDValue(and2, 1)
                                    );

// R16 = R26 == R30;// Instr #12
SDNode *eq0 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(ishr0, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt0, 1)
                                    );

// R09 = R16 & R17;// Instr #13
SDNode *and3 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt0, 0),
                                    SDValue(eq0, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq0, 1)
                                    );

// R09 = R09 == R31;// Instr #14
SDNode *eq1 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and3, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(and3, 1)
                                    );

SDValue ct8 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #15
SDNode *nop0 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct8,
                                    // glue (or chain) input edge
                                    SDValue(eq1, 1)
                                    );

// WHERE_EQ;// Instr #16
SDNode *whereeq0 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop0, 0)
                                    );

SDValue ct9 = CrtDAG->getSignedConstant(1, DL, MVT::i16, true, false);
// R26 = 1;// Instr #17
SDNode *vload7 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_SPECIAL_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct9,
                                    SDValue(ishr0, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq0, 0)
                                    );

// END_WHERE;// Instr #18
SDNode *endwhere0 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(vload7, 1)
                                    );

// R17 = R26 == R29;// Instr #19
SDNode *eq2 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere0, 0)
                                    );

// R17 = R17 | R16;// Instr #20
SDNode *or0 = CrtDAG->getMachineNode(
                                    Connex::ORV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq0, 0),
                                    SDValue(eq2, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq2, 1)
                                    );

// R17 = R17 == R30;// Instr #21
SDNode *eq3 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or0, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(or0, 1)
                                    );

SDValue ct10 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #22
SDNode *nop1 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct10,
                                    // glue (or chain) input edge
                                    SDValue(eq3, 1)
                                    );

// WHERE_EQ;// Instr #23
SDNode *whereeq1 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop1, 0)
                                    );

// R27 = R27 | R10;// Instr #24
SDNode *or1 = CrtDAG->getMachineNode(
                                    Connex::ORV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload6, 0),
                                    SDValue(and2, 0),
                                    SDValue(and2, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq1, 0)
                                    }
                                    );

// END_WHERE;// Instr #25
SDNode *endwhere1 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(or1, 1)
                                    );

// R18 = R26 == R29;// Instr #26
SDNode *eq4 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(vload2, 0),
                                    // glue (or chain) input edge
                                    SDValue(endwhere1, 0)
                                    );

// R17 = R27 == R30;// Instr #27
SDNode *eq5 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(or1, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq4, 1)
                                    );

// R09 = R31 - R17;// Instr #28
SDNode *sub0 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload0, 0),
                                    SDValue(eq5, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq5, 1)
                                    );

// R09 = R09 & R18;// Instr #29
SDNode *and4 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq4, 0),
                                    SDValue(sub0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub0, 1)
                                    );

// RED(R09);// Instr #30
SDNode *sumRed0 = CrtDAG->getMachineNode(
                                    Connex::RED_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(and4, 0),
                                    // glue (or chain) input edge
                                    SDValue(and4, 1)
                                    );

// R24 = R18 & R17;// Instr #31
SDNode *and5 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq5, 0),
                                    SDValue(eq4, 0),
                                    // glue (or chain) input edge
                                    SDValue(sumRed0, 0)
                                    );

// R09 = R25 == R30;// Instr #32
SDNode *eq6 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and0, 0),
                                    SDValue(vload1, 0),
                                    // glue (or chain) input edge
                                    SDValue(and5, 1)
                                    );

// R16 = R24 & R09;// Instr #33
SDNode *and6 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(eq6, 0),
                                    SDValue(and5, 0),
                                    // glue (or chain) input edge
                                    SDValue(eq6, 1)
                                    );

// RED(R16);// Instr #34
SDNode *sumRed1 = CrtDAG->getMachineNode(
                                    Connex::RED_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(and6, 0),
                                    // glue (or chain) input edge
                                    SDValue(and6, 1)
                                    );

// R09 = R31 - R09;// Instr #35
SDNode *sub1 = CrtDAG->getMachineNode(
                                    Connex::SUBV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload0, 0),
                                    SDValue(eq6, 0),
                                    // glue (or chain) input edge
                                    SDValue(sumRed1, 0)
                                    );

// R16 = R24 & R09;// Instr #36
SDNode *and7 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(sub1, 0),
                                    SDValue(and5, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub1, 1)
                                    );

// RED(R16);// Instr #37
SDNode *sumRed2 = CrtDAG->getMachineNode(
                                    Connex::RED_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(and7, 0),
                                    // glue (or chain) input edge
                                    SDValue(and7, 1)
                                    );

// R09 = R25 == R11;// Instr #38
SDNode *eq7 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and0, 0),
                                    SDValue(vload5, 0),
                                    // glue (or chain) input edge
                                    SDValue(sumRed2, 0)
                                    );

SDValue ct11 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #39
SDNode *nop2 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct11,
                                    // glue (or chain) input edge
                                    SDValue(eq7, 1)
                                    );

// WHERE_EQ;// Instr #40
SDNode *whereeq2 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop2, 0)
                                    );

// R27 = R30 - R27;// Instr #41
SDNode *sub2 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload1, 0),
                                    SDValue(or1, 0),
                                    SDValue(or1, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq2, 0)
                                    }
                                    );

// END_WHERE;// Instr #42
SDNode *endwhere2 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sub2, 1)
                                    );

SDValue ct12 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R02 = R26 << 0;// Instr #43
SDNode *ishl0 = CrtDAG->getMachineNode(
                                    Connex::ISHLV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    ct12,
                                    // glue (or chain) input edge
                                    SDValue(endwhere2, 0)
                                    );

SDValue ct13 = CrtDAG->getSignedConstant(0, DL, MVT::i16, true, false);
// R02 = 0;// Instr #44
SDNode *vload8 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct13,
                                    // glue (or chain) input edge
                                    SDValue(ishl0, 1)
                                    );

SDValue ct14 = CrtDAG->getSignedConstant(6, DL, MVT::i16, true, false);
// R24 = 6;// Instr #45
SDNode *vload9 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct14,
                                    // glue (or chain) input edge
                                    SDValue(vload8, 1)
                                    );

// R19 = R26 < R24;// Instr #46
SDNode *lt1 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(vload9, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload9, 1)
                                    );

// R17 = R02 < R26;// Instr #47
SDNode *lt2 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload8, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt1, 1)
                                    );

// R02 = R31 + R02;// Instr #48
SDNode *add0 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload8, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt2, 1)
                                    );

// R09 = R19 & R17;// Instr #49
SDNode *and8 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt2, 0),
                                    SDValue(lt1, 0),
                                    // glue (or chain) input edge
                                    SDValue(add0, 1)
                                    );

// R09 = R09 == R31;// Instr #50
SDNode *eq8 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and8, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(and8, 1)
                                    );

SDValue ct15 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #51
SDNode *nop3 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct15,
                                    // glue (or chain) input edge
                                    SDValue(eq8, 1)
                                    );

// WHERE_EQ;// Instr #52
SDNode *whereeq3 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop3, 0)
                                    );

// R19 = R26 - R02;// Instr #53
SDNode *sub3 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload7, 0),
                                    SDValue(add0, 0),
                                    SDValue(lt1, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq3, 0)
                                    }
                                    );

// R27 = R27 << R19;// Instr #54
SDNode *shl0 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(sub2, 0),
                                    SDValue(sub3, 0),
                                    SDValue(sub2, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub3, 1)
                                    }
                                    );

// RED(R27);// Instr #55
SDNode *sumRed3 = CrtDAG->getMachineNode(
                                    Connex::RED_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(shl0, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl0, 1)
                                    );

// END_WHERE;// Instr #56
SDNode *endwhere3 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sumRed3, 0)
                                    );

SDValue ct16 = CrtDAG->getSignedConstant(5, DL, MVT::i16, true, false);
// R02 = 5;// Instr #57
SDNode *vload10 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct16,
                                    // glue (or chain) input edge
                                    SDValue(endwhere3, 0)
                                    );

SDValue ct17 = CrtDAG->getSignedConstant(11, DL, MVT::i16, true, false);
// R24 = 11;// Instr #58
SDNode *vload11 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct17,
                                    // glue (or chain) input edge
                                    SDValue(vload10, 1)
                                    );

// R19 = R26 < R24;// Instr #59
SDNode *lt3 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(vload11, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload11, 1)
                                    );

// R17 = R02 < R26;// Instr #60
SDNode *lt4 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload10, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt3, 1)
                                    );

// R02 = R31 + R02;// Instr #61
SDNode *add1 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload10, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt4, 1)
                                    );

// R09 = R19 & R17;// Instr #62
SDNode *and9 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt4, 0),
                                    SDValue(lt3, 0),
                                    // glue (or chain) input edge
                                    SDValue(add1, 1)
                                    );

// R09 = R09 == R31;// Instr #63
SDNode *eq9 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and9, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(and9, 1)
                                    );

SDValue ct18 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #64
SDNode *nop4 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct18,
                                    // glue (or chain) input edge
                                    SDValue(eq9, 1)
                                    );

// WHERE_EQ;// Instr #65
SDNode *whereeq4 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop4, 0)
                                    );

// R19 = R26 - R02;// Instr #66
SDNode *sub4 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload7, 0),
                                    SDValue(add1, 0),
                                    SDValue(lt3, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq4, 0)
                                    }
                                    );

// R27 = R27 << R19;// Instr #67
SDNode *shl1 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl0, 0),
                                    SDValue(sub4, 0),
                                    SDValue(shl0, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub4, 1)
                                    }
                                    );

// RED(R27);// Instr #68
SDNode *sumRed4 = CrtDAG->getMachineNode(
                                    Connex::RED_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(shl1, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl1, 1)
                                    );

// END_WHERE;// Instr #69
SDNode *endwhere4 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sumRed4, 0)
                                    );

SDValue ct19 = CrtDAG->getSignedConstant(10, DL, MVT::i16, true, false);
// R02 = 10;// Instr #70
SDNode *vload12 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct19,
                                    // glue (or chain) input edge
                                    SDValue(endwhere4, 0)
                                    );

SDValue ct20 = CrtDAG->getSignedConstant(16, DL, MVT::i16, true, false);
// R24 = 16;// Instr #71
SDNode *vload13 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct20,
                                    // glue (or chain) input edge
                                    SDValue(vload12, 1)
                                    );

// R19 = R26 < R24;// Instr #72
SDNode *lt5 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(vload13, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload13, 1)
                                    );

// R17 = R02 < R26;// Instr #73
SDNode *lt6 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload12, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt5, 1)
                                    );

// R02 = R31 + R02;// Instr #74
SDNode *add2 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload12, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt6, 1)
                                    );

// R09 = R19 & R17;// Instr #75
SDNode *and10 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt6, 0),
                                    SDValue(lt5, 0),
                                    // glue (or chain) input edge
                                    SDValue(add2, 1)
                                    );

// R09 = R09 == R31;// Instr #76
SDNode *eq10 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and10, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(and10, 1)
                                    );

SDValue ct21 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #77
SDNode *nop5 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct21,
                                    // glue (or chain) input edge
                                    SDValue(eq10, 1)
                                    );

// WHERE_EQ;// Instr #78
SDNode *whereeq5 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop5, 0)
                                    );

// R19 = R26 - R02;// Instr #79
SDNode *sub5 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload7, 0),
                                    SDValue(add2, 0),
                                    SDValue(lt5, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq5, 0)
                                    }
                                    );

// R27 = R27 << R19;// Instr #80
SDNode *shl2 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl1, 0),
                                    SDValue(sub5, 0),
                                    SDValue(shl1, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub5, 1)
                                    }
                                    );

// RED(R27);// Instr #81
SDNode *sumRed5 = CrtDAG->getMachineNode(
                                    Connex::RED_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(shl2, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl2, 1)
                                    );

// END_WHERE;// Instr #82
SDNode *endwhere5 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sumRed5, 0)
                                    );

SDValue ct22 = CrtDAG->getSignedConstant(15, DL, MVT::i16, true, false);
// R02 = 15;// Instr #83
SDNode *vload14 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct22,
                                    // glue (or chain) input edge
                                    SDValue(endwhere5, 0)
                                    );

SDValue ct23 = CrtDAG->getSignedConstant(21, DL, MVT::i16, true, false);
// R24 = 21;// Instr #84
SDNode *vload15 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct23,
                                    // glue (or chain) input edge
                                    SDValue(vload14, 1)
                                    );

// R19 = R26 < R24;// Instr #85
SDNode *lt7 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(vload15, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload15, 1)
                                    );

// R17 = R02 < R26;// Instr #86
SDNode *lt8 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload14, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt7, 1)
                                    );

// R02 = R31 + R02;// Instr #87
SDNode *add3 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload14, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt8, 1)
                                    );

// R09 = R19 & R17;// Instr #88
SDNode *and11 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt8, 0),
                                    SDValue(lt7, 0),
                                    // glue (or chain) input edge
                                    SDValue(add3, 1)
                                    );

// R09 = R09 == R31;// Instr #89
SDNode *eq11 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and11, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(and11, 1)
                                    );

SDValue ct24 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #90
SDNode *nop6 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct24,
                                    // glue (or chain) input edge
                                    SDValue(eq11, 1)
                                    );

// WHERE_EQ;// Instr #91
SDNode *whereeq6 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop6, 0)
                                    );

// R19 = R26 - R02;// Instr #92
SDNode *sub6 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload7, 0),
                                    SDValue(add3, 0),
                                    SDValue(lt7, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq6, 0)
                                    }
                                    );

// R27 = R27 << R19;// Instr #93
SDNode *shl3 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl2, 0),
                                    SDValue(sub6, 0),
                                    SDValue(shl2, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub6, 1)
                                    }
                                    );

// RED(R27);// Instr #94
SDNode *sumRed6 = CrtDAG->getMachineNode(
                                    Connex::RED_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(shl3, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl3, 1)
                                    );

// END_WHERE;// Instr #95
SDNode *endwhere6 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sumRed6, 0)
                                    );

SDValue ct25 = CrtDAG->getSignedConstant(20, DL, MVT::i16, true, false);
// R02 = 20;// Instr #96
SDNode *vload16 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct25,
                                    // glue (or chain) input edge
                                    SDValue(endwhere6, 0)
                                    );

SDValue ct26 = CrtDAG->getSignedConstant(26, DL, MVT::i16, true, false);
// R24 = 26;// Instr #97
SDNode *vload17 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct26,
                                    // glue (or chain) input edge
                                    SDValue(vload16, 1)
                                    );

// R19 = R26 < R24;// Instr #98
SDNode *lt9 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(vload17, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload17, 1)
                                    );

// R17 = R02 < R26;// Instr #99
SDNode *lt10 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload16, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt9, 1)
                                    );

// R02 = R31 + R02;// Instr #100
SDNode *add4 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload16, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt10, 1)
                                    );

// R09 = R19 & R17;// Instr #101
SDNode *and12 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt10, 0),
                                    SDValue(lt9, 0),
                                    // glue (or chain) input edge
                                    SDValue(add4, 1)
                                    );

// R09 = R09 == R31;// Instr #102
SDNode *eq12 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and12, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(and12, 1)
                                    );

SDValue ct27 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #103
SDNode *nop7 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct27,
                                    // glue (or chain) input edge
                                    SDValue(eq12, 1)
                                    );

// WHERE_EQ;// Instr #104
SDNode *whereeq7 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop7, 0)
                                    );

// R19 = R26 - R02;// Instr #105
SDNode *sub7 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload7, 0),
                                    SDValue(add4, 0),
                                    SDValue(lt9, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq7, 0)
                                    }
                                    );

// R27 = R27 << R19;// Instr #106
SDNode *shl4 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl3, 0),
                                    SDValue(sub7, 0),
                                    SDValue(shl3, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub7, 1)
                                    }
                                    );

// RED(R27);// Instr #107
SDNode *sumRed7 = CrtDAG->getMachineNode(
                                    Connex::RED_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(shl4, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl4, 1)
                                    );

// END_WHERE;// Instr #108
SDNode *endwhere7 = CrtDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(sumRed7, 0)
                                    );

SDValue ct28 = CrtDAG->getSignedConstant(25, DL, MVT::i16, true, false);
// R02 = 25;// Instr #109
SDNode *vload18 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct28,
                                    // glue (or chain) input edge
                                    SDValue(endwhere7, 0)
                                    );

SDValue ct29 = CrtDAG->getSignedConstant(31, DL, MVT::i16, true, false);
// R24 = 31;// Instr #110
SDNode *vload19 = CrtDAG->getMachineNode(
                                    Connex::VLOAD_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    ct29,
                                    // glue (or chain) input edge
                                    SDValue(vload18, 1)
                                    );

// R19 = R26 < R24;// Instr #111
SDNode *lt11 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload7, 0),
                                    SDValue(vload19, 0),
                                    // glue (or chain) input edge
                                    SDValue(vload19, 1)
                                    );

// R17 = R02 < R26;// Instr #112
SDNode *lt12 = CrtDAG->getMachineNode(
                                    Connex::LT_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload18, 0),
                                    SDValue(vload7, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt11, 1)
                                    );

// R02 = R31 + R02;// Instr #113
SDNode *add5 = CrtDAG->getMachineNode(
                                    Connex::ADDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(vload18, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(lt12, 1)
                                    );

// R09 = R19 & R17;// Instr #114
SDNode *and13 = CrtDAG->getMachineNode(
                                    Connex::ANDV_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(lt12, 0),
                                    SDValue(lt11, 0),
                                    // glue (or chain) input edge
                                    SDValue(add5, 1)
                                    );

// R09 = R09 == R31;// Instr #115
SDNode *eq13 = CrtDAG->getMachineNode(
                                    Connex::EQ_H,
                                    DL,
                                    TYPE_VECTOR_I16,
                                    MVT::Glue,
                                    SDValue(and13, 0),
                                    SDValue(vload0, 0),
                                    // glue (or chain) input edge
                                    SDValue(and13, 1)
                                    );

SDValue ct30 = CrtDAG->getSignedConstant(1 /* Num of cycles to NOP */, DL, MVT::i16, true, false);
// NOP;// Instr #116
SDNode *nop8 = CrtDAG->getMachineNode(
                                    Connex::NOP_BPF,
                                    DL,
                                    MVT::Glue,
                                    ct30,
                                    // glue (or chain) input edge
                                    SDValue(eq13, 1)
                                    );

// WHERE_EQ;// Instr #117
SDNode *whereeq8 = CrtDAG->getMachineNode(
                                    Connex::WHEREEQ,
                                    DL,
                                    MVT::Glue,
                                    // glue (or chain) input edge
                                    SDValue(nop8, 0)
                                    );

// R19 = R26 - R02;// Instr #118
SDNode *sub8 = CrtDAG->getMachineNode(
                                    Connex::SUBV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(vload7, 0),
                                    SDValue(add5, 0),
                                    SDValue(lt11, 0),
                                    // glue (or chain) input edge
                                    SDValue(whereeq8, 0)
                                    }
                                    );

// R27 = R27 << R19;// Instr #119
SDNode *shl5 = CrtDAG->getMachineNode(
                                    Connex::SHLV_SPECIAL_H,
                                    DL,
                                    CrtDAG->getVTList(
                                    TYPE_VECTOR_I16,
                                    MVT::Glue
                                    ),
                                    {
                                    SDValue(shl4, 0),
                                    SDValue(sub8, 0),
                                    SDValue(shl4, 0),
                                    // glue (or chain) input edge
                                    SDValue(sub8, 1)
                                    }
                                    );

// RED(R27);// Instr #120
SDNode *sumRed8 = CrtDAG->getMachineNode(
                                    Connex::RED_H,
                                    DL,
                                    MVT::Glue,
                                    SDValue(shl5, 0),
                                    // glue (or chain) input edge
                                    SDValue(shl5, 1)
                                    );

// END_WHERE;// Instr #121
SDNode *reduceH /*endwhere8*/ = CurDAG->getMachineNode(
                                    Connex::END_WHERE,
                                    DL,
// Alex:                                    MVT::Glue,
                                    MVT::Other,
                                    // glue (or chain) input edge
                                    SDValue(sumRed8, 0)
                                    );

