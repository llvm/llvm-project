# UNSUPPORTED: zlib
# RUN: yaml2obj %p/Inputs/compress-debug-sections.yaml -o %t
# RUN: not llvm-objcopy --compress-sections '.debug_foo=zlib' %t /dev/null 2>&1 | FileCheck %s
# RUN: not llvm-objcopy --compress-debug-sections=zlib %t /dev/null 2>&1 | FileCheck %s

# CHECK: error: LLVM was not built with LLVM_ENABLE_ZLIB or did not find zlib at build time
