# Unit test for cmake/Modules/GetTripleCMakeSystemName.cmake, which maps a
# target triple to a CMake system name and must stay in sync with the OS and
# environment tables in TripleName.def.

# RUN: %cmake -P %S/get-triple-cmake-system-name.cmake 2>&1 | FileCheck %s

# CHECK: get-triple-cmake-system-name: OK
