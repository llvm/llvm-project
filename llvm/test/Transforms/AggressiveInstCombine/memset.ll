; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --version 6
; RUN: opt -passes=aggressive-instcombine -S < %s | FileCheck %s --check-prefix=AIC

declare void @llvm.memset.p0.i64(ptr, i8, i64, i1)
declare void @llvm.assume(i1)

define void @range_0_1(ptr %dst, i8 %value, i64 %n) {
; AIC-LABEL: define void @range_0_1(
; AIC-SAME: ptr [[DST:%.*]], i8 [[VALUE:%.*]], i64 [[N:%.*]]) {
; AIC-NEXT:  [[ENTRY:.*:]]
; AIC-NEXT:    [[LEN:%.*]] = and i64 [[N]], 1
; AIC-NEXT:    [[MEMSET_NOTZERO:%.*]] = icmp ne i64 [[LEN]], 0
; AIC-NEXT:    br i1 [[MEMSET_NOTZERO]], label %[[DO_MEMSET:.*]], label %[[END:.*]]
; AIC:       [[DO_MEMSET]]:
; AIC-NEXT:    store i8 [[VALUE]], ptr [[DST]], align 1
; AIC-NEXT:    br label %[[END]]
; AIC:       [[END]]:
; AIC-NEXT:    ret void
;
entry:
  %len = and i64 %n, 1
  call void @llvm.memset.p0.i64(ptr align 1 %dst, i8 %value, i64 %len, i1 false)
  ret void
}

define void @range_0_1_zext(ptr %dst, i8 %value, i32 %n) {
; AIC-LABEL: define void @range_0_1_zext(
; AIC-SAME: ptr [[DST:%.*]], i8 [[VALUE:%.*]], i32 [[N:%.*]]) {
; AIC-NEXT:  [[ENTRY:.*:]]
; AIC-NEXT:    [[MASKED:%.*]] = and i32 [[N]], 1
; AIC-NEXT:    [[LEN:%.*]] = zext i32 [[MASKED]] to i64
; AIC-NEXT:    [[MEMSET_NOTZERO:%.*]] = icmp ne i64 [[LEN]], 0
; AIC-NEXT:    br i1 [[MEMSET_NOTZERO]], label %[[DO_MEMSET:.*]], label %[[END:.*]]
; AIC:       [[DO_MEMSET]]:
; AIC-NEXT:    store i8 [[VALUE]], ptr [[DST]], align 1
; AIC-NEXT:    br label %[[END]]
; AIC:       [[END]]:
; AIC-NEXT:    ret void
;
entry:
  %masked = and i32 %n, 1
  %len = zext i32 %masked to i64
  call void @llvm.memset.p0.i64(ptr align 1 %dst, i8 %value, i64 %len, i1 false)
  ret void
}

define void @range_0_1_assume(ptr %dst, i8 %value, i64 %n) {
; AIC-LABEL: define void @range_0_1_assume(
; AIC-SAME: ptr [[DST:%.*]], i8 [[VALUE:%.*]], i64 [[N:%.*]]) {
; AIC-NEXT:  [[ENTRY:.*:]]
; AIC-NEXT:    [[IN_RANGE:%.*]] = icmp ule i64 [[N]], 1
; AIC-NEXT:    call void @llvm.assume(i1 [[IN_RANGE]])
; AIC-NEXT:    [[MEMSET_NOTZERO:%.*]] = icmp ne i64 [[N]], 0
; AIC-NEXT:    br i1 [[MEMSET_NOTZERO]], label %[[DO_MEMSET:.*]], label %[[END:.*]]
; AIC:       [[DO_MEMSET]]:
; AIC-NEXT:    store i8 [[VALUE]], ptr [[DST]], align 1
; AIC-NEXT:    br label %[[END]]
; AIC:       [[END]]:
; AIC-NEXT:    ret void
;
entry:
  %in.range = icmp ule i64 %n, 1
  call void @llvm.assume(i1 %in.range)
  call void @llvm.memset.p0.i64(ptr align 1 %dst, i8 %value, i64 %n, i1 false)
  ret void
}

define void @range_0_1_volatile(ptr %dst, i8 %value, i64 %n) {
; AIC-LABEL: define void @range_0_1_volatile(
; AIC-SAME: ptr [[DST:%.*]], i8 [[VALUE:%.*]], i64 [[N:%.*]]) {
; AIC-NEXT:  [[ENTRY:.*:]]
; AIC-NEXT:    [[LEN:%.*]] = and i64 [[N]], 1
; AIC-NEXT:    [[MEMSET_NOTZERO:%.*]] = icmp ne i64 [[LEN]], 0
; AIC-NEXT:    br i1 [[MEMSET_NOTZERO]], label %[[DO_MEMSET:.*]], label %[[END:.*]]
; AIC:       [[DO_MEMSET]]:
; AIC-NEXT:    store volatile i8 [[VALUE]], ptr [[DST]], align 1
; AIC-NEXT:    br label %[[END]]
; AIC:       [[END]]:
; AIC-NEXT:    ret void
;
entry:
  %len = and i64 %n, 1
  call void @llvm.memset.p0.i64(ptr align 1 %dst, i8 %value, i64 %len, i1 true)
  ret void
}

define void @range_0_2(ptr %dst, i8 %value, i64 %n) {
; AIC-LABEL: define void @range_0_2(
; AIC-SAME: ptr [[DST:%.*]], i8 [[VALUE:%.*]], i64 [[N:%.*]]) {
; AIC-NEXT:  [[ENTRY:.*:]]
; AIC-NEXT:    [[LEN:%.*]] = urem i64 [[N]], 3
; AIC-NEXT:    call void @llvm.memset.p0.i64(ptr align 1 [[DST]], i8 [[VALUE]], i64 [[LEN]], i1 false)
; AIC-NEXT:    ret void
;
entry:
  %len = urem i64 %n, 3
  call void @llvm.memset.p0.i64(ptr align 1 %dst, i8 %value, i64 %len, i1 false)
  ret void
}

define void @unknown_length(ptr %dst, i8 %value, i64 %n) {
; AIC-LABEL: define void @unknown_length(
; AIC-SAME: ptr [[DST:%.*]], i8 [[VALUE:%.*]], i64 [[N:%.*]]) {
; AIC-NEXT:  [[ENTRY:.*:]]
; AIC-NEXT:    call void @llvm.memset.p0.i64(ptr align 1 [[DST]], i8 [[VALUE]], i64 [[N]], i1 false)
; AIC-NEXT:    ret void
;
entry:
  call void @llvm.memset.p0.i64(ptr align 1 %dst, i8 %value, i64 %n, i1 false)
  ret void
}
