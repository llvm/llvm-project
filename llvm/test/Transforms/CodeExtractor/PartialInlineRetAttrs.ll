; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --version 6
; RUN: opt < %s -S -passes=partial-inliner -skip-partial-inlining-cost-analysis=true | FileCheck %s


define signext i32 @callee_most(i32 %v) unnamed_addr {
; CHECK-LABEL: define signext i32 @callee_most(
;
entry:
  %cmp = icmp sgt i32 %v, 2000
  br i1 %cmp, label %if.then, label %if.end

if.then:
  br label %if.then2

if.then2:
  %sub = sub i32 %v, 10
  br label %if.end

if.end:
  %v2 = phi i32 [ %v, %entry ], [ %sub, %if.then2 ]
  ret i32 %v2
}

define i32 @caller(i32 %v) {
; CHECK-LABEL: define i32 @caller(
; CHECK:       [[TMP0:%.*]] = call signext i32 @callee_most.1.if.then
;
entry:
  %c1 = call i32 @callee_most(i32 %v)
  ret i32 %c1
}

; CHECK-LABEL: define internal signext i32 @callee_most.1.if.then