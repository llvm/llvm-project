; NOTE: Assertions have been autogenerated by utils/update_test_checks.py
; RUN: opt -S -passes=loop-vectorize -prefer-predicate-over-epilogue=predicate-else-scalar-epilogue < %s | FileCheck --check-prefix=TF %s
; RUN: opt -S -passes=loop-vectorize < %s | FileCheck --check-prefix=DEFAULT %s

; This tests should produce the same result as with default options, and when tail folding
; is preferred, because the vectorizer can't fold the tail by masking (due to an
; outside user of %incdec.ptr in %end) and should fallback to a scalar epilogue.
;
; The first test (@basic_loop) simply relies on the command-line switches.
; The second test (@metadata) specificies its tail-folding preference via metadata.
; Both tests should always generate a scalar epilogue.

target datalayout = "e-m:e-p:32:32-Fi8-i64:64-v128:64:128-a:0:32-n32-S64"

define void @basic_loop(ptr nocapture readonly %ptr, i32 %size, ptr %pos) {
; TF-LABEL: @basic_loop(
; TF-NEXT:  header:
; TF-NEXT:    [[PTR0:%.*]] = load ptr, ptr [[POS:%.*]], align 4
; TF-NEXT:    br label [[BODY:%.*]]
; TF:       body:
; TF-NEXT:    [[DEC66:%.*]] = phi i32 [ [[DEC:%.*]], [[BODY]] ], [ [[SIZE:%.*]], [[HEADER:%.*]] ]
; TF-NEXT:    [[FOR:%.*]] = phi i8 [ [[TMP0:%.*]], [[BODY]] ], [ 0, [[HEADER]] ]
; TF-NEXT:    [[BUFF:%.*]] = phi ptr [ [[INCDEC_PTR:%.*]], [[BODY]] ], [ [[PTR:%.*]], [[HEADER]] ]
; TF-NEXT:    [[INCDEC_PTR]] = getelementptr inbounds i8, ptr [[BUFF]], i32 1
; TF-NEXT:    [[DEC]] = add nsw i32 [[DEC66]], -1
; TF-NEXT:    [[TMP0]] = load i8, ptr [[INCDEC_PTR]], align 1
; TF-NEXT:    store i8 [[TMP0]], ptr [[BUFF]], align 1
; TF-NEXT:    [[TOBOOL11:%.*]] = icmp eq i32 [[DEC]], 0
; TF-NEXT:    br i1 [[TOBOOL11]], label [[END:%.*]], label [[BODY]]
; TF:       end:
; TF-NEXT:    [[FOR_LCSSA:%.*]] = phi i8 [ [[FOR]], [[BODY]] ]
; TF-NEXT:    store i8 [[FOR_LCSSA]], ptr [[POS]], align 4
; TF-NEXT:    ret void
;
; DEFAULT-LABEL: @basic_loop(
; DEFAULT-NEXT:  header:
; DEFAULT-NEXT:    [[PTR0:%.*]] = load ptr, ptr [[POS:%.*]], align 4
; DEFAULT-NEXT:    [[MIN_ITERS_CHECK:%.*]] = icmp ult i32 [[SIZE:%.*]], 4
; DEFAULT-NEXT:    br i1 [[MIN_ITERS_CHECK]], label [[SCALAR_PH:%.*]], label [[VECTOR_PH:%.*]]
; DEFAULT:       vector.ph:
; DEFAULT-NEXT:    [[N_MOD_VF:%.*]] = urem i32 [[SIZE]], 4
; DEFAULT-NEXT:    [[N_VEC:%.*]] = sub i32 [[SIZE]], [[N_MOD_VF]]
; DEFAULT-NEXT:    [[TMP0:%.*]] = sub i32 [[SIZE]], [[N_VEC]]
; DEFAULT-NEXT:    [[TMP1:%.*]] = getelementptr i8, ptr [[PTR:%.*]], i32 [[N_VEC]]
; DEFAULT-NEXT:    br label [[VECTOR_BODY:%.*]]
; DEFAULT:       vector.body:
; DEFAULT-NEXT:    [[INDEX:%.*]] = phi i32 [ 0, [[VECTOR_PH]] ], [ [[INDEX_NEXT:%.*]], [[VECTOR_BODY]] ]
; DEFAULT-NEXT:    [[NEXT_GEP:%.*]] = getelementptr i8, ptr [[PTR]], i32 [[INDEX]]
; DEFAULT-NEXT:    [[TMP2:%.*]] = getelementptr inbounds i8, ptr [[NEXT_GEP]], i32 1
; DEFAULT-NEXT:    [[TMP3:%.*]] = getelementptr inbounds i8, ptr [[TMP2]], i32 0
; DEFAULT-NEXT:    [[WIDE_LOAD:%.*]] = load <4 x i8>, ptr [[TMP3]], align 1
; DEFAULT-NEXT:    [[TMP4:%.*]] = getelementptr i8, ptr [[NEXT_GEP]], i32 0
; DEFAULT-NEXT:    store <4 x i8> [[WIDE_LOAD]], ptr [[TMP4]], align 1
; DEFAULT-NEXT:    [[INDEX_NEXT]] = add nuw i32 [[INDEX]], 4
; DEFAULT-NEXT:    [[TMP5:%.*]] = icmp eq i32 [[INDEX_NEXT]], [[N_VEC]]
; DEFAULT-NEXT:    br i1 [[TMP5]], label [[MIDDLE_BLOCK:%.*]], label [[VECTOR_BODY]], !llvm.loop [[LOOP0:![0-9]+]]
; DEFAULT:       middle.block:
; DEFAULT-NEXT:    [[VECTOR_RECUR_EXTRACT:%.*]] = extractelement <4 x i8> [[WIDE_LOAD]], i32 3
; DEFAULT-NEXT:    [[VECTOR_RECUR_EXTRACT_FOR_PHI:%.*]] = extractelement <4 x i8> [[WIDE_LOAD]], i32 2
; DEFAULT-NEXT:    [[CMP_N:%.*]] = icmp eq i32 [[SIZE]], [[N_VEC]]
; DEFAULT-NEXT:    br i1 [[CMP_N]], label [[END:%.*]], label [[SCALAR_PH]]
; DEFAULT:       scalar.ph:
; DEFAULT-NEXT:    [[BC_RESUME_VAL:%.*]] = phi i32 [ [[TMP0]], [[MIDDLE_BLOCK]] ], [ [[SIZE]], [[HEADER:%.*]] ]
; DEFAULT-NEXT:    [[SCALAR_RECUR_INIT:%.*]] = phi i8 [ [[VECTOR_RECUR_EXTRACT]], [[MIDDLE_BLOCK]] ], [ 0, [[HEADER]] ]
; DEFAULT-NEXT:    [[BC_RESUME_VAL1:%.*]] = phi ptr [ [[TMP1]], [[MIDDLE_BLOCK]] ], [ [[PTR]], [[HEADER]] ]
; DEFAULT-NEXT:    br label [[BODY:%.*]]
; DEFAULT:       body:
; DEFAULT-NEXT:    [[DEC66:%.*]] = phi i32 [ [[DEC:%.*]], [[BODY]] ], [ [[BC_RESUME_VAL]], [[SCALAR_PH]] ]
; DEFAULT-NEXT:    [[FOR:%.*]] = phi i8 [ [[TMP6:%.*]], [[BODY]] ], [ [[SCALAR_RECUR_INIT]], [[SCALAR_PH]] ]
; DEFAULT-NEXT:    [[BUFF:%.*]] = phi ptr [ [[INCDEC_PTR:%.*]], [[BODY]] ], [ [[BC_RESUME_VAL1]], [[SCALAR_PH]] ]
; DEFAULT-NEXT:    [[INCDEC_PTR]] = getelementptr inbounds i8, ptr [[BUFF]], i32 1
; DEFAULT-NEXT:    [[DEC]] = add nsw i32 [[DEC66]], -1
; DEFAULT-NEXT:    [[TMP6]] = load i8, ptr [[INCDEC_PTR]], align 1
; DEFAULT-NEXT:    store i8 [[TMP6]], ptr [[BUFF]], align 1
; DEFAULT-NEXT:    [[TOBOOL11:%.*]] = icmp eq i32 [[DEC]], 0
; DEFAULT-NEXT:    br i1 [[TOBOOL11]], label [[END]], label [[BODY]], !llvm.loop [[LOOP3:![0-9]+]]
; DEFAULT:       end:
; DEFAULT-NEXT:    [[FOR_LCSSA:%.*]] = phi i8 [ [[FOR]], [[BODY]] ], [ [[VECTOR_RECUR_EXTRACT_FOR_PHI]], [[MIDDLE_BLOCK]] ]
; DEFAULT-NEXT:    store i8 [[FOR_LCSSA]], ptr [[POS]], align 4
; DEFAULT-NEXT:    ret void
;
header:
  %ptr0 = load ptr, ptr %pos, align 4
  br label %body

body:
  %dec66 = phi i32 [ %dec, %body ], [ %size, %header ]
  %for = phi i8 [ %0, %body ], [ 0, %header ]
  %buff = phi ptr [ %incdec.ptr, %body ], [ %ptr, %header ]
  %incdec.ptr = getelementptr inbounds i8, ptr %buff, i32 1
  %dec = add nsw i32 %dec66, -1
  %0 = load i8, ptr %incdec.ptr, align 1
  store i8 %0, ptr %buff, align 1
  %tobool11 = icmp eq i32 %dec, 0
  br i1 %tobool11, label %end, label %body

end:
  store i8 %for, ptr %pos, align 4
  ret void
}

define void @metadata(ptr nocapture readonly %ptr, i32 %size, ptr %pos) {
; TF-LABEL: @metadata(
; TF-NEXT:  header:
; TF-NEXT:    [[PTR0:%.*]] = load ptr, ptr [[POS:%.*]], align 4
; TF-NEXT:    br label [[BODY:%.*]]
; TF:       body:
; TF-NEXT:    [[DEC66:%.*]] = phi i32 [ [[DEC:%.*]], [[BODY]] ], [ [[BC_RESUME_VAL:%.*]], [[SCALAR_PH:%.*]] ]
; TF-NEXT:    [[FOR:%.*]] = phi i8 [ [[TMP26:%.*]], [[BODY]] ], [ 0, [[SCALAR_PH]] ]
; TF-NEXT:    [[BUFF:%.*]] = phi ptr [ [[INCDEC_PTR:%.*]], [[BODY]] ], [ [[BC_RESUME_VAL12:%.*]], [[SCALAR_PH]] ]
; TF-NEXT:    [[INCDEC_PTR]] = getelementptr inbounds i8, ptr [[BUFF]], i32 1
; TF-NEXT:    [[DEC]] = add nsw i32 [[DEC66]], -1
; TF-NEXT:    [[TMP26]] = load i8, ptr [[INCDEC_PTR]], align 1
; TF-NEXT:    store i8 [[TMP26]], ptr [[BUFF]], align 1
; TF-NEXT:    [[TOBOOL11:%.*]] = icmp eq i32 [[DEC]], 0
; TF-NEXT:    br i1 [[TOBOOL11]], label [[END:%.*]], label [[BODY]], !llvm.loop [[LOOP0:![0-9]+]]
; TF:       end:
; TF-NEXT:    [[FOR_LCSSA:%.*]] = phi i8 [ [[FOR]], [[BODY]] ]
; TF-NEXT:    store i8 [[FOR_LCSSA]], ptr [[POS]], align 4
; TF-NEXT:    ret void
;
; DEFAULT-LABEL: @metadata(
; DEFAULT-NEXT:  header:
; DEFAULT-NEXT:    [[PTR0:%.*]] = load ptr, ptr [[POS:%.*]], align 4
; DEFAULT-NEXT:    br label [[BODY:%.*]]
; DEFAULT:       body:
; DEFAULT-NEXT:    [[DEC66:%.*]] = phi i32 [ [[DEC:%.*]], [[BODY]] ], [ [[BC_RESUME_VAL:%.*]], [[SCALAR_PH:%.*]] ]
; DEFAULT-NEXT:    [[FOR:%.*]] = phi i8 [ [[TMP26:%.*]], [[BODY]] ], [ 0, [[SCALAR_PH]] ]
; DEFAULT-NEXT:    [[BUFF:%.*]] = phi ptr [ [[INCDEC_PTR:%.*]], [[BODY]] ], [ [[BC_RESUME_VAL12:%.*]], [[SCALAR_PH]] ]
; DEFAULT-NEXT:    [[INCDEC_PTR]] = getelementptr inbounds i8, ptr [[BUFF]], i32 1
; DEFAULT-NEXT:    [[DEC]] = add nsw i32 [[DEC66]], -1
; DEFAULT-NEXT:    [[TMP26]] = load i8, ptr [[INCDEC_PTR]], align 1
; DEFAULT-NEXT:    store i8 [[TMP26]], ptr [[BUFF]], align 1
; DEFAULT-NEXT:    [[TOBOOL11:%.*]] = icmp eq i32 [[DEC]], 0
; DEFAULT-NEXT:    br i1 [[TOBOOL11]], label [[END:%.*]], label [[BODY]], !llvm.loop [[LOOP4:![0-9]+]]
; DEFAULT:       end:
; DEFAULT-NEXT:    [[FOR_LCSSA:%.*]] = phi i8 [ [[FOR]], [[BODY]] ]
; DEFAULT-NEXT:    store i8 [[FOR_LCSSA]], ptr [[POS]], align 4
; DEFAULT-NEXT:    ret void
;
header:
  %ptr0 = load ptr, ptr %pos, align 4
  br label %body

body:
  %dec66 = phi i32 [ %dec, %body ], [ %size, %header ]
  %for = phi i8 [ %0, %body ], [ 0, %header ]
  %buff = phi ptr [ %incdec.ptr, %body ], [ %ptr, %header ]
  %incdec.ptr = getelementptr inbounds i8, ptr %buff, i32 1
  %dec = add nsw i32 %dec66, -1
  %0 = load i8, ptr %incdec.ptr, align 1
  store i8 %0, ptr %buff, align 1
  %tobool11 = icmp eq i32 %dec, 0
  br i1 %tobool11, label %end, label %body, !llvm.loop !1

end:
  store i8 %for, ptr %pos, align 4
  ret void
}

!1 = distinct !{!1, !2, !3}
!2 = !{!"llvm.loop.vectorize.predicate.enable", i1 true}
!3 = !{!"llvm.loop.vectorize.enable", i1 true}
