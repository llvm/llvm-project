; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --check-globals none --version 6
; RUN: opt < %s -mcpu=x86-64-v2 -passes=loop-vectorize -enable-histogram-loop-vectorization -S | FileCheck %s --check-prefix=X8664-V2
; RUN: opt < %s -mcpu=x86-64-v3 -passes=loop-vectorize -enable-histogram-loop-vectorization -S | FileCheck %s --check-prefix=X8664-V3
; RUN: opt < %s -mcpu=x86-64-v4 -passes=loop-vectorize -enable-histogram-loop-vectorization -S | FileCheck %s --check-prefix=X8664-V4

target triple = "x86_64-unknown-linux-gnu"

define void @simple_histogram(ptr noalias %buckets, ptr readonly %indices) {
; X8664-V2-LABEL: define void @simple_histogram(
; X8664-V2-SAME: ptr noalias [[BUCKETS:%.*]], ptr readonly [[INDICES:%.*]]) #[[ATTR0:[0-9]+]] {
; X8664-V2-NEXT:  [[ENTRY:.*]]:
; X8664-V2-NEXT:    br label %[[LOOP:.*]]
; X8664-V2:       [[LOOP]]:
; X8664-V2-NEXT:    [[IV:%.*]] = phi i64 [ 0, %[[ENTRY]] ], [ [[IV_NEXT:%.*]], %[[LOOP]] ]
; X8664-V2-NEXT:    [[GEP_INDICES:%.*]] = getelementptr inbounds i32, ptr [[INDICES]], i64 [[IV]]
; X8664-V2-NEXT:    [[L_IDX:%.*]] = load i32, ptr [[GEP_INDICES]], align 4
; X8664-V2-NEXT:    [[IDXPROM1:%.*]] = zext i32 [[L_IDX]] to i64
; X8664-V2-NEXT:    [[GEP_BUCKET:%.*]] = getelementptr inbounds i32, ptr [[BUCKETS]], i64 [[IDXPROM1]]
; X8664-V2-NEXT:    [[L_BUCKET:%.*]] = load i32, ptr [[GEP_BUCKET]], align 4
; X8664-V2-NEXT:    [[INC:%.*]] = add nsw i32 [[L_BUCKET]], 1
; X8664-V2-NEXT:    store i32 [[INC]], ptr [[GEP_BUCKET]], align 4
; X8664-V2-NEXT:    [[IV_NEXT]] = add nuw nsw i64 [[IV]], 1
; X8664-V2-NEXT:    [[EXITCOND:%.*]] = icmp eq i64 [[IV_NEXT]], 1000
; X8664-V2-NEXT:    br i1 [[EXITCOND]], label %[[EXIT:.*]], label %[[LOOP]]
; X8664-V2:       [[EXIT]]:
; X8664-V2-NEXT:    ret void
;
; X8664-V3-LABEL: define void @simple_histogram(
; X8664-V3-SAME: ptr noalias [[BUCKETS:%.*]], ptr readonly [[INDICES:%.*]]) #[[ATTR0:[0-9]+]] {
; X8664-V3-NEXT:  [[ENTRY:.*]]:
; X8664-V3-NEXT:    br label %[[LOOP:.*]]
; X8664-V3:       [[LOOP]]:
; X8664-V3-NEXT:    [[IV:%.*]] = phi i64 [ 0, %[[ENTRY]] ], [ [[IV_NEXT:%.*]], %[[LOOP]] ]
; X8664-V3-NEXT:    [[GEP_INDICES:%.*]] = getelementptr inbounds i32, ptr [[INDICES]], i64 [[IV]]
; X8664-V3-NEXT:    [[L_IDX:%.*]] = load i32, ptr [[GEP_INDICES]], align 4
; X8664-V3-NEXT:    [[IDXPROM1:%.*]] = zext i32 [[L_IDX]] to i64
; X8664-V3-NEXT:    [[GEP_BUCKET:%.*]] = getelementptr inbounds i32, ptr [[BUCKETS]], i64 [[IDXPROM1]]
; X8664-V3-NEXT:    [[L_BUCKET:%.*]] = load i32, ptr [[GEP_BUCKET]], align 4
; X8664-V3-NEXT:    [[INC:%.*]] = add nsw i32 [[L_BUCKET]], 1
; X8664-V3-NEXT:    store i32 [[INC]], ptr [[GEP_BUCKET]], align 4
; X8664-V3-NEXT:    [[IV_NEXT]] = add nuw nsw i64 [[IV]], 1
; X8664-V3-NEXT:    [[EXITCOND:%.*]] = icmp eq i64 [[IV_NEXT]], 1000
; X8664-V3-NEXT:    br i1 [[EXITCOND]], label %[[EXIT:.*]], label %[[LOOP]]
; X8664-V3:       [[EXIT]]:
; X8664-V3-NEXT:    ret void
;
; X8664-V4-LABEL: define void @simple_histogram(
; X8664-V4-SAME: ptr noalias [[BUCKETS:%.*]], ptr readonly [[INDICES:%.*]]) #[[ATTR0:[0-9]+]] {
; X8664-V4-NEXT:  [[ENTRY:.*]]:
; X8664-V4-NEXT:    br label %[[LOOP:.*]]
; X8664-V4:       [[LOOP]]:
; X8664-V4-NEXT:    [[IV:%.*]] = phi i64 [ 0, %[[ENTRY]] ], [ [[IV_NEXT:%.*]], %[[LOOP]] ]
; X8664-V4-NEXT:    [[GEP_INDICES:%.*]] = getelementptr inbounds i32, ptr [[INDICES]], i64 [[IV]]
; X8664-V4-NEXT:    [[L_IDX:%.*]] = load i32, ptr [[GEP_INDICES]], align 4
; X8664-V4-NEXT:    [[IDXPROM1:%.*]] = zext i32 [[L_IDX]] to i64
; X8664-V4-NEXT:    [[GEP_BUCKET:%.*]] = getelementptr inbounds i32, ptr [[BUCKETS]], i64 [[IDXPROM1]]
; X8664-V4-NEXT:    [[L_BUCKET:%.*]] = load i32, ptr [[GEP_BUCKET]], align 4
; X8664-V4-NEXT:    [[INC:%.*]] = add nsw i32 [[L_BUCKET]], 1
; X8664-V4-NEXT:    store i32 [[INC]], ptr [[GEP_BUCKET]], align 4
; X8664-V4-NEXT:    [[IV_NEXT]] = add nuw nsw i64 [[IV]], 1
; X8664-V4-NEXT:    [[EXITCOND:%.*]] = icmp eq i64 [[IV_NEXT]], 1000
; X8664-V4-NEXT:    br i1 [[EXITCOND]], label %[[EXIT:.*]], label %[[LOOP]]
; X8664-V4:       [[EXIT]]:
; X8664-V4-NEXT:    ret void
;
entry:
  br label %loop

loop:
  %iv = phi i64 [ 0, %entry ], [ %iv.next, %loop ]
  %gep.indices = getelementptr inbounds i32, ptr %indices, i64 %iv
  %l.idx = load i32, ptr %gep.indices, align 4
  %idxprom1 = zext i32 %l.idx to i64
  %gep.bucket = getelementptr inbounds i32, ptr %buckets, i64 %idxprom1
  %l.bucket = load i32, ptr %gep.bucket, align 4
  %inc = add nsw i32 %l.bucket, 1
  store i32 %inc, ptr %gep.bucket, align 4
  %iv.next = add nuw nsw i64 %iv, 1
  %exitcond = icmp eq i64 %iv.next, 1000
  br i1 %exitcond, label %exit, label %loop

exit:
  ret void
}
