; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --check-globals none --version 6
; RUN: opt < %s -mattr=+sse2 -passes=loop-vectorize -enable-histogram-loop-vectorization -S | FileCheck %s --check-prefix=SSE
; RUN: opt < %s -mattr=+avx512f -passes=loop-vectorize -enable-histogram-loop-vectorization -S | FileCheck %s --check-prefix=AVX512

target triple = "x86_64-unknown-linux-gnu"

define void @simple_histogram(ptr noalias %buckets, ptr readonly %indices) {
; SSE-LABEL: define void @simple_histogram(
; SSE-SAME: ptr noalias [[BUCKETS:%.*]], ptr readonly [[INDICES:%.*]]) #[[ATTR0:[0-9]+]] {
; SSE-NEXT:  [[ENTRY:.*]]:
; SSE-NEXT:    br label %[[LOOP:.*]]
; SSE:       [[LOOP]]:
; SSE-NEXT:    [[IV:%.*]] = phi i64 [ 0, %[[ENTRY]] ], [ [[IV_NEXT:%.*]], %[[LOOP]] ]
; SSE-NEXT:    [[GEP_INDICES:%.*]] = getelementptr inbounds i32, ptr [[INDICES]], i64 [[IV]]
; SSE-NEXT:    [[L_IDX:%.*]] = load i32, ptr [[GEP_INDICES]], align 4
; SSE-NEXT:    [[IDXPROM1:%.*]] = zext i32 [[L_IDX]] to i64
; SSE-NEXT:    [[GEP_BUCKET:%.*]] = getelementptr inbounds i32, ptr [[BUCKETS]], i64 [[IDXPROM1]]
; SSE-NEXT:    [[L_BUCKET:%.*]] = load i32, ptr [[GEP_BUCKET]], align 4
; SSE-NEXT:    [[INC:%.*]] = add nsw i32 [[L_BUCKET]], 1
; SSE-NEXT:    store i32 [[INC]], ptr [[GEP_BUCKET]], align 4
; SSE-NEXT:    [[IV_NEXT]] = add nuw nsw i64 [[IV]], 1
; SSE-NEXT:    [[EXITCOND:%.*]] = icmp eq i64 [[IV_NEXT]], 1000
; SSE-NEXT:    br i1 [[EXITCOND]], label %[[EXIT:.*]], label %[[LOOP]]
; SSE:       [[EXIT]]:
; SSE-NEXT:    ret void
;
; AVX512-LABEL: define void @simple_histogram(
; AVX512-SAME: ptr noalias [[BUCKETS:%.*]], ptr readonly [[INDICES:%.*]]) #[[ATTR0:[0-9]+]] {
; AVX512-NEXT:  [[VEC_EPILOG_SCALAR_PH:.*]]:
; AVX512-NEXT:    br label %[[LOOP:.*]]
; AVX512:       [[LOOP]]:
; AVX512-NEXT:    [[IV:%.*]] = phi i64 [ 0, %[[VEC_EPILOG_SCALAR_PH]] ], [ [[IV_NEXT:%.*]], %[[LOOP]] ]
; AVX512-NEXT:    [[GEP_INDICES:%.*]] = getelementptr inbounds i32, ptr [[INDICES]], i64 [[IV]]
; AVX512-NEXT:    [[L_IDX:%.*]] = load i32, ptr [[GEP_INDICES]], align 4
; AVX512-NEXT:    [[IDXPROM1:%.*]] = zext i32 [[L_IDX]] to i64
; AVX512-NEXT:    [[GEP_BUCKET:%.*]] = getelementptr inbounds i32, ptr [[BUCKETS]], i64 [[IDXPROM1]]
; AVX512-NEXT:    [[L_BUCKET:%.*]] = load i32, ptr [[GEP_BUCKET]], align 4
; AVX512-NEXT:    [[INC:%.*]] = add nsw i32 [[L_BUCKET]], 1
; AVX512-NEXT:    store i32 [[INC]], ptr [[GEP_BUCKET]], align 4
; AVX512-NEXT:    [[IV_NEXT]] = add nuw nsw i64 [[IV]], 1
; AVX512-NEXT:    [[EXITCOND:%.*]] = icmp eq i64 [[IV_NEXT]], 1000
; AVX512-NEXT:    br i1 [[EXITCOND]], label %[[EXIT:.*]], label %[[LOOP]]
; AVX512:       [[EXIT]]:
; AVX512-NEXT:    ret void
;
entry:
  br label %loop

loop:
  %iv = phi i64 [ 0, %entry ], [ %iv.next, %loop ]
  %gep.indices = getelementptr inbounds i32, ptr %indices, i64 %iv
  %l.idx = load i32, ptr %gep.indices, align 4
  %idxprom1 = zext i32 %l.idx to i64
  %gep.bucket = getelementptr inbounds i32, ptr %buckets, i64 %idxprom1
  %l.bucket = load i32, ptr %gep.bucket, align 4
  %inc = add nsw i32 %l.bucket, 1
  store i32 %inc, ptr %gep.bucket, align 4
  %iv.next = add nuw nsw i64 %iv, 1
  %exitcond = icmp eq i64 %iv.next, 1000
  br i1 %exitcond, label %exit, label %loop

exit:
  ret void
}
