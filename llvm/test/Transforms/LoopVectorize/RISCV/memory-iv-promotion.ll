; NOTE: Assertions have been autogenerated by utils/update_test_checks.py
; RUN: opt -passes=loop-vectorize -S %s -mtriple=riscv64 -mattr=+v | FileCheck %s

; Test for memory induction variable promotion: a loop with a load/add/store
; pattern to a loop-invariant address (e.g., ++m_size) alongside array copy.

define void @memory_iv_copy_and_count(ptr noalias %dst, ptr noalias %src, ptr %count, i64 %n) {
; CHECK-LABEL: @memory_iv_copy_and_count(
; CHECK-NEXT:  entry:
; CHECK-NEXT:    [[CMP:%.*]] = icmp sgt i64 [[N:%.*]], 0
; CHECK-NEXT:    br i1 [[CMP]], label [[LOOP_PREHEADER:%.*]], label [[EXIT:%.*]]
; CHECK:       loop.preheader:
; CHECK-NEXT:    br label [[LOOP:%.*]]
; CHECK:       loop:
; CHECK-NEXT:    [[IV:%.*]] = phi i64 [ [[IV_NEXT:%.*]], [[LOOP]] ], [ 0, [[LOOP_PREHEADER]] ]
; CHECK-NEXT:    [[COUNT_VAL:%.*]] = load i64, ptr [[COUNT:%.*]], align 8
; CHECK-NEXT:    [[COUNT_NEXT:%.*]] = add i64 [[COUNT_VAL]], 1
; CHECK-NEXT:    store i64 [[COUNT_NEXT]], ptr [[COUNT]], align 8
; CHECK-NEXT:    [[SRC_PTR:%.*]] = getelementptr inbounds i64, ptr [[SRC:%.*]], i64 [[IV]]
; CHECK-NEXT:    [[VAL:%.*]] = load i64, ptr [[SRC_PTR]], align 8
; CHECK-NEXT:    [[DST_PTR:%.*]] = getelementptr inbounds i64, ptr [[DST:%.*]], i64 [[IV]]
; CHECK-NEXT:    store i64 [[VAL]], ptr [[DST_PTR]], align 8
; CHECK-NEXT:    [[IV_NEXT]] = add nuw nsw i64 [[IV]], 1
; CHECK-NEXT:    [[EXITCOND:%.*]] = icmp eq i64 [[IV_NEXT]], [[N]]
; CHECK-NEXT:    br i1 [[EXITCOND]], label [[EXIT_LOOPEXIT:%.*]], label [[LOOP]]
; CHECK:       exit.loopexit:
; CHECK-NEXT:    br label [[EXIT]]
; CHECK:       exit:
; CHECK-NEXT:    ret void
;
entry:
  %cmp = icmp sgt i64 %n, 0
  br i1 %cmp, label %loop, label %exit

loop:
  %iv = phi i64 [ 0, %entry ], [ %iv.next, %loop ]
  %count.val = load i64, ptr %count, align 8
  %count.next = add i64 %count.val, 1
  store i64 %count.next, ptr %count, align 8
  %src.ptr = getelementptr inbounds i64, ptr %src, i64 %iv
  %val = load i64, ptr %src.ptr, align 8
  %dst.ptr = getelementptr inbounds i64, ptr %dst, i64 %iv
  store i64 %val, ptr %dst.ptr, align 8
  %iv.next = add nuw nsw i64 %iv, 1
  %exitcond = icmp eq i64 %iv.next, %n
  br i1 %exitcond, label %exit, label %loop

exit:
  ret void
}

; Non-unit step: counter incremented by 3 each iteration.
define void @memory_iv_step3(ptr noalias %dst, ptr noalias %src, ptr %count, i64 %n) {
; CHECK-LABEL: @memory_iv_step3(
; CHECK-NEXT:  entry:
; CHECK-NEXT:    [[CMP:%.*]] = icmp sgt i64 [[N:%.*]], 0
; CHECK-NEXT:    br i1 [[CMP]], label [[LOOP_PREHEADER:%.*]], label [[EXIT:%.*]]
; CHECK:       loop.preheader:
; CHECK-NEXT:    br label [[LOOP:%.*]]
; CHECK:       loop:
; CHECK-NEXT:    [[IV:%.*]] = phi i64 [ [[IV_NEXT:%.*]], [[LOOP]] ], [ 0, [[LOOP_PREHEADER]] ]
; CHECK-NEXT:    [[COUNT_VAL:%.*]] = load i64, ptr [[COUNT:%.*]], align 8
; CHECK-NEXT:    [[COUNT_NEXT:%.*]] = add i64 [[COUNT_VAL]], 3
; CHECK-NEXT:    store i64 [[COUNT_NEXT]], ptr [[COUNT]], align 8
; CHECK-NEXT:    [[SRC_PTR:%.*]] = getelementptr inbounds i64, ptr [[SRC:%.*]], i64 [[IV]]
; CHECK-NEXT:    [[VAL:%.*]] = load i64, ptr [[SRC_PTR]], align 8
; CHECK-NEXT:    [[DST_PTR:%.*]] = getelementptr inbounds i64, ptr [[DST:%.*]], i64 [[IV]]
; CHECK-NEXT:    store i64 [[VAL]], ptr [[DST_PTR]], align 8
; CHECK-NEXT:    [[IV_NEXT]] = add nuw nsw i64 [[IV]], 1
; CHECK-NEXT:    [[EXITCOND:%.*]] = icmp eq i64 [[IV_NEXT]], [[N]]
; CHECK-NEXT:    br i1 [[EXITCOND]], label [[EXIT_LOOPEXIT:%.*]], label [[LOOP]]
; CHECK:       exit.loopexit:
; CHECK-NEXT:    br label [[EXIT]]
; CHECK:       exit:
; CHECK-NEXT:    ret void
;
entry:
  %cmp = icmp sgt i64 %n, 0
  br i1 %cmp, label %loop, label %exit

loop:
  %iv = phi i64 [ 0, %entry ], [ %iv.next, %loop ]
  %count.val = load i64, ptr %count, align 8
  %count.next = add i64 %count.val, 3
  store i64 %count.next, ptr %count, align 8
  %src.ptr = getelementptr inbounds i64, ptr %src, i64 %iv
  %val = load i64, ptr %src.ptr, align 8
  %dst.ptr = getelementptr inbounds i64, ptr %dst, i64 %iv
  store i64 %val, ptr %dst.ptr, align 8
  %iv.next = add nuw nsw i64 %iv, 1
  %exitcond = icmp eq i64 %iv.next, %n
  br i1 %exitcond, label %exit, label %loop

exit:
  ret void
}

; Negative test: loaded counter value is used outside the load/add/store chain
; (passed to a call), so it cannot be promoted.
define void @memory_iv_external_use(ptr noalias %dst, ptr noalias %src, ptr %count, i64 %n) {
; CHECK-LABEL: @memory_iv_external_use(
; CHECK-NEXT:  entry:
; CHECK-NEXT:    [[CMP:%.*]] = icmp sgt i64 [[N:%.*]], 0
; CHECK-NEXT:    br i1 [[CMP]], label [[LOOP_PREHEADER:%.*]], label [[EXIT:%.*]]
; CHECK:       loop.preheader:
; CHECK-NEXT:    br label [[LOOP:%.*]]
; CHECK:       loop:
; CHECK-NEXT:    [[IV:%.*]] = phi i64 [ [[IV_NEXT:%.*]], [[LOOP]] ], [ 0, [[LOOP_PREHEADER]] ]
; CHECK-NEXT:    [[COUNT_VAL:%.*]] = load i64, ptr [[COUNT:%.*]], align 8
; CHECK-NEXT:    [[COUNT_NEXT:%.*]] = add i64 [[COUNT_VAL]], 1
; CHECK-NEXT:    store i64 [[COUNT_NEXT]], ptr [[COUNT]], align 8
; CHECK-NEXT:    call void @use(i64 [[COUNT_VAL]])
; CHECK-NEXT:    [[SRC_PTR:%.*]] = getelementptr inbounds i64, ptr [[SRC:%.*]], i64 [[IV]]
; CHECK-NEXT:    [[VAL:%.*]] = load i64, ptr [[SRC_PTR]], align 8
; CHECK-NEXT:    [[DST_PTR:%.*]] = getelementptr inbounds i64, ptr [[DST:%.*]], i64 [[IV]]
; CHECK-NEXT:    store i64 [[VAL]], ptr [[DST_PTR]], align 8
; CHECK-NEXT:    [[IV_NEXT]] = add nuw nsw i64 [[IV]], 1
; CHECK-NEXT:    [[EXITCOND:%.*]] = icmp eq i64 [[IV_NEXT]], [[N]]
; CHECK-NEXT:    br i1 [[EXITCOND]], label [[EXIT_LOOPEXIT:%.*]], label [[LOOP]]
; CHECK:       exit.loopexit:
; CHECK-NEXT:    br label [[EXIT]]
; CHECK:       exit:
; CHECK-NEXT:    ret void
;
entry:
  %cmp = icmp sgt i64 %n, 0
  br i1 %cmp, label %loop, label %exit

loop:
  %iv = phi i64 [ 0, %entry ], [ %iv.next, %loop ]
  %count.val = load i64, ptr %count, align 8
  %count.next = add i64 %count.val, 1
  store i64 %count.next, ptr %count, align 8
  call void @use(i64 %count.val)
  %src.ptr = getelementptr inbounds i64, ptr %src, i64 %iv
  %val = load i64, ptr %src.ptr, align 8
  %dst.ptr = getelementptr inbounds i64, ptr %dst, i64 %iv
  store i64 %val, ptr %dst.ptr, align 8
  %iv.next = add nuw nsw i64 %iv, 1
  %exitcond = icmp eq i64 %iv.next, %n
  br i1 %exitcond, label %exit, label %loop

exit:
  ret void
}
declare void @use(i64)
