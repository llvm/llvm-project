; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --filter "br i1|= select |!prof|!llvm.loop|^.*:" --version 6
; RUN: opt -passes=loop-vectorize -force-vector-width=4 -force-vector-interleave=1 -S %s | FileCheck %s

; The loop exits from the header, so there is no estimated trip count.
define void @header_exiting(ptr %p, i32 %n) !prof !0 {
; CHECK-LABEL: define void @header_exiting(
; CHECK-SAME: ptr [[P:%.*]], i32 [[N:%.*]]) !prof [[PROF0:![0-9]+]] {
; CHECK:  [[ENTRY:.*:]]
; CHECK:    br i1 [[MIN_ITERS_CHECK:%.*]], label %[[SCALAR_PH:.*]], label %[[VECTOR_PH:.*]]
; CHECK:  [[VECTOR_PH]]:
; CHECK:    [[TMP4:%.*]] = select i1 [[TMP3:%.*]], i32 4, i32 [[TMP2:%.*]]
; CHECK:  [[VECTOR_BODY:.*]]:
; CHECK:    br i1 [[TMP7:%.*]], label %[[MIDDLE_BLOCK:.*]], label %[[VECTOR_BODY]], !llvm.loop [[LOOP1:![0-9]+]]
; CHECK:  [[MIDDLE_BLOCK]]:
; CHECK:  [[SCALAR_PH]]:
; CHECK:  [[LOOP_HEADER:.*]]:
; CHECK:    br i1 [[EC:%.*]], label %[[LOOP_LATCH:.*]], label %[[EXIT:.*]]
; CHECK:  [[LOOP_LATCH]]:
; CHECK:    br label %[[LOOP_HEADER]], !llvm.loop [[LOOP4:![0-9]+]]
; CHECK:  [[EXIT]]:
;
entry:
  br label %loop.header

loop.header:
  %iv = phi i32 [ 0, %entry ], [ %iv.next, %loop.latch ]
  %ec = icmp slt i32 %iv, %n
  br i1 %ec, label %loop.latch, label %exit

loop.latch:
  %idx = sext i32 %iv to i64
  %gep = getelementptr inbounds i16, ptr %p, i64 %idx
  store i16 0, ptr %gep, align 2
  %iv.next = add nsw i32 %iv, 1
  br label %loop.header

exit:
  ret void
}

; Same, with a memory runtime check bypassing the vector loop as well.
define void @header_exiting_with_runtime_checks(ptr %dst, ptr %src, i32 %n) !prof !0 {
; CHECK-LABEL: define void @header_exiting_with_runtime_checks(
; CHECK-SAME: ptr [[DST:%.*]], ptr [[SRC:%.*]], i32 [[N:%.*]]) !prof [[PROF0]] {
; CHECK:  [[ENTRY:.*:]]
; CHECK:    br i1 [[MIN_ITERS_CHECK:%.*]], label %[[SCALAR_PH:.*]], label %[[VECTOR_MEMCHECK:.*]]
; CHECK:  [[VECTOR_MEMCHECK]]:
; CHECK:    br i1 [[DIFF_CHECK:%.*]], label %[[SCALAR_PH]], label %[[VECTOR_PH:.*]]
; CHECK:  [[VECTOR_PH]]:
; CHECK:    [[TMP6:%.*]] = select i1 [[TMP5:%.*]], i32 4, i32 [[TMP4:%.*]]
; CHECK:  [[VECTOR_BODY:.*]]:
; CHECK:    br i1 [[TMP10:%.*]], label %[[MIDDLE_BLOCK:.*]], label %[[VECTOR_BODY]], !llvm.loop [[LOOP5:![0-9]+]]
; CHECK:  [[MIDDLE_BLOCK]]:
; CHECK:  [[SCALAR_PH]]:
; CHECK:  [[LOOP_HEADER:.*]]:
; CHECK:    br i1 [[EC:%.*]], label %[[LOOP_LATCH:.*]], label %[[EXIT:.*]]
; CHECK:  [[LOOP_LATCH]]:
; CHECK:    br label %[[LOOP_HEADER]], !llvm.loop [[LOOP6:![0-9]+]]
; CHECK:  [[EXIT]]:
;
entry:
  br label %loop.header

loop.header:
  %iv = phi i32 [ 0, %entry ], [ %iv.next, %loop.latch ]
  %ec = icmp slt i32 %iv, %n
  br i1 %ec, label %loop.latch, label %exit

loop.latch:
  %idx = sext i32 %iv to i64
  %gep.src = getelementptr inbounds i16, ptr %src, i64 %idx
  %l = load i16, ptr %gep.src, align 2
  %gep.dst = getelementptr inbounds i16, ptr %dst, i64 %idx
  store i16 %l, ptr %gep.dst, align 2
  %iv.next = add nsw i32 %iv, 1
  br label %loop.header

exit:
  ret void
}

define void @header_exiting_not_profiled(ptr %p, i32 %n) {
; CHECK-LABEL: define void @header_exiting_not_profiled(
; CHECK-SAME: ptr [[P:%.*]], i32 [[N:%.*]]) {
; CHECK:  [[ENTRY:.*:]]
; CHECK:    br i1 [[MIN_ITERS_CHECK:%.*]], label %[[SCALAR_PH:.*]], label %[[VECTOR_PH:.*]]
; CHECK:  [[VECTOR_PH]]:
; CHECK:    [[TMP4:%.*]] = select i1 [[TMP3:%.*]], i32 4, i32 [[TMP2:%.*]]
; CHECK:  [[VECTOR_BODY:.*]]:
; CHECK:    br i1 [[TMP7:%.*]], label %[[MIDDLE_BLOCK:.*]], label %[[VECTOR_BODY]], !llvm.loop [[LOOP7:![0-9]+]]
; CHECK:  [[MIDDLE_BLOCK]]:
; CHECK:  [[SCALAR_PH]]:
; CHECK:  [[LOOP_HEADER:.*]]:
; CHECK:    br i1 [[EC:%.*]], label %[[LOOP_LATCH:.*]], label %[[EXIT:.*]]
; CHECK:  [[LOOP_LATCH]]:
; CHECK:    br label %[[LOOP_HEADER]], !llvm.loop [[LOOP8:![0-9]+]]
; CHECK:  [[EXIT]]:
;
entry:
  br label %loop.header

loop.header:
  %iv = phi i32 [ 0, %entry ], [ %iv.next, %loop.latch ]
  %ec = icmp slt i32 %iv, %n
  br i1 %ec, label %loop.latch, label %exit

loop.latch:
  %idx = sext i32 %iv to i64
  %gep = getelementptr inbounds i16, ptr %p, i64 %idx
  store i16 0, ptr %gep, align 2
  %iv.next = add nsw i32 %iv, 1
  br label %loop.header

exit:
  ret void
}

!0 = !{!"function_entry_count", i64 1000}
;.
; CHECK: [[PROF0]] = !{!"function_entry_count", i64 1000}
; CHECK: [[LOOP1]] = distinct !{[[LOOP1]], [[META2:![0-9]+]], [[META3:![0-9]+]]}
; CHECK: [[META2]] = !{!"llvm.loop.isvectorized", i32 1}
; CHECK: [[META3]] = !{!"llvm.loop.unroll.runtime.disable"}
; CHECK: [[LOOP4]] = distinct !{[[LOOP4]], [[META3]], [[META2]]}
; CHECK: [[LOOP5]] = distinct !{[[LOOP5]], [[META2]], [[META3]]}
; CHECK: [[LOOP6]] = distinct !{[[LOOP6]], [[META2]]}
; CHECK: [[LOOP7]] = distinct !{[[LOOP7]], [[META2]], [[META3]]}
; CHECK: [[LOOP8]] = distinct !{[[LOOP8]], [[META3]], [[META2]]}
;.
