; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --version 6
; RUN: opt < %s -passes='cgscc(inline,function(verify<domtree>))' -debug-pass-manager -S 2>&1 | FileCheck %s

; Verify that the DominatorTree is successfully updated and preserved in a valid state
; by the internalized batch pass during the single-block shortcut path.
; CHECK: Running pass: InlinerPass
; CHECK-NOT: Invalidating analysis: DominatorTreeAnalysis
; CHECK: Running pass: DominatorTreeVerifierPass

define internal i32 @callee_leaf() alwaysinline {
entry:
  ret i32 42
}

define i32 @caller_invoke_leaf() personality ptr @__gxx_personality_v0 {
; CHECK-LABEL: define i32 @caller_invoke_leaf() personality ptr @__gxx_personality_v0 {
; CHECK-NEXT:  [[ENTRY:.*:]]
; CHECK-NEXT:    br label %[[NORMAL:.*]]
; CHECK:       [[NORMAL]]:
; CHECK-NEXT:    ret i32 42
; CHECK:       [[LPAD:.*:]]
; CHECK-NEXT:    [[LP:%.*]] = landingpad { ptr, i32 }
; CHECK-NEXT:            cleanup
; CHECK-NEXT:    ret i32 0
;
entry:
  %res = invoke i32 @callee_leaf()
  to label %normal unwind label %lpad

normal:
  ret i32 %res

lpad:
  %lp = landingpad { ptr, i32 }
  cleanup
  ret i32 0
}

declare i32 @__gxx_personality_v0(...)
