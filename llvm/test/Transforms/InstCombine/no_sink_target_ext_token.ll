; NOTE: Assertions have been autogenerated by utils/update_test_checks.py
; RUN: opt -passes=instcombine -S < %s | FileCheck %s

; This test ensures that the token-like resource handle type prevents instcombine from
; moving the instruction through control-flow, it would otherwise be sunk into the
; successor block

define void @no_sink_dx_handle(i1 %c, i32 %index, float %data) {
; CHECK-LABEL: @no_sink_dx_handle(
; CHECK-NEXT:  entry:
; CHECK-NEXT:    [[H:%.*]] = call target("dx.RawBuffer", float, 1, 0, 0) @llvm.dx.resource.handlefrombinding.tdx.RawBuffer_f32_1_0_0t(i32 0, i32 0, i32 1, i32 0, ptr null)
; CHECK-NEXT:    br i1 [[C:%.*]], label [[USE:%.*]], label [[EXIT:%.*]]
; CHECK:       use:
; CHECK-NEXT:    call void @llvm.dx.resource.store.rawbuffer.tdx.RawBuffer_f32_1_0_0t.f32(target("dx.RawBuffer", float, 1, 0, 0) [[H]], i32 [[INDEX:%.*]], i32 0, float [[DATA:%.*]])
; CHECK-NEXT:    br label [[EXIT]]
; CHECK:       exit:
; CHECK-NEXT:    ret void
;
entry:
  %h = call target("dx.RawBuffer", float, 1, 0, 0) @llvm.dx.resource.handlefrombinding.tdx.RawBuffer_f32_1_0_0t(i32 0, i32 0, i32 1, i32 0, ptr null)
  br i1 %c, label %use, label %exit

use:
  call void @llvm.dx.resource.store.rawbuffer.tdx.RawBuffer_f32_1_0_0t.f32(target("dx.RawBuffer", float, 1, 0, 0) %h, i32 %index, i32 0, float %data)
  br label %exit

exit:
  ret void
}
