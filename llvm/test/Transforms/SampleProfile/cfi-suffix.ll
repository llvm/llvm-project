; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --version 6
; In the ThinLTO backend, LowerTypeTests renames the body of a function that is
; a CFI jump table member from foo to foo.cfi. Profiles are keyed by the
; original name, so the suffix must be ignored when looking up samples, also
; when it follows the suffix of a promoted local function.
; RUN: opt < %s -passes=sample-profile -sample-profile-file=%S/Inputs/cfi-suffix.prof -S | FileCheck %s

define void @foo.cfi() #0 !dbg !4 {
; CHECK-LABEL: define void @foo.cfi(
; CHECK-SAME: ) #[[ATTR0:[0-9]+]] !dbg [[DBG34:![0-9]+]] !prof [[PROF36:![0-9]+]] {
; CHECK-NEXT:  [[ENTRY:.*:]]
; CHECK-NEXT:    ret void, !dbg [[DBG37:![0-9]+]]
;
entry:
  ret void, !dbg !9
}

define void @bar.llvm.1234.cfi() #0 !dbg !10 {
; CHECK-LABEL: define void @bar.llvm.1234.cfi(
; CHECK-SAME: ) #[[ATTR0]] !dbg [[DBG38:![0-9]+]] !prof [[PROF39:![0-9]+]] {
; CHECK-NEXT:  [[ENTRY:.*:]]
; CHECK-NEXT:    ret void, !dbg [[DBG40:![0-9]+]]
;
entry:
  ret void, !dbg !11
}


attributes #0 = {"use-sample-profile" "sample-profile-suffix-elision-policy"="selected"}

!llvm.dbg.cu = !{!0}
!llvm.module.flags = !{!6, !7}

!0 = distinct !DICompileUnit(language: DW_LANG_C99, file: !1, producer: "clang", isOptimized: false, runtimeVersion: 0, emissionKind: LineTablesOnly, enums: !2, retainedTypes: !2, globals: !2, imports: !2)
!1 = !DIFile(filename: "cfi-suffix.c", directory: ".")
!2 = !{}
!4 = distinct !DISubprogram(name: "foo", scope: !1, file: !1, line: 1, type: !5, isLocal: false, isDefinition: true, scopeLine: 1, isOptimized: false, unit: !0, retainedNodes: !2)
!5 = !DISubroutineType(types: !2)
!6 = !{i32 2, !"Dwarf Version", i32 4}
!7 = !{i32 2, !"Debug Info Version", i32 3}
!9 = !DILocation(line: 1, column: 15, scope: !4)
!10 = distinct !DISubprogram(name: "bar", scope: !1, file: !1, line: 3, type: !5, isLocal: false, isDefinition: true, scopeLine: 3, isOptimized: false, unit: !0, retainedNodes: !2)
!11 = !DILocation(line: 3, column: 15, scope: !10)
;.
; CHECK: [[META0:![0-9]+]] = distinct !DICompileUnit(language: DW_LANG_C99, file: [[META1:![0-9]+]], producer: "clang", isOptimized: false, runtimeVersion: 0, emissionKind: LineTablesOnly, enums: [[META2:![0-9]+]], retainedTypes: [[META2]], globals: [[META2]], imports: [[META2]])
; CHECK: [[META1]] = !DIFile(filename: "{{.*}}cfi-suffix.c", directory: {{.*}})
; CHECK: [[META2]] = !{}
; CHECK: [[DBG34]] = distinct !DISubprogram(name: "foo", scope: [[META1]], file: [[META1]], line: 1, type: [[META35:![0-9]+]], scopeLine: 1, spFlags: DISPFlagDefinition, unit: [[META0]], retainedNodes: [[META2]])
; CHECK: [[META35]] = !DISubroutineType(types: [[META2]])
; CHECK: [[PROF36]] = !{!"function_entry_count", i64 1001}
; CHECK: [[DBG37]] = !DILocation(line: 1, column: 15, scope: [[DBG34]])
; CHECK: [[DBG38]] = distinct !DISubprogram(name: "bar", scope: [[META1]], file: [[META1]], line: 3, type: [[META35]], scopeLine: 3, spFlags: DISPFlagDefinition, unit: [[META0]], retainedNodes: [[META2]])
; CHECK: [[PROF39]] = !{!"function_entry_count", i64 2001}
; CHECK: [[DBG40]] = !DILocation(line: 3, column: 15, scope: [[DBG38]])
;.
