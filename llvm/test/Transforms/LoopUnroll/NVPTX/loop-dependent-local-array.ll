; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --version 6
; RUN: opt < %s -passes='loop-unroll,simplifycfg,sroa' \
; RUN:   -unroll-partial-threshold=0 -unroll-max-iteration-count-to-analyze=0 \
; RUN:   -S | FileCheck %s

target triple = "nvptx64-nvidia-cuda"

; A loop with an IV dependent store to an alloca should be fully unrolled.
define void @loop_computable_alloca(ptr %out) {
; CHECK-LABEL: define void @loop_computable_alloca(
; CHECK-SAME: ptr [[OUT:%.*]]) {
; CHECK-NEXT:  [[EXIT:.*:]]
; CHECK-NEXT:    store i32 0, ptr [[OUT]], align 4
; CHECK-NEXT:    ret void
;
entry:
  %arr = alloca [64 x i32], align 4
  br label %loop

loop:
  %iv = phi i32 [ 0, %entry ], [ %iv.next, %loop ]
  %ptr = getelementptr inbounds [64 x i32], ptr %arr, i32 0, i32 %iv
  store i32 %iv, ptr %ptr, align 4
  %iv.next = add nuw nsw i32 %iv, 1
  %exitcond = icmp eq i32 %iv.next, 64
  br i1 %exitcond, label %exit, label %loop

exit:
  %val = load i32, ptr %arr, align 4
  store i32 %val, ptr %out, align 4
  ret void
}

; A loop without any alloca access should not get the threshold boost.
define void @loop_computable_no_alloca(ptr %src, ptr %dst) {
; CHECK-LABEL: define void @loop_computable_no_alloca(
; CHECK-SAME: ptr [[SRC:%.*]], ptr [[DST:%.*]]) {
; CHECK-NEXT:  [[ENTRY:.*]]:
; CHECK-NEXT:    br label %[[LOOP:.*]]
; CHECK:       [[LOOP]]:
; CHECK-NEXT:    [[IV:%.*]] = phi i32 [ 0, %[[ENTRY]] ], [ [[IV_NEXT:%.*]], %[[LOOP]] ]
; CHECK-NEXT:    [[IDX:%.*]] = sext i32 [[IV]] to i64
; CHECK-NEXT:    [[GEP:%.*]] = getelementptr inbounds i32, ptr [[SRC]], i64 [[IDX]]
; CHECK-NEXT:    [[VAL:%.*]] = load i32, ptr [[GEP]], align 4
; CHECK-NEXT:    [[GEP2:%.*]] = getelementptr inbounds i32, ptr [[DST]], i64 [[IDX]]
; CHECK-NEXT:    store i32 [[VAL]], ptr [[GEP2]], align 4
; CHECK-NEXT:    [[IV_NEXT]] = add nuw nsw i32 [[IV]], 1
; CHECK-NEXT:    [[EXITCOND:%.*]] = icmp eq i32 [[IV_NEXT]], 32
; CHECK-NEXT:    br i1 [[EXITCOND]], label %[[EXIT:.*]], label %[[LOOP]]
; CHECK:       [[EXIT]]:
; CHECK-NEXT:    ret void
;
entry:
  br label %loop

loop:
  %iv = phi i32 [ 0, %entry ], [ %iv.next, %loop ]
  %idx = sext i32 %iv to i64
  %gep = getelementptr inbounds i32, ptr %src, i64 %idx
  %val = load i32, ptr %gep, align 4
  %gep2 = getelementptr inbounds i32, ptr %dst, i64 %idx
  store i32 %val, ptr %gep2, align 4
  %iv.next = add nuw nsw i32 %iv, 1
  %exitcond = icmp eq i32 %iv.next, 32
  br i1 %exitcond, label %exit, label %loop

exit:
  ret void
}


; A loop with an SCEV::LoopVariant store to an alloca should be fully unrolled.
define i32 @loop_variant_alloca() {
; CHECK-LABEL: define i32 @loop_variant_alloca() {
; CHECK-NEXT:  [[EXIT:.*:]]
; CHECK-NEXT:    ret i32 62
;
entry:
  %arr = alloca [64 x i32], align 4
  br label %loop

loop:
  %iv = phi i32 [ 0, %entry ], [ %iv.next, %loop ]
  %idx = and i32 %iv, 1
  %ptr = getelementptr inbounds [64 x i32], ptr %arr, i32 0, i32 %idx
  store i32 %iv, ptr %ptr, align 4
  %iv.next = add nuw nsw i32 %iv, 1
  %exitcond = icmp eq i32 %iv.next, 64
  br i1 %exitcond, label %exit, label %loop

exit:
  %result = load i32, ptr %arr, align 4
  ret i32 %result
}

;.
; CHECK: [[LOOP0]] = distinct !{[[LOOP0]], [[META1:![0-9]+]]}
; CHECK: [[META1]] = !{!"llvm.loop.unroll.disable"}
;.
