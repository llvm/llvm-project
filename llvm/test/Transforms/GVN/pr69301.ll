; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --version 6
; RUN: opt < %s -passes=gvn -S | FileCheck %s --check-prefixes=CHECK,MDEP
; RUN: opt < %s -passes='gvn<memoryssa>' -S | FileCheck %s --check-prefixes=CHECK,MSSA

; Make sure we don't have use-after-free due to dangling values in
; select available value.

define i64 @test(i1 %c, ptr %p) {
; MDEP-LABEL: define i64 @test(
; MDEP-SAME: i1 [[C:%.*]], ptr [[P:%.*]]) {
; MDEP-NEXT:  [[ENTRY:.*]]:
; MDEP-NEXT:    br label %[[LOOP:.*]]
; MDEP:       [[LOOP]]:
; MDEP-NEXT:    [[IV:%.*]] = phi i64 [ 0, %[[ENTRY]] ], [ [[ADD:%.*]], %[[LOOP_LATCH:.*]] ]
; MDEP-NEXT:    [[PTR_IV:%.*]] = phi ptr [ [[P]], %[[ENTRY]] ], [ [[SELECT:%.*]], %[[LOOP_LATCH]] ]
; MDEP-NEXT:    [[ICMP:%.*]] = icmp eq i64 [[IV]], 0
; MDEP-NEXT:    br i1 [[ICMP]], label %[[LOOP_EXIT_CRIT_EDGE:.*]], label %[[LOOP_CONT:.*]]
; MDEP:       [[LOOP_EXIT_CRIT_EDGE]]:
; MDEP-NEXT:    [[RES_PRE:%.*]] = load i64, ptr [[PTR_IV]], align 8
; MDEP-NEXT:    br label %[[EXIT:.*]]
; MDEP:       [[LOOP_CONT]]:
; MDEP-NEXT:    [[ADD]] = add i64 [[IV]], -1
; MDEP-NEXT:    [[RES_PRE1:%.*]] = load i64, ptr [[PTR_IV]], align 8
; MDEP-NEXT:    br i1 [[C]], label %[[EXITSPLIT:.*]], label %[[LOOP_LATCH]]
; MDEP:       [[LOOP_LATCH]]:
; MDEP-NEXT:    [[LOAD6:%.*]] = load i64, ptr [[P]], align 8
; MDEP-NEXT:    [[ICMP7:%.*]] = icmp ugt i64 [[RES_PRE1]], [[LOAD6]]
; MDEP-NEXT:    [[SELECT]] = select i1 [[ICMP7]], ptr [[PTR_IV]], ptr [[P]]
; MDEP-NEXT:    [[TMP0:%.*]] = select i1 [[ICMP7]], i64 [[RES_PRE1]], i64 [[LOAD6]]
; MDEP-NEXT:    br label %[[LOOP]]
; MDEP:       [[EXITSPLIT]]:
; MDEP-NEXT:    br label %[[EXIT]]
; MDEP:       [[EXIT]]:
; MDEP-NEXT:    [[RES:%.*]] = phi i64 [ [[RES_PRE1]], %[[EXITSPLIT]] ], [ [[RES_PRE]], %[[LOOP_EXIT_CRIT_EDGE]] ]
; MDEP-NEXT:    ret i64 [[RES]]
;
; MSSA-LABEL: define i64 @test(
; MSSA-SAME: i1 [[C:%.*]], ptr [[P:%.*]]) {
; MSSA-NEXT:  [[ENTRY:.*]]:
; MSSA-NEXT:    br label %[[LOOP:.*]]
; MSSA:       [[LOOP]]:
; MSSA-NEXT:    [[IV:%.*]] = phi i64 [ 0, %[[ENTRY]] ], [ [[ADD:%.*]], %[[LOOP_LATCH:.*]] ]
; MSSA-NEXT:    [[PTR_IV:%.*]] = phi ptr [ [[P]], %[[ENTRY]] ], [ [[SELECT:%.*]], %[[LOOP_LATCH]] ]
; MSSA-NEXT:    [[ICMP:%.*]] = icmp eq i64 [[IV]], 0
; MSSA-NEXT:    br i1 [[ICMP]], label %[[EXIT:.*]], label %[[LOOP_CONT:.*]]
; MSSA:       [[LOOP_CONT]]:
; MSSA-NEXT:    [[ADD]] = add i64 [[IV]], -1
; MSSA-NEXT:    br i1 [[C]], label %[[EXIT]], label %[[LOOP_LATCH]]
; MSSA:       [[LOOP_LATCH]]:
; MSSA-NEXT:    [[LOAD:%.*]] = load i64, ptr [[PTR_IV]], align 8
; MSSA-NEXT:    [[LOAD6:%.*]] = load i64, ptr [[P]], align 8
; MSSA-NEXT:    [[ICMP7:%.*]] = icmp ugt i64 [[LOAD]], [[LOAD6]]
; MSSA-NEXT:    [[SELECT]] = select i1 [[ICMP7]], ptr [[PTR_IV]], ptr [[P]]
; MSSA-NEXT:    br label %[[LOOP]]
; MSSA:       [[EXIT]]:
; MSSA-NEXT:    [[RES:%.*]] = load i64, ptr [[PTR_IV]], align 8
; MSSA-NEXT:    ret i64 [[RES]]
;
entry:
  br label %loop

loop:
  %iv = phi i64 [ 0, %entry ], [ %add, %loop.latch ]
  %ptr.iv = phi ptr [ %p, %entry ], [ %select, %loop.latch ]
  %icmp = icmp eq i64 %iv, 0
  br i1 %icmp, label %exit, label %loop.cont

loop.cont:
  %add = add i64 %iv, -1
  br i1 %c, label %exit, label %loop.latch

loop.latch:
  %load = load i64, ptr %ptr.iv, align 8
  %load6 = load i64, ptr %p, align 8
  %icmp7 = icmp ugt i64 %load, %load6
  %select = select i1 %icmp7, ptr %ptr.iv, ptr %p
  br label %loop

exit:
  %res = load i64, ptr %ptr.iv, align 8
  ret i64 %res
}
;; NOTE: These prefixes are unused and the list is autogenerated. Do not add tests below this line:
; CHECK: {{.*}}
