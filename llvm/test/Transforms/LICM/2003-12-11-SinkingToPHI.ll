; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --version 6
; RUN: opt -S < %s -passes=licm | FileCheck %s

define i32 @main() {
; CHECK-LABEL: define i32 @main() {
; CHECK-NEXT:  [[ENTRY:.*:]]
; CHECK-NEXT:    br label %[[LOOP:.*]]
; CHECK:       [[LOOP]]:
; CHECK-NEXT:    br i1 true, label %[[LOOPCONT:.*]], label %[[OUT_SPLIT_LOOP_EXIT1:.*]]
; CHECK:       [[LOOPCONT]]:
; CHECK-NEXT:    br i1 true, label %[[OUT_SPLIT_LOOP_EXIT:.*]], label %[[LOOP]]
; CHECK:       [[OUT_SPLIT_LOOP_EXIT]]:
; CHECK-NEXT:    [[X_LE:%.*]] = add i32 1, 0
; CHECK-NEXT:    br label %[[OUT:.*]]
; CHECK:       [[OUT_SPLIT_LOOP_EXIT1]]:
; CHECK-NEXT:    [[V_PH2:%.*]] = phi i32 [ 2, %[[LOOP]] ]
; CHECK-NEXT:    br label %[[OUT]]
; CHECK:       [[OUT]]:
; CHECK-NEXT:    [[V:%.*]] = phi i32 [ [[X_LE]], %[[OUT_SPLIT_LOOP_EXIT]] ], [ [[V_PH2]], %[[OUT_SPLIT_LOOP_EXIT1]] ]
; CHECK-NEXT:    [[V2:%.*]] = sub i32 [[V]], 1
; CHECK-NEXT:    ret i32 [[V2]]
;
entry:
  br label %Loop
Loop:		; preds = %LoopCont, %entry
  br i1 true, label %LoopCont, label %Out
LoopCont:		; preds = %Loop
  %X = add i32 1, 0		; <i32> [#uses=1]
  br i1 true, label %Out, label %Loop
Out:		; preds = %LoopCont, %Loop
  %V = phi i32 [ 2, %Loop ], [ %X, %LoopCont ]		; <i32> [#uses=1]
  %V2 = sub i32 %V, 1		; <i32> [#uses=1]
  ret i32 %V2
}
