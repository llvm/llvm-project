; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --version 6
; RUN: opt -mtriple=thumbv8.1m.main -mattr=+mve.fp -passes=simplifycfg -simplifycfg-require-and-preserve-domtree=1 -S < %s | FileCheck %s

define <4 x float> @speculate_v4f32(i32 %n, <4 x float> %a, <4 x float> %b, i1 %other) {
; CHECK-LABEL: define <4 x float> @speculate_v4f32(
; CHECK-SAME: i32 [[N:%.*]], <4 x float> [[A:%.*]], <4 x float> [[B:%.*]], i1 [[OTHER:%.*]]) #[[ATTR0:[0-9]+]] {
; CHECK-NEXT:  [[ENTRY:.*]]:
; CHECK-NEXT:    br i1 [[OTHER]], label %[[MERGE:.*]], label %[[GUARDED:.*]]
; CHECK:       [[GUARDED]]:
; CHECK-NEXT:    [[CMP:%.*]] = icmp sgt i32 [[N]], 0
; CHECK-NEXT:    [[ADD:%.*]] = fadd <4 x float> [[A]], [[B]]
; CHECK-NEXT:    [[SPEC_SELECT:%.*]] = select i1 [[CMP]], <4 x float> [[ADD]], <4 x float> [[B]]
; CHECK-NEXT:    br label %[[MERGE]]
; CHECK:       [[MERGE]]:
; CHECK-NEXT:    [[R:%.*]] = phi <4 x float> [ zeroinitializer, %[[ENTRY]] ], [ [[SPEC_SELECT]], %[[GUARDED]] ]
; CHECK-NEXT:    ret <4 x float> [[R]]
;
entry:
  br i1 %other, label %merge, label %guarded

guarded:
  %cmp = icmp sgt i32 %n, 0
  br i1 %cmp, label %then, label %merge

then:
  %add = fadd <4 x float> %a, %b
  br label %merge

merge:
  %r = phi <4 x float> [ zeroinitializer, %entry ], [ %add, %then ], [ %b, %guarded ]
  ret <4 x float> %r
}

define <8 x half> @speculate_v8f16(i32 %n, <8 x half> %a, <8 x half> %b, i1 %other) {
; CHECK-LABEL: define <8 x half> @speculate_v8f16(
; CHECK-SAME: i32 [[N:%.*]], <8 x half> [[A:%.*]], <8 x half> [[B:%.*]], i1 [[OTHER:%.*]]) #[[ATTR0]] {
; CHECK-NEXT:  [[ENTRY:.*]]:
; CHECK-NEXT:    br i1 [[OTHER]], label %[[MERGE:.*]], label %[[GUARDED:.*]]
; CHECK:       [[GUARDED]]:
; CHECK-NEXT:    [[CMP:%.*]] = icmp sgt i32 [[N]], 0
; CHECK-NEXT:    [[ADD:%.*]] = fadd <8 x half> [[A]], [[B]]
; CHECK-NEXT:    [[SPEC_SELECT:%.*]] = select i1 [[CMP]], <8 x half> [[ADD]], <8 x half> [[B]]
; CHECK-NEXT:    br label %[[MERGE]]
; CHECK:       [[MERGE]]:
; CHECK-NEXT:    [[R:%.*]] = phi <8 x half> [ zeroinitializer, %[[ENTRY]] ], [ [[SPEC_SELECT]], %[[GUARDED]] ]
; CHECK-NEXT:    ret <8 x half> [[R]]
;
entry:
  br i1 %other, label %merge, label %guarded

guarded:
  %cmp = icmp sgt i32 %n, 0
  br i1 %cmp, label %then, label %merge

then:
  %add = fadd <8 x half> %a, %b
  br label %merge

merge:
  %r = phi <8 x half> [ zeroinitializer, %entry ], [ %add, %then ], [ %b, %guarded ]
  ret <8 x half> %r
}

define float @speculate_f32(i32 %n, float %a, float %b, i1 %other) {
; CHECK-LABEL: define float @speculate_f32(
; CHECK-SAME: i32 [[N:%.*]], float [[A:%.*]], float [[B:%.*]], i1 [[OTHER:%.*]]) #[[ATTR0]] {
; CHECK-NEXT:  [[ENTRY:.*:]]
; CHECK-NEXT:    [[CMP:%.*]] = icmp sgt i32 [[N]], 0
; CHECK-NEXT:    [[ADD:%.*]] = fadd float [[A]], [[B]]
; CHECK-NEXT:    [[SPEC_SELECT:%.*]] = select i1 [[CMP]], float [[ADD]], float [[B]]
; CHECK-NEXT:    [[R:%.*]] = select i1 [[OTHER]], float 0.000000e+00, float [[SPEC_SELECT]]
; CHECK-NEXT:    ret float [[R]]
;
entry:
  br i1 %other, label %merge, label %guarded

guarded:
  %cmp = icmp sgt i32 %n, 0
  br i1 %cmp, label %then, label %merge

then:
  %add = fadd float %a, %b
  br label %merge

merge:
  %r = phi float [ 0.0, %entry ], [ %add, %then ], [ %b, %guarded ]
  ret float %r
}
