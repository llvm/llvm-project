; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --version 6
; RUN: opt -mtriple=riscv64 -mattr=+v -passes=simplifycfg -simplifycfg-require-and-preserve-domtree=1 -S < %s | FileCheck %s

define <4 x i1> @speculate_v4i1(i32 %n, <4 x i1> %a, <4 x i1> %b, i1 %other) {
; CHECK-LABEL: define <4 x i1> @speculate_v4i1(
; CHECK-SAME: i32 [[N:%.*]], <4 x i1> [[A:%.*]], <4 x i1> [[B:%.*]], i1 [[OTHER:%.*]]) #[[ATTR0:[0-9]+]] {
; CHECK-NEXT:  [[ENTRY:.*]]:
; CHECK-NEXT:    br i1 [[OTHER]], label %[[MERGE:.*]], label %[[GUARDED:.*]]
; CHECK:       [[GUARDED]]:
; CHECK-NEXT:    [[CMP:%.*]] = icmp sgt i32 [[N]], 0
; CHECK-NEXT:    br i1 [[CMP]], label %[[THEN:.*]], label %[[MERGE]]
; CHECK:       [[THEN]]:
; CHECK-NEXT:    [[AND:%.*]] = and <4 x i1> [[A]], [[B]]
; CHECK-NEXT:    br label %[[MERGE]]
; CHECK:       [[MERGE]]:
; CHECK-NEXT:    [[R:%.*]] = phi <4 x i1> [ zeroinitializer, %[[ENTRY]] ], [ [[AND]], %[[THEN]] ], [ [[B]], %[[GUARDED]] ]
; CHECK-NEXT:    ret <4 x i1> [[R]]
;
entry:
  br i1 %other, label %merge, label %guarded

guarded:
  %cmp = icmp sgt i32 %n, 0
  br i1 %cmp, label %then, label %merge

then:
  %and = and <4 x i1> %a, %b
  br label %merge

merge:
  %r = phi <4 x i1> [ zeroinitializer, %entry ], [ %and, %then ], [ %b, %guarded ]
  ret <4 x i1> %r
}

define <8 x i1> @speculate_v8i1(i32 %n, <8 x i1> %a, <8 x i1> %b, i1 %other) {
; CHECK-LABEL: define <8 x i1> @speculate_v8i1(
; CHECK-SAME: i32 [[N:%.*]], <8 x i1> [[A:%.*]], <8 x i1> [[B:%.*]], i1 [[OTHER:%.*]]) #[[ATTR0]] {
; CHECK-NEXT:  [[ENTRY:.*]]:
; CHECK-NEXT:    br i1 [[OTHER]], label %[[MERGE:.*]], label %[[GUARDED:.*]]
; CHECK:       [[GUARDED]]:
; CHECK-NEXT:    [[CMP:%.*]] = icmp sgt i32 [[N]], 0
; CHECK-NEXT:    br i1 [[CMP]], label %[[THEN:.*]], label %[[MERGE]]
; CHECK:       [[THEN]]:
; CHECK-NEXT:    [[AND:%.*]] = and <8 x i1> [[A]], [[B]]
; CHECK-NEXT:    br label %[[MERGE]]
; CHECK:       [[MERGE]]:
; CHECK-NEXT:    [[R:%.*]] = phi <8 x i1> [ zeroinitializer, %[[ENTRY]] ], [ [[AND]], %[[THEN]] ], [ [[B]], %[[GUARDED]] ]
; CHECK-NEXT:    ret <8 x i1> [[R]]
;
entry:
  br i1 %other, label %merge, label %guarded

guarded:
  %cmp = icmp sgt i32 %n, 0
  br i1 %cmp, label %then, label %merge

then:
  %and = and <8 x i1> %a, %b
  br label %merge

merge:
  %r = phi <8 x i1> [ zeroinitializer, %entry ], [ %and, %then ], [ %b, %guarded ]
  ret <8 x i1> %r
}

define <4 x float> @speculate_v4f32(i32 %n, <4 x float> %a, <4 x float> %b, i1 %other) {
; CHECK-LABEL: define <4 x float> @speculate_v4f32(
; CHECK-SAME: i32 [[N:%.*]], <4 x float> [[A:%.*]], <4 x float> [[B:%.*]], i1 [[OTHER:%.*]]) #[[ATTR0]] {
; CHECK-NEXT:  [[ENTRY:.*:]]
; CHECK-NEXT:    [[CMP:%.*]] = icmp sgt i32 [[N]], 0
; CHECK-NEXT:    [[ADD:%.*]] = fadd <4 x float> [[A]], [[B]]
; CHECK-NEXT:    [[SPEC_SELECT:%.*]] = select i1 [[CMP]], <4 x float> [[ADD]], <4 x float> [[B]]
; CHECK-NEXT:    [[R:%.*]] = select i1 [[OTHER]], <4 x float> zeroinitializer, <4 x float> [[SPEC_SELECT]]
; CHECK-NEXT:    ret <4 x float> [[R]]
;
entry:
  br i1 %other, label %merge, label %guarded

guarded:
  %cmp = icmp sgt i32 %n, 0
  br i1 %cmp, label %then, label %merge

then:
  %add = fadd <4 x float> %a, %b
  br label %merge

merge:
  %r = phi <4 x float> [ zeroinitializer, %entry ], [ %add, %then ], [ %b, %guarded ]
  ret <4 x float> %r
}
