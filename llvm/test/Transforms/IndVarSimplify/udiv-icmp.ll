; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --version 6
; RUN: opt < %s -S -passes='loop(indvars)' | FileCheck %s

define i1 @quotient_eq_zero(i64 %a, i64 %b) {
; CHECK-LABEL: define i1 @quotient_eq_zero(
; CHECK-SAME: i64 [[A:%.*]], i64 [[B:%.*]]) {
; CHECK-NEXT:  [[ENTRY:.*]]:
; CHECK-NEXT:    [[Q:%.*]] = udiv i64 [[A]], [[B]]
; CHECK-NEXT:    [[GUARD:%.*]] = icmp eq i64 [[Q]], 0
; CHECK-NEXT:    br i1 [[GUARD]], label %[[LOOP_PREHEADER:.*]], label %[[EXIT:.*]]
; CHECK:       [[LOOP_PREHEADER]]:
; CHECK-NEXT:    br label %[[LOOP:.*]]
; CHECK:       [[LOOP]]:
; CHECK-NEXT:    br i1 false, label %[[LOOP]], label %[[EXIT_LOOPEXIT:.*]]
; CHECK:       [[EXIT_LOOPEXIT]]:
; CHECK-NEXT:    [[CMP_LCSSA:%.*]] = phi i1 [ true, %[[LOOP]] ]
; CHECK-NEXT:    br label %[[EXIT]]
; CHECK:       [[EXIT]]:
; CHECK-NEXT:    [[RESULT:%.*]] = phi i1 [ false, %[[ENTRY]] ], [ [[CMP_LCSSA]], %[[EXIT_LOOPEXIT]] ]
; CHECK-NEXT:    ret i1 [[RESULT]]
;
entry:
  %q = udiv i64 %a, %b
  %guard = icmp eq i64 %q, 0
  br i1 %guard, label %loop, label %exit

loop:
  %same = phi i64 [ %a, %entry ], [ %same.next, %loop ]
  %cmp = icmp ult i64 %same, %b
  %same.next = add i64 %same, 1
  br i1 false, label %loop, label %exit

exit:
  %result = phi i1 [ false, %entry ], [ %cmp, %loop ]
  ret i1 %result
}

define i1 @quotient_ule_zero(i64 %a, i64 %b) {
; CHECK-LABEL: define i1 @quotient_ule_zero(
; CHECK-SAME: i64 [[A:%.*]], i64 [[B:%.*]]) {
; CHECK-NEXT:  [[ENTRY:.*]]:
; CHECK-NEXT:    [[Q:%.*]] = udiv i64 [[A]], [[B]]
; CHECK-NEXT:    [[GUARD:%.*]] = icmp ule i64 [[Q]], 0
; CHECK-NEXT:    br i1 [[GUARD]], label %[[LOOP_PREHEADER:.*]], label %[[EXIT:.*]]
; CHECK:       [[LOOP_PREHEADER]]:
; CHECK-NEXT:    br label %[[LOOP:.*]]
; CHECK:       [[LOOP]]:
; CHECK-NEXT:    br i1 false, label %[[LOOP]], label %[[EXIT_LOOPEXIT:.*]]
; CHECK:       [[EXIT_LOOPEXIT]]:
; CHECK-NEXT:    [[CMP_LCSSA:%.*]] = phi i1 [ true, %[[LOOP]] ]
; CHECK-NEXT:    br label %[[EXIT]]
; CHECK:       [[EXIT]]:
; CHECK-NEXT:    [[RESULT:%.*]] = phi i1 [ false, %[[ENTRY]] ], [ [[CMP_LCSSA]], %[[EXIT_LOOPEXIT]] ]
; CHECK-NEXT:    ret i1 [[RESULT]]
;
entry:
  %q = udiv i64 %a, %b
  %guard = icmp ule i64 %q, 0
  br i1 %guard, label %loop, label %exit

loop:
  %same = phi i64 [ %a, %entry ], [ %same.next, %loop ]
  %cmp = icmp ult i64 %same, %b
  %same.next = add i64 %same, 1
  br i1 false, label %loop, label %exit

exit:
  %result = phi i1 [ false, %entry ], [ %cmp, %loop ]
  ret i1 %result
}

define i1 @quotient_ult_one(i64 %a, i64 %b) {
; CHECK-LABEL: define i1 @quotient_ult_one(
; CHECK-SAME: i64 [[A:%.*]], i64 [[B:%.*]]) {
; CHECK-NEXT:  [[ENTRY:.*]]:
; CHECK-NEXT:    [[Q:%.*]] = udiv i64 [[A]], [[B]]
; CHECK-NEXT:    [[GUARD:%.*]] = icmp ult i64 [[Q]], 1
; CHECK-NEXT:    br i1 [[GUARD]], label %[[LOOP_PREHEADER:.*]], label %[[EXIT:.*]]
; CHECK:       [[LOOP_PREHEADER]]:
; CHECK-NEXT:    br label %[[LOOP:.*]]
; CHECK:       [[LOOP]]:
; CHECK-NEXT:    br i1 false, label %[[LOOP]], label %[[EXIT_LOOPEXIT:.*]]
; CHECK:       [[EXIT_LOOPEXIT]]:
; CHECK-NEXT:    [[CMP_LCSSA:%.*]] = phi i1 [ true, %[[LOOP]] ]
; CHECK-NEXT:    br label %[[EXIT]]
; CHECK:       [[EXIT]]:
; CHECK-NEXT:    [[RESULT:%.*]] = phi i1 [ false, %[[ENTRY]] ], [ [[CMP_LCSSA]], %[[EXIT_LOOPEXIT]] ]
; CHECK-NEXT:    ret i1 [[RESULT]]
;
entry:
  %q = udiv i64 %a, %b
  %guard = icmp ult i64 %q, 1
  br i1 %guard, label %loop, label %exit

loop:
  %same = phi i64 [ %a, %entry ], [ %same.next, %loop ]
  %cmp = icmp ult i64 %same, %b
  %same.next = add i64 %same, 1
  br i1 false, label %loop, label %exit

exit:
  %result = phi i1 [ false, %entry ], [ %cmp, %loop ]
  ret i1 %result
}

define i1 @quotient_ne_zero(i64 %a, i64 %b) {
; CHECK-LABEL: define i1 @quotient_ne_zero(
; CHECK-SAME: i64 [[A:%.*]], i64 [[B:%.*]]) {
; CHECK-NEXT:  [[ENTRY:.*]]:
; CHECK-NEXT:    [[Q:%.*]] = udiv i64 [[A]], [[B]]
; CHECK-NEXT:    [[GUARD:%.*]] = icmp ne i64 [[Q]], 0
; CHECK-NEXT:    br i1 [[GUARD]], label %[[LOOP_PREHEADER:.*]], label %[[EXIT:.*]]
; CHECK:       [[LOOP_PREHEADER]]:
; CHECK-NEXT:    br label %[[LOOP:.*]]
; CHECK:       [[LOOP]]:
; CHECK-NEXT:    br i1 false, label %[[LOOP]], label %[[EXIT_LOOPEXIT:.*]]
; CHECK:       [[EXIT_LOOPEXIT]]:
; CHECK-NEXT:    [[CMP_LCSSA:%.*]] = phi i1 [ true, %[[LOOP]] ]
; CHECK-NEXT:    br label %[[EXIT]]
; CHECK:       [[EXIT]]:
; CHECK-NEXT:    [[RESULT:%.*]] = phi i1 [ false, %[[ENTRY]] ], [ [[CMP_LCSSA]], %[[EXIT_LOOPEXIT]] ]
; CHECK-NEXT:    ret i1 [[RESULT]]
;
entry:
  %q = udiv i64 %a, %b
  %guard = icmp ne i64 %q, 0
  br i1 %guard, label %loop, label %exit

loop:
  %same = phi i64 [ %a, %entry ], [ %same.next, %loop ]
  %cmp = icmp uge i64 %same, %b
  %same.next = add i64 %same, 1
  br i1 false, label %loop, label %exit

exit:
  %result = phi i1 [ false, %entry ], [ %cmp, %loop ]
  ret i1 %result
}

define i1 @quotient_ugt_zero(i64 %a, i64 %b) {
; CHECK-LABEL: define i1 @quotient_ugt_zero(
; CHECK-SAME: i64 [[A:%.*]], i64 [[B:%.*]]) {
; CHECK-NEXT:  [[ENTRY:.*]]:
; CHECK-NEXT:    [[Q:%.*]] = udiv i64 [[A]], [[B]]
; CHECK-NEXT:    [[GUARD:%.*]] = icmp ugt i64 [[Q]], 0
; CHECK-NEXT:    br i1 [[GUARD]], label %[[LOOP_PREHEADER:.*]], label %[[EXIT:.*]]
; CHECK:       [[LOOP_PREHEADER]]:
; CHECK-NEXT:    br label %[[LOOP:.*]]
; CHECK:       [[LOOP]]:
; CHECK-NEXT:    br i1 false, label %[[LOOP]], label %[[EXIT_LOOPEXIT:.*]]
; CHECK:       [[EXIT_LOOPEXIT]]:
; CHECK-NEXT:    [[CMP_LCSSA:%.*]] = phi i1 [ true, %[[LOOP]] ]
; CHECK-NEXT:    br label %[[EXIT]]
; CHECK:       [[EXIT]]:
; CHECK-NEXT:    [[RESULT:%.*]] = phi i1 [ false, %[[ENTRY]] ], [ [[CMP_LCSSA]], %[[EXIT_LOOPEXIT]] ]
; CHECK-NEXT:    ret i1 [[RESULT]]
;
entry:
  %q = udiv i64 %a, %b
  %guard = icmp ugt i64 %q, 0
  br i1 %guard, label %loop, label %exit

loop:
  %same = phi i64 [ %a, %entry ], [ %same.next, %loop ]
  %cmp = icmp uge i64 %same, %b
  %same.next = add i64 %same, 1
  br i1 false, label %loop, label %exit

exit:
  %result = phi i1 [ false, %entry ], [ %cmp, %loop ]
  ret i1 %result
}

define i1 @quotient_uge_one(i64 %a, i64 %b) {
; CHECK-LABEL: define i1 @quotient_uge_one(
; CHECK-SAME: i64 [[A:%.*]], i64 [[B:%.*]]) {
; CHECK-NEXT:  [[ENTRY:.*]]:
; CHECK-NEXT:    [[Q:%.*]] = udiv i64 [[A]], [[B]]
; CHECK-NEXT:    [[GUARD:%.*]] = icmp uge i64 [[Q]], 1
; CHECK-NEXT:    br i1 [[GUARD]], label %[[LOOP_PREHEADER:.*]], label %[[EXIT:.*]]
; CHECK:       [[LOOP_PREHEADER]]:
; CHECK-NEXT:    br label %[[LOOP:.*]]
; CHECK:       [[LOOP]]:
; CHECK-NEXT:    br i1 false, label %[[LOOP]], label %[[EXIT_LOOPEXIT:.*]]
; CHECK:       [[EXIT_LOOPEXIT]]:
; CHECK-NEXT:    [[CMP_LCSSA:%.*]] = phi i1 [ true, %[[LOOP]] ]
; CHECK-NEXT:    br label %[[EXIT]]
; CHECK:       [[EXIT]]:
; CHECK-NEXT:    [[RESULT:%.*]] = phi i1 [ false, %[[ENTRY]] ], [ [[CMP_LCSSA]], %[[EXIT_LOOPEXIT]] ]
; CHECK-NEXT:    ret i1 [[RESULT]]
;
entry:
  %q = udiv i64 %a, %b
  %guard = icmp uge i64 %q, 1
  br i1 %guard, label %loop, label %exit

loop:
  %same = phi i64 [ %a, %entry ], [ %same.next, %loop ]
  %cmp = icmp uge i64 %same, %b
  %same.next = add i64 %same, 1
  br i1 false, label %loop, label %exit

exit:
  %result = phi i1 [ false, %entry ], [ %cmp, %loop ]
  ret i1 %result
}

define i1 @constant_on_left(i64 %a, i64 %b) {
; CHECK-LABEL: define i1 @constant_on_left(
; CHECK-SAME: i64 [[A:%.*]], i64 [[B:%.*]]) {
; CHECK-NEXT:  [[ENTRY:.*]]:
; CHECK-NEXT:    [[Q:%.*]] = udiv i64 [[A]], [[B]]
; CHECK-NEXT:    [[GUARD:%.*]] = icmp ult i64 0, [[Q]]
; CHECK-NEXT:    br i1 [[GUARD]], label %[[LOOP_PREHEADER:.*]], label %[[EXIT:.*]]
; CHECK:       [[LOOP_PREHEADER]]:
; CHECK-NEXT:    br label %[[LOOP:.*]]
; CHECK:       [[LOOP]]:
; CHECK-NEXT:    br i1 false, label %[[LOOP]], label %[[EXIT_LOOPEXIT:.*]]
; CHECK:       [[EXIT_LOOPEXIT]]:
; CHECK-NEXT:    [[CMP_LCSSA:%.*]] = phi i1 [ true, %[[LOOP]] ]
; CHECK-NEXT:    br label %[[EXIT]]
; CHECK:       [[EXIT]]:
; CHECK-NEXT:    [[RESULT:%.*]] = phi i1 [ false, %[[ENTRY]] ], [ [[CMP_LCSSA]], %[[EXIT_LOOPEXIT]] ]
; CHECK-NEXT:    ret i1 [[RESULT]]
;
entry:
  %q = udiv i64 %a, %b
  %guard = icmp ult i64 0, %q
  br i1 %guard, label %loop, label %exit

loop:
  %same = phi i64 [ %a, %entry ], [ %same.next, %loop ]
  %cmp = icmp uge i64 %same, %b
  %same.next = add i64 %same, 1
  br i1 false, label %loop, label %exit

exit:
  %result = phi i1 [ false, %entry ], [ %cmp, %loop ]
  ret i1 %result
}
