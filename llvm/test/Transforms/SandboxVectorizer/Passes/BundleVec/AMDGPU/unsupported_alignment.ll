; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --version 6
; RUN: opt -mattr=-unaligned-access-mode,+unaligned-scratch-access -mtriple=amdgpu-amd-amdhsa -passes=sandbox-vectorizer -sbvec-passes="seed-collection<bundle-vec(bottom-up)>" %s -S | FileCheck %s

; We should not be vectorizing this code because a <2 x i8> store with align 1
; is not supported by the target.
define void @unsupported_alignment(ptr %ptr0, i32 %offset) {
; CHECK-LABEL: define void @unsupported_alignment(
; CHECK-SAME: ptr [[PTR0:%.*]], i32 [[OFFSET:%.*]]) #[[ATTR0:[0-9]+]] {
; CHECK-NEXT:    store <2 x i8> <i8 0, i8 1>, ptr [[PTR0]], align 1, !sandboxvec [[META0:![0-9]+]]
; CHECK-NEXT:    ret void
;
  %ptr1 = getelementptr i8, ptr %ptr0, i32 1
  store i8 0, ptr %ptr0, align 1
  store i8 1, ptr %ptr1, align 1
  ret void
}

;.
; CHECK: [[META0]] = distinct !{!"sandboxregion"}
;.
