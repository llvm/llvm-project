; NOTE: Assertions have been autogenerated by utils/update_analyze_test_checks.py UTC_ARGS: --version 6
; RUN: opt -passes='print<scalar-evolution>' -scalar-evolution-classify-expressions=0 -disable-output %s 2>&1 | FileCheck %s

define void @lt_end_is_start_plus_bounded_n(i8 %nraw, i8 %a) {
; CHECK-LABEL: 'lt_end_is_start_plus_bounded_n'
; CHECK-NEXT:  Determining loop execution counts for: @lt_end_is_start_plus_bounded_n
; CHECK-NEXT:  Loop %loop: backedge-taken count is ((-1 * %a) + (((zext i3 (trunc i8 %nraw to i3) to i8) + %a) umax %a))
; CHECK-NEXT:  Loop %loop: constant max backedge-taken count is i8 7
; CHECK-NEXT:  Loop %loop: symbolic max backedge-taken count is ((-1 * %a) + (((zext i3 (trunc i8 %nraw to i3) to i8) + %a) umax %a))
; CHECK-NEXT:  Loop %loop: Trip multiple is 1
;
entry:
  %n = and i8 %nraw, 7
  %end = add i8 %a, %n
  br label %loop

loop:
  %iv = phi i8 [ %a, %entry ], [ %iv.next, %loop ]
  %iv.next = add i8 %iv, 1
  %c = icmp ult i8 %iv, %end
  br i1 %c, label %loop, label %exit

exit:
  ret void
}

define void @slt_end_is_start_plus_bounded_n(i8 %nraw, i8 %a) {
; CHECK-LABEL: 'slt_end_is_start_plus_bounded_n'
; CHECK-NEXT:  Determining loop execution counts for: @slt_end_is_start_plus_bounded_n
; CHECK-NEXT:  Loop %loop: backedge-taken count is ((-1 * %a) + (((zext i3 (trunc i8 %nraw to i3) to i8) + %a)<nsw> smax %a))
; CHECK-NEXT:  Loop %loop: constant max backedge-taken count is i8 7
; CHECK-NEXT:  Loop %loop: symbolic max backedge-taken count is ((-1 * %a) + (((zext i3 (trunc i8 %nraw to i3) to i8) + %a)<nsw> smax %a))
; CHECK-NEXT:  Loop %loop: Trip multiple is 1
;
entry:
  %n = and i8 %nraw, 7
  %end = add nsw i8 %a, %n
  br label %loop

loop:
  %iv = phi i8 [ %a, %entry ], [ %iv.next, %loop ]
  %iv.next = add nsw i8 %iv, 1
  %c = icmp slt i8 %iv, %end
  br i1 %c, label %loop, label %exit

exit:
  ret void
}

define void @lt_end_is_start_plus_bounded_n_stride_3(i8 %nraw, i8 %a) {
; CHECK-LABEL: 'lt_end_is_start_plus_bounded_n_stride_3'
; CHECK-NEXT:  Determining loop execution counts for: @lt_end_is_start_plus_bounded_n_stride_3
; CHECK-NEXT:  Loop %loop: backedge-taken count is ((((-1 * (1 umin ((-1 * %a) + (((zext i3 (trunc i8 %nraw to i3) to i8) + %a)<nuw><nsw> umax %a))))<nuw><nsw> + (-1 * %a) + (((zext i3 (trunc i8 %nraw to i3) to i8) + %a)<nuw><nsw> umax %a)) /u 3) + (1 umin ((-1 * %a) + (((zext i3 (trunc i8 %nraw to i3) to i8) + %a)<nuw><nsw> umax %a))))
; CHECK-NEXT:  Loop %loop: constant max backedge-taken count is i8 3
; CHECK-NEXT:  Loop %loop: symbolic max backedge-taken count is ((((-1 * (1 umin ((-1 * %a) + (((zext i3 (trunc i8 %nraw to i3) to i8) + %a)<nuw><nsw> umax %a))))<nuw><nsw> + (-1 * %a) + (((zext i3 (trunc i8 %nraw to i3) to i8) + %a)<nuw><nsw> umax %a)) /u 3) + (1 umin ((-1 * %a) + (((zext i3 (trunc i8 %nraw to i3) to i8) + %a)<nuw><nsw> umax %a))))
; CHECK-NEXT:  Loop %loop: Trip multiple is 1
;
entry:
  %n = and i8 %nraw, 7
  %end = add nuw nsw i8 %a, %n
  br label %loop

loop:
  %iv = phi i8 [ %a, %entry ], [ %iv.next, %loop ]
  %iv.next = add nuw nsw i8 %iv, 3
  %c = icmp ult i8 %iv, %end
  br i1 %c, label %loop, label %exit

exit:
  ret void
}


define void @lt_difference_may_wrap(i8 %nraw, i8 %a) {
; CHECK-LABEL: 'lt_difference_may_wrap'
; CHECK-NEXT:  Determining loop execution counts for: @lt_difference_may_wrap
; CHECK-NEXT:  Loop %loop: backedge-taken count is (-1 + (-1 * %a) + ((1 + %a) umax ((zext i3 (trunc i8 %nraw to i3) to i8) + %a)))
; CHECK-NEXT:  Loop %loop: constant max backedge-taken count is i8 -1
; CHECK-NEXT:  Loop %loop: symbolic max backedge-taken count is (-1 + (-1 * %a) + ((1 + %a) umax ((zext i3 (trunc i8 %nraw to i3) to i8) + %a)))
; CHECK-NEXT:  Loop %loop: Trip multiple is 1
;
entry:
  %n = and i8 %nraw, 7
  %end = add i8 %a, %n
  br label %loop

loop:
  %iv = phi i8 [ %a, %entry ], [ %iv.next, %loop ]
  %iv.next = add i8 %iv, 1
  %c = icmp ult i8 %iv.next, %end
  br i1 %c, label %loop, label %exit

exit:
  ret void
}

define void @lt_end_is_start_plus_unbounded_n(i8 %n, i8 %a) {
; CHECK-LABEL: 'lt_end_is_start_plus_unbounded_n'
; CHECK-NEXT:  Determining loop execution counts for: @lt_end_is_start_plus_unbounded_n
; CHECK-NEXT:  Loop %loop: backedge-taken count is ((-1 * %a) + ((%n + %a) umax %a))
; CHECK-NEXT:  Loop %loop: constant max backedge-taken count is i8 -1
; CHECK-NEXT:  Loop %loop: symbolic max backedge-taken count is ((-1 * %a) + ((%n + %a) umax %a))
; CHECK-NEXT:  Loop %loop: Trip multiple is 1
;
entry:
  %end = add i8 %a, %n
  br label %loop

loop:
  %iv = phi i8 [ %a, %entry ], [ %iv.next, %loop ]
  %iv.next = add i8 %iv, 1
  %c = icmp ult i8 %iv, %end
  br i1 %c, label %loop, label %exit

exit:
  ret void
}

define void @gt_end_is_start_minus_bounded_n(i8 %nraw, i8 %a) {
; CHECK-LABEL: 'gt_end_is_start_minus_bounded_n'
; CHECK-NEXT:  Determining loop execution counts for: @gt_end_is_start_minus_bounded_n
; CHECK-NEXT:  Loop %loop: backedge-taken count is ((-1 * (((-1 * (zext i3 (trunc i8 %nraw to i3) to i8))<nsw> + %a) umin %a)) + %a)
; CHECK-NEXT:  Loop %loop: constant max backedge-taken count is i8 -1
; CHECK-NEXT:  Loop %loop: symbolic max backedge-taken count is ((-1 * (((-1 * (zext i3 (trunc i8 %nraw to i3) to i8))<nsw> + %a) umin %a)) + %a)
; CHECK-NEXT:  Loop %loop: Trip multiple is 1
;
entry:
  %n = and i8 %nraw, 7
  %end = sub i8 %a, %n
  br label %loop

loop:
  %iv = phi i8 [ %a, %entry ], [ %iv.next, %loop ]
  %iv.next = sub i8 %iv, 1
  %c = icmp ugt i8 %iv, %end
  br i1 %c, label %loop, label %exit

exit:
  ret void
}
