; NOTE: Assertions have been autogenerated by utils/update_analyze_test_checks.py UTC_ARGS: --version 6
; RUN: opt < %s -disable-output "-passes=print<da>" 2>&1 \
; RUN:     | FileCheck %s --check-prefixes=CHECK,CHECK-ALL
; RUN: opt < %s -disable-output "-passes=print<da>" -da-enable-dependence-test=weak-crossing-siv 2>&1 \
; RUN:     | FileCheck %s --check-prefixes=CHECK,CHECK-WEAK-CROSSING-SIV
;
; i0 = INT64_MIN;
; i1 = INT64_MAX;
; for (; i0 != INT64_MAX; i0++, i1-) {
;   if (i0 == 0)
;     A[i0] = 0;
;   if (i1 == 0)
;     A[i1] = 0;
; }
;
; FIXME: Both `A[i0] = 0` and `A[i1] = 0` must be executed, so there is a
; dependency between them.
;
define void @weak_crossing_siv_large_btc(ptr %A) {
; CHECK-ALL-LABEL: 'weak_crossing_siv_large_btc'
; CHECK-ALL-NEXT:  Src: store i8 0, ptr %gep.0, align 1 --> Dst: store i8 0, ptr %gep.0, align 1
; CHECK-ALL-NEXT:    da analyze - none!
; CHECK-ALL-NEXT:  Src: store i8 0, ptr %gep.0, align 1 --> Dst: store i8 0, ptr %gep.1, align 1
; CHECK-ALL-NEXT:    da analyze - none!
; CHECK-ALL-NEXT:  Src: store i8 0, ptr %gep.1, align 1 --> Dst: store i8 0, ptr %gep.1, align 1
; CHECK-ALL-NEXT:    da analyze - none!
;
; CHECK-WEAK-CROSSING-SIV-LABEL: 'weak_crossing_siv_large_btc'
; CHECK-WEAK-CROSSING-SIV-NEXT:  Src: store i8 0, ptr %gep.0, align 1 --> Dst: store i8 0, ptr %gep.0, align 1
; CHECK-WEAK-CROSSING-SIV-NEXT:    da analyze - consistent output [*]!
; CHECK-WEAK-CROSSING-SIV-NEXT:  Src: store i8 0, ptr %gep.0, align 1 --> Dst: store i8 0, ptr %gep.1, align 1
; CHECK-WEAK-CROSSING-SIV-NEXT:    da analyze - none!
; CHECK-WEAK-CROSSING-SIV-NEXT:  Src: store i8 0, ptr %gep.1, align 1 --> Dst: store i8 0, ptr %gep.1, align 1
; CHECK-WEAK-CROSSING-SIV-NEXT:    da analyze - consistent output [*]!
;
entry:
  br label %loop.header

loop.header:
  %i.0 = phi i64 [ -9223372036854775808, %entry ], [ %i.0.inc, %loop.latch ]
  %i.1 = phi i64 [ 9223372036854775807, %entry ], [ %i.1.dec, %loop.latch ]
  %cond.0 = icmp eq i64 %i.0, 0
  br i1 %cond.0, label %if.then.0, label %loop.middle

if.then.0:
  %gep.0 = getelementptr i8, ptr %A, i64 %i.0
  store i8 0, ptr %gep.0
  br label %loop.middle

loop.middle:
  %cond.1 = icmp eq i64 %i.1, 0
  br i1 %cond.1, label %if.then.1, label %loop.latch

if.then.1:
  %gep.1 = getelementptr i8, ptr %A, i64 %i.1
  store i8 0, ptr %gep.1
  br label %loop.latch

loop.latch:
  %i.0.inc = add i64 %i.0, 1
  %i.1.dec = sub i64 %i.1, 1
  %ec = icmp eq i64 %i.0.inc, 9223372036854775807
  br i1 %ec, label %exit, label %loop.header

exit:
  ret void
}
;; NOTE: These prefixes are unused and the list is autogenerated. Do not add tests below this line:
; CHECK: {{.*}}
