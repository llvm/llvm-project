; NOTE: Assertions have been autogenerated by utils/update_llc_test_checks.py UTC_ARGS: --version 5
; RUN: llc < %s -mtriple=x86_64-unknown-unknown -verify-machineinstrs | FileCheck %s --check-prefixes=SSE2
; RUN: llc < %s -mtriple=x86_64-unknown-unknown -mattr=+avx -verify-machineinstrs | FileCheck %s --check-prefixes=AVX
; RUN: llc < %s -mtriple=x86_64-unknown-unknown -mattr=+avx512f -verify-machineinstrs | FileCheck %s --check-prefixes=AVX512
; RUN: llc < %s -mtriple=x86_64-unknown-unknown -mattr=+avxneconvert,+f16c -verify-machineinstrs | FileCheck %s --check-prefixes=AVXNC
; RUN: llc < %s -mtriple=x86_64-unknown-unknown -mattr=+avx512bf16,+avx512vl -verify-machineinstrs | FileCheck %s --check-prefixes=AVX512BF16

; bfloat is now a real register-class type (FR16 without AVX512, FR16X with it),
; so a live bfloat value across a full-XMM clobber has to be spilled and
; reloaded through that register class. This is the most direct test of the
; branch's central claim, and the counterpart to fp16-spill.ll.

define bfloat @test(float %f, ptr %p) nounwind {
; SSE2-LABEL: test:
; SSE2:       # %bb.0:
; SSE2-NEXT:    pushq %rbx
; SSE2-NEXT:    subq $16, %rsp
; SSE2-NEXT:    movq %rdi, %rbx
; SSE2-NEXT:    callq __truncsfbf2@PLT
; SSE2-NEXT:    movdqa %xmm0, (%rsp) # 16-byte Spill
; SSE2-NEXT:    pextrw $0, %xmm0, %eax
; SSE2-NEXT:    shll $16, %eax
; SSE2-NEXT:    #APP
; SSE2-NEXT:    #NO_APP
; SSE2-NEXT:    movl %eax, (%rbx)
; SSE2-NEXT:    movaps (%rsp), %xmm0 # 16-byte Reload
; SSE2-NEXT:    addq $16, %rsp
; SSE2-NEXT:    popq %rbx
; SSE2-NEXT:    retq
;
; AVX-LABEL: test:
; AVX:       # %bb.0:
; AVX-NEXT:    pushq %rbx
; AVX-NEXT:    subq $16, %rsp
; AVX-NEXT:    movq %rdi, %rbx
; AVX-NEXT:    callq __truncsfbf2@PLT
; AVX-NEXT:    vmovdqa %xmm0, (%rsp) # 16-byte Spill
; AVX-NEXT:    vpextrw $0, %xmm0, %eax
; AVX-NEXT:    shll $16, %eax
; AVX-NEXT:    #APP
; AVX-NEXT:    #NO_APP
; AVX-NEXT:    movl %eax, (%rbx)
; AVX-NEXT:    vmovaps (%rsp), %xmm0 # 16-byte Reload
; AVX-NEXT:    addq $16, %rsp
; AVX-NEXT:    popq %rbx
; AVX-NEXT:    retq
;
; AVX512-LABEL: test:
; AVX512:       # %bb.0:
; AVX512-NEXT:    pushq %rbx
; AVX512-NEXT:    subq $16, %rsp
; AVX512-NEXT:    movq %rdi, %rbx
; AVX512-NEXT:    callq __truncsfbf2@PLT
; AVX512-NEXT:    vmovdqa %xmm0, (%rsp) # 16-byte Spill
; AVX512-NEXT:    vpextrw $0, %xmm0, %eax
; AVX512-NEXT:    shll $16, %eax
; AVX512-NEXT:    #APP
; AVX512-NEXT:    #NO_APP
; AVX512-NEXT:    movl %eax, (%rbx)
; AVX512-NEXT:    vmovaps (%rsp), %xmm0 # 16-byte Reload
; AVX512-NEXT:    addq $16, %rsp
; AVX512-NEXT:    popq %rbx
; AVX512-NEXT:    retq
;
; AVXNC-LABEL: test:
; AVXNC:       # %bb.0:
; AVXNC-NEXT:    {vex} vcvtneps2bf16 %xmm0, %xmm0
; AVXNC-NEXT:    vmovdqa %xmm0, {{[-0-9]+}}(%r{{[sb]}}p) # 16-byte Spill
; AVXNC-NEXT:    vmovd %xmm0, %eax
; AVXNC-NEXT:    shll $16, %eax
; AVXNC-NEXT:    #APP
; AVXNC-NEXT:    #NO_APP
; AVXNC-NEXT:    movl %eax, (%rdi)
; AVXNC-NEXT:    vmovaps {{[-0-9]+}}(%r{{[sb]}}p), %xmm0 # 16-byte Reload
; AVXNC-NEXT:    retq
;
; AVX512BF16-LABEL: test:
; AVX512BF16:       # %bb.0:
; AVX512BF16-NEXT:    vcvtneps2bf16 %xmm0, %xmm16
; AVX512BF16-NEXT:    vmovd %xmm16, %eax
; AVX512BF16-NEXT:    shll $16, %eax
; AVX512BF16-NEXT:    #APP
; AVX512BF16-NEXT:    #NO_APP
; AVX512BF16-NEXT:    movl %eax, (%rdi)
; AVX512BF16-NEXT:    vmovaps %xmm16, %xmm0
; AVX512BF16-NEXT:    retq
  %t = fptrunc float %f to bfloat
  %t2 = fpext bfloat %t to float
  tail call void asm sideeffect "", "~{xmm0},~{xmm1},~{xmm2},~{xmm3},~{xmm4},~{xmm5},~{xmm6},~{xmm7},~{xmm8},~{xmm9},~{xmm10},~{xmm11},~{xmm12},~{xmm13},~{xmm14},~{xmm15},~{dirflag},~{fpsr},~{flags}"()
  store float %t2, ptr %p
  ret bfloat %t
}

; Two live bfloat values across the clobber, so the allocator has to spill both
; rather than keeping one in a callee-saved slot by luck.
define bfloat @test_two(bfloat %a, bfloat %b, ptr %p) nounwind {
; SSE2-LABEL: test_two:
; SSE2:       # %bb.0:
; SSE2-NEXT:    movaps %xmm1, {{[-0-9]+}}(%r{{[sb]}}p) # 16-byte Spill
; SSE2-NEXT:    movss %xmm0, {{[-0-9]+}}(%r{{[sb]}}p) # 4-byte Spill
; SSE2-NEXT:    #APP
; SSE2-NEXT:    #NO_APP
; SSE2-NEXT:    movdqa {{[-0-9]+}}(%r{{[sb]}}p), %xmm0 # 16-byte Reload
; SSE2-NEXT:    pextrw $0, %xmm0, %eax
; SSE2-NEXT:    movw %ax, (%rdi)
; SSE2-NEXT:    movss {{[-0-9]+}}(%r{{[sb]}}p), %xmm0 # 4-byte Reload
; SSE2-NEXT:    # xmm0 = mem[0],zero,zero,zero
; SSE2-NEXT:    retq
;
; AVX-LABEL: test_two:
; AVX:       # %bb.0:
; AVX-NEXT:    vmovaps %xmm1, {{[-0-9]+}}(%r{{[sb]}}p) # 16-byte Spill
; AVX-NEXT:    vmovss %xmm0, {{[-0-9]+}}(%r{{[sb]}}p) # 4-byte Spill
; AVX-NEXT:    #APP
; AVX-NEXT:    #NO_APP
; AVX-NEXT:    vmovdqa {{[-0-9]+}}(%r{{[sb]}}p), %xmm0 # 16-byte Reload
; AVX-NEXT:    vpextrw $0, %xmm0, (%rdi)
; AVX-NEXT:    vmovss {{[-0-9]+}}(%r{{[sb]}}p), %xmm0 # 4-byte Reload
; AVX-NEXT:    # xmm0 = mem[0],zero,zero,zero
; AVX-NEXT:    retq
;
; AVX512-LABEL: test_two:
; AVX512:       # %bb.0:
; AVX512-NEXT:    vmovaps %xmm1, {{[-0-9]+}}(%r{{[sb]}}p) # 16-byte Spill
; AVX512-NEXT:    vmovaps %zmm0, %zmm16
; AVX512-NEXT:    #APP
; AVX512-NEXT:    #NO_APP
; AVX512-NEXT:    vmovdqa {{[-0-9]+}}(%r{{[sb]}}p), %xmm0 # 16-byte Reload
; AVX512-NEXT:    vpextrw $0, %xmm0, (%rdi)
; AVX512-NEXT:    vmovaps %zmm16, %zmm0
; AVX512-NEXT:    vzeroupper
; AVX512-NEXT:    retq
;
; AVXNC-LABEL: test_two:
; AVXNC:       # %bb.0:
; AVXNC-NEXT:    vmovaps %xmm1, {{[-0-9]+}}(%r{{[sb]}}p) # 16-byte Spill
; AVXNC-NEXT:    vmovss %xmm0, {{[-0-9]+}}(%r{{[sb]}}p) # 4-byte Spill
; AVXNC-NEXT:    #APP
; AVXNC-NEXT:    #NO_APP
; AVXNC-NEXT:    vmovdqa {{[-0-9]+}}(%r{{[sb]}}p), %xmm0 # 16-byte Reload
; AVXNC-NEXT:    vpextrw $0, %xmm0, (%rdi)
; AVXNC-NEXT:    vmovss {{[-0-9]+}}(%r{{[sb]}}p), %xmm0 # 4-byte Reload
; AVXNC-NEXT:    # xmm0 = mem[0],zero,zero,zero
; AVXNC-NEXT:    retq
;
; AVX512BF16-LABEL: test_two:
; AVX512BF16:       # %bb.0:
; AVX512BF16-NEXT:    vmovdqa64 %xmm1, %xmm16
; AVX512BF16-NEXT:    vmovaps %xmm0, %xmm17
; AVX512BF16-NEXT:    #APP
; AVX512BF16-NEXT:    #NO_APP
; AVX512BF16-NEXT:    vpextrw $0, %xmm16, (%rdi)
; AVX512BF16-NEXT:    vmovaps %xmm17, %xmm0
; AVX512BF16-NEXT:    retq
  tail call void asm sideeffect "", "~{xmm0},~{xmm1},~{xmm2},~{xmm3},~{xmm4},~{xmm5},~{xmm6},~{xmm7},~{xmm8},~{xmm9},~{xmm10},~{xmm11},~{xmm12},~{xmm13},~{xmm14},~{xmm15},~{dirflag},~{fpsr},~{flags}"()
  store bfloat %b, ptr %p
  ret bfloat %a
}

; A bfloat vector across the same clobber, to cover the v8bf16 register class.
define <8 x bfloat> @test_v8(<8 x bfloat> %a, ptr %p) nounwind {
; SSE2-LABEL: test_v8:
; SSE2:       # %bb.0:
; SSE2-NEXT:    movaps %xmm0, {{[-0-9]+}}(%r{{[sb]}}p) # 16-byte Spill
; SSE2-NEXT:    #APP
; SSE2-NEXT:    #NO_APP
; SSE2-NEXT:    movaps {{[-0-9]+}}(%r{{[sb]}}p), %xmm0 # 16-byte Reload
; SSE2-NEXT:    retq
;
; AVX-LABEL: test_v8:
; AVX:       # %bb.0:
; AVX-NEXT:    vmovaps %xmm0, {{[-0-9]+}}(%r{{[sb]}}p) # 16-byte Spill
; AVX-NEXT:    #APP
; AVX-NEXT:    #NO_APP
; AVX-NEXT:    vmovaps {{[-0-9]+}}(%r{{[sb]}}p), %xmm0 # 16-byte Reload
; AVX-NEXT:    retq
;
; AVX512-LABEL: test_v8:
; AVX512:       # %bb.0:
; AVX512-NEXT:    vmovaps %xmm0, {{[-0-9]+}}(%r{{[sb]}}p) # 16-byte Spill
; AVX512-NEXT:    #APP
; AVX512-NEXT:    #NO_APP
; AVX512-NEXT:    vmovaps {{[-0-9]+}}(%r{{[sb]}}p), %xmm0 # 16-byte Reload
; AVX512-NEXT:    retq
;
; AVXNC-LABEL: test_v8:
; AVXNC:       # %bb.0:
; AVXNC-NEXT:    vmovaps %xmm0, {{[-0-9]+}}(%r{{[sb]}}p) # 16-byte Spill
; AVXNC-NEXT:    #APP
; AVXNC-NEXT:    #NO_APP
; AVXNC-NEXT:    vmovaps {{[-0-9]+}}(%r{{[sb]}}p), %xmm0 # 16-byte Reload
; AVXNC-NEXT:    retq
;
; AVX512BF16-LABEL: test_v8:
; AVX512BF16:       # %bb.0:
; AVX512BF16-NEXT:    vmovaps %xmm0, %xmm16
; AVX512BF16-NEXT:    #APP
; AVX512BF16-NEXT:    #NO_APP
; AVX512BF16-NEXT:    vmovaps %xmm16, %xmm0
; AVX512BF16-NEXT:    retq
  tail call void asm sideeffect "", "~{xmm0},~{xmm1},~{xmm2},~{xmm3},~{xmm4},~{xmm5},~{xmm6},~{xmm7},~{xmm8},~{xmm9},~{xmm10},~{xmm11},~{xmm12},~{xmm13},~{xmm14},~{xmm15},~{dirflag},~{fpsr},~{flags}"()
  ret <8 x bfloat> %a
}
