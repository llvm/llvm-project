; NOTE: Assertions have been autogenerated by utils/update_llc_test_checks.py UTC_ARGS: --version 6
; RUN: not --crash llc < %s -mtriple=s390x-linux-gnu 2>&1 | FileCheck %s

; CHECK: error: TRANSLATE length must be a compile-time constant between 1 and 256
; CHECK: error: TRANSLATE length must be a compile-time constant between 1 and 256

declare void @llvm.s390.translate(ptr, i64, ptr)

define void @tr_invalid_len_zero(ptr %src, ptr %table) {
  call void @llvm.s390.translate(ptr %src, i64 0, ptr %table)
  ret void
}

define void @tr_invalid_len_260(ptr %src, ptr %table) {
  call void @llvm.s390.translate(ptr %src, i64 260, ptr %table)
  ret void
}
