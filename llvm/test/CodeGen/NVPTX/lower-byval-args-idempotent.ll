; NOTE: Assertions have been autogenerated by utils/update_test_checks.py UTC_ARGS: --version 6
; Running nvptx-lower-args twice must be a no-op the second time: once a byval
; kernel argument has been rewritten into the param address space, the pass
; recognizes it as already lowered and leaves it alone. Both RUN lines share the
; same CHECK prefix, so the single-run and double-run outputs must be identical.
; RUN: opt < %s -mtriple nvptx64 -mcpu=sm_70 -mattr=ptx77 -passes=nvptx-lower-args -S | FileCheck %s
; RUN: opt < %s -mtriple nvptx64 -mcpu=sm_70 -mattr=ptx77 -passes=nvptx-lower-args,nvptx-lower-args -S | FileCheck %s

%struct.S = type { i32, i32 }

define ptx_kernel void @read_only(ptr %out, ptr byval(%struct.S) align 4 %s) {
; CHECK-LABEL: define ptx_kernel void @read_only(
; CHECK-SAME: ptr [[OUT:%.*]], ptr addrspace(101) readonly byval([[STRUCT_S:%.*]]) align 4 [[S:%.*]]) #[[ATTR0:[0-9]+]] {
; CHECK-NEXT:    [[I:%.*]] = load i32, ptr addrspace(101) [[S]], align 4
; CHECK-NEXT:    store i32 [[I]], ptr [[OUT]], align 4
; CHECK-NEXT:    ret void
;
  %i = load i32, ptr %s, align 4
  store i32 %i, ptr %out, align 4
  ret void
}
